/**
 * Tag Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all tag queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['tags'] - All tag queries
 * - ['tags', 'list'] - All list queries
 * - ['tags', 'list', { filters }] - Specific filtered list
 * - ['tags', 'detail', id] - Specific tag by ID
 *
 * @example
 * ```typescript
 * import { tagKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: tagKeys.list({ is_active: true }),
 *   queryFn: () => tagService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all tag queries
 * queryClient.invalidateQueries({ queryKey: tagKeys.all });
 *
 * // Invalidate specific tag
 * queryClient.invalidateQueries({ queryKey: tagKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:25.779216Z
 */

import type { TagFilterOptions } from '../types/tag.types';

/**
 * Root query key for Tag entity
 * Used as namespace prefix for all tag queries
 */
const TAG_ROOT = 'tags' as const;

/**
 * Tag Query Keys
 *
 * Hierarchical query key factory for tag queries:
 * - all: Base key for all tag queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific tag by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = tagKeys.list({ is_active: true });
 * // Result: ['tags', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = tagKeys.detail('uuid-here');
 * // Result: ['tags', 'detail', 'uuid-here']
 *
 * // Invalidate all tag data
 * queryClient.invalidateQueries({ queryKey: tagKeys.all });
 * ```
 */
export const tagKeys = {
  /**
   * Base key for all tag queries
   * Use this to invalidate all tag-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: tagKeys.all });
   * ```
   */
  all: [TAG_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: tagKeys.lists() });
   * ```
   */
  lists: () => [...tagKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = tagKeys.list({ is_active: true });
   *
   * // List with search
   * const key = tagKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = tagKeys.list();
   * ```
   */
  list: (filters?: TagFilterOptions) =>
    [...tagKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: tagKeys.details() });
   * ```
   */
  details: () => [...tagKeys.all, 'detail'] as const,

  /**
   * Key for a specific tag by ID
   * Use this for fetching and invalidating individual tag records
   *
   * @param id - Tag ID (UUID)
   * @returns Query key tuple for specific tag
   *
   * @example
   * ```typescript
   * // Fetch specific tag
   * const { data } = useQuery({
   *   queryKey: tagKeys.detail('uuid-here'),
   *   queryFn: () => tagService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: tagKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...tagKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active tags (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = tagKeys.dropdown();
   *
   * // Include inactive items
   * const key = tagKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...tagKeys.all, 'dropdown', { activeOnly }] as const,
} as const;

/**
 * Type representing any valid Tag query key
 * Useful for typing query key arrays in generic contexts
 */
export type TagQueryKey =
  | typeof tagKeys.all
  | ReturnType<typeof tagKeys.lists>
  | ReturnType<typeof tagKeys.list>
  | ReturnType<typeof tagKeys.details>
  | ReturnType<typeof tagKeys.detail>
  | ReturnType<typeof tagKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default tagKeys;
