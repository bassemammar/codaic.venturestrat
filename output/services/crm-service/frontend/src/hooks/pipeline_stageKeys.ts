/**
 * PipelineStage Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all pipeline_stage queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['pipelinestages'] - All pipeline_stage queries
 * - ['pipelinestages', 'list'] - All list queries
 * - ['pipelinestages', 'list', { filters }] - Specific filtered list
 * - ['pipelinestages', 'detail', id] - Specific pipeline_stage by ID
 *
 * @example
 * ```typescript
 * import { pipeline_stageKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: pipeline_stageKeys.list({ is_active: true }),
 *   queryFn: () => pipeline_stageService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all pipeline_stage queries
 * queryClient.invalidateQueries({ queryKey: pipeline_stageKeys.all });
 *
 * // Invalidate specific pipeline_stage
 * queryClient.invalidateQueries({ queryKey: pipeline_stageKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:24.021110Z
 */

import type { PipelineStageFilterOptions } from '../types/pipeline_stage.types';

/**
 * Root query key for PipelineStage entity
 * Used as namespace prefix for all pipeline_stage queries
 */
const PIPELINE_STAGE_ROOT = 'pipelinestages' as const;

/**
 * PipelineStage Query Keys
 *
 * Hierarchical query key factory for pipeline_stage queries:
 * - all: Base key for all pipeline_stage queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific pipeline_stage by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = pipeline_stageKeys.list({ is_active: true });
 * // Result: ['pipelinestages', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = pipeline_stageKeys.detail('uuid-here');
 * // Result: ['pipelinestages', 'detail', 'uuid-here']
 *
 * // Invalidate all pipeline_stage data
 * queryClient.invalidateQueries({ queryKey: pipeline_stageKeys.all });
 * ```
 */
export const pipeline_stageKeys = {
  /**
   * Base key for all pipeline_stage queries
   * Use this to invalidate all pipeline_stage-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: pipeline_stageKeys.all });
   * ```
   */
  all: [PIPELINE_STAGE_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: pipeline_stageKeys.lists() });
   * ```
   */
  lists: () => [...pipeline_stageKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = pipeline_stageKeys.list({ is_active: true });
   *
   * // List with search
   * const key = pipeline_stageKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = pipeline_stageKeys.list();
   * ```
   */
  list: (filters?: PipelineStageFilterOptions) =>
    [...pipeline_stageKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: pipeline_stageKeys.details() });
   * ```
   */
  details: () => [...pipeline_stageKeys.all, 'detail'] as const,

  /**
   * Key for a specific pipeline_stage by ID
   * Use this for fetching and invalidating individual pipeline_stage records
   *
   * @param id - PipelineStage ID (UUID)
   * @returns Query key tuple for specific pipeline_stage
   *
   * @example
   * ```typescript
   * // Fetch specific pipeline_stage
   * const { data } = useQuery({
   *   queryKey: pipeline_stageKeys.detail('uuid-here'),
   *   queryFn: () => pipeline_stageService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: pipeline_stageKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...pipeline_stageKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active pipeline_stages (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = pipeline_stageKeys.dropdown();
   *
   * // Include inactive items
   * const key = pipeline_stageKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...pipeline_stageKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * Key for pipeline_stage by code lookup
   * Use this when fetching by business code instead of UUID
   *
   * @param code - PipelineStage code (e.g., 'USD', 'EUR')
   * @returns Query key tuple for code lookup
   *
   * @example
   * ```typescript
   * const { data } = useQuery({
   *   queryKey: pipeline_stageKeys.byCode('USD'),
   *   queryFn: () => pipeline_stageService.getByCode('USD'),
   * });
   * ```
   */
  byCode: (code: string) =>
    [...pipeline_stageKeys.all, 'by-code', code.toUpperCase()] as const,
} as const;

/**
 * Type representing any valid PipelineStage query key
 * Useful for typing query key arrays in generic contexts
 */
export type PipelineStageQueryKey =
  | typeof pipeline_stageKeys.all
  | ReturnType<typeof pipeline_stageKeys.lists>
  | ReturnType<typeof pipeline_stageKeys.list>
  | ReturnType<typeof pipeline_stageKeys.details>
  | ReturnType<typeof pipeline_stageKeys.detail>
  | ReturnType<typeof pipeline_stageKeys.dropdown>
  | ReturnType<typeof pipeline_stageKeys.byCode>
;

/**
 * Default export for convenience
 */
export default pipeline_stageKeys;
