/**
 * ShortlistTag Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all shortlist_tag queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['shortlisttags'] - All shortlist_tag queries
 * - ['shortlisttags', 'list'] - All list queries
 * - ['shortlisttags', 'list', { filters }] - Specific filtered list
 * - ['shortlisttags', 'detail', id] - Specific shortlist_tag by ID
 *
 * @example
 * ```typescript
 * import { shortlist_tagKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: shortlist_tagKeys.list({ is_active: true }),
 *   queryFn: () => shortlist_tagService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all shortlist_tag queries
 * queryClient.invalidateQueries({ queryKey: shortlist_tagKeys.all });
 *
 * // Invalidate specific shortlist_tag
 * queryClient.invalidateQueries({ queryKey: shortlist_tagKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:25.350000Z
 */

import type { ShortlistTagFilterOptions } from '../types/shortlist_tag.types';

/**
 * Root query key for ShortlistTag entity
 * Used as namespace prefix for all shortlist_tag queries
 */
const SHORTLIST_TAG_ROOT = 'shortlisttags' as const;

/**
 * ShortlistTag Query Keys
 *
 * Hierarchical query key factory for shortlist_tag queries:
 * - all: Base key for all shortlist_tag queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific shortlist_tag by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = shortlist_tagKeys.list({ is_active: true });
 * // Result: ['shortlisttags', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = shortlist_tagKeys.detail('uuid-here');
 * // Result: ['shortlisttags', 'detail', 'uuid-here']
 *
 * // Invalidate all shortlist_tag data
 * queryClient.invalidateQueries({ queryKey: shortlist_tagKeys.all });
 * ```
 */
export const shortlist_tagKeys = {
  /**
   * Base key for all shortlist_tag queries
   * Use this to invalidate all shortlist_tag-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: shortlist_tagKeys.all });
   * ```
   */
  all: [SHORTLIST_TAG_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: shortlist_tagKeys.lists() });
   * ```
   */
  lists: () => [...shortlist_tagKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = shortlist_tagKeys.list({ is_active: true });
   *
   * // List with search
   * const key = shortlist_tagKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = shortlist_tagKeys.list();
   * ```
   */
  list: (filters?: ShortlistTagFilterOptions) =>
    [...shortlist_tagKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: shortlist_tagKeys.details() });
   * ```
   */
  details: () => [...shortlist_tagKeys.all, 'detail'] as const,

  /**
   * Key for a specific shortlist_tag by ID
   * Use this for fetching and invalidating individual shortlist_tag records
   *
   * @param id - ShortlistTag ID (UUID)
   * @returns Query key tuple for specific shortlist_tag
   *
   * @example
   * ```typescript
   * // Fetch specific shortlist_tag
   * const { data } = useQuery({
   *   queryKey: shortlist_tagKeys.detail('uuid-here'),
   *   queryFn: () => shortlist_tagService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: shortlist_tagKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...shortlist_tagKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active shortlist_tags (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = shortlist_tagKeys.dropdown();
   *
   * // Include inactive items
   * const key = shortlist_tagKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...shortlist_tagKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * ShortlistTags filtered by shortlist ID
   *
   * @param shortlistId - Shortlist ID (UUID)
   * @returns Query key tuple
   */
  byShortlist: (shortlistId: string) =>
    [...shortlist_tagKeys.all, 'by-shortlist', shortlistId] as const,

  /**
   * ShortlistTags filtered by tag ID
   *
   * @param tagId - Tag ID (UUID)
   * @returns Query key tuple
   */
  byTag: (tagId: string) =>
    [...shortlist_tagKeys.all, 'by-tag', tagId] as const,
} as const;

/**
 * Type representing any valid ShortlistTag query key
 * Useful for typing query key arrays in generic contexts
 */
export type ShortlistTagQueryKey =
  | typeof shortlist_tagKeys.all
  | ReturnType<typeof shortlist_tagKeys.lists>
  | ReturnType<typeof shortlist_tagKeys.list>
  | ReturnType<typeof shortlist_tagKeys.details>
  | ReturnType<typeof shortlist_tagKeys.detail>
  | ReturnType<typeof shortlist_tagKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default shortlist_tagKeys;
