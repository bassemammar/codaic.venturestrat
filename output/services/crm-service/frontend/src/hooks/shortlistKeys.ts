/**
 * Shortlist Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all shortlist queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['shortlists'] - All shortlist queries
 * - ['shortlists', 'list'] - All list queries
 * - ['shortlists', 'list', { filters }] - Specific filtered list
 * - ['shortlists', 'detail', id] - Specific shortlist by ID
 *
 * @example
 * ```typescript
 * import { shortlistKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: shortlistKeys.list({ is_active: true }),
 *   queryFn: () => shortlistService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all shortlist queries
 * queryClient.invalidateQueries({ queryKey: shortlistKeys.all });
 *
 * // Invalidate specific shortlist
 * queryClient.invalidateQueries({ queryKey: shortlistKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:24.711755Z
 */

import type { ShortlistFilterOptions } from '../types/shortlist.types';

/**
 * Root query key for Shortlist entity
 * Used as namespace prefix for all shortlist queries
 */
const SHORTLIST_ROOT = 'shortlists' as const;

/**
 * Shortlist Query Keys
 *
 * Hierarchical query key factory for shortlist queries:
 * - all: Base key for all shortlist queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific shortlist by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = shortlistKeys.list({ is_active: true });
 * // Result: ['shortlists', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = shortlistKeys.detail('uuid-here');
 * // Result: ['shortlists', 'detail', 'uuid-here']
 *
 * // Invalidate all shortlist data
 * queryClient.invalidateQueries({ queryKey: shortlistKeys.all });
 * ```
 */
export const shortlistKeys = {
  /**
   * Base key for all shortlist queries
   * Use this to invalidate all shortlist-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: shortlistKeys.all });
   * ```
   */
  all: [SHORTLIST_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: shortlistKeys.lists() });
   * ```
   */
  lists: () => [...shortlistKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = shortlistKeys.list({ is_active: true });
   *
   * // List with search
   * const key = shortlistKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = shortlistKeys.list();
   * ```
   */
  list: (filters?: ShortlistFilterOptions) =>
    [...shortlistKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: shortlistKeys.details() });
   * ```
   */
  details: () => [...shortlistKeys.all, 'detail'] as const,

  /**
   * Key for a specific shortlist by ID
   * Use this for fetching and invalidating individual shortlist records
   *
   * @param id - Shortlist ID (UUID)
   * @returns Query key tuple for specific shortlist
   *
   * @example
   * ```typescript
   * // Fetch specific shortlist
   * const { data } = useQuery({
   *   queryKey: shortlistKeys.detail('uuid-here'),
   *   queryFn: () => shortlistService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: shortlistKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...shortlistKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active shortlists (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = shortlistKeys.dropdown();
   *
   * // Include inactive items
   * const key = shortlistKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...shortlistKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * Shortlists filtered by user ID
   *
   * @param userId - User ID (UUID)
   * @returns Query key tuple
   */
  byUser: (userId: string) =>
    [...shortlistKeys.all, 'by-user', userId] as const,

  /**
   * Shortlists filtered by investor ID
   *
   * @param investorId - Investor ID (UUID)
   * @returns Query key tuple
   */
  byInvestor: (investorId: string) =>
    [...shortlistKeys.all, 'by-investor', investorId] as const,

  /**
   * Shortlists filtered by stage ID
   *
   * @param stageId - Stage ID (UUID)
   * @returns Query key tuple
   */
  byStage: (stageId: string) =>
    [...shortlistKeys.all, 'by-stage', stageId] as const,

  /**
   * Shortlists filtered by status
   *
   * @param status - Status value
   * @returns Query key tuple
   */
  byStatus: (status: string) =>
    [...shortlistKeys.all, 'by-status', status] as const,
} as const;

/**
 * Type representing any valid Shortlist query key
 * Useful for typing query key arrays in generic contexts
 */
export type ShortlistQueryKey =
  | typeof shortlistKeys.all
  | ReturnType<typeof shortlistKeys.lists>
  | ReturnType<typeof shortlistKeys.list>
  | ReturnType<typeof shortlistKeys.details>
  | ReturnType<typeof shortlistKeys.detail>
  | ReturnType<typeof shortlistKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default shortlistKeys;
