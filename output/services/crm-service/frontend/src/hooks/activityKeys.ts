/**
 * Activity Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all activity queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['activities'] - All activity queries
 * - ['activities', 'list'] - All list queries
 * - ['activities', 'list', { filters }] - Specific filtered list
 * - ['activities', 'detail', id] - Specific activity by ID
 *
 * @example
 * ```typescript
 * import { activityKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: activityKeys.list({ is_active: true }),
 *   queryFn: () => activityService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all activity queries
 * queryClient.invalidateQueries({ queryKey: activityKeys.all });
 *
 * // Invalidate specific activity
 * queryClient.invalidateQueries({ queryKey: activityKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:23.423788Z
 */

import type { ActivityFilterOptions } from '../types/activity.types';

/**
 * Root query key for Activity entity
 * Used as namespace prefix for all activity queries
 */
const ACTIVITY_ROOT = 'activities' as const;

/**
 * Activity Query Keys
 *
 * Hierarchical query key factory for activity queries:
 * - all: Base key for all activity queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific activity by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = activityKeys.list({ is_active: true });
 * // Result: ['activities', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = activityKeys.detail('uuid-here');
 * // Result: ['activities', 'detail', 'uuid-here']
 *
 * // Invalidate all activity data
 * queryClient.invalidateQueries({ queryKey: activityKeys.all });
 * ```
 */
export const activityKeys = {
  /**
   * Base key for all activity queries
   * Use this to invalidate all activity-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: activityKeys.all });
   * ```
   */
  all: [ACTIVITY_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: activityKeys.lists() });
   * ```
   */
  lists: () => [...activityKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = activityKeys.list({ is_active: true });
   *
   * // List with search
   * const key = activityKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = activityKeys.list();
   * ```
   */
  list: (filters?: ActivityFilterOptions) =>
    [...activityKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: activityKeys.details() });
   * ```
   */
  details: () => [...activityKeys.all, 'detail'] as const,

  /**
   * Key for a specific activity by ID
   * Use this for fetching and invalidating individual activity records
   *
   * @param id - Activity ID (UUID)
   * @returns Query key tuple for specific activity
   *
   * @example
   * ```typescript
   * // Fetch specific activity
   * const { data } = useQuery({
   *   queryKey: activityKeys.detail('uuid-here'),
   *   queryFn: () => activityService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: activityKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...activityKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active activities (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = activityKeys.dropdown();
   *
   * // Include inactive items
   * const key = activityKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...activityKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * Activitys filtered by shortlist ID
   *
   * @param shortlistId - Shortlist ID (UUID)
   * @returns Query key tuple
   */
  byShortlist: (shortlistId: string) =>
    [...activityKeys.all, 'by-shortlist', shortlistId] as const,

  /**
   * Activitys filtered by user ID
   *
   * @param userId - User ID (UUID)
   * @returns Query key tuple
   */
  byUser: (userId: string) =>
    [...activityKeys.all, 'by-user', userId] as const,

  /**
   * Activitys filtered by reference ID
   *
   * @param referenceId - Reference ID (UUID)
   * @returns Query key tuple
   */
  byReference: (referenceId: string) =>
    [...activityKeys.all, 'by-reference', referenceId] as const,
} as const;

/**
 * Type representing any valid Activity query key
 * Useful for typing query key arrays in generic contexts
 */
export type ActivityQueryKey =
  | typeof activityKeys.all
  | ReturnType<typeof activityKeys.lists>
  | ReturnType<typeof activityKeys.list>
  | ReturnType<typeof activityKeys.details>
  | ReturnType<typeof activityKeys.detail>
  | ReturnType<typeof activityKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default activityKeys;
