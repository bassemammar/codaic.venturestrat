/**
 * Tag Type Definitions
 *
 * This file contains type definitions for the Tag entity:
 * - Tag: Full entity model with all fields
 * - TagCreateRequest: Data for creating a new tag
 * - TagUpdateRequest: Partial data for updating a tag
 * - TagListResponse: Paginated list response
 * - TagFilterOptions: Query parameters for filtering
 * - TagDropdownOption: Options for select components
 *
 * Tag for categorizing shortlisted investors
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:25.755140Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// Tag Entity
// ============================================================================

/**
 * Tag entity model
 *
 * Represents the full tag entity as returned by the API.
 * Tag for categorizing shortlisted investors
 *
 * @remarks
 * This interface matches the backend TagDTO schema exactly.
 */
export interface Tag {
  /** Unique identifier (UUID) */
  id: string;

  /** Name */
  name: string;

  /** Hex color code */
  color: string | null;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * Tag creation request data
 *
 * Used when creating a new tag. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend TagCreateDTO schema.
 */
export interface TagCreateRequest {
  /** Name (required) */
  name: string;

  /** Hex color code (optional) */
  color?: string;

  /** Active status (defaults to true for new tags) */
  is_active?: boolean;
}

/**
 * Tag update request data
 *
 * Used when updating an existing tag. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend TagUpdateDTO schema.
 */
export interface TagUpdateRequest {
  /** Name */
  name?: string;

  /** Hex color code */
  color?: string | null;

  /** Active status */
  is_active?: boolean;
}

/**
 * Tag list response
 *
 * Wrapper for paginated tag list queries.
 */
export interface TagListResponse {
  /** Array of tag items */
  items: Tag[];

  /** Alternative property name for items (for API compatibility) */
  tags?: Tag[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * Tag filter options for list queries
 *
 * Extends BaseEntityFilters with tag-specific filtering options.
 */
export interface TagFilterOptions extends BaseEntityFilters {
}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * Tag dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface TagDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: Tag;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * Tag summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface TagSummary {
  /** Unique identifier */
  id: string;

  /** Name */
  name: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * Tag form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type TagFormData = TagCreateRequest;

/**
 * Default form values for new tag
 *
 * Use this when initializing form state for creating a new tag.
 */
export const DEFAULT_TAG_FORM_DATA: Partial<TagFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single tag API response
 */
export interface TagResponse {
  /** The requested tag entity */
  data: Tag;
}

/**
 * Tag mutation response (create/update)
 */
export interface TagMutationResponse {
  /** The created or updated tag */
  data: Tag;

  /** Success message */
  message?: string;
}

/**
 * API error response for tag operations
 */
export interface TagApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a Tag
 *
 * @param value - Value to check
 * @returns True if value is a Tag
 */
export function isTag(value: unknown): value is Tag {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.name === 'string'
  );
}

/**
 * Type guard to check if a value is a TagDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a TagDropdownOption
 */
export function isTagDropdownOption(
  value: unknown
): value is TagDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isTag(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All tag types re-exported for convenience */
export type {
  Tag as TagEntity,
  TagCreateRequest as CreateTagInput,
  TagUpdateRequest as UpdateTagInput,
};
