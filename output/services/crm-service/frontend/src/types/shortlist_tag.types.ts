/**
 * ShortlistTag Type Definitions
 *
 * This file contains type definitions for the ShortlistTag entity:
 * - ShortlistTag: Full entity model with all fields
 * - ShortlistTagCreateRequest: Data for creating a new shortlist_tag
 * - ShortlistTagUpdateRequest: Partial data for updating a shortlist_tag
 * - ShortlistTagListResponse: Paginated list response
 * - ShortlistTagFilterOptions: Query parameters for filtering
 * - ShortlistTagDropdownOption: Options for select components
 *
 * Many-to-many junction between shortlists and tags
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:25.322797Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// ShortlistTag Entity
// ============================================================================

/**
 * ShortlistTag entity model
 *
 * Represents the full shortlist_tag entity as returned by the API.
 * Many-to-many junction between shortlists and tags
 *
 * @remarks
 * This interface matches the backend ShortlistTagDTO schema exactly.
 */
export interface ShortlistTag {
  /** Unique identifier (UUID) */
  id: string;

  /** Shortlist Id */
  shortlist_id: string;

  /** Tag Id */
  tag_id: string;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * ShortlistTag creation request data
 *
 * Used when creating a new shortlist_tag. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend ShortlistTagCreateDTO schema.
 */
export interface ShortlistTagCreateRequest {
  /** Shortlist Id (required) */
  shortlist_id: string;

  /** Tag Id (required) */
  tag_id: string;

  /** Active status (defaults to true for new shortlist_tags) */
  is_active?: boolean;
}

/**
 * ShortlistTag update request data
 *
 * Used when updating an existing shortlist_tag. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend ShortlistTagUpdateDTO schema.
 */
export interface ShortlistTagUpdateRequest {
  /** Shortlist Id */
  shortlist_id?: string;

  /** Tag Id */
  tag_id?: string;

  /** Active status */
  is_active?: boolean;
}

/**
 * ShortlistTag list response
 *
 * Wrapper for paginated shortlist_tag list queries.
 */
export interface ShortlistTagListResponse {
  /** Array of shortlist_tag items */
  items: ShortlistTag[];

  /** Alternative property name for items (for API compatibility) */
  shortlist_tags?: ShortlistTag[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * ShortlistTag filter options for list queries
 *
 * Extends BaseEntityFilters with shortlist_tag-specific filtering options.
 */
export interface ShortlistTagFilterOptions extends BaseEntityFilters {
  /** Filter by shortlist id */
  shortlist_id?: string;

  /** Filter by tag id */
  tag_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * ShortlistTag dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface ShortlistTagDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: ShortlistTag;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * ShortlistTag summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface ShortlistTagSummary {
  /** Unique identifier */
  id: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * ShortlistTag form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type ShortlistTagFormData = ShortlistTagCreateRequest;

/**
 * Default form values for new shortlist_tag
 *
 * Use this when initializing form state for creating a new shortlist_tag.
 */
export const DEFAULT_SHORTLIST_TAG_FORM_DATA: Partial<ShortlistTagFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single shortlist_tag API response
 */
export interface ShortlistTagResponse {
  /** The requested shortlist_tag entity */
  data: ShortlistTag;
}

/**
 * ShortlistTag mutation response (create/update)
 */
export interface ShortlistTagMutationResponse {
  /** The created or updated shortlist_tag */
  data: ShortlistTag;

  /** Success message */
  message?: string;
}

/**
 * API error response for shortlist_tag operations
 */
export interface ShortlistTagApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a ShortlistTag
 *
 * @param value - Value to check
 * @returns True if value is a ShortlistTag
 */
export function isShortlistTag(value: unknown): value is ShortlistTag {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.shortlist_id === 'string' &&
    typeof obj.tag_id === 'string'
  );
}

/**
 * Type guard to check if a value is a ShortlistTagDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a ShortlistTagDropdownOption
 */
export function isShortlistTagDropdownOption(
  value: unknown
): value is ShortlistTagDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isShortlistTag(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All shortlist_tag types re-exported for convenience */
export type {
  ShortlistTag as ShortlistTagEntity,
  ShortlistTagCreateRequest as CreateShortlistTagInput,
  ShortlistTagUpdateRequest as UpdateShortlistTagInput,
};
