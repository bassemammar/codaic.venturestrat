/**
 * Activity Type Definitions
 *
 * This file contains type definitions for the Activity entity:
 * - Activity: Full entity model with all fields
 * - ActivityCreateRequest: Data for creating a new activity
 * - ActivityUpdateRequest: Partial data for updating a activity
 * - ActivityListResponse: Paginated list response
 * - ActivityFilterOptions: Query parameters for filtering
 * - ActivityDropdownOption: Options for select components
 *
 * Outreach activity touchpoint on a shortlisted investor
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:23.391035Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// Activity Entity
// ============================================================================

/**
 * Activity entity model
 *
 * Represents the full activity entity as returned by the API.
 * Outreach activity touchpoint on a shortlisted investor
 *
 * @remarks
 * This interface matches the backend ActivityDTO schema exactly.
 */
export interface Activity {
  /** Unique identifier (UUID) */
  id: string;

  /** Parent shortlist record */
  shortlist_id: string;

  /** Activity Type */
  activity_type: string;

  /** Summary */
  summary: string | null;

  /** Details */
  details: string | null;

  /** When the activity occurred */
  date: string;

  /** Auth user who performed the activity */
  user_id: string;

  /** External reference, e.g. message ID */
  reference_id: string | null;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * Activity creation request data
 *
 * Used when creating a new activity. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend ActivityCreateDTO schema.
 */
export interface ActivityCreateRequest {
  /** Parent shortlist record (required) */
  shortlist_id: string;

  /** Activity Type (required) */
  activity_type: string;

  /** Summary (optional) */
  summary?: string;

  /** Details (optional) */
  details?: string;

  /** When the activity occurred (required) */
  date: string;

  /** Auth user who performed the activity (required) */
  user_id: string;

  /** External reference, e.g. message ID (optional) */
  reference_id?: string;

  /** Active status (defaults to true for new activities) */
  is_active?: boolean;
}

/**
 * Activity update request data
 *
 * Used when updating an existing activity. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend ActivityUpdateDTO schema.
 */
export interface ActivityUpdateRequest {
  /** Parent shortlist record */
  shortlist_id?: string;

  /** Activity Type */
  activity_type?: string;

  /** Summary */
  summary?: string | null;

  /** Details */
  details?: string | null;

  /** When the activity occurred */
  date?: string;

  /** Auth user who performed the activity */
  user_id?: string;

  /** External reference, e.g. message ID */
  reference_id?: string | null;

  /** Active status */
  is_active?: boolean;
}

/**
 * Activity list response
 *
 * Wrapper for paginated activity list queries.
 */
export interface ActivityListResponse {
  /** Array of activity items */
  items: Activity[];

  /** Alternative property name for items (for API compatibility) */
  activities?: Activity[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * Activity filter options for list queries
 *
 * Extends BaseEntityFilters with activity-specific filtering options.
 */
export interface ActivityFilterOptions extends BaseEntityFilters {
  /** Filter by parent shortlist record */
  shortlist_id?: string;

  /** Filter by activity type */
  activity_type?: string;

  /** Filter by auth user who performed the activity */
  user_id?: string;

  /** Filter by external reference, e.g. message id */
  reference_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * Activity dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface ActivityDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: Activity;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * Activity summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface ActivitySummary {
  /** Unique identifier */
  id: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * Activity form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type ActivityFormData = ActivityCreateRequest;

/**
 * Default form values for new activity
 *
 * Use this when initializing form state for creating a new activity.
 */
export const DEFAULT_ACTIVITY_FORM_DATA: Partial<ActivityFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single activity API response
 */
export interface ActivityResponse {
  /** The requested activity entity */
  data: Activity;
}

/**
 * Activity mutation response (create/update)
 */
export interface ActivityMutationResponse {
  /** The created or updated activity */
  data: Activity;

  /** Success message */
  message?: string;
}

/**
 * API error response for activity operations
 */
export interface ActivityApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a Activity
 *
 * @param value - Value to check
 * @returns True if value is a Activity
 */
export function isActivity(value: unknown): value is Activity {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.shortlist_id === 'string' &&
    typeof obj.activity_type === 'string' &&
    typeof obj.date === 'string' &&
    typeof obj.user_id === 'string'
  );
}

/**
 * Type guard to check if a value is a ActivityDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a ActivityDropdownOption
 */
export function isActivityDropdownOption(
  value: unknown
): value is ActivityDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isActivity(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All activity types re-exported for convenience */
export type {
  Activity as ActivityEntity,
  ActivityCreateRequest as CreateActivityInput,
  ActivityUpdateRequest as UpdateActivityInput,
};
