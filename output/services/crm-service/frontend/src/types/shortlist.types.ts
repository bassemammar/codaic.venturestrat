/**
 * Shortlist Type Definitions
 *
 * This file contains type definitions for the Shortlist entity:
 * - Shortlist: Full entity model with all fields
 * - ShortlistCreateRequest: Data for creating a new shortlist
 * - ShortlistUpdateRequest: Partial data for updating a shortlist
 * - ShortlistListResponse: Paginated list response
 * - ShortlistFilterOptions: Query parameters for filtering
 * - ShortlistDropdownOption: Options for select components
 *
 * User's investor pipeline — tracks shortlisted investors with CRM status
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:24.675314Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// Shortlist Entity
// ============================================================================

/**
 * Shortlist entity model
 *
 * Represents the full shortlist entity as returned by the API.
 * User's investor pipeline — tracks shortlisted investors with CRM status
 *
 * @remarks
 * This interface matches the backend ShortlistDTO schema exactly.
 */
export interface Shortlist {
  /** Unique identifier (UUID) */
  id: string;

  /** Auth user reference */
  user_id: string;

  /** Cross-service reference to investor-service Investor */
  investor_id: string;

  /** Pipeline stage reference */
  stage_id: string | null;

  /** Current pipeline status */
  status: string;

  /** Notes */
  notes: string | null;

  /** When investor was shortlisted */
  added_at: string;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * Shortlist creation request data
 *
 * Used when creating a new shortlist. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend ShortlistCreateDTO schema.
 */
export interface ShortlistCreateRequest {
  /** Auth user reference (required) */
  user_id: string;

  /** Cross-service reference to investor-service Investor (required) */
  investor_id: string;

  /** Pipeline stage reference (optional) */
  stage_id?: string;

  /** Current pipeline status (optional) */
  status?: string;

  /** Notes (optional) */
  notes?: string;

  /** When investor was shortlisted (required) */
  added_at: string;

  /** Active status (defaults to true for new shortlists) */
  is_active?: boolean;
}

/**
 * Shortlist update request data
 *
 * Used when updating an existing shortlist. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend ShortlistUpdateDTO schema.
 */
export interface ShortlistUpdateRequest {
  /** Auth user reference */
  user_id?: string;

  /** Cross-service reference to investor-service Investor */
  investor_id?: string;

  /** Pipeline stage reference */
  stage_id?: string | null;

  /** Current pipeline status */
  status?: string;

  /** Notes */
  notes?: string | null;

  /** When investor was shortlisted */
  added_at?: string;

  /** Active status */
  is_active?: boolean;
}

/**
 * Shortlist list response
 *
 * Wrapper for paginated shortlist list queries.
 */
export interface ShortlistListResponse {
  /** Array of shortlist items */
  items: Shortlist[];

  /** Alternative property name for items (for API compatibility) */
  shortlists?: Shortlist[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * Shortlist filter options for list queries
 *
 * Extends BaseEntityFilters with shortlist-specific filtering options.
 */
export interface ShortlistFilterOptions extends BaseEntityFilters {
  /** Filter by auth user reference */
  user_id?: string;

  /** Filter by cross-service reference to investor-service investor */
  investor_id?: string;

  /** Filter by pipeline stage reference */
  stage_id?: string;

  /** Filter by current pipeline status */
  status?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * Shortlist dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface ShortlistDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: Shortlist;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * Shortlist summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface ShortlistSummary {
  /** Unique identifier */
  id: string;

  /** Current pipeline status */
  status: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * Shortlist form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type ShortlistFormData = ShortlistCreateRequest;

/**
 * Default form values for new shortlist
 *
 * Use this when initializing form state for creating a new shortlist.
 */
export const DEFAULT_SHORTLIST_FORM_DATA: Partial<ShortlistFormData> = {
  is_active: true,
  status: "target",
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single shortlist API response
 */
export interface ShortlistResponse {
  /** The requested shortlist entity */
  data: Shortlist;
}

/**
 * Shortlist mutation response (create/update)
 */
export interface ShortlistMutationResponse {
  /** The created or updated shortlist */
  data: Shortlist;

  /** Success message */
  message?: string;
}

/**
 * API error response for shortlist operations
 */
export interface ShortlistApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a Shortlist
 *
 * @param value - Value to check
 * @returns True if value is a Shortlist
 */
export function isShortlist(value: unknown): value is Shortlist {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.user_id === 'string' &&
    typeof obj.investor_id === 'string' &&
    typeof obj.added_at === 'string'
  );
}

/**
 * Type guard to check if a value is a ShortlistDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a ShortlistDropdownOption
 */
export function isShortlistDropdownOption(
  value: unknown
): value is ShortlistDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isShortlist(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All shortlist types re-exported for convenience */
export type {
  Shortlist as ShortlistEntity,
  ShortlistCreateRequest as CreateShortlistInput,
  ShortlistUpdateRequest as UpdateShortlistInput,
};
