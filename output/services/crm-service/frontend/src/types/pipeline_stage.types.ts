/**
 * PipelineStage Type Definitions
 *
 * This file contains type definitions for the PipelineStage entity:
 * - PipelineStage: Full entity model with all fields
 * - PipelineStageCreateRequest: Data for creating a new pipeline_stage
 * - PipelineStageUpdateRequest: Partial data for updating a pipeline_stage
 * - PipelineStageListResponse: Paginated list response
 * - PipelineStageFilterOptions: Query parameters for filtering
 * - PipelineStageDropdownOption: Options for select components
 *
 * CRM pipeline stage for investor shortlisting
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:23.993325Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// PipelineStage Entity
// ============================================================================

/**
 * PipelineStage entity model
 *
 * Represents the full pipeline_stage entity as returned by the API.
 * CRM pipeline stage for investor shortlisting
 *
 * @remarks
 * This interface matches the backend PipelineStageDTO schema exactly.
 */
export interface PipelineStage {
  /** Unique identifier (UUID) */
  id: string;

  /** Stage display name */
  name: string;

  /** Stage code for programmatic use */
  code: string;

  /** Display ordering */
  sequence: number;

  /** Hex color for Kanban column */
  color: string | null;

  /** Is Active */
  is_active: boolean;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * PipelineStage creation request data
 *
 * Used when creating a new pipeline_stage. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend PipelineStageCreateDTO schema.
 */
export interface PipelineStageCreateRequest {
  /** Stage display name (required) */
  name: string;

  /** Stage code for programmatic use (required) */
  code: string;

  /** Display ordering (required) */
  sequence: number;

  /** Hex color for Kanban column (optional) */
  color?: string;

  /** Is Active (optional) */
  is_active?: boolean;

}

/**
 * PipelineStage update request data
 *
 * Used when updating an existing pipeline_stage. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend PipelineStageUpdateDTO schema.
 */
export interface PipelineStageUpdateRequest {
  /** Stage display name */
  name?: string;

  /** Stage code for programmatic use */
  code?: string;

  /** Display ordering */
  sequence?: number;

  /** Hex color for Kanban column */
  color?: string | null;

  /** Is Active */
  is_active?: boolean;

}

/**
 * PipelineStage list response
 *
 * Wrapper for paginated pipeline_stage list queries.
 */
export interface PipelineStageListResponse {
  /** Array of pipeline_stage items */
  items: PipelineStage[];

  /** Alternative property name for items (for API compatibility) */
  pipeline_stages?: PipelineStage[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * PipelineStage filter options for list queries
 *
 * Extends BaseEntityFilters with pipeline_stage-specific filtering options.
 */
export interface PipelineStageFilterOptions extends BaseEntityFilters {
}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * PipelineStage dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface PipelineStageDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: PipelineStage;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * PipelineStage summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface PipelineStageSummary {
  /** Unique identifier */
  id: string;

  /** Stage display name */
  name: string;

  /** Stage code for programmatic use */
  code: string;

}

// ============================================================================
// Form Types
// ============================================================================

/**
 * PipelineStage form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type PipelineStageFormData = PipelineStageCreateRequest;

/**
 * Default form values for new pipeline_stage
 *
 * Use this when initializing form state for creating a new pipeline_stage.
 */
export const DEFAULT_PIPELINE_STAGE_FORM_DATA: Partial<PipelineStageFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single pipeline_stage API response
 */
export interface PipelineStageResponse {
  /** The requested pipeline_stage entity */
  data: PipelineStage;
}

/**
 * PipelineStage mutation response (create/update)
 */
export interface PipelineStageMutationResponse {
  /** The created or updated pipeline_stage */
  data: PipelineStage;

  /** Success message */
  message?: string;
}

/**
 * API error response for pipeline_stage operations
 */
export interface PipelineStageApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a PipelineStage
 *
 * @param value - Value to check
 * @returns True if value is a PipelineStage
 */
export function isPipelineStage(value: unknown): value is PipelineStage {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.name === 'string' &&
    typeof obj.code === 'string' &&
    typeof obj.sequence === 'number'
  );
}

/**
 * Type guard to check if a value is a PipelineStageDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a PipelineStageDropdownOption
 */
export function isPipelineStageDropdownOption(
  value: unknown
): value is PipelineStageDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isPipelineStage(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All pipeline_stage types re-exported for convenience */
export type {
  PipelineStage as PipelineStageEntity,
  PipelineStageCreateRequest as CreatePipelineStageInput,
  PipelineStageUpdateRequest as UpdatePipelineStageInput,
};
