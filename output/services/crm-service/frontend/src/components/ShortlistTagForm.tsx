/**
 * ShortlistTagForm Component
 *
 * Form component for creating and editing shortlist_tags.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * Features:
 * - Create and Edit mode support
 * - Form validation with error messages
 * - Material UI components
 * - Loading state handling
 * - Accessible form fields (WCAG 2.1 Level AA)
 * - Modal wrapper support (form_display: modal)
 *
 * Many-to-many junction between shortlists and tags
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:25.389953Z
 */

import React, { useMemo, useEffect } from 'react';
import { useForm, Controller, FormProvider } from 'react-hook-form';
import {
  Alert as MuiAlert,
  Box,
  Grid,
  TextField,
  Button,
  CircularProgress,
  Dialog,
  DialogTitle,
  DialogContent,
  IconButton,
} from '@mui/material';
import { Close as CloseIcon } from '@mui/icons-material';
import type { ShortlistTagCreateRequest, ShortlistTag } from '../types/shortlist_tag.types';
import { useNotification } from '../contexts/NotificationContext';
// Form select components for foreign key fields
import { FormShortlistSelect } from './FormShortlistSelect';
import { FormTagSelect } from './FormTagSelect';

// ============================================================================
// Types
// ============================================================================

/**
 * Form data type for ShortlistTag
 */
export type ShortlistTagFormData = ShortlistTagCreateRequest;

/**
 * Props for ShortlistTagForm component
 */
export interface ShortlistTagFormProps {
  /** Existing shortlist_tag data for edit mode (optional) */
  shortlistTag?: ShortlistTag;
  /** Form submission handler (can be sync or async) */
  onSubmit: (data: ShortlistTagFormData) => void | Promise<void>;
  /** Cancel handler */
  onCancel: () => void;
  /** Loading state (disables form during submission) */
  isLoading?: boolean;
  /** Custom className for the form container */
  className?: string;
}

/**
 * Props for ShortlistTagFormModal component (modal wrapper)
 */
export interface ShortlistTagFormModalProps extends ShortlistTagFormProps {
  /** Whether the modal is open */
  open: boolean;
  /** Handler called when the modal requests to be closed */
  onClose: () => void;
  /** Optional title override for the modal */
  title?: string;
}


// ============================================================================
// Constants
// ============================================================================

/**
 * Default form values for creating a new shortlist_tag
 */
const DEFAULT_FORM_VALUES: Partial<ShortlistTagFormData> = {
  shortlist_id: '',
  tag_id: '',
};

// ============================================================================
// Component
// ============================================================================

/**
 * ShortlistTagForm Component
 *
 * Renders a form for creating or editing shortlist_tags.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * @example
 * ```tsx
 * // Create mode
 * <ShortlistTagForm
 *   onSubmit={handleCreate}
 *   onCancel={handleCancel}
 *   isLoading={isCreating}
 * />
 *
 * // Edit mode
 * <ShortlistTagForm
 *   shortlistTag={existingShortlistTag}
 *   onSubmit={handleUpdate}
 *   onCancel={handleCancel}
 *   isLoading={isUpdating}
 * />
 * ```
 */
export function ShortlistTagForm({
  shortlistTag,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: ShortlistTagFormProps): React.ReactElement {
  // Determine if we're in edit mode
  const isEditMode = Boolean(shortlistTag);

  // Notification context for showing success/error messages
  const { showSuccess, showError } = useNotification();

  // Build default values from existing entity or defaults
  const defaultValues = useMemo<Partial<ShortlistTagFormData>>(() => {
    if (shortlistTag) {
      // Edit mode: use existing entity values
      return {
        shortlist_id: shortlistTag.shortlist_id,
        tag_id: shortlistTag.tag_id,
      };
    }
    // Create mode: use defaults
    return DEFAULT_FORM_VALUES;
  }, [shortlistTag]);

  // Initialize React Hook Form
  const methods = useForm<ShortlistTagFormData>({
    defaultValues,
    mode: 'onBlur',
  });

  const {
    control,
    handleSubmit,
    formState: { errors },
    reset,
  } = methods;

  // Reset form when entity data changes (e.g., switching from create to edit mode)
  useEffect(() => {
    reset(defaultValues);
  }, [defaultValues, reset]);

  // Log validation errors to console for debugging
  useEffect(() => {
    if (Object.keys(errors).length > 0) {
      console.log('[ShortlistTagForm] Validation errors:', errors);
    }
  }, [errors]);
  // -------------------------------------------------------------------------
  // Form Submission
  // -------------------------------------------------------------------------

  const handleFormSubmit = async (data: ShortlistTagFormData) => {
    console.log('[ShortlistTagForm] Submitting:', { data, isEditMode });
    try {
      const result = await onSubmit(data);
      console.log('[ShortlistTagForm] Form submission successful', { data, result, isEditMode });
      // Show success notification for create operation
      if (!isEditMode) {
        showSuccess('ShortlistTag created successfully');
      }
    } catch (error) {
      // Log full error details to console for debugging
      console.error('[ShortlistTagForm] Form submission failed', {
        error,
        errorMessage: error instanceof Error ? error.message : String(error),
        errorStack: error instanceof Error ? error.stack : undefined,
        data,
        isEditMode,
      });
      // Show error notification on failure
      const errorMessage = error instanceof Error ? error.message : 'An unexpected error occurred';
      showError(`Failed to ${isEditMode ? 'update' : 'create'} ShortlistTag: ${errorMessage}`);
    }
  };

  // -------------------------------------------------------------------------
  // Render
  // -------------------------------------------------------------------------

  // Count validation errors
  const errorCount = Object.keys(errors).length;

  return (
    <FormProvider {...methods}>
      <Box
        component="form"
        onSubmit={handleSubmit(handleFormSubmit)}
        className={className}
        data-testid="form-shortlist_tag"
        noValidate
      >
        {/* Error Summary Alert */}
        {errorCount > 0 && (
          <MuiAlert
            severity="error"
            sx={{ mb: 3 }}
            data-testid="form-errors-summary"
          >
            Please fix {errorCount} error{errorCount !== 1 ? 's' : ''} before submitting
          </MuiAlert>
        )}

        <Grid container spacing={3}>
          {/* Shortlist Id field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* Foreign key field - renders as dropdown select */}
            <FormShortlistSelect
              name="shortlist_id"
              label="Shortlist"
              required={false}
              disabled={isLoading}
            />
          </Grid>

          {/* Tag Id field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* Foreign key field - renders as dropdown select */}
            <FormTagSelect
              name="tag_id"
              label="Tag"
              required={false}
              disabled={isLoading}
            />
          </Grid>

          {/* Form Actions */}
          <Grid size={12}>
            <Box display="flex" gap={2} pt={2}>
              <Button
                type="submit"
                variant="contained"
                color="primary"
                disabled={isLoading}
                startIcon={isLoading ? <CircularProgress size={20} /> : undefined}
                data-testid="submit-button"
              >
                {isLoading
                  ? 'Saving...'
                  : isEditMode
                    ? 'Update ShortlistTag'
                    : 'Create ShortlistTag'}
              </Button>

              <Button
                type="button"
                variant="outlined"
                onClick={onCancel}
                disabled={isLoading}
                data-testid="cancel-button"
              >
                Cancel
              </Button>
            </Box>
          </Grid>
        </Grid>
      </Box>
    </FormProvider>
  );
}

// ============================================================================
// Modal Wrapper Component
// ============================================================================

/**
 * ShortlistTagFormModal Component
 *
 * Wraps the ShortlistTagForm in a Material UI Dialog for modal display.
 * Use this component when you want to show the form in a popup dialog.
 *
 * @example
 * ```tsx
 * <ShortlistTagFormModal
 *   open={isModalOpen}
 *   onClose={handleClose}
 *   onSubmit={handleCreate}
 *   onCancel={handleClose}
 *   isLoading={isCreating}
 * />
 * ```
 */
export function ShortlistTagFormModal({
  open,
  onClose,
  title,
  shortlistTag,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: ShortlistTagFormModalProps): React.ReactElement {
  const isEditMode = Boolean(shortlistTag);
  const modalTitle = title ?? (isEditMode ? 'Edit ShortlistTag' : 'Create ShortlistTag');

  // Handle modal close - prevent closing during loading
  const handleClose = () => {
    if (!isLoading) {
      onClose();
    }
  };

  return (
    <Dialog
      open={open}
      onClose={handleClose}
      maxWidth="md"
      fullWidth
      data-testid="modal-shortlist_tag-form"
    >
      <DialogTitle sx={{ m: 0, p: 2, display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        {modalTitle}
        <IconButton
          aria-label="close"
          onClick={handleClose}
          disabled={isLoading}
          data-testid="modal-close-button"
          sx={{ color: (theme) => theme.palette.grey[500] }}
        >
          <CloseIcon />
        </IconButton>
      </DialogTitle>
      <DialogContent dividers>
        <ShortlistTagForm
          shortlistTag={shortlistTag}
          onSubmit={onSubmit}
          onCancel={onCancel}
          isLoading={isLoading}
          className={className}
        />
      </DialogContent>
    </Dialog>
  );
}


// Default export for convenience
export default ShortlistTagForm;
