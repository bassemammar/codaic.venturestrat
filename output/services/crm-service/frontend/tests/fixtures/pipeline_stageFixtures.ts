/**
 * PipelineStage Test Fixtures
 *
 * Test data fixtures for PipelineStage E2E and unit tests.
 * Provides predefined test data and factory functions for generating
 * test pipeline_stages.
 *
 * Fixture patterns:
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields with realistic test values
 * - invalid: Invalid data for validation testing
 * - unique: Unique values using timestamps for test isolation
 *
 * CRM pipeline stage for investor shortlisting
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:24.278474Z
 */

import type { PipelineStageCreateRequest } from '../../types/pipeline_stage.types';

// ============================================================================
// Types
// ============================================================================

/**
 * Test data pattern types
 *
 * @description
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields populated with realistic test data
 * - invalid: Data that should fail validation
 * - unique: Data with timestamps for test isolation
 */
export type TestDataPattern = 'minimal' | 'complete' | 'invalid' | 'unique';

/**
 * PipelineStage test fixture containing all test data patterns
 */
export interface PipelineStageTestFixtures {
  /** Minimal valid pipeline_stage with only required fields */
  minimal: PipelineStageCreateRequest;
  /** Complete pipeline_stage with all fields populated */
  complete: PipelineStageCreateRequest;
  /** Invalid pipeline_stage for testing validation errors */
  invalid: Partial<PipelineStageCreateRequest>;
  /** Unique pipeline_stage with timestamp-based values */
  unique: PipelineStageCreateRequest;
  /** Multiple pipeline_stages for list testing */
  list: PipelineStageCreateRequest[];
}

// ============================================================================
// Helper Functions
// ============================================================================

/**
 * Generate a unique identifier based on current timestamp
 * Useful for creating unique test data that won't conflict
 *
 * @param prefix - Optional prefix for the identifier
 * @returns Unique string identifier
 */
function generateUniqueId(prefix = ''): string {
  const timestamp = Date.now();
  const random = Math.random().toString(36).substring(2, 8);
  return prefix ? `${prefix}-${timestamp}-${random}` : `${timestamp}-${random}`;
}

/**
 * Generate a random string of specified length
 *
 * @param length - Length of the string to generate
 * @param charset - Character set to use (default: alphanumeric)
 * @returns Random string
 */
function randomString(
  length: number,
  charset = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'
): string {
  let result = '';
  for (let i = 0; i < length; i++) {
    result += charset.charAt(Math.floor(Math.random() * charset.length));
  }
  return result;
}

/**
 * Generate a random integer between min and max (inclusive)
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @returns Random integer
 */
function randomInt(min: number, max: number): number {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

/**
 * Generate a random decimal with specified precision
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @param precision - Number of decimal places
 * @returns Random decimal number
 */
function randomDecimal(min: number, max: number, precision = 2): number {
  const value = Math.random() * (max - min) + min;
  const factor = Math.pow(10, precision);
  return Math.round(value * factor) / factor;
}

/**
 * Generate a random date within a range
 *
 * @param startDate - Start of the range
 * @param endDate - End of the range
 * @returns Random date as ISO string
 */
function randomDate(startDate: Date, endDate: Date): string {
  const start = startDate.getTime();
  const end = endDate.getTime();
  const randomTime = start + Math.random() * (end - start);
  return new Date(randomTime).toISOString().split('T')[0];
}

/**
 * Pick a random element from an array
 *
 * @param array - Array to pick from
 * @returns Random element
 */
function randomPick<T>(array: T[]): T {
  return array[Math.floor(Math.random() * array.length)];
}

// ============================================================================
// Static Test Data
// ============================================================================


/**
 * Sample pipeline_stage names for generating realistic test data
 */
const SAMPLE_NAMES = [
  'Test PipelineStage Alpha',
  'Test PipelineStage Beta',
  'Test PipelineStage Gamma',
  'Sample PipelineStage One',
  'Sample PipelineStage Two',
  'Demo PipelineStage',
  'Example PipelineStage',
];

/**
 * Sample codes for generating realistic test data
 */
const SAMPLE_CODES = [
  'TST001',
  'TST002',
  'TST003',
  'DEMO01',
  'DEMO02',
  'SMPL01',
  'SMPL02',
];

/**
 * Sample descriptions for generating realistic test data
 */
const SAMPLE_DESCRIPTIONS = [
  'This is a test pipeline_stage for automated testing.',
  'Sample pipeline_stage created for E2E tests.',
  'Demo pipeline_stage with all fields populated.',
  'Example pipeline_stage for validation testing.',
  'Test data fixture for pipeline_stage operations.',
];

// ============================================================================
// Predefined Fixtures
// ============================================================================

/**
 * Minimal valid pipeline_stage fixture
 *
 * Contains only required fields with minimal valid values.
 * Use this when testing that minimal data is accepted.
 */
export const minimalPipelineStageFixture: PipelineStageCreateRequest = {
  is_active: true,
};

/**
 * Complete pipeline_stage fixture
 *
 * Contains all fields with realistic test values.
 * Use this when testing full CRUD operations.
 */
export const completePipelineStageFixture: PipelineStageCreateRequest = {
  name: 'Test PipelineStage Complete',
  code: 'TST-COMPLETE',
  sequence: 42,
  color: 'Test Color',
  is_active: true,
};

/**
 * Unique pipeline_stage fixture
 *
 * Contains all fields with timestamp-based unique values.
 * Use this when testing to ensure data isolation between test runs.
 * Each field that needs uniqueness includes a timestamp/random component.
 */
export const uniquePipelineStageFixture: PipelineStageCreateRequest = {
  name: `Test PipelineStage ${generateUniqueId()}`,
  code: `TST-${generateUniqueId()}`.substring(0, 20),
  sequence: Math.floor(Math.random() * 1000),
  color: `Color ${generateUniqueId()}`,
  is_active: true,
};

/**
 * Invalid pipeline_stage fixture
 *
 * Contains invalid data that should fail validation.
 * Use this when testing validation error handling.
 * Covers: empty required fields, values out of range, invalid formats.
 */
export const invalidPipelineStageFixture: Partial<PipelineStageCreateRequest> = {
};

/**
 * Invalid data patterns for comprehensive validation testing
 *
 * Contains various invalid data scenarios:
 * - emptyRequired: All required fields left empty
 * - tooShort: String fields below minimum length
 * - tooLong: String fields exceeding maximum length
 * - outOfRangeMin: Numeric fields below minimum value
 * - outOfRangeMax: Numeric fields above maximum value
 * - wrongType: Fields with incorrect data types (as strings for type coercion testing)
 * - specialChars: Fields with potentially problematic characters
 */
export const pipelineStageInvalidPatterns = {
  /** All required string fields empty */
  emptyRequired: {
  } as Partial<PipelineStageCreateRequest>,


  /** String fields exceeding maximum length */
  tooLong: {
    name: 'x'.repeat(150), // max: 100
    code: 'x'.repeat(100), // max: 50
    color: 'x'.repeat(70), // max: 20
  } as Partial<PipelineStageCreateRequest>,



  /** Fields with SQL injection attempts (for security testing) */
  sqlInjection: {
    name: "'; DROP TABLE pipeline_stages; --",
    code: "' OR '1'='1",
  } as Partial<PipelineStageCreateRequest>,

  /** Fields with XSS attempts (for security testing) */
  xssAttempts: {
    name: '<script>alert("XSS")</script>',
  } as Partial<PipelineStageCreateRequest>,

  /** Fields with null values (for nullable field testing) */
  nullValues: {
    color: null,
  } as Partial<PipelineStageCreateRequest>,

  /** Fields with undefined values (for missing field testing) */
  undefinedValues: {
    // Intentionally empty - represents missing fields
  } as Partial<PipelineStageCreateRequest>,
};

/**
 * Collection of pipeline_stage fixtures for list testing
 *
 * Contains multiple pipeline_stages for testing list operations,
 * pagination, filtering, and sorting.
 */
export const pipelineStageListFixtures: PipelineStageCreateRequest[] = [
  {
    name: 'Test PipelineStage Alpha',
    code: 'TST-001',
    sequence: 10,
    color: 'Color 1',
    is_active: true,
  },
  {
    name: 'Test PipelineStage Beta',
    code: 'TST-002',
    sequence: 20,
    color: 'Color 2',
    is_active: true,
  },
  {
    name: 'Test PipelineStage Gamma',
    code: 'TST-003',
    sequence: 30,
    color: 'Color 3',
    is_active: true,
  },
  {
    name: 'Test PipelineStage Delta',
    code: 'TST-004',
    sequence: 40,
    color: 'Color 4',
    is_active: true,
  },
  {
    name: 'Test PipelineStage Epsilon',
    code: 'TST-005',
    sequence: 50,
    color: 'Color 5',
    is_active: false,
  },
];

// ============================================================================
// Fixture Collection
// ============================================================================

/**
 * All pipeline_stage test fixtures
 *
 * @example
 * ```typescript
 * import { pipelineStageFixtures } from '../fixtures/pipeline_stageFixtures';
 *
 * // Use minimal fixture
 * const minData = pipelineStageFixtures.minimal;
 *
 * // Use complete fixture
 * const fullData = pipelineStageFixtures.complete;
 *
 * // Use unique fixture (with timestamp)
 * const uniqueData = generatePipelineStageTestData('unique');
 * ```
 */
export const pipelineStageFixtures: PipelineStageTestFixtures = {
  minimal: minimalPipelineStageFixture,
  complete: completePipelineStageFixture,
  invalid: invalidPipelineStageFixture as PipelineStageCreateRequest,
  unique: uniquePipelineStageFixture,
  list: pipelineStageListFixtures,
};

// ============================================================================
// Factory Functions
// ============================================================================

/**
 * Generate pipeline_stage test data based on pattern
 *
 * @param pattern - The test data pattern to generate
 * @param overrides - Optional field overrides
 * @returns Generated test data
 *
 * @example
 * ```typescript
 * // Generate complete test data
 * const data = generatePipelineStageTestData('complete');
 *
 * // Generate unique test data with overrides
 * const customData = generatePipelineStageTestData('unique', {
 *   is_active: false,
 * });
 * ```
 */
export function generatePipelineStageTestData(
  pattern: TestDataPattern = 'complete',
  overrides: Partial<PipelineStageCreateRequest> = {}
): PipelineStageCreateRequest {
  let baseData: PipelineStageCreateRequest;

  switch (pattern) {
    case 'minimal':
      baseData = { ...minimalPipelineStageFixture };
      break;

    case 'complete':
      baseData = { ...completePipelineStageFixture };
      break;

    case 'invalid':
      // Return invalid data (caller should handle validation failure)
      baseData = { ...invalidPipelineStageFixture } as PipelineStageCreateRequest;
      break;

    case 'unique':
      // Generate fresh unique values for each call
      baseData = {
        ...uniquePipelineStageFixture,
        name: `Test PipelineStage ${generateUniqueId()}`,
        code: `TST-${generateUniqueId()}`.substring(0, 20),
      };
      break;

    default:
      baseData = { ...completePipelineStageFixture };
  }

  return { ...baseData, ...overrides };
}

/**
 * Generate multiple pipeline_stage fixtures
 *
 * @param count - Number of fixtures to generate
 * @param pattern - The test data pattern to use as base
 * @param modifier - Optional function to modify each fixture
 * @returns Array of generated test data
 *
 * @example
 * ```typescript
 * // Generate 5 unique fixtures
 * const fixtures = generateMultiplePipelineStageFixtures(5, 'unique');
 *
 * // Generate fixtures with sequential numbering
 * const numbered = generateMultiplePipelineStageFixtures(3, 'complete', (data, index) => ({
 *   ...data,
 *   name: `PipelineStage #${index + 1}`,
 * }));
 * ```
 */
export function generateMultiplePipelineStageFixtures(
  count: number,
  pattern: TestDataPattern = 'unique',
  modifier?: (data: PipelineStageCreateRequest, index: number) => PipelineStageCreateRequest
): PipelineStageCreateRequest[] {
  const fixtures: PipelineStageCreateRequest[] = [];

  for (let i = 0; i < count; i++) {
    let fixture = generatePipelineStageTestData(pattern);

    if (modifier) {
      fixture = modifier(fixture, i);
    } else if (pattern === 'unique') {
      // Add index to ensure uniqueness even with same timestamp
      fixture.name = `Test PipelineStage ${generateUniqueId()}-${i}`;
      fixture.code = `TST-${i}-${generateUniqueId()}`.substring(0, 20);
    }

    fixtures.push(fixture);
  }

  return fixtures;
}

/**
 * Generate random pipeline_stage test data
 *
 * Creates test data with randomized values within valid ranges.
 * Useful for fuzz testing and edge case discovery.
 *
 * @param overrides - Optional field overrides
 * @returns Randomly generated test data
 *
 * @example
 * ```typescript
 * // Generate random test data
 * const randomData = generateRandomPipelineStageData();
 *
 * // Generate random data with specific field
 * const customRandom = generateRandomPipelineStageData({
 *   is_active: true,
 * });
 * ```
 */
export function generateRandomPipelineStageData(
  overrides: Partial<PipelineStageCreateRequest> = {}
): PipelineStageCreateRequest {
  const data: PipelineStageCreateRequest = {
    name: randomPick(SAMPLE_NAMES),
    code: randomPick(SAMPLE_CODES),
    sequence: randomInt(0, 1000),
    color: randomString(randomInt(None, Math.min(20, 50))),
    is_active: Math.random() > 0.2, // 80% chance of being active
  };

  return { ...data, ...overrides };
}

// ============================================================================
// Validation Test Data
// ============================================================================

/**
 * Edge case test data for boundary testing
 *
 * Contains values at the boundaries of valid ranges.
 */
export const pipelineStageEdgeCases = {
  /** name at maximum length (100 chars) */
  nameMaxLength: 'x'.repeat(100),
  /** code at maximum length (50 chars) */
  codeMaxLength: 'x'.repeat(50),
  /** color at maximum length (20 chars) */
  colorMaxLength: 'x'.repeat(20),
};

/**
 * Special character test data for input sanitization testing
 */
export const pipelineStageSpecialCharacterData: Partial<PipelineStageCreateRequest> = {
  name: 'Test <script>alert("xss")</script> & "quotes" \'apostrophe\'',
  code: 'CODE-WITH-SPECIAL-CHARS!@#',
};

/**
 * Whitespace test data for trimming validation
 */
export const pipelineStageWhitespaceData: Partial<PipelineStageCreateRequest> = {
  name: '  Padded Name  ',
  code: ' PADDED-CODE ',
};

// ============================================================================
// Exports
// ============================================================================

// Default export - main fixture collection
export default pipelineStageFixtures;
