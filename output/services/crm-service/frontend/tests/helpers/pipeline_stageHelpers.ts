/**
 * PipelineStage E2E Test Helpers
 *
 * Utility functions for PipelineStage E2E tests using Playwright.
 * Provides form filling, navigation, and entity manipulation helpers.
 *
 * Form Display Mode: modal
 *
 * Helper functions:
 * - fillPipelineStageForm: Fill all form fields with test data
 * - waitForPipelineStageList: Wait for list page to load
 * - waitForPipelineStageForm: Wait for form to be visible and ready
 * - waitForPipelineStageModal: Wait for modal form to open
 * - closePipelineStageModal: Close the modal form
 * - findPipelineStageInList: Locate entity row by display value
 * - deletePipelineStageByName: Delete entity and confirm
 * - getFormField: Get form field locator by name
 * - getFormErrorMessage: Get validation error message
 *
 * CRM pipeline stage for investor shortlisting
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:24.402345Z
 */

import { Page, Locator, expect } from '@playwright/test';
import type { PipelineStageCreateRequest } from '../../types/pipeline_stage.types';

// ============================================================================
// Configuration
// ============================================================================

/**
 * Default timeout for waiting operations (ms)
 */
const DEFAULT_TIMEOUT = 10000;

/**
 * Short timeout for quick checks (ms)
 */
const SHORT_TIMEOUT = 3000;

/**
 * Debounce delay for form interactions (ms)
 */
const DEBOUNCE_DELAY = 100;

// ============================================================================
// Form Field Helpers
// ============================================================================

/**
 * Get a form field locator by field name
 *
 * Supports different field types:
 * - text/number fields: [data-testid="field-{fieldName}"]
 * - select fields: [data-testid="select-{fieldName}"]
 * - checkbox fields: [data-testid="checkbox-{fieldName}"]
 *
 * @param page - Playwright page object
 * @param fieldName - Name of the field (snake_case or camelCase)
 * @returns Locator for the form field
 *
 * @example
 * ```typescript
 * const nameField = getFormField(page, 'name');
 * await nameField.fill('Test Value');
 * ```
 */
export function getFormField(page: Page, fieldName: string): Locator {
  // Convert camelCase to kebab-case for test IDs
  const kebabName = fieldName.replace(/_/g, '-').replace(/([a-z])([A-Z])/g, '$1-$2').toLowerCase();

  // Try text/number field first (most common)
  return page.locator(`[data-testid="field-${kebabName}"] input, [data-testid="field-${kebabName}"] textarea`);
}

/**
 * Get a select field locator by field name
 *
 * @param page - Playwright page object
 * @param fieldName - Name of the select field
 * @returns Locator for the select trigger element
 *
 * @example
 * ```typescript
 * const statusSelect = getSelectField(page, 'status');
 * await statusSelect.click();
 * await page.locator('[role="option"]').filter({ hasText: 'Active' }).click();
 * ```
 */
export function getSelectField(page: Page, fieldName: string): Locator {
  const kebabName = fieldName.replace(/_/g, '-').replace(/([a-z])([A-Z])/g, '$1-$2').toLowerCase();
  return page.locator(`[data-testid="select-${kebabName}"]`);
}

/**
 * Get a checkbox field locator by field name
 *
 * @param page - Playwright page object
 * @param fieldName - Name of the checkbox field
 * @returns Locator for the checkbox input element
 *
 * @example
 * ```typescript
 * const isActiveCheckbox = getCheckboxField(page, 'is_active');
 * await isActiveCheckbox.check();
 * ```
 */
export function getCheckboxField(page: Page, fieldName: string): Locator {
  const kebabName = fieldName.replace(/_/g, '-').replace(/([a-z])([A-Z])/g, '$1-$2').toLowerCase();
  return page.locator(`[data-testid="checkbox-${kebabName}"]`);
}

/**
 * Get the validation error message for a form field
 *
 * @param page - Playwright page object
 * @param fieldName - Name of the field with error
 * @returns Error message text or null if no error
 *
 * @example
 * ```typescript
 * const error = await getFormErrorMessage(page, 'email');
 * expect(error).toContain('Invalid email');
 * ```
 */
export async function getFormErrorMessage(page: Page, fieldName: string): Promise<string | null> {
  const kebabName = fieldName.replace(/_/g, '-').replace(/([a-z])([A-Z])/g, '$1-$2').toLowerCase();
  const errorLocator = page.locator(`[data-testid-error="field-${kebabName}-error"]`);

  try {
    await errorLocator.waitFor({ state: 'visible', timeout: SHORT_TIMEOUT });
    return await errorLocator.textContent();
  } catch {
    // No error message visible
    return null;
  }
}

/**
 * Check if a form field has a validation error
 *
 * @param page - Playwright page object
 * @param fieldName - Name of the field to check
 * @returns True if field has error, false otherwise
 */
export async function hasFieldError(page: Page, fieldName: string): Promise<boolean> {
  const error = await getFormErrorMessage(page, fieldName);
  return error !== null;
}

/**
 * Get all form validation errors as a map
 *
 * @param page - Playwright page object
 * @returns Map of field names to error messages
 */
export async function getAllFormErrors(page: Page): Promise<Map<string, string>> {
  const errors = new Map<string, string>();
  const errorElements = page.locator('[data-testid-error]');
  const count = await errorElements.count();

  for (let i = 0; i < count; i++) {
    const element = errorElements.nth(i);
    const testId = await element.getAttribute('data-testid-error');
    const message = await element.textContent();

    if (testId && message) {
      // Extract field name from testId (format: field-{name}-error)
      const match = testId.match(/^field-(.+)-error$/);
      if (match) {
        errors.set(match[1], message);
      }
    }
  }

  return errors;
}

// ============================================================================
// Form Filling Helpers
// ============================================================================

/**
 * Fill the PipelineStage form with provided test data
 *
 * Handles all field types:
 * - Text fields: Direct input
 * - Number fields: Converts to string and inputs
 * - Boolean fields: Checkbox check/uncheck
 * - Enum/Select fields: Dropdown selection
 *
 * @param page - Playwright page object
 * @param data - Test data to fill the form with
 * @param options - Optional configuration
 * @param options.clearFirst - Clear fields before filling (default: true)
 * @param options.skipReadonly - Skip readonly fields (default: true)
 *
 * @example
 * ```typescript
 * const testData = generatePipelineStageTestData('complete');
 * await fillPipelineStageForm(page, testData);
 * ```
 */
export async function fillPipelineStageForm(
  page: Page,
  data: Partial<PipelineStageCreateRequest>,
  options: { clearFirst?: boolean; skipReadonly?: boolean } = {}
): Promise<void> {
  const { clearFirst = true, skipReadonly = true } = options;

  console.log(`[PipelineStageHelpers] Filling form with data:`, JSON.stringify(data, null, 2));

  // Fill name field
  if (data.name !== undefined) {
    // name is a text field
    const nameField = getFormField(page, 'name');
    if (clearFirst) {
      await nameField.clear();
    }
    await nameField.fill(data.name || '');
  }

  // Fill code field
  if (data.code !== undefined) {
    // code is a text field
    const codeField = getFormField(page, 'code');
    if (clearFirst) {
      await codeField.clear();
    }
    await codeField.fill(data.code || '');
  }

  // Fill sequence field
  if (data.sequence !== undefined) {
    // sequence is a number field
    const sequenceField = getFormField(page, 'sequence');
    if (clearFirst) {
      await sequenceField.clear();
    }
    await sequenceField.fill(String(data.sequence));
  }

  // Fill color field
  if (data.color !== undefined) {
    // color is a text field
    const colorField = getFormField(page, 'color');
    if (clearFirst) {
      await colorField.clear();
    }
    await colorField.fill(data.color || '');
  }

  // Fill is_active checkbox (special handling)
  if (data.is_active !== undefined) {
    const isActiveCheckbox = getCheckboxField(page, 'is_active');
    const isChecked = await isActiveCheckbox.isChecked();

    if (data.is_active && !isChecked) {
      await isActiveCheckbox.check();
    } else if (!data.is_active && isChecked) {
      await isActiveCheckbox.uncheck();
    }
  }

  console.log(`[PipelineStageHelpers] Form filled successfully`);
}

/**
 * Fill a single form field by name
 *
 * @param page - Playwright page object
 * @param fieldName - Name of the field to fill
 * @param value - Value to fill
 * @param fieldType - Type of the field (text, number, boolean, select)
 */
export async function fillFormField(
  page: Page,
  fieldName: string,
  value: string | number | boolean,
  fieldType: 'text' | 'number' | 'boolean' | 'select' = 'text'
): Promise<void> {
  switch (fieldType) {
    case 'boolean':
      const checkbox = getCheckboxField(page, fieldName);
      const isChecked = await checkbox.isChecked();
      if (value && !isChecked) {
        await checkbox.check();
      } else if (!value && isChecked) {
        await checkbox.uncheck();
      }
      break;

    case 'select':
      const select = getSelectField(page, fieldName);
      await select.click();
      await page.waitForTimeout(DEBOUNCE_DELAY);
      await page.locator('[role="option"]').filter({ hasText: String(value) }).click();
      break;

    case 'number':
    case 'text':
    default:
      const field = getFormField(page, fieldName);
      await field.clear();
      await field.fill(String(value));
      break;
  }
}

// ============================================================================
// Navigation Helpers
// ============================================================================

/**
 * Wait for the PipelineStage list page to fully load
 *
 * Waits for:
 * - Page heading to be visible
 * - Add button to be visible
 * - Loading indicators to disappear
 * - Table or empty state to be visible
 *
 * @param page - Playwright page object
 * @param timeout - Maximum wait time in ms (default: 10000)
 *
 * @example
 * ```typescript
 * await page.goto('/pipeline_stages');
 * await waitForPipelineStageList(page);
 * ```
 */
export async function waitForPipelineStageList(page: Page, timeout = DEFAULT_TIMEOUT): Promise<void> {
  console.log(`[PipelineStageHelpers] Waiting for PipelineStage list to load...`);

  // Wait for add button to be visible (indicates page is loaded)
  await expect(
    page.locator('[data-testid="btn-add-pipeline_stage"]')
  ).toBeVisible({ timeout });

  // Wait for any loading indicators to disappear
  const loadingIndicator = page.locator('[data-testid="loading"], [role="progressbar"]');
  await loadingIndicator.waitFor({ state: 'hidden', timeout }).catch(() => {
    // Loading indicator might not exist, that's okay
  });

  // Wait for either table or empty state to be visible
  const table = page.locator('table');
  const emptyState = page.getByText(/no pipeline_stages found/i);

  await Promise.race([
    table.waitFor({ state: 'visible', timeout }).catch(() => {}),
    emptyState.waitFor({ state: 'visible', timeout }).catch(() => {}),
  ]);

  console.log(`[PipelineStageHelpers] PipelineStage list loaded`);
}

/**
 * Wait for the PipelineStage form to be visible and ready
 *
 * @param page - Playwright page object
 * @param timeout - Maximum wait time in ms
 */
export async function waitForPipelineStageForm(page: Page, timeout = DEFAULT_TIMEOUT): Promise<void> {
  console.log(`[PipelineStageHelpers] Waiting for PipelineStage form...`);

  await expect(
    page.locator('[data-testid="form-pipeline_stage"]')
  ).toBeVisible({ timeout });

  // Wait for form fields to be interactive
  await page.locator('[data-testid="submit-button"]').waitFor({ state: 'visible', timeout });

  console.log(`[PipelineStageHelpers] PipelineStage form ready`);
}

/**
 * Wait for the PipelineStage form modal to open
 *
 * Waits for the modal dialog wrapper and the form inside it to be visible.
 * Use this function when the entity is configured with form_display: modal.
 *
 * @param page - Playwright page object
 * @param timeout - Maximum wait time in ms
 *
 * @example
 * ```typescript
 * await page.locator('[data-testid="btn-add-pipeline_stage"]').click();
 * await waitForPipelineStageModal(page);
 * // Modal is now open and ready for interaction
 * ```
 */
export async function waitForPipelineStageModal(page: Page, timeout = DEFAULT_TIMEOUT): Promise<void> {
  console.log(`[PipelineStageHelpers] Waiting for PipelineStage modal to open...`);

  // Wait for the modal wrapper to be visible
  await expect(
    page.locator('[data-testid="modal-pipeline_stage-form"]')
  ).toBeVisible({ timeout });

  // Wait for the form inside the modal to be visible
  await expect(
    page.locator('[data-testid="form-pipeline_stage"]')
  ).toBeVisible({ timeout });

  // Wait for form fields to be interactive
  await page.locator('[data-testid="submit-button"]').waitFor({ state: 'visible', timeout });

  console.log(`[PipelineStageHelpers] PipelineStage modal opened`);
}

/**
 * Close the PipelineStage form modal
 *
 * Closes the modal using the close button and waits for it to disappear.
 * Use this function to dismiss the modal without submitting the form.
 *
 * @param page - Playwright page object
 * @param timeout - Maximum wait time in ms
 *
 * @example
 * ```typescript
 * // After opening the modal and deciding to cancel
 * await closePipelineStageModal(page);
 * // Modal is now closed
 * ```
 */
export async function closePipelineStageModal(page: Page, timeout = DEFAULT_TIMEOUT): Promise<void> {
  console.log(`[PipelineStageHelpers] Closing PipelineStage modal...`);

  // Click the modal close button
  const closeButton = page.locator('[data-testid="modal-close-button"]');
  if (await closeButton.isVisible()) {
    await closeButton.click();
  } else {
    // Fallback: try the cancel button
    await page.locator('[data-testid="cancel-button"]').click();
  }

  // Wait for modal to be hidden
  await expect(
    page.locator('[data-testid="modal-pipeline_stage-form"]')
  ).not.toBeVisible({ timeout });

  console.log(`[PipelineStageHelpers] PipelineStage modal closed`);
}


/**
 * Navigate to the PipelineStage list page
 *
 * @param page - Playwright page object
 * @param baseUrl - Base URL of the application
 */
export async function navigateToPipelineStageList(page: Page, baseUrl = 'http://localhost:5173'): Promise<void> {
  await page.goto(`${baseUrl}/pipeline_stages`);
  await waitForPipelineStageList(page);
}

/**
 * Navigate to create a new PipelineStage
 *
 * Clicks the add button and waits for the appropriate form display:
 * - Modal: Waits for modal dialog to open
 * - Drawer: Waits for drawer panel to open
 * - Inline: Waits for inline form to be visible
 *
 * @param page - Playwright page object
 */
export async function navigateToCreatePipelineStage(page: Page): Promise<void> {
  await page.locator('[data-testid="btn-add-pipeline_stage"]').click();
  await waitForPipelineStageModal(page);
}

// ============================================================================
// Entity Finding Helpers
// ============================================================================

/**
 * Find a PipelineStage row in the list by display value
 *
 * Searches the table for a row containing the specified text.
 * Returns the row locator if found, or null if not found.
 *
 * @param page - Playwright page object
 * @param displayValue - Value to search for (name, code, or other identifier)
 * @param column - Optional column index to search in (0-based)
 * @returns Locator for the table row, or null if not found
 *
 * @example
 * ```typescript
 * const row = await findPipelineStageInList(page, 'Test Currency');
 * if (row) {
 *   await row.locator('[data-testid^="btn-edit"]').click();
 * }
 * ```
 */
export async function findPipelineStageInList(
  page: Page,
  displayValue: string,
  column?: number
): Promise<Locator | null> {
  console.log(`[PipelineStageHelpers] Finding PipelineStage with value: ${displayValue}`);

  // Wait for table to be visible
  const table = page.locator('table');
  await table.waitFor({ state: 'visible', timeout: SHORT_TIMEOUT }).catch(() => null);

  if (!(await table.isVisible())) {
    console.log(`[PipelineStageHelpers] Table not visible`);
    return null;
  }

  // Search in table rows
  const rows = page.locator('tbody tr');
  const rowCount = await rows.count();

  for (let i = 0; i < rowCount; i++) {
    const row = rows.nth(i);
    const rowText = await row.textContent();

    if (rowText && rowText.includes(displayValue)) {
      console.log(`[PipelineStageHelpers] Found PipelineStage at row ${i}`);
      return row;
    }

    // Also check specific column if provided
    if (column !== undefined) {
      const cell = row.locator('td').nth(column);
      const cellText = await cell.textContent();
      if (cellText && cellText.includes(displayValue)) {
        console.log(`[PipelineStageHelpers] Found PipelineStage at row ${i}, column ${column}`);
        return row;
      }
    }
  }

  console.log(`[PipelineStageHelpers] PipelineStage not found: ${displayValue}`);
  return null;
}

/**
 * Check if a PipelineStage exists in the list
 *
 * @param page - Playwright page object
 * @param displayValue - Value to search for
 * @returns True if entity exists in list, false otherwise
 */
export async function pipelineStageExistsInList(page: Page, displayValue: string): Promise<boolean> {
  const row = await findPipelineStageInList(page, displayValue);
  return row !== null;
}

/**
 * Get the count of PipelineStages in the list
 *
 * @param page - Playwright page object
 * @returns Number of entities in the table
 */
export async function getPipelineStageCount(page: Page): Promise<number> {
  const rows = page.locator('tbody tr');
  return await rows.count();
}

// ============================================================================
// Entity Action Helpers
// ============================================================================

/**
 * Delete a PipelineStage by its display name
 *
 * Finds the entity in the list and clicks the delete button.
 * Handles the confirmation dialog.
 *
 * @param page - Playwright page object
 * @param displayValue - Display value of the entity to delete
 * @returns True if deletion was initiated, false if entity not found
 *
 * @example
 * ```typescript
 * const deleted = await deletePipelineStageByName(page, 'Test Currency');
 * expect(deleted).toBeTruthy();
 * ```
 */
export async function deletePipelineStageByName(page: Page, displayValue: string): Promise<boolean> {
  console.log(`[PipelineStageHelpers] Deleting PipelineStage: ${displayValue}`);

  const row = await findPipelineStageInList(page, displayValue);

  if (!row) {
    console.log(`[PipelineStageHelpers] PipelineStage not found for deletion: ${displayValue}`);
    return false;
  }

  // Find and click the delete button in this row
  const deleteButton = row.locator('[data-testid^="btn-delete-pipeline_stage"]');

  if (!(await deleteButton.isVisible())) {
    console.log(`[PipelineStageHelpers] Delete button not visible for: ${displayValue}`);
    return false;
  }

  // Set up dialog handler for confirmation
  page.once('dialog', async (dialog) => {
    console.log(`[PipelineStageHelpers] Confirming delete dialog`);
    await dialog.accept();
  });

  await deleteButton.click();

  console.log(`[PipelineStageHelpers] Delete initiated for: ${displayValue}`);
  return true;
}

/**
 * Edit a PipelineStage by its display name
 *
 * Opens the edit form for the specified entity. The form will be displayed
 * according to the configured display mode (modal).
 *
 * @param page - Playwright page object
 * @param displayValue - Display value of the entity to edit
 * @returns True if edit form was opened, false if entity not found
 */
export async function editPipelineStageByName(page: Page, displayValue: string): Promise<boolean> {
  console.log(`[PipelineStageHelpers] Editing PipelineStage: ${displayValue}`);

  const row = await findPipelineStageInList(page, displayValue);

  if (!row) {
    console.log(`[PipelineStageHelpers] PipelineStage not found for editing: ${displayValue}`);
    return false;
  }

  const editButton = row.locator('[data-testid^="btn-edit-pipeline_stage"]');

  if (!(await editButton.isVisible())) {
    console.log(`[PipelineStageHelpers] Edit button not visible for: ${displayValue}`);
    return false;
  }

  await editButton.click();
  await waitForPipelineStageModal(page);

  console.log(`[PipelineStageHelpers] Edit form opened for: ${displayValue}`);
  return true;
}

/**
 * View PipelineStage details by its display name
 *
 * @param page - Playwright page object
 * @param displayValue - Display value of the entity to view
 * @returns True if view was opened, false if entity not found
 */
export async function viewPipelineStageByName(page: Page, displayValue: string): Promise<boolean> {
  console.log(`[PipelineStageHelpers] Viewing PipelineStage: ${displayValue}`);

  const row = await findPipelineStageInList(page, displayValue);

  if (!row) {
    return false;
  }

  const viewButton = row.locator('[data-testid^="btn-view-pipeline_stage"]');

  if (await viewButton.isVisible()) {
    await viewButton.click();
    return true;
  }

  // Fallback: try clicking the row itself
  await row.click();
  return true;
}

// ============================================================================
// Notification Helpers
// ============================================================================

/**
 * Wait for a success notification to appear
 *
 * @param page - Playwright page object
 * @param timeout - Maximum wait time in ms
 */
export async function waitForSuccessNotification(page: Page, timeout = DEFAULT_TIMEOUT): Promise<void> {
  await expect(
    page.locator('[data-testid="notification-success"]')
  ).toBeVisible({ timeout });
}

/**
 * Wait for an error notification to appear
 *
 * @param page - Playwright page object
 * @param timeout - Maximum wait time in ms
 */
export async function waitForErrorNotification(page: Page, timeout = DEFAULT_TIMEOUT): Promise<void> {
  await expect(
    page.locator('[data-testid="notification-error"]')
  ).toBeVisible({ timeout });
}

/**
 * Close any visible notification
 *
 * @param page - Playwright page object
 */
export async function closeNotification(page: Page): Promise<void> {
  const closeButtons = page.locator('[data-testid^="notification-"] button[aria-label="Close"]');
  const count = await closeButtons.count();

  for (let i = 0; i < count; i++) {
    const button = closeButtons.nth(i);
    if (await button.isVisible()) {
      await button.click();
    }
  }
}

/**
 * Get the notification message text
 *
 * @param page - Playwright page object
 * @param type - Type of notification to check (success, error, warning, info)
 * @returns Notification message text or null
 */
export async function getNotificationMessage(
  page: Page,
  type: 'success' | 'error' | 'warning' | 'info' = 'success'
): Promise<string | null> {
  const notification = page.locator(`[data-testid="notification-${type}"]`);

  if (await notification.isVisible()) {
    return await notification.textContent();
  }

  return null;
}

// ============================================================================
// Form Submission Helpers
// ============================================================================

/**
 * Submit the PipelineStage form and wait for response
 *
 * @param page - Playwright page object
 * @param expectSuccess - Whether to expect success (default: true)
 * @returns True if form was submitted (success or expected error)
 */
export async function submitPipelineStageForm(
  page: Page,
  expectSuccess = true
): Promise<boolean> {
  console.log(`[PipelineStageHelpers] Submitting form, expecting ${expectSuccess ? 'success' : 'error'}`);

  const submitButton = page.locator('[data-testid="submit-button"]');
  await submitButton.click();

  if (expectSuccess) {
    await waitForSuccessNotification(page);
    console.log(`[PipelineStageHelpers] Form submitted successfully`);
    return true;
  } else {
    // Wait for either error notification or form errors
    await Promise.race([
      waitForErrorNotification(page).catch(() => {}),
      page.locator('[data-testid="form-errors-summary"]').waitFor({ state: 'visible' }).catch(() => {}),
    ]);
    console.log(`[PipelineStageHelpers] Form submission failed as expected`);
    return true;
  }
}

/**
 * Cancel the PipelineStage form and return to list
 *
 * @param page - Playwright page object
 */
export async function cancelPipelineStageForm(page: Page): Promise<void> {
  console.log(`[PipelineStageHelpers] Cancelling form`);

  const cancelButton = page.locator('[data-testid="cancel-button"]');
  await cancelButton.click();
  await waitForPipelineStageList(page);

  console.log(`[PipelineStageHelpers] Form cancelled, back to list`);
}

// ============================================================================
// CRUD Workflow Helpers
// ============================================================================

/**
 * Create a new PipelineStage with provided data
 *
 * Complete workflow: navigate to form, fill data, submit, verify success
 *
 * @param page - Playwright page object
 * @param data - Test data to create with
 * @returns True if creation was successful
 *
 * @example
 * ```typescript
 * const testData = generatePipelineStageTestData('complete');
 * const created = await createPipelineStage(page, testData);
 * expect(created).toBeTruthy();
 * ```
 */
export async function createPipelineStage(
  page: Page,
  data: Partial<PipelineStageCreateRequest>
): Promise<boolean> {
  console.log(`[PipelineStageHelpers] Creating new PipelineStage`);

  try {
    // Navigate to create form
    await navigateToCreatePipelineStage(page);

    // Fill form with data
    await fillPipelineStageForm(page, data);

    // Submit and wait for success
    await submitPipelineStageForm(page, true);

    // Wait for list to reload
    await waitForPipelineStageList(page);

    console.log(`[PipelineStageHelpers] PipelineStage created successfully`);
    return true;
  } catch (error) {
    console.error(`[PipelineStageHelpers] Failed to create PipelineStage:`, error);
    return false;
  }
}

/**
 * Update an existing PipelineStage
 *
 * @param page - Playwright page object
 * @param displayValue - Display value to find the entity
 * @param data - Updated data
 * @returns True if update was successful
 */
export async function updatePipelineStage(
  page: Page,
  displayValue: string,
  data: Partial<PipelineStageCreateRequest>
): Promise<boolean> {
  console.log(`[PipelineStageHelpers] Updating PipelineStage: ${displayValue}`);

  try {
    // Find and open edit form
    const opened = await editPipelineStageByName(page, displayValue);
    if (!opened) {
      return false;
    }

    // Fill form with updated data
    await fillPipelineStageForm(page, data, { clearFirst: true });

    // Submit and wait for success
    await submitPipelineStageForm(page, true);

    // Wait for list to reload
    await waitForPipelineStageList(page);

    console.log(`[PipelineStageHelpers] PipelineStage updated successfully`);
    return true;
  } catch (error) {
    console.error(`[PipelineStageHelpers] Failed to update PipelineStage:`, error);
    return false;
  }
}

/**
 * Delete a PipelineStage and verify deletion
 *
 * @param page - Playwright page object
 * @param displayValue - Display value to find the entity
 * @returns True if deletion was successful
 */
export async function deletePipelineStage(
  page: Page,
  displayValue: string
): Promise<boolean> {
  console.log(`[PipelineStageHelpers] Deleting PipelineStage: ${displayValue}`);

  try {
    const deleted = await deletePipelineStageByName(page, displayValue);
    if (!deleted) {
      return false;
    }

    // Wait for success notification
    await waitForSuccessNotification(page);

    // Verify entity is removed from list
    await waitForPipelineStageList(page);
    const stillExists = await pipelineStageExistsInList(page, displayValue);

    if (stillExists) {
      console.error(`[PipelineStageHelpers] PipelineStage still exists after deletion`);
      return false;
    }

    console.log(`[PipelineStageHelpers] PipelineStage deleted successfully`);
    return true;
  } catch (error) {
    console.error(`[PipelineStageHelpers] Failed to delete PipelineStage:`, error);
    return false;
  }
}

// ============================================================================
// Search and Filter Helpers
// ============================================================================

/**
 * Search for PipelineStages by query
 *
 * @param page - Playwright page object
 * @param query - Search query string
 */
export async function searchPipelineStages(page: Page, query: string): Promise<void> {
  console.log(`[PipelineStageHelpers] Searching for: ${query}`);

  const searchInput = page.getByPlaceholder(/search/i);
  await searchInput.fill(query);

  // Wait for debounced search
  await page.waitForTimeout(700);
}

/**
 * Clear the search filter
 *
 * @param page - Playwright page object
 */
export async function clearSearch(page: Page): Promise<void> {
  const searchInput = page.getByPlaceholder(/search/i);
  await searchInput.clear();
  await page.waitForTimeout(700);
}

// Note: All functions are already exported via 'export function' declarations above.
