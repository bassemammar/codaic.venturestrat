"""Tag Application Service - Generated by Code Generator v1.0.0"""

from typing import List, Optional
from uuid import UUID
import structlog

from crm_service.infrastructure.repositories.tag_repository import TagRepository
from crm_service.infrastructure.orm.tag import Tag
from crm_service.schemas.tag import TagCreate, TagUpdate, TagResponse
from crm_service.integrations.events import event_publisher
from crm_service.integrations.metrics import entity_operations_total

logger = structlog.get_logger(__name__)


class TagService:
    """Application service for Tag use cases."""

    def __init__(self, session=None):
        """Initialize service with repository."""
        self.repository = TagRepository(session)

    async def create_tag(self, data: TagCreate, tenant_id: str = None) -> TagResponse:
        """Create new Tag."""
        try:
            data_dict = data.model_dump()
            if tenant_id:
                data_dict["tenant_id"] = tenant_id

            # BaseModel.create() returns the created instance directly
            created = await self.repository.create(**data_dict)

            # Publish event
            await event_publisher.publish(
                entity_name="tag",
                action="created",
                data=created.to_dict(),
                tenant_id=tenant_id,
            )

            # Track metric
            entity_operations_total.labels(entity="tag", operation="create", status="success").inc()

            # Convert BaseModel to dict for response
            return TagResponse(**created.to_dict())

        except Exception as e:
            entity_operations_total.labels(entity="tag", operation="create", status="error").inc()
            logger.error("create_tag_failed", error=str(e))
            raise

    async def get_tag(self, id: UUID) -> Optional[TagResponse]:
        """Get Tag by ID."""
        orm_obj = await self.repository.get_by_id(id)
        return TagResponse(**orm_obj.to_dict()) if orm_obj else None

    async def list_tags(self, skip: int = 0, limit: int = 100) -> List[TagResponse]:
        """List Tags with pagination."""
        orm_objs = await self.repository.get_all(skip, limit)
        return [TagResponse(**obj.to_dict()) for obj in orm_objs]

    async def update_tag(self, id: UUID, data: TagUpdate) -> Optional[TagResponse]:
        """Update Tag."""
        try:
            orm_obj = await self.repository.get_by_id(id)
            if not orm_obj:
                return None

            # Update fields from DTO
            for key, value in data.model_dump(exclude_unset=True).items():
                setattr(orm_obj, key, value)

            # BaseModel.save() returns self
            updated = await self.repository.update(orm_obj)

            # Publish event
            await event_publisher.publish(
                entity_name="tag",
                action="updated",
                data=updated.to_dict(),
                tenant_id=getattr(updated, "tenant_id", None),
            )

            # Track metric
            entity_operations_total.labels(entity="tag", operation="update", status="success").inc()

            return TagResponse(**updated.to_dict())

        except Exception as e:
            entity_operations_total.labels(entity="tag", operation="update", status="error").inc()
            logger.error("update_tag_failed", id=str(id), error=str(e))
            raise

    async def delete_tag(self, id: UUID) -> bool:
        """Delete Tag."""
        try:
            # Get entity before deletion
            orm_obj = await self.repository.get_by_id(id)
            if not orm_obj:
                return False

            entity_data = orm_obj.to_dict()

            # Delete
            deleted = await self.repository.delete(id)

            if deleted:
                # Publish event
                await event_publisher.publish(
                    entity_name="tag",
                    action="deleted",
                    data=entity_data,
                    tenant_id=entity_data.get("tenant_id"),
                )

                # Track metric
                entity_operations_total.labels(
                    entity="tag", operation="delete", status="success"
                ).inc()

            return deleted

        except Exception as e:
            entity_operations_total.labels(entity="tag", operation="delete", status="error").inc()
            logger.error("delete_tag_failed", id=str(id), error=str(e))
            raise

    async def search_tags(
        self, domain: list = None, skip: int = 0, limit: int = 100
    ) -> List[TagResponse]:
        """
        Search Tags with domain filtering.

        Args:
            domain: domain filter (e.g., [('field', '=', 'value')])
            skip: Offset for pagination
            limit: Maximum records to return

        Returns:
            List of matching Tag instances
        """
        orm_objs = await self.repository.search(domain, skip, limit)
        return [TagResponse(**obj.to_dict()) for obj in orm_objs]

    async def count_tags(self, domain: list = None) -> int:
        """
        Count Tags matching domain.

        Args:
            domain: Optional domain filter

        Returns:
            Number of matching records
        """
        return await self.repository.count(domain)

    async def bulk_create_tags(self, records_data: List[TagCreate]) -> List[TagResponse]:
        """
        Create multiple Tags efficiently.

        Args:
            records_data: List of create DTOs

        Returns:
            List of created Tag instances
        """
        # Convert DTOs to dicts
        data_dicts = [record.model_dump() for record in records_data]

        # Bulk create
        created_objs = await self.repository.bulk_create(data_dicts)

        return [TagResponse(**obj.to_dict()) for obj in created_objs]
