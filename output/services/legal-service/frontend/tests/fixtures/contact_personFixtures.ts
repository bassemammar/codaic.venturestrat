/**
 * ContactPerson Test Fixtures
 *
 * Test data fixtures for ContactPerson E2E and unit tests.
 * Provides predefined test data and factory functions for generating
 * test contact_persons.
 *
 * Fixture patterns:
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields with realistic test values
 * - invalid: Invalid data for validation testing
 * - unique: Unique values using timestamps for test isolation
 *
 * Individual associated with a legal entity — founder, director, signatory, or counterparty
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:50.416845Z
 */

import type { ContactPersonCreateRequest } from '../../types/contact_person.types';

// ============================================================================
// Types
// ============================================================================

/**
 * Test data pattern types
 *
 * @description
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields populated with realistic test data
 * - invalid: Data that should fail validation
 * - unique: Data with timestamps for test isolation
 */
export type TestDataPattern = 'minimal' | 'complete' | 'invalid' | 'unique';

/**
 * ContactPerson test fixture containing all test data patterns
 */
export interface ContactPersonTestFixtures {
  /** Minimal valid contact_person with only required fields */
  minimal: ContactPersonCreateRequest;
  /** Complete contact_person with all fields populated */
  complete: ContactPersonCreateRequest;
  /** Invalid contact_person for testing validation errors */
  invalid: Partial<ContactPersonCreateRequest>;
  /** Unique contact_person with timestamp-based values */
  unique: ContactPersonCreateRequest;
  /** Multiple contact_persons for list testing */
  list: ContactPersonCreateRequest[];
}

// ============================================================================
// Helper Functions
// ============================================================================

/**
 * Generate a unique identifier based on current timestamp
 * Useful for creating unique test data that won't conflict
 *
 * @param prefix - Optional prefix for the identifier
 * @returns Unique string identifier
 */
function generateUniqueId(prefix = ''): string {
  const timestamp = Date.now();
  const random = Math.random().toString(36).substring(2, 8);
  return prefix ? `${prefix}-${timestamp}-${random}` : `${timestamp}-${random}`;
}

/**
 * Generate a random string of specified length
 *
 * @param length - Length of the string to generate
 * @param charset - Character set to use (default: alphanumeric)
 * @returns Random string
 */
function randomString(
  length: number,
  charset = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'
): string {
  let result = '';
  for (let i = 0; i < length; i++) {
    result += charset.charAt(Math.floor(Math.random() * charset.length));
  }
  return result;
}

/**
 * Generate a random integer between min and max (inclusive)
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @returns Random integer
 */
function randomInt(min: number, max: number): number {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

/**
 * Generate a random decimal with specified precision
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @param precision - Number of decimal places
 * @returns Random decimal number
 */
function randomDecimal(min: number, max: number, precision = 2): number {
  const value = Math.random() * (max - min) + min;
  const factor = Math.pow(10, precision);
  return Math.round(value * factor) / factor;
}

/**
 * Generate a random date within a range
 *
 * @param startDate - Start of the range
 * @param endDate - End of the range
 * @returns Random date as ISO string
 */
function randomDate(startDate: Date, endDate: Date): string {
  const start = startDate.getTime();
  const end = endDate.getTime();
  const randomTime = start + Math.random() * (end - start);
  return new Date(randomTime).toISOString().split('T')[0];
}

/**
 * Pick a random element from an array
 *
 * @param array - Array to pick from
 * @returns Random element
 */
function randomPick<T>(array: T[]): T {
  return array[Math.floor(Math.random() * array.length)];
}

// ============================================================================
// Static Test Data
// ============================================================================


/**
 * Sample contact_person names for generating realistic test data
 */
const SAMPLE_NAMES = [
  'Test ContactPerson Alpha',
  'Test ContactPerson Beta',
  'Test ContactPerson Gamma',
  'Sample ContactPerson One',
  'Sample ContactPerson Two',
  'Demo ContactPerson',
  'Example ContactPerson',
];

/**
 * Sample codes for generating realistic test data
 */
const SAMPLE_CODES = [
  'TST001',
  'TST002',
  'TST003',
  'DEMO01',
  'DEMO02',
  'SMPL01',
  'SMPL02',
];

/**
 * Sample descriptions for generating realistic test data
 */
const SAMPLE_DESCRIPTIONS = [
  'This is a test contact_person for automated testing.',
  'Sample contact_person created for E2E tests.',
  'Demo contact_person with all fields populated.',
  'Example contact_person for validation testing.',
  'Test data fixture for contact_person operations.',
];

// ============================================================================
// Predefined Fixtures
// ============================================================================

/**
 * Minimal valid contact_person fixture
 *
 * Contains only required fields with minimal valid values.
 * Use this when testing that minimal data is accepted.
 */
export const minimalContactPersonFixture: ContactPersonCreateRequest = {
};

/**
 * Complete contact_person fixture
 *
 * Contains all fields with realistic test values.
 * Use this when testing full CRUD operations.
 */
export const completeContactPersonFixture: ContactPersonCreateRequest = {
  user_id: 'Test User Id',
  legal_entity_id: 'Test Legal Entity Id',
  full_name: 'Test Full Name',
  email: 'test@example.com',
  role: 'Test Role',
  is_primary: true,
  date_of_birth: 'Test Date Of Birth',
  residential_address_id: 'Test Residential Address Id',
};

/**
 * Unique contact_person fixture
 *
 * Contains all fields with timestamp-based unique values.
 * Use this when testing to ensure data isolation between test runs.
 * Each field that needs uniqueness includes a timestamp/random component.
 */
export const uniqueContactPersonFixture: ContactPersonCreateRequest = {
  user_id: `User Id ${generateUniqueId()}`,
  legal_entity_id: `Legal Entity Id ${generateUniqueId()}`,
  full_name: `Full Name ${generateUniqueId()}`,
  email: `test-${generateUniqueId()}@example.com`,
  role: `Role ${generateUniqueId()}`,
  is_primary: true,
  date_of_birth: `Date Of Birth ${generateUniqueId()}`,
  residential_address_id: `Residential Address Id ${generateUniqueId()}`,
};

/**
 * Invalid contact_person fixture
 *
 * Contains invalid data that should fail validation.
 * Use this when testing validation error handling.
 * Covers: empty required fields, values out of range, invalid formats.
 */
export const invalidContactPersonFixture: Partial<ContactPersonCreateRequest> = {
};

/**
 * Invalid data patterns for comprehensive validation testing
 *
 * Contains various invalid data scenarios:
 * - emptyRequired: All required fields left empty
 * - tooShort: String fields below minimum length
 * - tooLong: String fields exceeding maximum length
 * - outOfRangeMin: Numeric fields below minimum value
 * - outOfRangeMax: Numeric fields above maximum value
 * - wrongType: Fields with incorrect data types (as strings for type coercion testing)
 * - specialChars: Fields with potentially problematic characters
 */
export const contactPersonInvalidPatterns = {
  /** All required string fields empty */
  emptyRequired: {
  } as Partial<ContactPersonCreateRequest>,


  /** String fields exceeding maximum length */
  tooLong: {
    user_id: 'x'.repeat(150), // max: 100
    full_name: 'x'.repeat(150), // max: 100
    email: 'x'.repeat(305), // max: 255
    role: 'x'.repeat(70), // max: 20
  } as Partial<ContactPersonCreateRequest>,



  /** Fields with SQL injection attempts (for security testing) */
  sqlInjection: {
  } as Partial<ContactPersonCreateRequest>,

  /** Fields with XSS attempts (for security testing) */
  xssAttempts: {
  } as Partial<ContactPersonCreateRequest>,

  /** Fields with null values (for nullable field testing) */
  nullValues: {
    legal_entity_id: null,
    date_of_birth: null,
    residential_address_id: null,
  } as Partial<ContactPersonCreateRequest>,

  /** Fields with undefined values (for missing field testing) */
  undefinedValues: {
    // Intentionally empty - represents missing fields
  } as Partial<ContactPersonCreateRequest>,
};

/**
 * Collection of contact_person fixtures for list testing
 *
 * Contains multiple contact_persons for testing list operations,
 * pagination, filtering, and sorting.
 */
export const contactPersonListFixtures: ContactPersonCreateRequest[] = [
  {
    user_id: 'User Id 1',
    legal_entity_id: 'Legal Entity Id 1',
    full_name: 'Full Name 1',
    email: 'Email 1',
    role: 'Role 1',
    is_primary: true,
    date_of_birth: 'Date Of Birth 1',
    residential_address_id: 'Residential Address Id 1',
  },
  {
    user_id: 'User Id 2',
    legal_entity_id: 'Legal Entity Id 2',
    full_name: 'Full Name 2',
    email: 'Email 2',
    role: 'Role 2',
    is_primary: false,
    date_of_birth: 'Date Of Birth 2',
    residential_address_id: 'Residential Address Id 2',
  },
  {
    user_id: 'User Id 3',
    legal_entity_id: 'Legal Entity Id 3',
    full_name: 'Full Name 3',
    email: 'Email 3',
    role: 'Role 3',
    is_primary: true,
    date_of_birth: 'Date Of Birth 3',
    residential_address_id: 'Residential Address Id 3',
  },
  {
    user_id: 'User Id 4',
    legal_entity_id: 'Legal Entity Id 4',
    full_name: 'Full Name 4',
    email: 'Email 4',
    role: 'Role 4',
    is_primary: false,
    date_of_birth: 'Date Of Birth 4',
    residential_address_id: 'Residential Address Id 4',
  },
  {
    user_id: 'User Id 5',
    legal_entity_id: 'Legal Entity Id 5',
    full_name: 'Full Name 5',
    email: 'Email 5',
    role: 'Role 5',
    is_primary: true,
    date_of_birth: 'Date Of Birth 5',
    residential_address_id: 'Residential Address Id 5',
  },
];

// ============================================================================
// Fixture Collection
// ============================================================================

/**
 * All contact_person test fixtures
 *
 * @example
 * ```typescript
 * import { contactPersonFixtures } from '../fixtures/contact_personFixtures';
 *
 * // Use minimal fixture
 * const minData = contactPersonFixtures.minimal;
 *
 * // Use complete fixture
 * const fullData = contactPersonFixtures.complete;
 *
 * // Use unique fixture (with timestamp)
 * const uniqueData = generateContactPersonTestData('unique');
 * ```
 */
export const contactPersonFixtures: ContactPersonTestFixtures = {
  minimal: minimalContactPersonFixture,
  complete: completeContactPersonFixture,
  invalid: invalidContactPersonFixture as ContactPersonCreateRequest,
  unique: uniqueContactPersonFixture,
  list: contactPersonListFixtures,
};

// ============================================================================
// Factory Functions
// ============================================================================

/**
 * Generate contact_person test data based on pattern
 *
 * @param pattern - The test data pattern to generate
 * @param overrides - Optional field overrides
 * @returns Generated test data
 *
 * @example
 * ```typescript
 * // Generate complete test data
 * const data = generateContactPersonTestData('complete');
 *
 * // Generate unique test data with overrides
 * const customData = generateContactPersonTestData('unique', {
 *   is_active: false,
 * });
 * ```
 */
export function generateContactPersonTestData(
  pattern: TestDataPattern = 'complete',
  overrides: Partial<ContactPersonCreateRequest> = {}
): ContactPersonCreateRequest {
  let baseData: ContactPersonCreateRequest;

  switch (pattern) {
    case 'minimal':
      baseData = { ...minimalContactPersonFixture };
      break;

    case 'complete':
      baseData = { ...completeContactPersonFixture };
      break;

    case 'invalid':
      // Return invalid data (caller should handle validation failure)
      baseData = { ...invalidContactPersonFixture } as ContactPersonCreateRequest;
      break;

    case 'unique':
      // Generate fresh unique values for each call
      baseData = {
        ...uniqueContactPersonFixture,
      };
      break;

    default:
      baseData = { ...completeContactPersonFixture };
  }

  return { ...baseData, ...overrides };
}

/**
 * Generate multiple contact_person fixtures
 *
 * @param count - Number of fixtures to generate
 * @param pattern - The test data pattern to use as base
 * @param modifier - Optional function to modify each fixture
 * @returns Array of generated test data
 *
 * @example
 * ```typescript
 * // Generate 5 unique fixtures
 * const fixtures = generateMultipleContactPersonFixtures(5, 'unique');
 *
 * // Generate fixtures with sequential numbering
 * const numbered = generateMultipleContactPersonFixtures(3, 'complete', (data, index) => ({
 *   ...data,
 *   name: `ContactPerson #${index + 1}`,
 * }));
 * ```
 */
export function generateMultipleContactPersonFixtures(
  count: number,
  pattern: TestDataPattern = 'unique',
  modifier?: (data: ContactPersonCreateRequest, index: number) => ContactPersonCreateRequest
): ContactPersonCreateRequest[] {
  const fixtures: ContactPersonCreateRequest[] = [];

  for (let i = 0; i < count; i++) {
    let fixture = generateContactPersonTestData(pattern);

    if (modifier) {
      fixture = modifier(fixture, i);
    } else if (pattern === 'unique') {
      // Add index to ensure uniqueness even with same timestamp
    }

    fixtures.push(fixture);
  }

  return fixtures;
}

/**
 * Generate random contact_person test data
 *
 * Creates test data with randomized values within valid ranges.
 * Useful for fuzz testing and edge case discovery.
 *
 * @param overrides - Optional field overrides
 * @returns Randomly generated test data
 *
 * @example
 * ```typescript
 * // Generate random test data
 * const randomData = generateRandomContactPersonData();
 *
 * // Generate random data with specific field
 * const customRandom = generateRandomContactPersonData({
 *   is_active: true,
 * });
 * ```
 */
export function generateRandomContactPersonData(
  overrides: Partial<ContactPersonCreateRequest> = {}
): ContactPersonCreateRequest {
  const data: ContactPersonCreateRequest = {
    user_id: randomString(randomInt(None, Math.min(100, 50))),
    legal_entity_id: randomString(randomInt(None, Math.min(None, 50))),
    full_name: randomString(randomInt(None, Math.min(100, 50))),
    email: `test-${randomString(6)}@example.com`,
    role: randomString(randomInt(None, Math.min(20, 50))),
    is_primary: Math.random() > 0.5,
    date_of_birth: randomString(randomInt(None, Math.min(None, 50))),
    residential_address_id: randomString(randomInt(None, Math.min(None, 50))),
  };

  return { ...data, ...overrides };
}

// ============================================================================
// Validation Test Data
// ============================================================================

/**
 * Edge case test data for boundary testing
 *
 * Contains values at the boundaries of valid ranges.
 */
export const contactPersonEdgeCases = {
  /** user_id at maximum length (100 chars) */
  user_idMaxLength: 'x'.repeat(100),
  /** full_name at maximum length (100 chars) */
  full_nameMaxLength: 'x'.repeat(100),
  /** email at maximum length (255 chars) */
  emailMaxLength: 'x'.repeat(255),
  /** role at maximum length (20 chars) */
  roleMaxLength: 'x'.repeat(20),
};

/**
 * Special character test data for input sanitization testing
 */
export const contactPersonSpecialCharacterData: Partial<ContactPersonCreateRequest> = {
};

/**
 * Whitespace test data for trimming validation
 */
export const contactPersonWhitespaceData: Partial<ContactPersonCreateRequest> = {
};

// ============================================================================
// Exports
// ============================================================================

// Default export - main fixture collection
export default contactPersonFixtures;
