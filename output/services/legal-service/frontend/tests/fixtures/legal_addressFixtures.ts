/**
 * LegalAddress Test Fixtures
 *
 * Test data fixtures for LegalAddress E2E and unit tests.
 * Provides predefined test data and factory functions for generating
 * test legal_addresses.
 *
 * Fixture patterns:
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields with realistic test values
 * - invalid: Invalid data for validation testing
 * - unique: Unique values using timestamps for test isolation
 *
 * Physical address for legal entities and persons with jurisdiction classification
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:53.510193Z
 */

import type { LegalAddressCreateRequest } from '../../types/legal_address.types';

// ============================================================================
// Types
// ============================================================================

/**
 * Test data pattern types
 *
 * @description
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields populated with realistic test data
 * - invalid: Data that should fail validation
 * - unique: Data with timestamps for test isolation
 */
export type TestDataPattern = 'minimal' | 'complete' | 'invalid' | 'unique';

/**
 * LegalAddress test fixture containing all test data patterns
 */
export interface LegalAddressTestFixtures {
  /** Minimal valid legal_address with only required fields */
  minimal: LegalAddressCreateRequest;
  /** Complete legal_address with all fields populated */
  complete: LegalAddressCreateRequest;
  /** Invalid legal_address for testing validation errors */
  invalid: Partial<LegalAddressCreateRequest>;
  /** Unique legal_address with timestamp-based values */
  unique: LegalAddressCreateRequest;
  /** Multiple legal_addresses for list testing */
  list: LegalAddressCreateRequest[];
}

// ============================================================================
// Helper Functions
// ============================================================================

/**
 * Generate a unique identifier based on current timestamp
 * Useful for creating unique test data that won't conflict
 *
 * @param prefix - Optional prefix for the identifier
 * @returns Unique string identifier
 */
function generateUniqueId(prefix = ''): string {
  const timestamp = Date.now();
  const random = Math.random().toString(36).substring(2, 8);
  return prefix ? `${prefix}-${timestamp}-${random}` : `${timestamp}-${random}`;
}

/**
 * Generate a random string of specified length
 *
 * @param length - Length of the string to generate
 * @param charset - Character set to use (default: alphanumeric)
 * @returns Random string
 */
function randomString(
  length: number,
  charset = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'
): string {
  let result = '';
  for (let i = 0; i < length; i++) {
    result += charset.charAt(Math.floor(Math.random() * charset.length));
  }
  return result;
}

/**
 * Generate a random integer between min and max (inclusive)
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @returns Random integer
 */
function randomInt(min: number, max: number): number {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

/**
 * Generate a random decimal with specified precision
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @param precision - Number of decimal places
 * @returns Random decimal number
 */
function randomDecimal(min: number, max: number, precision = 2): number {
  const value = Math.random() * (max - min) + min;
  const factor = Math.pow(10, precision);
  return Math.round(value * factor) / factor;
}

/**
 * Generate a random date within a range
 *
 * @param startDate - Start of the range
 * @param endDate - End of the range
 * @returns Random date as ISO string
 */
function randomDate(startDate: Date, endDate: Date): string {
  const start = startDate.getTime();
  const end = endDate.getTime();
  const randomTime = start + Math.random() * (end - start);
  return new Date(randomTime).toISOString().split('T')[0];
}

/**
 * Pick a random element from an array
 *
 * @param array - Array to pick from
 * @returns Random element
 */
function randomPick<T>(array: T[]): T {
  return array[Math.floor(Math.random() * array.length)];
}

// ============================================================================
// Static Test Data
// ============================================================================


/**
 * Sample legal_address names for generating realistic test data
 */
const SAMPLE_NAMES = [
  'Test LegalAddress Alpha',
  'Test LegalAddress Beta',
  'Test LegalAddress Gamma',
  'Sample LegalAddress One',
  'Sample LegalAddress Two',
  'Demo LegalAddress',
  'Example LegalAddress',
];

/**
 * Sample codes for generating realistic test data
 */
const SAMPLE_CODES = [
  'TST001',
  'TST002',
  'TST003',
  'DEMO01',
  'DEMO02',
  'SMPL01',
  'SMPL02',
];

/**
 * Sample descriptions for generating realistic test data
 */
const SAMPLE_DESCRIPTIONS = [
  'This is a test legal_address for automated testing.',
  'Sample legal_address created for E2E tests.',
  'Demo legal_address with all fields populated.',
  'Example legal_address for validation testing.',
  'Test data fixture for legal_address operations.',
];

// ============================================================================
// Predefined Fixtures
// ============================================================================

/**
 * Minimal valid legal_address fixture
 *
 * Contains only required fields with minimal valid values.
 * Use this when testing that minimal data is accepted.
 */
export const minimalLegalAddressFixture: LegalAddressCreateRequest = {
};

/**
 * Complete legal_address fixture
 *
 * Contains all fields with realistic test values.
 * Use this when testing full CRUD operations.
 */
export const completeLegalAddressFixture: LegalAddressCreateRequest = {
  address_line_1: 'Test Address Line 1',
  address_line_2: 'Test Address Line 2',
  city: 'Test City',
  state_province: 'Test State Province',
  postal_code: 'Test Postal Code',
  country: 'Test Country',
  jurisdiction: 'Test Jurisdiction',
};

/**
 * Unique legal_address fixture
 *
 * Contains all fields with timestamp-based unique values.
 * Use this when testing to ensure data isolation between test runs.
 * Each field that needs uniqueness includes a timestamp/random component.
 */
export const uniqueLegalAddressFixture: LegalAddressCreateRequest = {
  address_line_1: `Address Line 1 ${generateUniqueId()}`,
  address_line_2: `Address Line 2 ${generateUniqueId()}`,
  city: `City ${generateUniqueId()}`,
  state_province: `State Province ${generateUniqueId()}`,
  postal_code: `Postal Code ${generateUniqueId()}`,
  country: `Country ${generateUniqueId()}`,
  jurisdiction: `Jurisdiction ${generateUniqueId()}`,
};

/**
 * Invalid legal_address fixture
 *
 * Contains invalid data that should fail validation.
 * Use this when testing validation error handling.
 * Covers: empty required fields, values out of range, invalid formats.
 */
export const invalidLegalAddressFixture: Partial<LegalAddressCreateRequest> = {
};

/**
 * Invalid data patterns for comprehensive validation testing
 *
 * Contains various invalid data scenarios:
 * - emptyRequired: All required fields left empty
 * - tooShort: String fields below minimum length
 * - tooLong: String fields exceeding maximum length
 * - outOfRangeMin: Numeric fields below minimum value
 * - outOfRangeMax: Numeric fields above maximum value
 * - wrongType: Fields with incorrect data types (as strings for type coercion testing)
 * - specialChars: Fields with potentially problematic characters
 */
export const legalAddressInvalidPatterns = {
  /** All required string fields empty */
  emptyRequired: {
  } as Partial<LegalAddressCreateRequest>,


  /** String fields exceeding maximum length */
  tooLong: {
    address_line_1: 'x'.repeat(150), // max: 100
    address_line_2: 'x'.repeat(150), // max: 100
    city: 'x'.repeat(100), // max: 50
    state_province: 'x'.repeat(100), // max: 50
    postal_code: 'x'.repeat(70), // max: 20
    country: 'x'.repeat(52), // max: 2
    jurisdiction: 'x'.repeat(80), // max: 30
  } as Partial<LegalAddressCreateRequest>,



  /** Fields with SQL injection attempts (for security testing) */
  sqlInjection: {
  } as Partial<LegalAddressCreateRequest>,

  /** Fields with XSS attempts (for security testing) */
  xssAttempts: {
  } as Partial<LegalAddressCreateRequest>,

  /** Fields with null values (for nullable field testing) */
  nullValues: {
    address_line_2: null,
    state_province: null,
  } as Partial<LegalAddressCreateRequest>,

  /** Fields with undefined values (for missing field testing) */
  undefinedValues: {
    // Intentionally empty - represents missing fields
  } as Partial<LegalAddressCreateRequest>,
};

/**
 * Collection of legal_address fixtures for list testing
 *
 * Contains multiple legal_addresses for testing list operations,
 * pagination, filtering, and sorting.
 */
export const legalAddressListFixtures: LegalAddressCreateRequest[] = [
  {
    address_line_1: 'Address Line 1 1',
    address_line_2: 'Address Line 2 1',
    city: 'City 1',
    state_province: 'State Province 1',
    postal_code: 'Postal Code 1',
    country: 'Country 1',
    jurisdiction: 'Jurisdiction 1',
  },
  {
    address_line_1: 'Address Line 1 2',
    address_line_2: 'Address Line 2 2',
    city: 'City 2',
    state_province: 'State Province 2',
    postal_code: 'Postal Code 2',
    country: 'Country 2',
    jurisdiction: 'Jurisdiction 2',
  },
  {
    address_line_1: 'Address Line 1 3',
    address_line_2: 'Address Line 2 3',
    city: 'City 3',
    state_province: 'State Province 3',
    postal_code: 'Postal Code 3',
    country: 'Country 3',
    jurisdiction: 'Jurisdiction 3',
  },
  {
    address_line_1: 'Address Line 1 4',
    address_line_2: 'Address Line 2 4',
    city: 'City 4',
    state_province: 'State Province 4',
    postal_code: 'Postal Code 4',
    country: 'Country 4',
    jurisdiction: 'Jurisdiction 4',
  },
  {
    address_line_1: 'Address Line 1 5',
    address_line_2: 'Address Line 2 5',
    city: 'City 5',
    state_province: 'State Province 5',
    postal_code: 'Postal Code 5',
    country: 'Country 5',
    jurisdiction: 'Jurisdiction 5',
  },
];

// ============================================================================
// Fixture Collection
// ============================================================================

/**
 * All legal_address test fixtures
 *
 * @example
 * ```typescript
 * import { legalAddressFixtures } from '../fixtures/legal_addressFixtures';
 *
 * // Use minimal fixture
 * const minData = legalAddressFixtures.minimal;
 *
 * // Use complete fixture
 * const fullData = legalAddressFixtures.complete;
 *
 * // Use unique fixture (with timestamp)
 * const uniqueData = generateLegalAddressTestData('unique');
 * ```
 */
export const legalAddressFixtures: LegalAddressTestFixtures = {
  minimal: minimalLegalAddressFixture,
  complete: completeLegalAddressFixture,
  invalid: invalidLegalAddressFixture as LegalAddressCreateRequest,
  unique: uniqueLegalAddressFixture,
  list: legalAddressListFixtures,
};

// ============================================================================
// Factory Functions
// ============================================================================

/**
 * Generate legal_address test data based on pattern
 *
 * @param pattern - The test data pattern to generate
 * @param overrides - Optional field overrides
 * @returns Generated test data
 *
 * @example
 * ```typescript
 * // Generate complete test data
 * const data = generateLegalAddressTestData('complete');
 *
 * // Generate unique test data with overrides
 * const customData = generateLegalAddressTestData('unique', {
 *   is_active: false,
 * });
 * ```
 */
export function generateLegalAddressTestData(
  pattern: TestDataPattern = 'complete',
  overrides: Partial<LegalAddressCreateRequest> = {}
): LegalAddressCreateRequest {
  let baseData: LegalAddressCreateRequest;

  switch (pattern) {
    case 'minimal':
      baseData = { ...minimalLegalAddressFixture };
      break;

    case 'complete':
      baseData = { ...completeLegalAddressFixture };
      break;

    case 'invalid':
      // Return invalid data (caller should handle validation failure)
      baseData = { ...invalidLegalAddressFixture } as LegalAddressCreateRequest;
      break;

    case 'unique':
      // Generate fresh unique values for each call
      baseData = {
        ...uniqueLegalAddressFixture,
      };
      break;

    default:
      baseData = { ...completeLegalAddressFixture };
  }

  return { ...baseData, ...overrides };
}

/**
 * Generate multiple legal_address fixtures
 *
 * @param count - Number of fixtures to generate
 * @param pattern - The test data pattern to use as base
 * @param modifier - Optional function to modify each fixture
 * @returns Array of generated test data
 *
 * @example
 * ```typescript
 * // Generate 5 unique fixtures
 * const fixtures = generateMultipleLegalAddressFixtures(5, 'unique');
 *
 * // Generate fixtures with sequential numbering
 * const numbered = generateMultipleLegalAddressFixtures(3, 'complete', (data, index) => ({
 *   ...data,
 *   name: `LegalAddress #${index + 1}`,
 * }));
 * ```
 */
export function generateMultipleLegalAddressFixtures(
  count: number,
  pattern: TestDataPattern = 'unique',
  modifier?: (data: LegalAddressCreateRequest, index: number) => LegalAddressCreateRequest
): LegalAddressCreateRequest[] {
  const fixtures: LegalAddressCreateRequest[] = [];

  for (let i = 0; i < count; i++) {
    let fixture = generateLegalAddressTestData(pattern);

    if (modifier) {
      fixture = modifier(fixture, i);
    } else if (pattern === 'unique') {
      // Add index to ensure uniqueness even with same timestamp
    }

    fixtures.push(fixture);
  }

  return fixtures;
}

/**
 * Generate random legal_address test data
 *
 * Creates test data with randomized values within valid ranges.
 * Useful for fuzz testing and edge case discovery.
 *
 * @param overrides - Optional field overrides
 * @returns Randomly generated test data
 *
 * @example
 * ```typescript
 * // Generate random test data
 * const randomData = generateRandomLegalAddressData();
 *
 * // Generate random data with specific field
 * const customRandom = generateRandomLegalAddressData({
 *   is_active: true,
 * });
 * ```
 */
export function generateRandomLegalAddressData(
  overrides: Partial<LegalAddressCreateRequest> = {}
): LegalAddressCreateRequest {
  const data: LegalAddressCreateRequest = {
    address_line_1: randomString(randomInt(None, Math.min(100, 50))),
    address_line_2: randomString(randomInt(None, Math.min(100, 50))),
    city: randomString(randomInt(None, Math.min(50, 50))),
    state_province: randomString(randomInt(None, Math.min(50, 50))),
    postal_code: randomString(randomInt(None, Math.min(20, 50))),
    country: randomString(randomInt(None, Math.min(2, 50))),
    jurisdiction: randomString(randomInt(None, Math.min(30, 50))),
  };

  return { ...data, ...overrides };
}

// ============================================================================
// Validation Test Data
// ============================================================================

/**
 * Edge case test data for boundary testing
 *
 * Contains values at the boundaries of valid ranges.
 */
export const legalAddressEdgeCases = {
  /** address_line_1 at maximum length (100 chars) */
  address_line_1MaxLength: 'x'.repeat(100),
  /** address_line_2 at maximum length (100 chars) */
  address_line_2MaxLength: 'x'.repeat(100),
  /** city at maximum length (50 chars) */
  cityMaxLength: 'x'.repeat(50),
  /** state_province at maximum length (50 chars) */
  state_provinceMaxLength: 'x'.repeat(50),
  /** postal_code at maximum length (20 chars) */
  postal_codeMaxLength: 'x'.repeat(20),
  /** country at maximum length (2 chars) */
  countryMaxLength: 'x'.repeat(2),
  /** jurisdiction at maximum length (30 chars) */
  jurisdictionMaxLength: 'x'.repeat(30),
};

/**
 * Special character test data for input sanitization testing
 */
export const legalAddressSpecialCharacterData: Partial<LegalAddressCreateRequest> = {
};

/**
 * Whitespace test data for trimming validation
 */
export const legalAddressWhitespaceData: Partial<LegalAddressCreateRequest> = {
};

// ============================================================================
// Exports
// ============================================================================

// Default export - main fixture collection
export default legalAddressFixtures;
