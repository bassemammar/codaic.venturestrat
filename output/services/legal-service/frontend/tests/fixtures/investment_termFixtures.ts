/**
 * InvestmentTerm Test Fixtures
 *
 * Test data fixtures for InvestmentTerm E2E and unit tests.
 * Provides predefined test data and factory functions for generating
 * test investment_terms.
 *
 * Fixture patterns:
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields with realistic test values
 * - invalid: Invalid data for validation testing
 * - unique: Unique values using timestamps for test isolation
 *
 * SAFE or priced round investment terms linked to a legal entity and investor person
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:53.064383Z
 */

import type { InvestmentTermCreateRequest } from '../../types/investment_term.types';

// ============================================================================
// Types
// ============================================================================

/**
 * Test data pattern types
 *
 * @description
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields populated with realistic test data
 * - invalid: Data that should fail validation
 * - unique: Data with timestamps for test isolation
 */
export type TestDataPattern = 'minimal' | 'complete' | 'invalid' | 'unique';

/**
 * InvestmentTerm test fixture containing all test data patterns
 */
export interface InvestmentTermTestFixtures {
  /** Minimal valid investment_term with only required fields */
  minimal: InvestmentTermCreateRequest;
  /** Complete investment_term with all fields populated */
  complete: InvestmentTermCreateRequest;
  /** Invalid investment_term for testing validation errors */
  invalid: Partial<InvestmentTermCreateRequest>;
  /** Unique investment_term with timestamp-based values */
  unique: InvestmentTermCreateRequest;
  /** Multiple investment_terms for list testing */
  list: InvestmentTermCreateRequest[];
}

// ============================================================================
// Helper Functions
// ============================================================================

/**
 * Generate a unique identifier based on current timestamp
 * Useful for creating unique test data that won't conflict
 *
 * @param prefix - Optional prefix for the identifier
 * @returns Unique string identifier
 */
function generateUniqueId(prefix = ''): string {
  const timestamp = Date.now();
  const random = Math.random().toString(36).substring(2, 8);
  return prefix ? `${prefix}-${timestamp}-${random}` : `${timestamp}-${random}`;
}

/**
 * Generate a random string of specified length
 *
 * @param length - Length of the string to generate
 * @param charset - Character set to use (default: alphanumeric)
 * @returns Random string
 */
function randomString(
  length: number,
  charset = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'
): string {
  let result = '';
  for (let i = 0; i < length; i++) {
    result += charset.charAt(Math.floor(Math.random() * charset.length));
  }
  return result;
}

/**
 * Generate a random integer between min and max (inclusive)
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @returns Random integer
 */
function randomInt(min: number, max: number): number {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

/**
 * Generate a random decimal with specified precision
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @param precision - Number of decimal places
 * @returns Random decimal number
 */
function randomDecimal(min: number, max: number, precision = 2): number {
  const value = Math.random() * (max - min) + min;
  const factor = Math.pow(10, precision);
  return Math.round(value * factor) / factor;
}

/**
 * Generate a random date within a range
 *
 * @param startDate - Start of the range
 * @param endDate - End of the range
 * @returns Random date as ISO string
 */
function randomDate(startDate: Date, endDate: Date): string {
  const start = startDate.getTime();
  const end = endDate.getTime();
  const randomTime = start + Math.random() * (end - start);
  return new Date(randomTime).toISOString().split('T')[0];
}

/**
 * Pick a random element from an array
 *
 * @param array - Array to pick from
 * @returns Random element
 */
function randomPick<T>(array: T[]): T {
  return array[Math.floor(Math.random() * array.length)];
}

// ============================================================================
// Static Test Data
// ============================================================================


/**
 * Sample investment_term names for generating realistic test data
 */
const SAMPLE_NAMES = [
  'Test InvestmentTerm Alpha',
  'Test InvestmentTerm Beta',
  'Test InvestmentTerm Gamma',
  'Sample InvestmentTerm One',
  'Sample InvestmentTerm Two',
  'Demo InvestmentTerm',
  'Example InvestmentTerm',
];

/**
 * Sample codes for generating realistic test data
 */
const SAMPLE_CODES = [
  'TST001',
  'TST002',
  'TST003',
  'DEMO01',
  'DEMO02',
  'SMPL01',
  'SMPL02',
];

/**
 * Sample descriptions for generating realistic test data
 */
const SAMPLE_DESCRIPTIONS = [
  'This is a test investment_term for automated testing.',
  'Sample investment_term created for E2E tests.',
  'Demo investment_term with all fields populated.',
  'Example investment_term for validation testing.',
  'Test data fixture for investment_term operations.',
];

// ============================================================================
// Predefined Fixtures
// ============================================================================

/**
 * Minimal valid investment_term fixture
 *
 * Contains only required fields with minimal valid values.
 * Use this when testing that minimal data is accepted.
 */
export const minimalInvestmentTermFixture: InvestmentTermCreateRequest = {
};

/**
 * Complete investment_term fixture
 *
 * Contains all fields with realistic test values.
 * Use this when testing full CRUD operations.
 */
export const completeInvestmentTermFixture: InvestmentTermCreateRequest = {
  legal_entity_id: 'Test Legal Entity Id',
  investor_person_id: 'Test Investor Person Id',
  investor_id: 'Test Investor Id',
  investment_amount: 42,
  currency: 'Test Currency',
  valuation_cap: 42,
  discount_percentage: 42,
  investment_date: 'Test Investment Date',
  pro_rata_rights: true,
  source_document_id: 'Test Source Document Id',
};

/**
 * Unique investment_term fixture
 *
 * Contains all fields with timestamp-based unique values.
 * Use this when testing to ensure data isolation between test runs.
 * Each field that needs uniqueness includes a timestamp/random component.
 */
export const uniqueInvestmentTermFixture: InvestmentTermCreateRequest = {
  legal_entity_id: `Legal Entity Id ${generateUniqueId()}`,
  investor_person_id: `Investor Person Id ${generateUniqueId()}`,
  investor_id: `Investor Id ${generateUniqueId()}`,
  investment_amount: Math.floor(Math.random() * 1000),
  currency: `Currency ${generateUniqueId()}`,
  valuation_cap: Math.floor(Math.random() * 1000),
  discount_percentage: Math.floor(Math.random() * 1000),
  investment_date: `Investment Date ${generateUniqueId()}`,
  pro_rata_rights: true,
  source_document_id: `Source Document Id ${generateUniqueId()}`,
};

/**
 * Invalid investment_term fixture
 *
 * Contains invalid data that should fail validation.
 * Use this when testing validation error handling.
 * Covers: empty required fields, values out of range, invalid formats.
 */
export const invalidInvestmentTermFixture: Partial<InvestmentTermCreateRequest> = {
};

/**
 * Invalid data patterns for comprehensive validation testing
 *
 * Contains various invalid data scenarios:
 * - emptyRequired: All required fields left empty
 * - tooShort: String fields below minimum length
 * - tooLong: String fields exceeding maximum length
 * - outOfRangeMin: Numeric fields below minimum value
 * - outOfRangeMax: Numeric fields above maximum value
 * - wrongType: Fields with incorrect data types (as strings for type coercion testing)
 * - specialChars: Fields with potentially problematic characters
 */
export const investmentTermInvalidPatterns = {
  /** All required string fields empty */
  emptyRequired: {
  } as Partial<InvestmentTermCreateRequest>,


  /** String fields exceeding maximum length */
  tooLong: {
    currency: 'x'.repeat(53), // max: 3
  } as Partial<InvestmentTermCreateRequest>,



  /** Fields with SQL injection attempts (for security testing) */
  sqlInjection: {
  } as Partial<InvestmentTermCreateRequest>,

  /** Fields with XSS attempts (for security testing) */
  xssAttempts: {
  } as Partial<InvestmentTermCreateRequest>,

  /** Fields with null values (for nullable field testing) */
  nullValues: {
    investor_id: null,
    valuation_cap: null,
    discount_percentage: null,
    source_document_id: null,
  } as Partial<InvestmentTermCreateRequest>,

  /** Fields with undefined values (for missing field testing) */
  undefinedValues: {
    // Intentionally empty - represents missing fields
  } as Partial<InvestmentTermCreateRequest>,
};

/**
 * Collection of investment_term fixtures for list testing
 *
 * Contains multiple investment_terms for testing list operations,
 * pagination, filtering, and sorting.
 */
export const investmentTermListFixtures: InvestmentTermCreateRequest[] = [
  {
    legal_entity_id: 'Legal Entity Id 1',
    investor_person_id: 'Investor Person Id 1',
    investor_id: 'Investor Id 1',
    investment_amount: 10,
    currency: 'Currency 1',
    valuation_cap: 10,
    discount_percentage: 10,
    investment_date: 'Investment Date 1',
    pro_rata_rights: true,
    source_document_id: 'Source Document Id 1',
  },
  {
    legal_entity_id: 'Legal Entity Id 2',
    investor_person_id: 'Investor Person Id 2',
    investor_id: 'Investor Id 2',
    investment_amount: 20,
    currency: 'Currency 2',
    valuation_cap: 20,
    discount_percentage: 20,
    investment_date: 'Investment Date 2',
    pro_rata_rights: false,
    source_document_id: 'Source Document Id 2',
  },
  {
    legal_entity_id: 'Legal Entity Id 3',
    investor_person_id: 'Investor Person Id 3',
    investor_id: 'Investor Id 3',
    investment_amount: 30,
    currency: 'Currency 3',
    valuation_cap: 30,
    discount_percentage: 30,
    investment_date: 'Investment Date 3',
    pro_rata_rights: true,
    source_document_id: 'Source Document Id 3',
  },
  {
    legal_entity_id: 'Legal Entity Id 4',
    investor_person_id: 'Investor Person Id 4',
    investor_id: 'Investor Id 4',
    investment_amount: 40,
    currency: 'Currency 4',
    valuation_cap: 40,
    discount_percentage: 40,
    investment_date: 'Investment Date 4',
    pro_rata_rights: false,
    source_document_id: 'Source Document Id 4',
  },
  {
    legal_entity_id: 'Legal Entity Id 5',
    investor_person_id: 'Investor Person Id 5',
    investor_id: 'Investor Id 5',
    investment_amount: 50,
    currency: 'Currency 5',
    valuation_cap: 50,
    discount_percentage: 50,
    investment_date: 'Investment Date 5',
    pro_rata_rights: true,
    source_document_id: 'Source Document Id 5',
  },
];

// ============================================================================
// Fixture Collection
// ============================================================================

/**
 * All investment_term test fixtures
 *
 * @example
 * ```typescript
 * import { investmentTermFixtures } from '../fixtures/investment_termFixtures';
 *
 * // Use minimal fixture
 * const minData = investmentTermFixtures.minimal;
 *
 * // Use complete fixture
 * const fullData = investmentTermFixtures.complete;
 *
 * // Use unique fixture (with timestamp)
 * const uniqueData = generateInvestmentTermTestData('unique');
 * ```
 */
export const investmentTermFixtures: InvestmentTermTestFixtures = {
  minimal: minimalInvestmentTermFixture,
  complete: completeInvestmentTermFixture,
  invalid: invalidInvestmentTermFixture as InvestmentTermCreateRequest,
  unique: uniqueInvestmentTermFixture,
  list: investmentTermListFixtures,
};

// ============================================================================
// Factory Functions
// ============================================================================

/**
 * Generate investment_term test data based on pattern
 *
 * @param pattern - The test data pattern to generate
 * @param overrides - Optional field overrides
 * @returns Generated test data
 *
 * @example
 * ```typescript
 * // Generate complete test data
 * const data = generateInvestmentTermTestData('complete');
 *
 * // Generate unique test data with overrides
 * const customData = generateInvestmentTermTestData('unique', {
 *   is_active: false,
 * });
 * ```
 */
export function generateInvestmentTermTestData(
  pattern: TestDataPattern = 'complete',
  overrides: Partial<InvestmentTermCreateRequest> = {}
): InvestmentTermCreateRequest {
  let baseData: InvestmentTermCreateRequest;

  switch (pattern) {
    case 'minimal':
      baseData = { ...minimalInvestmentTermFixture };
      break;

    case 'complete':
      baseData = { ...completeInvestmentTermFixture };
      break;

    case 'invalid':
      // Return invalid data (caller should handle validation failure)
      baseData = { ...invalidInvestmentTermFixture } as InvestmentTermCreateRequest;
      break;

    case 'unique':
      // Generate fresh unique values for each call
      baseData = {
        ...uniqueInvestmentTermFixture,
      };
      break;

    default:
      baseData = { ...completeInvestmentTermFixture };
  }

  return { ...baseData, ...overrides };
}

/**
 * Generate multiple investment_term fixtures
 *
 * @param count - Number of fixtures to generate
 * @param pattern - The test data pattern to use as base
 * @param modifier - Optional function to modify each fixture
 * @returns Array of generated test data
 *
 * @example
 * ```typescript
 * // Generate 5 unique fixtures
 * const fixtures = generateMultipleInvestmentTermFixtures(5, 'unique');
 *
 * // Generate fixtures with sequential numbering
 * const numbered = generateMultipleInvestmentTermFixtures(3, 'complete', (data, index) => ({
 *   ...data,
 *   name: `InvestmentTerm #${index + 1}`,
 * }));
 * ```
 */
export function generateMultipleInvestmentTermFixtures(
  count: number,
  pattern: TestDataPattern = 'unique',
  modifier?: (data: InvestmentTermCreateRequest, index: number) => InvestmentTermCreateRequest
): InvestmentTermCreateRequest[] {
  const fixtures: InvestmentTermCreateRequest[] = [];

  for (let i = 0; i < count; i++) {
    let fixture = generateInvestmentTermTestData(pattern);

    if (modifier) {
      fixture = modifier(fixture, i);
    } else if (pattern === 'unique') {
      // Add index to ensure uniqueness even with same timestamp
    }

    fixtures.push(fixture);
  }

  return fixtures;
}

/**
 * Generate random investment_term test data
 *
 * Creates test data with randomized values within valid ranges.
 * Useful for fuzz testing and edge case discovery.
 *
 * @param overrides - Optional field overrides
 * @returns Randomly generated test data
 *
 * @example
 * ```typescript
 * // Generate random test data
 * const randomData = generateRandomInvestmentTermData();
 *
 * // Generate random data with specific field
 * const customRandom = generateRandomInvestmentTermData({
 *   is_active: true,
 * });
 * ```
 */
export function generateRandomInvestmentTermData(
  overrides: Partial<InvestmentTermCreateRequest> = {}
): InvestmentTermCreateRequest {
  const data: InvestmentTermCreateRequest = {
    legal_entity_id: randomString(randomInt(None, Math.min(None, 50))),
    investor_person_id: randomString(randomInt(None, Math.min(None, 50))),
    investor_id: randomString(randomInt(None, Math.min(None, 50))),
    investment_amount: randomInt(0, 1000),
    currency: randomString(randomInt(None, Math.min(3, 50))),
    valuation_cap: randomInt(0, 1000),
    discount_percentage: randomInt(0, 1000),
    investment_date: randomString(randomInt(None, Math.min(None, 50))),
    pro_rata_rights: Math.random() > 0.5,
    source_document_id: randomString(randomInt(None, Math.min(None, 50))),
  };

  return { ...data, ...overrides };
}

// ============================================================================
// Validation Test Data
// ============================================================================

/**
 * Edge case test data for boundary testing
 *
 * Contains values at the boundaries of valid ranges.
 */
export const investmentTermEdgeCases = {
  /** currency at maximum length (3 chars) */
  currencyMaxLength: 'x'.repeat(3),
};

/**
 * Special character test data for input sanitization testing
 */
export const investmentTermSpecialCharacterData: Partial<InvestmentTermCreateRequest> = {
};

/**
 * Whitespace test data for trimming validation
 */
export const investmentTermWhitespaceData: Partial<InvestmentTermCreateRequest> = {
};

// ============================================================================
// Exports
// ============================================================================

// Default export - main fixture collection
export default investmentTermFixtures;
