/**
 * LegalDocument Test Fixtures
 *
 * Test data fixtures for LegalDocument E2E and unit tests.
 * Provides predefined test data and factory functions for generating
 * test legal_documents.
 *
 * Fixture patterns:
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields with realistic test values
 * - invalid: Invalid data for validation testing
 * - unique: Unique values using timestamps for test isolation
 *
 * Generated legal document with full lifecycle tracking, template reference, and party links
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:54.201739Z
 */

import type { LegalDocumentCreateRequest } from '../../types/legal_document.types';

// ============================================================================
// Types
// ============================================================================

/**
 * Test data pattern types
 *
 * @description
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields populated with realistic test data
 * - invalid: Data that should fail validation
 * - unique: Data with timestamps for test isolation
 */
export type TestDataPattern = 'minimal' | 'complete' | 'invalid' | 'unique';

/**
 * LegalDocument test fixture containing all test data patterns
 */
export interface LegalDocumentTestFixtures {
  /** Minimal valid legal_document with only required fields */
  minimal: LegalDocumentCreateRequest;
  /** Complete legal_document with all fields populated */
  complete: LegalDocumentCreateRequest;
  /** Invalid legal_document for testing validation errors */
  invalid: Partial<LegalDocumentCreateRequest>;
  /** Unique legal_document with timestamp-based values */
  unique: LegalDocumentCreateRequest;
  /** Multiple legal_documents for list testing */
  list: LegalDocumentCreateRequest[];
}

// ============================================================================
// Helper Functions
// ============================================================================

/**
 * Generate a unique identifier based on current timestamp
 * Useful for creating unique test data that won't conflict
 *
 * @param prefix - Optional prefix for the identifier
 * @returns Unique string identifier
 */
function generateUniqueId(prefix = ''): string {
  const timestamp = Date.now();
  const random = Math.random().toString(36).substring(2, 8);
  return prefix ? `${prefix}-${timestamp}-${random}` : `${timestamp}-${random}`;
}

/**
 * Generate a random string of specified length
 *
 * @param length - Length of the string to generate
 * @param charset - Character set to use (default: alphanumeric)
 * @returns Random string
 */
function randomString(
  length: number,
  charset = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'
): string {
  let result = '';
  for (let i = 0; i < length; i++) {
    result += charset.charAt(Math.floor(Math.random() * charset.length));
  }
  return result;
}

/**
 * Generate a random integer between min and max (inclusive)
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @returns Random integer
 */
function randomInt(min: number, max: number): number {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

/**
 * Generate a random decimal with specified precision
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @param precision - Number of decimal places
 * @returns Random decimal number
 */
function randomDecimal(min: number, max: number, precision = 2): number {
  const value = Math.random() * (max - min) + min;
  const factor = Math.pow(10, precision);
  return Math.round(value * factor) / factor;
}

/**
 * Generate a random date within a range
 *
 * @param startDate - Start of the range
 * @param endDate - End of the range
 * @returns Random date as ISO string
 */
function randomDate(startDate: Date, endDate: Date): string {
  const start = startDate.getTime();
  const end = endDate.getTime();
  const randomTime = start + Math.random() * (end - start);
  return new Date(randomTime).toISOString().split('T')[0];
}

/**
 * Pick a random element from an array
 *
 * @param array - Array to pick from
 * @returns Random element
 */
function randomPick<T>(array: T[]): T {
  return array[Math.floor(Math.random() * array.length)];
}

// ============================================================================
// Static Test Data
// ============================================================================


/**
 * Sample legal_document names for generating realistic test data
 */
const SAMPLE_NAMES = [
  'Test LegalDocument Alpha',
  'Test LegalDocument Beta',
  'Test LegalDocument Gamma',
  'Sample LegalDocument One',
  'Sample LegalDocument Two',
  'Demo LegalDocument',
  'Example LegalDocument',
];

/**
 * Sample codes for generating realistic test data
 */
const SAMPLE_CODES = [
  'TST001',
  'TST002',
  'TST003',
  'DEMO01',
  'DEMO02',
  'SMPL01',
  'SMPL02',
];

/**
 * Sample descriptions for generating realistic test data
 */
const SAMPLE_DESCRIPTIONS = [
  'This is a test legal_document for automated testing.',
  'Sample legal_document created for E2E tests.',
  'Demo legal_document with all fields populated.',
  'Example legal_document for validation testing.',
  'Test data fixture for legal_document operations.',
];

// ============================================================================
// Predefined Fixtures
// ============================================================================

/**
 * Minimal valid legal_document fixture
 *
 * Contains only required fields with minimal valid values.
 * Use this when testing that minimal data is accepted.
 */
export const minimalLegalDocumentFixture: LegalDocumentCreateRequest = {
};

/**
 * Complete legal_document fixture
 *
 * Contains all fields with realistic test values.
 * Use this when testing full CRUD operations.
 */
export const completeLegalDocumentFixture: LegalDocumentCreateRequest = {
  user_id: 'Test User Id',
  template_id: 'Test Template Id',
  investor_id: 'Test Investor Id',
  document_type: 'Test Document Type',
  title: 'Test LegalDocument Complete',
  status: 'Test Status',
  configuration: null,
  content_markdown: 'Test Content Markdown',
  content_html: 'Test Content Html',
  file_path_docx: 'Test File Path Docx',
  file_path_pdf: 'Test File Path Pdf',
  version: 42,
  generated_at: 'Test Generated At',
};

/**
 * Unique legal_document fixture
 *
 * Contains all fields with timestamp-based unique values.
 * Use this when testing to ensure data isolation between test runs.
 * Each field that needs uniqueness includes a timestamp/random component.
 */
export const uniqueLegalDocumentFixture: LegalDocumentCreateRequest = {
  user_id: `User Id ${generateUniqueId()}`,
  template_id: `Template Id ${generateUniqueId()}`,
  investor_id: `Investor Id ${generateUniqueId()}`,
  document_type: `Document Type ${generateUniqueId()}`,
  title: `Test LegalDocument ${generateUniqueId()}`,
  status: `Status ${generateUniqueId()}`,
  configuration: null,
  content_markdown: `Content Markdown ${generateUniqueId()}`,
  content_html: `Content Html ${generateUniqueId()}`,
  file_path_docx: `File Path Docx ${generateUniqueId()}`,
  file_path_pdf: `File Path Pdf ${generateUniqueId()}`,
  version: Math.floor(Math.random() * 1000),
  generated_at: `Generated At ${generateUniqueId()}`,
};

/**
 * Invalid legal_document fixture
 *
 * Contains invalid data that should fail validation.
 * Use this when testing validation error handling.
 * Covers: empty required fields, values out of range, invalid formats.
 */
export const invalidLegalDocumentFixture: Partial<LegalDocumentCreateRequest> = {
};

/**
 * Invalid data patterns for comprehensive validation testing
 *
 * Contains various invalid data scenarios:
 * - emptyRequired: All required fields left empty
 * - tooShort: String fields below minimum length
 * - tooLong: String fields exceeding maximum length
 * - outOfRangeMin: Numeric fields below minimum value
 * - outOfRangeMax: Numeric fields above maximum value
 * - wrongType: Fields with incorrect data types (as strings for type coercion testing)
 * - specialChars: Fields with potentially problematic characters
 */
export const legalDocumentInvalidPatterns = {
  /** All required string fields empty */
  emptyRequired: {
  } as Partial<LegalDocumentCreateRequest>,


  /** String fields exceeding maximum length */
  tooLong: {
    user_id: 'x'.repeat(150), // max: 100
    document_type: 'x'.repeat(100), // max: 50
    title: 'x'.repeat(305), // max: 255
    status: 'x'.repeat(70), // max: 20
    file_path_docx: 'x'.repeat(550), // max: 500
    file_path_pdf: 'x'.repeat(550), // max: 500
  } as Partial<LegalDocumentCreateRequest>,



  /** Fields with SQL injection attempts (for security testing) */
  sqlInjection: {
  } as Partial<LegalDocumentCreateRequest>,

  /** Fields with XSS attempts (for security testing) */
  xssAttempts: {
  } as Partial<LegalDocumentCreateRequest>,

  /** Fields with null values (for nullable field testing) */
  nullValues: {
    template_id: null,
    investor_id: null,
    content_markdown: null,
    content_html: null,
    file_path_docx: null,
    file_path_pdf: null,
    generated_at: null,
  } as Partial<LegalDocumentCreateRequest>,

  /** Fields with undefined values (for missing field testing) */
  undefinedValues: {
    // Intentionally empty - represents missing fields
  } as Partial<LegalDocumentCreateRequest>,
};

/**
 * Collection of legal_document fixtures for list testing
 *
 * Contains multiple legal_documents for testing list operations,
 * pagination, filtering, and sorting.
 */
export const legalDocumentListFixtures: LegalDocumentCreateRequest[] = [
  {
    user_id: 'User Id 1',
    template_id: 'Template Id 1',
    investor_id: 'Investor Id 1',
    document_type: 'Document Type 1',
    title: 'Test LegalDocument Alpha',
    status: 'Status 1',
    configuration: null,
    content_markdown: 'Content Markdown 1',
    content_html: 'Content Html 1',
    file_path_docx: 'File Path Docx 1',
    file_path_pdf: 'File Path Pdf 1',
    version: 10,
    generated_at: 'Generated At 1',
  },
  {
    user_id: 'User Id 2',
    template_id: 'Template Id 2',
    investor_id: 'Investor Id 2',
    document_type: 'Document Type 2',
    title: 'Test LegalDocument Beta',
    status: 'Status 2',
    configuration: null,
    content_markdown: 'Content Markdown 2',
    content_html: 'Content Html 2',
    file_path_docx: 'File Path Docx 2',
    file_path_pdf: 'File Path Pdf 2',
    version: 20,
    generated_at: 'Generated At 2',
  },
  {
    user_id: 'User Id 3',
    template_id: 'Template Id 3',
    investor_id: 'Investor Id 3',
    document_type: 'Document Type 3',
    title: 'Test LegalDocument Gamma',
    status: 'Status 3',
    configuration: null,
    content_markdown: 'Content Markdown 3',
    content_html: 'Content Html 3',
    file_path_docx: 'File Path Docx 3',
    file_path_pdf: 'File Path Pdf 3',
    version: 30,
    generated_at: 'Generated At 3',
  },
  {
    user_id: 'User Id 4',
    template_id: 'Template Id 4',
    investor_id: 'Investor Id 4',
    document_type: 'Document Type 4',
    title: 'Test LegalDocument Delta',
    status: 'Status 4',
    configuration: null,
    content_markdown: 'Content Markdown 4',
    content_html: 'Content Html 4',
    file_path_docx: 'File Path Docx 4',
    file_path_pdf: 'File Path Pdf 4',
    version: 40,
    generated_at: 'Generated At 4',
  },
  {
    user_id: 'User Id 5',
    template_id: 'Template Id 5',
    investor_id: 'Investor Id 5',
    document_type: 'Document Type 5',
    title: 'Test LegalDocument Epsilon',
    status: 'Status 5',
    configuration: null,
    content_markdown: 'Content Markdown 5',
    content_html: 'Content Html 5',
    file_path_docx: 'File Path Docx 5',
    file_path_pdf: 'File Path Pdf 5',
    version: 50,
    generated_at: 'Generated At 5',
  },
];

// ============================================================================
// Fixture Collection
// ============================================================================

/**
 * All legal_document test fixtures
 *
 * @example
 * ```typescript
 * import { legalDocumentFixtures } from '../fixtures/legal_documentFixtures';
 *
 * // Use minimal fixture
 * const minData = legalDocumentFixtures.minimal;
 *
 * // Use complete fixture
 * const fullData = legalDocumentFixtures.complete;
 *
 * // Use unique fixture (with timestamp)
 * const uniqueData = generateLegalDocumentTestData('unique');
 * ```
 */
export const legalDocumentFixtures: LegalDocumentTestFixtures = {
  minimal: minimalLegalDocumentFixture,
  complete: completeLegalDocumentFixture,
  invalid: invalidLegalDocumentFixture as LegalDocumentCreateRequest,
  unique: uniqueLegalDocumentFixture,
  list: legalDocumentListFixtures,
};

// ============================================================================
// Factory Functions
// ============================================================================

/**
 * Generate legal_document test data based on pattern
 *
 * @param pattern - The test data pattern to generate
 * @param overrides - Optional field overrides
 * @returns Generated test data
 *
 * @example
 * ```typescript
 * // Generate complete test data
 * const data = generateLegalDocumentTestData('complete');
 *
 * // Generate unique test data with overrides
 * const customData = generateLegalDocumentTestData('unique', {
 *   is_active: false,
 * });
 * ```
 */
export function generateLegalDocumentTestData(
  pattern: TestDataPattern = 'complete',
  overrides: Partial<LegalDocumentCreateRequest> = {}
): LegalDocumentCreateRequest {
  let baseData: LegalDocumentCreateRequest;

  switch (pattern) {
    case 'minimal':
      baseData = { ...minimalLegalDocumentFixture };
      break;

    case 'complete':
      baseData = { ...completeLegalDocumentFixture };
      break;

    case 'invalid':
      // Return invalid data (caller should handle validation failure)
      baseData = { ...invalidLegalDocumentFixture } as LegalDocumentCreateRequest;
      break;

    case 'unique':
      // Generate fresh unique values for each call
      baseData = {
        ...uniqueLegalDocumentFixture,
        title: `Test LegalDocument ${generateUniqueId()}`,
      };
      break;

    default:
      baseData = { ...completeLegalDocumentFixture };
  }

  return { ...baseData, ...overrides };
}

/**
 * Generate multiple legal_document fixtures
 *
 * @param count - Number of fixtures to generate
 * @param pattern - The test data pattern to use as base
 * @param modifier - Optional function to modify each fixture
 * @returns Array of generated test data
 *
 * @example
 * ```typescript
 * // Generate 5 unique fixtures
 * const fixtures = generateMultipleLegalDocumentFixtures(5, 'unique');
 *
 * // Generate fixtures with sequential numbering
 * const numbered = generateMultipleLegalDocumentFixtures(3, 'complete', (data, index) => ({
 *   ...data,
 *   name: `LegalDocument #${index + 1}`,
 * }));
 * ```
 */
export function generateMultipleLegalDocumentFixtures(
  count: number,
  pattern: TestDataPattern = 'unique',
  modifier?: (data: LegalDocumentCreateRequest, index: number) => LegalDocumentCreateRequest
): LegalDocumentCreateRequest[] {
  const fixtures: LegalDocumentCreateRequest[] = [];

  for (let i = 0; i < count; i++) {
    let fixture = generateLegalDocumentTestData(pattern);

    if (modifier) {
      fixture = modifier(fixture, i);
    } else if (pattern === 'unique') {
      // Add index to ensure uniqueness even with same timestamp
    }

    fixtures.push(fixture);
  }

  return fixtures;
}

/**
 * Generate random legal_document test data
 *
 * Creates test data with randomized values within valid ranges.
 * Useful for fuzz testing and edge case discovery.
 *
 * @param overrides - Optional field overrides
 * @returns Randomly generated test data
 *
 * @example
 * ```typescript
 * // Generate random test data
 * const randomData = generateRandomLegalDocumentData();
 *
 * // Generate random data with specific field
 * const customRandom = generateRandomLegalDocumentData({
 *   is_active: true,
 * });
 * ```
 */
export function generateRandomLegalDocumentData(
  overrides: Partial<LegalDocumentCreateRequest> = {}
): LegalDocumentCreateRequest {
  const data: LegalDocumentCreateRequest = {
    user_id: randomString(randomInt(None, Math.min(100, 50))),
    template_id: randomString(randomInt(None, Math.min(None, 50))),
    investor_id: randomString(randomInt(None, Math.min(None, 50))),
    document_type: randomString(randomInt(None, Math.min(50, 50))),
    title: randomPick(SAMPLE_NAMES),
    status: randomString(randomInt(None, Math.min(20, 50))),
    configuration: '',
    content_markdown: randomString(randomInt(None, Math.min(None, 50))),
    content_html: randomString(randomInt(None, Math.min(None, 50))),
    file_path_docx: randomString(randomInt(None, Math.min(500, 50))),
    file_path_pdf: randomString(randomInt(None, Math.min(500, 50))),
    version: randomInt(0, 1000),
    generated_at: randomString(randomInt(None, Math.min(None, 50))),
  };

  return { ...data, ...overrides };
}

// ============================================================================
// Validation Test Data
// ============================================================================

/**
 * Edge case test data for boundary testing
 *
 * Contains values at the boundaries of valid ranges.
 */
export const legalDocumentEdgeCases = {
  /** user_id at maximum length (100 chars) */
  user_idMaxLength: 'x'.repeat(100),
  /** document_type at maximum length (50 chars) */
  document_typeMaxLength: 'x'.repeat(50),
  /** title at maximum length (255 chars) */
  titleMaxLength: 'x'.repeat(255),
  /** status at maximum length (20 chars) */
  statusMaxLength: 'x'.repeat(20),
  /** file_path_docx at maximum length (500 chars) */
  file_path_docxMaxLength: 'x'.repeat(500),
  /** file_path_pdf at maximum length (500 chars) */
  file_path_pdfMaxLength: 'x'.repeat(500),
};

/**
 * Special character test data for input sanitization testing
 */
export const legalDocumentSpecialCharacterData: Partial<LegalDocumentCreateRequest> = {
};

/**
 * Whitespace test data for trimming validation
 */
export const legalDocumentWhitespaceData: Partial<LegalDocumentCreateRequest> = {
};

// ============================================================================
// Exports
// ============================================================================

// Default export - main fixture collection
export default legalDocumentFixtures;
