/**
 * VestingSchedule Test Fixtures
 *
 * Test data fixtures for VestingSchedule E2E and unit tests.
 * Provides predefined test data and factory functions for generating
 * test vesting_schedules.
 *
 * Fixture patterns:
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields with realistic test values
 * - invalid: Invalid data for validation testing
 * - unique: Unique values using timestamps for test isolation
 *
 * Vesting schedule for equity grants — cliff period, total period, and acceleration triggers
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:55.773189Z
 */

import type { VestingScheduleCreateRequest } from '../../types/vesting_schedule.types';

// ============================================================================
// Types
// ============================================================================

/**
 * Test data pattern types
 *
 * @description
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields populated with realistic test data
 * - invalid: Data that should fail validation
 * - unique: Data with timestamps for test isolation
 */
export type TestDataPattern = 'minimal' | 'complete' | 'invalid' | 'unique';

/**
 * VestingSchedule test fixture containing all test data patterns
 */
export interface VestingScheduleTestFixtures {
  /** Minimal valid vesting_schedule with only required fields */
  minimal: VestingScheduleCreateRequest;
  /** Complete vesting_schedule with all fields populated */
  complete: VestingScheduleCreateRequest;
  /** Invalid vesting_schedule for testing validation errors */
  invalid: Partial<VestingScheduleCreateRequest>;
  /** Unique vesting_schedule with timestamp-based values */
  unique: VestingScheduleCreateRequest;
  /** Multiple vesting_schedules for list testing */
  list: VestingScheduleCreateRequest[];
}

// ============================================================================
// Helper Functions
// ============================================================================

/**
 * Generate a unique identifier based on current timestamp
 * Useful for creating unique test data that won't conflict
 *
 * @param prefix - Optional prefix for the identifier
 * @returns Unique string identifier
 */
function generateUniqueId(prefix = ''): string {
  const timestamp = Date.now();
  const random = Math.random().toString(36).substring(2, 8);
  return prefix ? `${prefix}-${timestamp}-${random}` : `${timestamp}-${random}`;
}

/**
 * Generate a random string of specified length
 *
 * @param length - Length of the string to generate
 * @param charset - Character set to use (default: alphanumeric)
 * @returns Random string
 */
function randomString(
  length: number,
  charset = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'
): string {
  let result = '';
  for (let i = 0; i < length; i++) {
    result += charset.charAt(Math.floor(Math.random() * charset.length));
  }
  return result;
}

/**
 * Generate a random integer between min and max (inclusive)
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @returns Random integer
 */
function randomInt(min: number, max: number): number {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

/**
 * Generate a random decimal with specified precision
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @param precision - Number of decimal places
 * @returns Random decimal number
 */
function randomDecimal(min: number, max: number, precision = 2): number {
  const value = Math.random() * (max - min) + min;
  const factor = Math.pow(10, precision);
  return Math.round(value * factor) / factor;
}

/**
 * Generate a random date within a range
 *
 * @param startDate - Start of the range
 * @param endDate - End of the range
 * @returns Random date as ISO string
 */
function randomDate(startDate: Date, endDate: Date): string {
  const start = startDate.getTime();
  const end = endDate.getTime();
  const randomTime = start + Math.random() * (end - start);
  return new Date(randomTime).toISOString().split('T')[0];
}

/**
 * Pick a random element from an array
 *
 * @param array - Array to pick from
 * @returns Random element
 */
function randomPick<T>(array: T[]): T {
  return array[Math.floor(Math.random() * array.length)];
}

// ============================================================================
// Static Test Data
// ============================================================================


/**
 * Sample vesting_schedule names for generating realistic test data
 */
const SAMPLE_NAMES = [
  'Test VestingSchedule Alpha',
  'Test VestingSchedule Beta',
  'Test VestingSchedule Gamma',
  'Sample VestingSchedule One',
  'Sample VestingSchedule Two',
  'Demo VestingSchedule',
  'Example VestingSchedule',
];

/**
 * Sample codes for generating realistic test data
 */
const SAMPLE_CODES = [
  'TST001',
  'TST002',
  'TST003',
  'DEMO01',
  'DEMO02',
  'SMPL01',
  'SMPL02',
];

/**
 * Sample descriptions for generating realistic test data
 */
const SAMPLE_DESCRIPTIONS = [
  'This is a test vesting_schedule for automated testing.',
  'Sample vesting_schedule created for E2E tests.',
  'Demo vesting_schedule with all fields populated.',
  'Example vesting_schedule for validation testing.',
  'Test data fixture for vesting_schedule operations.',
];

// ============================================================================
// Predefined Fixtures
// ============================================================================

/**
 * Minimal valid vesting_schedule fixture
 *
 * Contains only required fields with minimal valid values.
 * Use this when testing that minimal data is accepted.
 */
export const minimalVestingScheduleFixture: VestingScheduleCreateRequest = {
};

/**
 * Complete vesting_schedule fixture
 *
 * Contains all fields with realistic test values.
 * Use this when testing full CRUD operations.
 */
export const completeVestingScheduleFixture: VestingScheduleCreateRequest = {
  equity_grant_id: 'Test Equity Grant Id',
  total_period_months: 42,
  cliff_months: 42,
  start_date: 'Test Start Date',
  acceleration_trigger: 'Test Acceleration Trigger',
};

/**
 * Unique vesting_schedule fixture
 *
 * Contains all fields with timestamp-based unique values.
 * Use this when testing to ensure data isolation between test runs.
 * Each field that needs uniqueness includes a timestamp/random component.
 */
export const uniqueVestingScheduleFixture: VestingScheduleCreateRequest = {
  equity_grant_id: `Equity Grant Id ${generateUniqueId()}`,
  total_period_months: Math.floor(Math.random() * 1000),
  cliff_months: Math.floor(Math.random() * 1000),
  start_date: `Start Date ${generateUniqueId()}`,
  acceleration_trigger: `Acceleration Trigger ${generateUniqueId()}`,
};

/**
 * Invalid vesting_schedule fixture
 *
 * Contains invalid data that should fail validation.
 * Use this when testing validation error handling.
 * Covers: empty required fields, values out of range, invalid formats.
 */
export const invalidVestingScheduleFixture: Partial<VestingScheduleCreateRequest> = {
};

/**
 * Invalid data patterns for comprehensive validation testing
 *
 * Contains various invalid data scenarios:
 * - emptyRequired: All required fields left empty
 * - tooShort: String fields below minimum length
 * - tooLong: String fields exceeding maximum length
 * - outOfRangeMin: Numeric fields below minimum value
 * - outOfRangeMax: Numeric fields above maximum value
 * - wrongType: Fields with incorrect data types (as strings for type coercion testing)
 * - specialChars: Fields with potentially problematic characters
 */
export const vestingScheduleInvalidPatterns = {
  /** All required string fields empty */
  emptyRequired: {
  } as Partial<VestingScheduleCreateRequest>,


  /** String fields exceeding maximum length */
  tooLong: {
    acceleration_trigger: 'x'.repeat(70), // max: 20
  } as Partial<VestingScheduleCreateRequest>,



  /** Fields with SQL injection attempts (for security testing) */
  sqlInjection: {
  } as Partial<VestingScheduleCreateRequest>,

  /** Fields with XSS attempts (for security testing) */
  xssAttempts: {
  } as Partial<VestingScheduleCreateRequest>,

  /** Fields with null values (for nullable field testing) */
  nullValues: {
  } as Partial<VestingScheduleCreateRequest>,

  /** Fields with undefined values (for missing field testing) */
  undefinedValues: {
    // Intentionally empty - represents missing fields
  } as Partial<VestingScheduleCreateRequest>,
};

/**
 * Collection of vesting_schedule fixtures for list testing
 *
 * Contains multiple vesting_schedules for testing list operations,
 * pagination, filtering, and sorting.
 */
export const vestingScheduleListFixtures: VestingScheduleCreateRequest[] = [
  {
    equity_grant_id: 'Equity Grant Id 1',
    total_period_months: 10,
    cliff_months: 10,
    start_date: 'Start Date 1',
    acceleration_trigger: 'Acceleration Trigger 1',
  },
  {
    equity_grant_id: 'Equity Grant Id 2',
    total_period_months: 20,
    cliff_months: 20,
    start_date: 'Start Date 2',
    acceleration_trigger: 'Acceleration Trigger 2',
  },
  {
    equity_grant_id: 'Equity Grant Id 3',
    total_period_months: 30,
    cliff_months: 30,
    start_date: 'Start Date 3',
    acceleration_trigger: 'Acceleration Trigger 3',
  },
  {
    equity_grant_id: 'Equity Grant Id 4',
    total_period_months: 40,
    cliff_months: 40,
    start_date: 'Start Date 4',
    acceleration_trigger: 'Acceleration Trigger 4',
  },
  {
    equity_grant_id: 'Equity Grant Id 5',
    total_period_months: 50,
    cliff_months: 50,
    start_date: 'Start Date 5',
    acceleration_trigger: 'Acceleration Trigger 5',
  },
];

// ============================================================================
// Fixture Collection
// ============================================================================

/**
 * All vesting_schedule test fixtures
 *
 * @example
 * ```typescript
 * import { vestingScheduleFixtures } from '../fixtures/vesting_scheduleFixtures';
 *
 * // Use minimal fixture
 * const minData = vestingScheduleFixtures.minimal;
 *
 * // Use complete fixture
 * const fullData = vestingScheduleFixtures.complete;
 *
 * // Use unique fixture (with timestamp)
 * const uniqueData = generateVestingScheduleTestData('unique');
 * ```
 */
export const vestingScheduleFixtures: VestingScheduleTestFixtures = {
  minimal: minimalVestingScheduleFixture,
  complete: completeVestingScheduleFixture,
  invalid: invalidVestingScheduleFixture as VestingScheduleCreateRequest,
  unique: uniqueVestingScheduleFixture,
  list: vestingScheduleListFixtures,
};

// ============================================================================
// Factory Functions
// ============================================================================

/**
 * Generate vesting_schedule test data based on pattern
 *
 * @param pattern - The test data pattern to generate
 * @param overrides - Optional field overrides
 * @returns Generated test data
 *
 * @example
 * ```typescript
 * // Generate complete test data
 * const data = generateVestingScheduleTestData('complete');
 *
 * // Generate unique test data with overrides
 * const customData = generateVestingScheduleTestData('unique', {
 *   is_active: false,
 * });
 * ```
 */
export function generateVestingScheduleTestData(
  pattern: TestDataPattern = 'complete',
  overrides: Partial<VestingScheduleCreateRequest> = {}
): VestingScheduleCreateRequest {
  let baseData: VestingScheduleCreateRequest;

  switch (pattern) {
    case 'minimal':
      baseData = { ...minimalVestingScheduleFixture };
      break;

    case 'complete':
      baseData = { ...completeVestingScheduleFixture };
      break;

    case 'invalid':
      // Return invalid data (caller should handle validation failure)
      baseData = { ...invalidVestingScheduleFixture } as VestingScheduleCreateRequest;
      break;

    case 'unique':
      // Generate fresh unique values for each call
      baseData = {
        ...uniqueVestingScheduleFixture,
      };
      break;

    default:
      baseData = { ...completeVestingScheduleFixture };
  }

  return { ...baseData, ...overrides };
}

/**
 * Generate multiple vesting_schedule fixtures
 *
 * @param count - Number of fixtures to generate
 * @param pattern - The test data pattern to use as base
 * @param modifier - Optional function to modify each fixture
 * @returns Array of generated test data
 *
 * @example
 * ```typescript
 * // Generate 5 unique fixtures
 * const fixtures = generateMultipleVestingScheduleFixtures(5, 'unique');
 *
 * // Generate fixtures with sequential numbering
 * const numbered = generateMultipleVestingScheduleFixtures(3, 'complete', (data, index) => ({
 *   ...data,
 *   name: `VestingSchedule #${index + 1}`,
 * }));
 * ```
 */
export function generateMultipleVestingScheduleFixtures(
  count: number,
  pattern: TestDataPattern = 'unique',
  modifier?: (data: VestingScheduleCreateRequest, index: number) => VestingScheduleCreateRequest
): VestingScheduleCreateRequest[] {
  const fixtures: VestingScheduleCreateRequest[] = [];

  for (let i = 0; i < count; i++) {
    let fixture = generateVestingScheduleTestData(pattern);

    if (modifier) {
      fixture = modifier(fixture, i);
    } else if (pattern === 'unique') {
      // Add index to ensure uniqueness even with same timestamp
    }

    fixtures.push(fixture);
  }

  return fixtures;
}

/**
 * Generate random vesting_schedule test data
 *
 * Creates test data with randomized values within valid ranges.
 * Useful for fuzz testing and edge case discovery.
 *
 * @param overrides - Optional field overrides
 * @returns Randomly generated test data
 *
 * @example
 * ```typescript
 * // Generate random test data
 * const randomData = generateRandomVestingScheduleData();
 *
 * // Generate random data with specific field
 * const customRandom = generateRandomVestingScheduleData({
 *   is_active: true,
 * });
 * ```
 */
export function generateRandomVestingScheduleData(
  overrides: Partial<VestingScheduleCreateRequest> = {}
): VestingScheduleCreateRequest {
  const data: VestingScheduleCreateRequest = {
    equity_grant_id: randomString(randomInt(None, Math.min(None, 50))),
    total_period_months: randomInt(0, 1000),
    cliff_months: randomInt(0, 1000),
    start_date: randomString(randomInt(None, Math.min(None, 50))),
    acceleration_trigger: randomString(randomInt(None, Math.min(20, 50))),
  };

  return { ...data, ...overrides };
}

// ============================================================================
// Validation Test Data
// ============================================================================

/**
 * Edge case test data for boundary testing
 *
 * Contains values at the boundaries of valid ranges.
 */
export const vestingScheduleEdgeCases = {
  /** acceleration_trigger at maximum length (20 chars) */
  acceleration_triggerMaxLength: 'x'.repeat(20),
};

/**
 * Special character test data for input sanitization testing
 */
export const vestingScheduleSpecialCharacterData: Partial<VestingScheduleCreateRequest> = {
};

/**
 * Whitespace test data for trimming validation
 */
export const vestingScheduleWhitespaceData: Partial<VestingScheduleCreateRequest> = {
};

// ============================================================================
// Exports
// ============================================================================

// Default export - main fixture collection
export default vestingScheduleFixtures;
