/**
 * DocumentParty Test Fixtures
 *
 * Test data fixtures for DocumentParty E2E and unit tests.
 * Provides predefined test data and factory functions for generating
 * test document_parties.
 *
 * Fixture patterns:
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields with realistic test values
 * - invalid: Invalid data for validation testing
 * - unique: Unique values using timestamps for test isolation
 *
 * Links a legal document to its parties (entity + signatory) with role designation
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:51.112119Z
 */

import type { DocumentPartyCreateRequest } from '../../types/document_party.types';

// ============================================================================
// Types
// ============================================================================

/**
 * Test data pattern types
 *
 * @description
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields populated with realistic test data
 * - invalid: Data that should fail validation
 * - unique: Data with timestamps for test isolation
 */
export type TestDataPattern = 'minimal' | 'complete' | 'invalid' | 'unique';

/**
 * DocumentParty test fixture containing all test data patterns
 */
export interface DocumentPartyTestFixtures {
  /** Minimal valid document_party with only required fields */
  minimal: DocumentPartyCreateRequest;
  /** Complete document_party with all fields populated */
  complete: DocumentPartyCreateRequest;
  /** Invalid document_party for testing validation errors */
  invalid: Partial<DocumentPartyCreateRequest>;
  /** Unique document_party with timestamp-based values */
  unique: DocumentPartyCreateRequest;
  /** Multiple document_parties for list testing */
  list: DocumentPartyCreateRequest[];
}

// ============================================================================
// Helper Functions
// ============================================================================

/**
 * Generate a unique identifier based on current timestamp
 * Useful for creating unique test data that won't conflict
 *
 * @param prefix - Optional prefix for the identifier
 * @returns Unique string identifier
 */
function generateUniqueId(prefix = ''): string {
  const timestamp = Date.now();
  const random = Math.random().toString(36).substring(2, 8);
  return prefix ? `${prefix}-${timestamp}-${random}` : `${timestamp}-${random}`;
}

/**
 * Generate a random string of specified length
 *
 * @param length - Length of the string to generate
 * @param charset - Character set to use (default: alphanumeric)
 * @returns Random string
 */
function randomString(
  length: number,
  charset = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'
): string {
  let result = '';
  for (let i = 0; i < length; i++) {
    result += charset.charAt(Math.floor(Math.random() * charset.length));
  }
  return result;
}

/**
 * Generate a random integer between min and max (inclusive)
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @returns Random integer
 */
function randomInt(min: number, max: number): number {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

/**
 * Generate a random decimal with specified precision
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @param precision - Number of decimal places
 * @returns Random decimal number
 */
function randomDecimal(min: number, max: number, precision = 2): number {
  const value = Math.random() * (max - min) + min;
  const factor = Math.pow(10, precision);
  return Math.round(value * factor) / factor;
}

/**
 * Generate a random date within a range
 *
 * @param startDate - Start of the range
 * @param endDate - End of the range
 * @returns Random date as ISO string
 */
function randomDate(startDate: Date, endDate: Date): string {
  const start = startDate.getTime();
  const end = endDate.getTime();
  const randomTime = start + Math.random() * (end - start);
  return new Date(randomTime).toISOString().split('T')[0];
}

/**
 * Pick a random element from an array
 *
 * @param array - Array to pick from
 * @returns Random element
 */
function randomPick<T>(array: T[]): T {
  return array[Math.floor(Math.random() * array.length)];
}

// ============================================================================
// Static Test Data
// ============================================================================


/**
 * Sample document_party names for generating realistic test data
 */
const SAMPLE_NAMES = [
  'Test DocumentParty Alpha',
  'Test DocumentParty Beta',
  'Test DocumentParty Gamma',
  'Sample DocumentParty One',
  'Sample DocumentParty Two',
  'Demo DocumentParty',
  'Example DocumentParty',
];

/**
 * Sample codes for generating realistic test data
 */
const SAMPLE_CODES = [
  'TST001',
  'TST002',
  'TST003',
  'DEMO01',
  'DEMO02',
  'SMPL01',
  'SMPL02',
];

/**
 * Sample descriptions for generating realistic test data
 */
const SAMPLE_DESCRIPTIONS = [
  'This is a test document_party for automated testing.',
  'Sample document_party created for E2E tests.',
  'Demo document_party with all fields populated.',
  'Example document_party for validation testing.',
  'Test data fixture for document_party operations.',
];

// ============================================================================
// Predefined Fixtures
// ============================================================================

/**
 * Minimal valid document_party fixture
 *
 * Contains only required fields with minimal valid values.
 * Use this when testing that minimal data is accepted.
 */
export const minimalDocumentPartyFixture: DocumentPartyCreateRequest = {
};

/**
 * Complete document_party fixture
 *
 * Contains all fields with realistic test values.
 * Use this when testing full CRUD operations.
 */
export const completeDocumentPartyFixture: DocumentPartyCreateRequest = {
  legal_document_id: 'Test Legal Document Id',
  legal_entity_id: 'Test Legal Entity Id',
  signatory_id: 'Test Signatory Id',
  party_role: 'Test Party Role',
  party_label: 'Test Party Label',
};

/**
 * Unique document_party fixture
 *
 * Contains all fields with timestamp-based unique values.
 * Use this when testing to ensure data isolation between test runs.
 * Each field that needs uniqueness includes a timestamp/random component.
 */
export const uniqueDocumentPartyFixture: DocumentPartyCreateRequest = {
  legal_document_id: `Legal Document Id ${generateUniqueId()}`,
  legal_entity_id: `Legal Entity Id ${generateUniqueId()}`,
  signatory_id: `Signatory Id ${generateUniqueId()}`,
  party_role: `Party Role ${generateUniqueId()}`,
  party_label: `Party Label ${generateUniqueId()}`,
};

/**
 * Invalid document_party fixture
 *
 * Contains invalid data that should fail validation.
 * Use this when testing validation error handling.
 * Covers: empty required fields, values out of range, invalid formats.
 */
export const invalidDocumentPartyFixture: Partial<DocumentPartyCreateRequest> = {
};

/**
 * Invalid data patterns for comprehensive validation testing
 *
 * Contains various invalid data scenarios:
 * - emptyRequired: All required fields left empty
 * - tooShort: String fields below minimum length
 * - tooLong: String fields exceeding maximum length
 * - outOfRangeMin: Numeric fields below minimum value
 * - outOfRangeMax: Numeric fields above maximum value
 * - wrongType: Fields with incorrect data types (as strings for type coercion testing)
 * - specialChars: Fields with potentially problematic characters
 */
export const documentPartyInvalidPatterns = {
  /** All required string fields empty */
  emptyRequired: {
  } as Partial<DocumentPartyCreateRequest>,


  /** String fields exceeding maximum length */
  tooLong: {
    party_role: 'x'.repeat(70), // max: 20
    party_label: 'x'.repeat(100), // max: 50
  } as Partial<DocumentPartyCreateRequest>,



  /** Fields with SQL injection attempts (for security testing) */
  sqlInjection: {
  } as Partial<DocumentPartyCreateRequest>,

  /** Fields with XSS attempts (for security testing) */
  xssAttempts: {
  } as Partial<DocumentPartyCreateRequest>,

  /** Fields with null values (for nullable field testing) */
  nullValues: {
    signatory_id: null,
    party_label: null,
  } as Partial<DocumentPartyCreateRequest>,

  /** Fields with undefined values (for missing field testing) */
  undefinedValues: {
    // Intentionally empty - represents missing fields
  } as Partial<DocumentPartyCreateRequest>,
};

/**
 * Collection of document_party fixtures for list testing
 *
 * Contains multiple document_parties for testing list operations,
 * pagination, filtering, and sorting.
 */
export const documentPartyListFixtures: DocumentPartyCreateRequest[] = [
  {
    legal_document_id: 'Legal Document Id 1',
    legal_entity_id: 'Legal Entity Id 1',
    signatory_id: 'Signatory Id 1',
    party_role: 'Party Role 1',
    party_label: 'Party Label 1',
  },
  {
    legal_document_id: 'Legal Document Id 2',
    legal_entity_id: 'Legal Entity Id 2',
    signatory_id: 'Signatory Id 2',
    party_role: 'Party Role 2',
    party_label: 'Party Label 2',
  },
  {
    legal_document_id: 'Legal Document Id 3',
    legal_entity_id: 'Legal Entity Id 3',
    signatory_id: 'Signatory Id 3',
    party_role: 'Party Role 3',
    party_label: 'Party Label 3',
  },
  {
    legal_document_id: 'Legal Document Id 4',
    legal_entity_id: 'Legal Entity Id 4',
    signatory_id: 'Signatory Id 4',
    party_role: 'Party Role 4',
    party_label: 'Party Label 4',
  },
  {
    legal_document_id: 'Legal Document Id 5',
    legal_entity_id: 'Legal Entity Id 5',
    signatory_id: 'Signatory Id 5',
    party_role: 'Party Role 5',
    party_label: 'Party Label 5',
  },
];

// ============================================================================
// Fixture Collection
// ============================================================================

/**
 * All document_party test fixtures
 *
 * @example
 * ```typescript
 * import { documentPartyFixtures } from '../fixtures/document_partyFixtures';
 *
 * // Use minimal fixture
 * const minData = documentPartyFixtures.minimal;
 *
 * // Use complete fixture
 * const fullData = documentPartyFixtures.complete;
 *
 * // Use unique fixture (with timestamp)
 * const uniqueData = generateDocumentPartyTestData('unique');
 * ```
 */
export const documentPartyFixtures: DocumentPartyTestFixtures = {
  minimal: minimalDocumentPartyFixture,
  complete: completeDocumentPartyFixture,
  invalid: invalidDocumentPartyFixture as DocumentPartyCreateRequest,
  unique: uniqueDocumentPartyFixture,
  list: documentPartyListFixtures,
};

// ============================================================================
// Factory Functions
// ============================================================================

/**
 * Generate document_party test data based on pattern
 *
 * @param pattern - The test data pattern to generate
 * @param overrides - Optional field overrides
 * @returns Generated test data
 *
 * @example
 * ```typescript
 * // Generate complete test data
 * const data = generateDocumentPartyTestData('complete');
 *
 * // Generate unique test data with overrides
 * const customData = generateDocumentPartyTestData('unique', {
 *   is_active: false,
 * });
 * ```
 */
export function generateDocumentPartyTestData(
  pattern: TestDataPattern = 'complete',
  overrides: Partial<DocumentPartyCreateRequest> = {}
): DocumentPartyCreateRequest {
  let baseData: DocumentPartyCreateRequest;

  switch (pattern) {
    case 'minimal':
      baseData = { ...minimalDocumentPartyFixture };
      break;

    case 'complete':
      baseData = { ...completeDocumentPartyFixture };
      break;

    case 'invalid':
      // Return invalid data (caller should handle validation failure)
      baseData = { ...invalidDocumentPartyFixture } as DocumentPartyCreateRequest;
      break;

    case 'unique':
      // Generate fresh unique values for each call
      baseData = {
        ...uniqueDocumentPartyFixture,
      };
      break;

    default:
      baseData = { ...completeDocumentPartyFixture };
  }

  return { ...baseData, ...overrides };
}

/**
 * Generate multiple document_party fixtures
 *
 * @param count - Number of fixtures to generate
 * @param pattern - The test data pattern to use as base
 * @param modifier - Optional function to modify each fixture
 * @returns Array of generated test data
 *
 * @example
 * ```typescript
 * // Generate 5 unique fixtures
 * const fixtures = generateMultipleDocumentPartyFixtures(5, 'unique');
 *
 * // Generate fixtures with sequential numbering
 * const numbered = generateMultipleDocumentPartyFixtures(3, 'complete', (data, index) => ({
 *   ...data,
 *   name: `DocumentParty #${index + 1}`,
 * }));
 * ```
 */
export function generateMultipleDocumentPartyFixtures(
  count: number,
  pattern: TestDataPattern = 'unique',
  modifier?: (data: DocumentPartyCreateRequest, index: number) => DocumentPartyCreateRequest
): DocumentPartyCreateRequest[] {
  const fixtures: DocumentPartyCreateRequest[] = [];

  for (let i = 0; i < count; i++) {
    let fixture = generateDocumentPartyTestData(pattern);

    if (modifier) {
      fixture = modifier(fixture, i);
    } else if (pattern === 'unique') {
      // Add index to ensure uniqueness even with same timestamp
    }

    fixtures.push(fixture);
  }

  return fixtures;
}

/**
 * Generate random document_party test data
 *
 * Creates test data with randomized values within valid ranges.
 * Useful for fuzz testing and edge case discovery.
 *
 * @param overrides - Optional field overrides
 * @returns Randomly generated test data
 *
 * @example
 * ```typescript
 * // Generate random test data
 * const randomData = generateRandomDocumentPartyData();
 *
 * // Generate random data with specific field
 * const customRandom = generateRandomDocumentPartyData({
 *   is_active: true,
 * });
 * ```
 */
export function generateRandomDocumentPartyData(
  overrides: Partial<DocumentPartyCreateRequest> = {}
): DocumentPartyCreateRequest {
  const data: DocumentPartyCreateRequest = {
    legal_document_id: randomString(randomInt(None, Math.min(None, 50))),
    legal_entity_id: randomString(randomInt(None, Math.min(None, 50))),
    signatory_id: randomString(randomInt(None, Math.min(None, 50))),
    party_role: randomString(randomInt(None, Math.min(20, 50))),
    party_label: randomString(randomInt(None, Math.min(50, 50))),
  };

  return { ...data, ...overrides };
}

// ============================================================================
// Validation Test Data
// ============================================================================

/**
 * Edge case test data for boundary testing
 *
 * Contains values at the boundaries of valid ranges.
 */
export const documentPartyEdgeCases = {
  /** party_role at maximum length (20 chars) */
  party_roleMaxLength: 'x'.repeat(20),
  /** party_label at maximum length (50 chars) */
  party_labelMaxLength: 'x'.repeat(50),
};

/**
 * Special character test data for input sanitization testing
 */
export const documentPartySpecialCharacterData: Partial<DocumentPartyCreateRequest> = {
};

/**
 * Whitespace test data for trimming validation
 */
export const documentPartyWhitespaceData: Partial<DocumentPartyCreateRequest> = {
};

// ============================================================================
// Exports
// ============================================================================

// Default export - main fixture collection
export default documentPartyFixtures;
