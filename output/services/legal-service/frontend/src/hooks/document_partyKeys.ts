/**
 * DocumentParty Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all document_party queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['documentparties'] - All document_party queries
 * - ['documentparties', 'list'] - All list queries
 * - ['documentparties', 'list', { filters }] - Specific filtered list
 * - ['documentparties', 'detail', id] - Specific document_party by ID
 *
 * @example
 * ```typescript
 * import { document_partyKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: document_partyKeys.list({ is_active: true }),
 *   queryFn: () => document_partyService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all document_party queries
 * queryClient.invalidateQueries({ queryKey: document_partyKeys.all });
 *
 * // Invalidate specific document_party
 * queryClient.invalidateQueries({ queryKey: document_partyKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:50.881926Z
 */

import type { DocumentPartyFilterOptions } from '../types/document_party.types';

/**
 * Root query key for DocumentParty entity
 * Used as namespace prefix for all document_party queries
 */
const DOCUMENT_PARTY_ROOT = 'documentparties' as const;

/**
 * DocumentParty Query Keys
 *
 * Hierarchical query key factory for document_party queries:
 * - all: Base key for all document_party queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific document_party by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = document_partyKeys.list({ is_active: true });
 * // Result: ['documentparties', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = document_partyKeys.detail('uuid-here');
 * // Result: ['documentparties', 'detail', 'uuid-here']
 *
 * // Invalidate all document_party data
 * queryClient.invalidateQueries({ queryKey: document_partyKeys.all });
 * ```
 */
export const document_partyKeys = {
  /**
   * Base key for all document_party queries
   * Use this to invalidate all document_party-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: document_partyKeys.all });
   * ```
   */
  all: [DOCUMENT_PARTY_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: document_partyKeys.lists() });
   * ```
   */
  lists: () => [...document_partyKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = document_partyKeys.list({ is_active: true });
   *
   * // List with search
   * const key = document_partyKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = document_partyKeys.list();
   * ```
   */
  list: (filters?: DocumentPartyFilterOptions) =>
    [...document_partyKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: document_partyKeys.details() });
   * ```
   */
  details: () => [...document_partyKeys.all, 'detail'] as const,

  /**
   * Key for a specific document_party by ID
   * Use this for fetching and invalidating individual document_party records
   *
   * @param id - DocumentParty ID (UUID)
   * @returns Query key tuple for specific document_party
   *
   * @example
   * ```typescript
   * // Fetch specific document_party
   * const { data } = useQuery({
   *   queryKey: document_partyKeys.detail('uuid-here'),
   *   queryFn: () => document_partyService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: document_partyKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...document_partyKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active document_parties (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = document_partyKeys.dropdown();
   *
   * // Include inactive items
   * const key = document_partyKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...document_partyKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * DocumentPartys filtered by legal document ID
   *
   * @param legalDocumentId - LegalDocument ID (UUID)
   * @returns Query key tuple
   */
  byLegalDocument: (legalDocumentId: string) =>
    [...document_partyKeys.all, 'by-legal-document', legalDocumentId] as const,

  /**
   * DocumentPartys filtered by legal entity ID
   *
   * @param legalEntityId - LegalEntity ID (UUID)
   * @returns Query key tuple
   */
  byLegalEntity: (legalEntityId: string) =>
    [...document_partyKeys.all, 'by-legal-entity', legalEntityId] as const,

  /**
   * DocumentPartys filtered by signatory ID
   *
   * @param signatoryId - Signatory ID (UUID)
   * @returns Query key tuple
   */
  bySignatory: (signatoryId: string) =>
    [...document_partyKeys.all, 'by-signatory', signatoryId] as const,
} as const;

/**
 * Type representing any valid DocumentParty query key
 * Useful for typing query key arrays in generic contexts
 */
export type DocumentPartyQueryKey =
  | typeof document_partyKeys.all
  | ReturnType<typeof document_partyKeys.lists>
  | ReturnType<typeof document_partyKeys.list>
  | ReturnType<typeof document_partyKeys.details>
  | ReturnType<typeof document_partyKeys.detail>
  | ReturnType<typeof document_partyKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default document_partyKeys;
