/**
 * EquityGrant Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all equity_grant queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['equitygrants'] - All equity_grant queries
 * - ['equitygrants', 'list'] - All list queries
 * - ['equitygrants', 'list', { filters }] - Specific filtered list
 * - ['equitygrants', 'detail', id] - Specific equity_grant by ID
 *
 * @example
 * ```typescript
 * import { equity_grantKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: equity_grantKeys.list({ is_active: true }),
 *   queryFn: () => equity_grantService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all equity_grant queries
 * queryClient.invalidateQueries({ queryKey: equity_grantKeys.all });
 *
 * // Invalidate specific equity_grant
 * queryClient.invalidateQueries({ queryKey: equity_grantKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:52.360772Z
 */

import type { EquityGrantFilterOptions } from '../types/equity_grant.types';

/**
 * Root query key for EquityGrant entity
 * Used as namespace prefix for all equity_grant queries
 */
const EQUITY_GRANT_ROOT = 'equitygrants' as const;

/**
 * EquityGrant Query Keys
 *
 * Hierarchical query key factory for equity_grant queries:
 * - all: Base key for all equity_grant queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific equity_grant by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = equity_grantKeys.list({ is_active: true });
 * // Result: ['equitygrants', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = equity_grantKeys.detail('uuid-here');
 * // Result: ['equitygrants', 'detail', 'uuid-here']
 *
 * // Invalidate all equity_grant data
 * queryClient.invalidateQueries({ queryKey: equity_grantKeys.all });
 * ```
 */
export const equity_grantKeys = {
  /**
   * Base key for all equity_grant queries
   * Use this to invalidate all equity_grant-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: equity_grantKeys.all });
   * ```
   */
  all: [EQUITY_GRANT_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: equity_grantKeys.lists() });
   * ```
   */
  lists: () => [...equity_grantKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = equity_grantKeys.list({ is_active: true });
   *
   * // List with search
   * const key = equity_grantKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = equity_grantKeys.list();
   * ```
   */
  list: (filters?: EquityGrantFilterOptions) =>
    [...equity_grantKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: equity_grantKeys.details() });
   * ```
   */
  details: () => [...equity_grantKeys.all, 'detail'] as const,

  /**
   * Key for a specific equity_grant by ID
   * Use this for fetching and invalidating individual equity_grant records
   *
   * @param id - EquityGrant ID (UUID)
   * @returns Query key tuple for specific equity_grant
   *
   * @example
   * ```typescript
   * // Fetch specific equity_grant
   * const { data } = useQuery({
   *   queryKey: equity_grantKeys.detail('uuid-here'),
   *   queryFn: () => equity_grantService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: equity_grantKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...equity_grantKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active equity_grants (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = equity_grantKeys.dropdown();
   *
   * // Include inactive items
   * const key = equity_grantKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...equity_grantKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * EquityGrants filtered by legal entity ID
   *
   * @param legalEntityId - LegalEntity ID (UUID)
   * @returns Query key tuple
   */
  byLegalEntity: (legalEntityId: string) =>
    [...equity_grantKeys.all, 'by-legal-entity', legalEntityId] as const,

  /**
   * EquityGrants filtered by holder person ID
   *
   * @param holderPersonId - HolderPerson ID (UUID)
   * @returns Query key tuple
   */
  byHolderPerson: (holderPersonId: string) =>
    [...equity_grantKeys.all, 'by-holder-person', holderPersonId] as const,

  /**
   * EquityGrants filtered by holder entity ID
   *
   * @param holderEntityId - HolderEntity ID (UUID)
   * @returns Query key tuple
   */
  byHolderEntity: (holderEntityId: string) =>
    [...equity_grantKeys.all, 'by-holder-entity', holderEntityId] as const,

  /**
   * EquityGrants filtered by source document ID
   *
   * @param sourceDocumentId - SourceDocument ID (UUID)
   * @returns Query key tuple
   */
  bySourceDocument: (sourceDocumentId: string) =>
    [...equity_grantKeys.all, 'by-source-document', sourceDocumentId] as const,
} as const;

/**
 * Type representing any valid EquityGrant query key
 * Useful for typing query key arrays in generic contexts
 */
export type EquityGrantQueryKey =
  | typeof equity_grantKeys.all
  | ReturnType<typeof equity_grantKeys.lists>
  | ReturnType<typeof equity_grantKeys.list>
  | ReturnType<typeof equity_grantKeys.details>
  | ReturnType<typeof equity_grantKeys.detail>
  | ReturnType<typeof equity_grantKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default equity_grantKeys;
