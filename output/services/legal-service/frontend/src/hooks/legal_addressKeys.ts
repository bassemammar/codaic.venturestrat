/**
 * LegalAddress Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all legal_address queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['legaladdresses'] - All legal_address queries
 * - ['legaladdresses', 'list'] - All list queries
 * - ['legaladdresses', 'list', { filters }] - Specific filtered list
 * - ['legaladdresses', 'detail', id] - Specific legal_address by ID
 *
 * @example
 * ```typescript
 * import { legal_addressKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: legal_addressKeys.list({ is_active: true }),
 *   queryFn: () => legal_addressService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all legal_address queries
 * queryClient.invalidateQueries({ queryKey: legal_addressKeys.all });
 *
 * // Invalidate specific legal_address
 * queryClient.invalidateQueries({ queryKey: legal_addressKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:53.383443Z
 */

import type { LegalAddressFilterOptions } from '../types/legal_address.types';

/**
 * Root query key for LegalAddress entity
 * Used as namespace prefix for all legal_address queries
 */
const LEGAL_ADDRESS_ROOT = 'legaladdresses' as const;

/**
 * LegalAddress Query Keys
 *
 * Hierarchical query key factory for legal_address queries:
 * - all: Base key for all legal_address queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific legal_address by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = legal_addressKeys.list({ is_active: true });
 * // Result: ['legaladdresses', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = legal_addressKeys.detail('uuid-here');
 * // Result: ['legaladdresses', 'detail', 'uuid-here']
 *
 * // Invalidate all legal_address data
 * queryClient.invalidateQueries({ queryKey: legal_addressKeys.all });
 * ```
 */
export const legal_addressKeys = {
  /**
   * Base key for all legal_address queries
   * Use this to invalidate all legal_address-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: legal_addressKeys.all });
   * ```
   */
  all: [LEGAL_ADDRESS_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: legal_addressKeys.lists() });
   * ```
   */
  lists: () => [...legal_addressKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = legal_addressKeys.list({ is_active: true });
   *
   * // List with search
   * const key = legal_addressKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = legal_addressKeys.list();
   * ```
   */
  list: (filters?: LegalAddressFilterOptions) =>
    [...legal_addressKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: legal_addressKeys.details() });
   * ```
   */
  details: () => [...legal_addressKeys.all, 'detail'] as const,

  /**
   * Key for a specific legal_address by ID
   * Use this for fetching and invalidating individual legal_address records
   *
   * @param id - LegalAddress ID (UUID)
   * @returns Query key tuple for specific legal_address
   *
   * @example
   * ```typescript
   * // Fetch specific legal_address
   * const { data } = useQuery({
   *   queryKey: legal_addressKeys.detail('uuid-here'),
   *   queryFn: () => legal_addressService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: legal_addressKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...legal_addressKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active legal_addresses (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = legal_addressKeys.dropdown();
   *
   * // Include inactive items
   * const key = legal_addressKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...legal_addressKeys.all, 'dropdown', { activeOnly }] as const,
} as const;

/**
 * Type representing any valid LegalAddress query key
 * Useful for typing query key arrays in generic contexts
 */
export type LegalAddressQueryKey =
  | typeof legal_addressKeys.all
  | ReturnType<typeof legal_addressKeys.lists>
  | ReturnType<typeof legal_addressKeys.list>
  | ReturnType<typeof legal_addressKeys.details>
  | ReturnType<typeof legal_addressKeys.detail>
  | ReturnType<typeof legal_addressKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default legal_addressKeys;
