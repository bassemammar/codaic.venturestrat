/**
 * DocumentTemplate Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all document_template queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['documenttemplates'] - All document_template queries
 * - ['documenttemplates', 'list'] - All list queries
 * - ['documenttemplates', 'list', { filters }] - Specific filtered list
 * - ['documenttemplates', 'detail', id] - Specific document_template by ID
 *
 * @example
 * ```typescript
 * import { document_templateKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: document_templateKeys.list({ is_active: true }),
 *   queryFn: () => document_templateService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all document_template queries
 * queryClient.invalidateQueries({ queryKey: document_templateKeys.all });
 *
 * // Invalidate specific document_template
 * queryClient.invalidateQueries({ queryKey: document_templateKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:51.592211Z
 */

import type { DocumentTemplateFilterOptions } from '../types/document_template.types';

/**
 * Root query key for DocumentTemplate entity
 * Used as namespace prefix for all document_template queries
 */
const DOCUMENT_TEMPLATE_ROOT = 'documenttemplates' as const;

/**
 * DocumentTemplate Query Keys
 *
 * Hierarchical query key factory for document_template queries:
 * - all: Base key for all document_template queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific document_template by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = document_templateKeys.list({ is_active: true });
 * // Result: ['documenttemplates', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = document_templateKeys.detail('uuid-here');
 * // Result: ['documenttemplates', 'detail', 'uuid-here']
 *
 * // Invalidate all document_template data
 * queryClient.invalidateQueries({ queryKey: document_templateKeys.all });
 * ```
 */
export const document_templateKeys = {
  /**
   * Base key for all document_template queries
   * Use this to invalidate all document_template-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: document_templateKeys.all });
   * ```
   */
  all: [DOCUMENT_TEMPLATE_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: document_templateKeys.lists() });
   * ```
   */
  lists: () => [...document_templateKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = document_templateKeys.list({ is_active: true });
   *
   * // List with search
   * const key = document_templateKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = document_templateKeys.list();
   * ```
   */
  list: (filters?: DocumentTemplateFilterOptions) =>
    [...document_templateKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: document_templateKeys.details() });
   * ```
   */
  details: () => [...document_templateKeys.all, 'detail'] as const,

  /**
   * Key for a specific document_template by ID
   * Use this for fetching and invalidating individual document_template records
   *
   * @param id - DocumentTemplate ID (UUID)
   * @returns Query key tuple for specific document_template
   *
   * @example
   * ```typescript
   * // Fetch specific document_template
   * const { data } = useQuery({
   *   queryKey: document_templateKeys.detail('uuid-here'),
   *   queryFn: () => document_templateService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: document_templateKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...document_templateKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active document_templates (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = document_templateKeys.dropdown();
   *
   * // Include inactive items
   * const key = document_templateKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...document_templateKeys.all, 'dropdown', { activeOnly }] as const,
} as const;

/**
 * Type representing any valid DocumentTemplate query key
 * Useful for typing query key arrays in generic contexts
 */
export type DocumentTemplateQueryKey =
  | typeof document_templateKeys.all
  | ReturnType<typeof document_templateKeys.lists>
  | ReturnType<typeof document_templateKeys.list>
  | ReturnType<typeof document_templateKeys.details>
  | ReturnType<typeof document_templateKeys.detail>
  | ReturnType<typeof document_templateKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default document_templateKeys;
