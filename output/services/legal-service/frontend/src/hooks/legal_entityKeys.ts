/**
 * LegalEntity Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all legal_entity queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['legalentities'] - All legal_entity queries
 * - ['legalentities', 'list'] - All list queries
 * - ['legalentities', 'list', { filters }] - Specific filtered list
 * - ['legalentities', 'detail', id] - Specific legal_entity by ID
 *
 * @example
 * ```typescript
 * import { legal_entityKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: legal_entityKeys.list({ is_active: true }),
 *   queryFn: () => legal_entityService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all legal_entity queries
 * queryClient.invalidateQueries({ queryKey: legal_entityKeys.all });
 *
 * // Invalidate specific legal_entity
 * queryClient.invalidateQueries({ queryKey: legal_entityKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:54.480797Z
 */

import type { LegalEntityFilterOptions } from '../types/legal_entity.types';

/**
 * Root query key for LegalEntity entity
 * Used as namespace prefix for all legal_entity queries
 */
const LEGAL_ENTITY_ROOT = 'legalentities' as const;

/**
 * LegalEntity Query Keys
 *
 * Hierarchical query key factory for legal_entity queries:
 * - all: Base key for all legal_entity queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific legal_entity by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = legal_entityKeys.list({ is_active: true });
 * // Result: ['legalentities', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = legal_entityKeys.detail('uuid-here');
 * // Result: ['legalentities', 'detail', 'uuid-here']
 *
 * // Invalidate all legal_entity data
 * queryClient.invalidateQueries({ queryKey: legal_entityKeys.all });
 * ```
 */
export const legal_entityKeys = {
  /**
   * Base key for all legal_entity queries
   * Use this to invalidate all legal_entity-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: legal_entityKeys.all });
   * ```
   */
  all: [LEGAL_ENTITY_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: legal_entityKeys.lists() });
   * ```
   */
  lists: () => [...legal_entityKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = legal_entityKeys.list({ is_active: true });
   *
   * // List with search
   * const key = legal_entityKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = legal_entityKeys.list();
   * ```
   */
  list: (filters?: LegalEntityFilterOptions) =>
    [...legal_entityKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: legal_entityKeys.details() });
   * ```
   */
  details: () => [...legal_entityKeys.all, 'detail'] as const,

  /**
   * Key for a specific legal_entity by ID
   * Use this for fetching and invalidating individual legal_entity records
   *
   * @param id - LegalEntity ID (UUID)
   * @returns Query key tuple for specific legal_entity
   *
   * @example
   * ```typescript
   * // Fetch specific legal_entity
   * const { data } = useQuery({
   *   queryKey: legal_entityKeys.detail('uuid-here'),
   *   queryFn: () => legal_entityService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: legal_entityKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...legal_entityKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active legal_entities (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = legal_entityKeys.dropdown();
   *
   * // Include inactive items
   * const key = legal_entityKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...legal_entityKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * LegalEntitys filtered by user ID
   *
   * @param userId - User ID (UUID)
   * @returns Query key tuple
   */
  byUser: (userId: string) =>
    [...legal_entityKeys.all, 'by-user', userId] as const,

  /**
   * LegalEntitys filtered by registered address ID
   *
   * @param registeredAddressId - RegisteredAddress ID (UUID)
   * @returns Query key tuple
   */
  byRegisteredAddress: (registeredAddressId: string) =>
    [...legal_entityKeys.all, 'by-registered-address', registeredAddressId] as const,
} as const;

/**
 * Type representing any valid LegalEntity query key
 * Useful for typing query key arrays in generic contexts
 */
export type LegalEntityQueryKey =
  | typeof legal_entityKeys.all
  | ReturnType<typeof legal_entityKeys.lists>
  | ReturnType<typeof legal_entityKeys.list>
  | ReturnType<typeof legal_entityKeys.details>
  | ReturnType<typeof legal_entityKeys.detail>
  | ReturnType<typeof legal_entityKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default legal_entityKeys;
