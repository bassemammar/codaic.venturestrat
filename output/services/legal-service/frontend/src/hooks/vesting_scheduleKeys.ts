/**
 * VestingSchedule Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all vesting_schedule queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['vestingschedules'] - All vesting_schedule queries
 * - ['vestingschedules', 'list'] - All list queries
 * - ['vestingschedules', 'list', { filters }] - Specific filtered list
 * - ['vestingschedules', 'detail', id] - Specific vesting_schedule by ID
 *
 * @example
 * ```typescript
 * import { vesting_scheduleKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: vesting_scheduleKeys.list({ is_active: true }),
 *   queryFn: () => vesting_scheduleService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all vesting_schedule queries
 * queryClient.invalidateQueries({ queryKey: vesting_scheduleKeys.all });
 *
 * // Invalidate specific vesting_schedule
 * queryClient.invalidateQueries({ queryKey: vesting_scheduleKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:55.638315Z
 */

import type { VestingScheduleFilterOptions } from '../types/vesting_schedule.types';

/**
 * Root query key for VestingSchedule entity
 * Used as namespace prefix for all vesting_schedule queries
 */
const VESTING_SCHEDULE_ROOT = 'vestingschedules' as const;

/**
 * VestingSchedule Query Keys
 *
 * Hierarchical query key factory for vesting_schedule queries:
 * - all: Base key for all vesting_schedule queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific vesting_schedule by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = vesting_scheduleKeys.list({ is_active: true });
 * // Result: ['vestingschedules', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = vesting_scheduleKeys.detail('uuid-here');
 * // Result: ['vestingschedules', 'detail', 'uuid-here']
 *
 * // Invalidate all vesting_schedule data
 * queryClient.invalidateQueries({ queryKey: vesting_scheduleKeys.all });
 * ```
 */
export const vesting_scheduleKeys = {
  /**
   * Base key for all vesting_schedule queries
   * Use this to invalidate all vesting_schedule-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: vesting_scheduleKeys.all });
   * ```
   */
  all: [VESTING_SCHEDULE_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: vesting_scheduleKeys.lists() });
   * ```
   */
  lists: () => [...vesting_scheduleKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = vesting_scheduleKeys.list({ is_active: true });
   *
   * // List with search
   * const key = vesting_scheduleKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = vesting_scheduleKeys.list();
   * ```
   */
  list: (filters?: VestingScheduleFilterOptions) =>
    [...vesting_scheduleKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: vesting_scheduleKeys.details() });
   * ```
   */
  details: () => [...vesting_scheduleKeys.all, 'detail'] as const,

  /**
   * Key for a specific vesting_schedule by ID
   * Use this for fetching and invalidating individual vesting_schedule records
   *
   * @param id - VestingSchedule ID (UUID)
   * @returns Query key tuple for specific vesting_schedule
   *
   * @example
   * ```typescript
   * // Fetch specific vesting_schedule
   * const { data } = useQuery({
   *   queryKey: vesting_scheduleKeys.detail('uuid-here'),
   *   queryFn: () => vesting_scheduleService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: vesting_scheduleKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...vesting_scheduleKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active vesting_schedules (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = vesting_scheduleKeys.dropdown();
   *
   * // Include inactive items
   * const key = vesting_scheduleKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...vesting_scheduleKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * VestingSchedules filtered by equity grant ID
   *
   * @param equityGrantId - EquityGrant ID (UUID)
   * @returns Query key tuple
   */
  byEquityGrant: (equityGrantId: string) =>
    [...vesting_scheduleKeys.all, 'by-equity-grant', equityGrantId] as const,
} as const;

/**
 * Type representing any valid VestingSchedule query key
 * Useful for typing query key arrays in generic contexts
 */
export type VestingScheduleQueryKey =
  | typeof vesting_scheduleKeys.all
  | ReturnType<typeof vesting_scheduleKeys.lists>
  | ReturnType<typeof vesting_scheduleKeys.list>
  | ReturnType<typeof vesting_scheduleKeys.details>
  | ReturnType<typeof vesting_scheduleKeys.detail>
  | ReturnType<typeof vesting_scheduleKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default vesting_scheduleKeys;
