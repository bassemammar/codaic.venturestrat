/**
 * InvestmentTerm Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all investment_term queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['investmentterms'] - All investment_term queries
 * - ['investmentterms', 'list'] - All list queries
 * - ['investmentterms', 'list', { filters }] - Specific filtered list
 * - ['investmentterms', 'detail', id] - Specific investment_term by ID
 *
 * @example
 * ```typescript
 * import { investment_termKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: investment_termKeys.list({ is_active: true }),
 *   queryFn: () => investment_termService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all investment_term queries
 * queryClient.invalidateQueries({ queryKey: investment_termKeys.all });
 *
 * // Invalidate specific investment_term
 * queryClient.invalidateQueries({ queryKey: investment_termKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:52.934267Z
 */

import type { InvestmentTermFilterOptions } from '../types/investment_term.types';

/**
 * Root query key for InvestmentTerm entity
 * Used as namespace prefix for all investment_term queries
 */
const INVESTMENT_TERM_ROOT = 'investmentterms' as const;

/**
 * InvestmentTerm Query Keys
 *
 * Hierarchical query key factory for investment_term queries:
 * - all: Base key for all investment_term queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific investment_term by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = investment_termKeys.list({ is_active: true });
 * // Result: ['investmentterms', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = investment_termKeys.detail('uuid-here');
 * // Result: ['investmentterms', 'detail', 'uuid-here']
 *
 * // Invalidate all investment_term data
 * queryClient.invalidateQueries({ queryKey: investment_termKeys.all });
 * ```
 */
export const investment_termKeys = {
  /**
   * Base key for all investment_term queries
   * Use this to invalidate all investment_term-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: investment_termKeys.all });
   * ```
   */
  all: [INVESTMENT_TERM_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: investment_termKeys.lists() });
   * ```
   */
  lists: () => [...investment_termKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = investment_termKeys.list({ is_active: true });
   *
   * // List with search
   * const key = investment_termKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = investment_termKeys.list();
   * ```
   */
  list: (filters?: InvestmentTermFilterOptions) =>
    [...investment_termKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: investment_termKeys.details() });
   * ```
   */
  details: () => [...investment_termKeys.all, 'detail'] as const,

  /**
   * Key for a specific investment_term by ID
   * Use this for fetching and invalidating individual investment_term records
   *
   * @param id - InvestmentTerm ID (UUID)
   * @returns Query key tuple for specific investment_term
   *
   * @example
   * ```typescript
   * // Fetch specific investment_term
   * const { data } = useQuery({
   *   queryKey: investment_termKeys.detail('uuid-here'),
   *   queryFn: () => investment_termService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: investment_termKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...investment_termKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active investment_terms (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = investment_termKeys.dropdown();
   *
   * // Include inactive items
   * const key = investment_termKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...investment_termKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * InvestmentTerms filtered by legal entity ID
   *
   * @param legalEntityId - LegalEntity ID (UUID)
   * @returns Query key tuple
   */
  byLegalEntity: (legalEntityId: string) =>
    [...investment_termKeys.all, 'by-legal-entity', legalEntityId] as const,

  /**
   * InvestmentTerms filtered by investor person ID
   *
   * @param investorPersonId - InvestorPerson ID (UUID)
   * @returns Query key tuple
   */
  byInvestorPerson: (investorPersonId: string) =>
    [...investment_termKeys.all, 'by-investor-person', investorPersonId] as const,

  /**
   * InvestmentTerms filtered by investor ID
   *
   * @param investorId - Investor ID (UUID)
   * @returns Query key tuple
   */
  byInvestor: (investorId: string) =>
    [...investment_termKeys.all, 'by-investor', investorId] as const,

  /**
   * InvestmentTerms filtered by source document ID
   *
   * @param sourceDocumentId - SourceDocument ID (UUID)
   * @returns Query key tuple
   */
  bySourceDocument: (sourceDocumentId: string) =>
    [...investment_termKeys.all, 'by-source-document', sourceDocumentId] as const,
} as const;

/**
 * Type representing any valid InvestmentTerm query key
 * Useful for typing query key arrays in generic contexts
 */
export type InvestmentTermQueryKey =
  | typeof investment_termKeys.all
  | ReturnType<typeof investment_termKeys.lists>
  | ReturnType<typeof investment_termKeys.list>
  | ReturnType<typeof investment_termKeys.details>
  | ReturnType<typeof investment_termKeys.detail>
  | ReturnType<typeof investment_termKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default investment_termKeys;
