/**
 * TemplateClause Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all template_clause queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['templateclauses'] - All template_clause queries
 * - ['templateclauses', 'list'] - All list queries
 * - ['templateclauses', 'list', { filters }] - Specific filtered list
 * - ['templateclauses', 'detail', id] - Specific template_clause by ID
 *
 * @example
 * ```typescript
 * import { template_clauseKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: template_clauseKeys.list({ is_active: true }),
 *   queryFn: () => template_clauseService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all template_clause queries
 * queryClient.invalidateQueries({ queryKey: template_clauseKeys.all });
 *
 * // Invalidate specific template_clause
 * queryClient.invalidateQueries({ queryKey: template_clauseKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:55.194805Z
 */

import type { TemplateClauseFilterOptions } from '../types/template_clause.types';

/**
 * Root query key for TemplateClause entity
 * Used as namespace prefix for all template_clause queries
 */
const TEMPLATE_CLAUSE_ROOT = 'templateclauses' as const;

/**
 * TemplateClause Query Keys
 *
 * Hierarchical query key factory for template_clause queries:
 * - all: Base key for all template_clause queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific template_clause by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = template_clauseKeys.list({ is_active: true });
 * // Result: ['templateclauses', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = template_clauseKeys.detail('uuid-here');
 * // Result: ['templateclauses', 'detail', 'uuid-here']
 *
 * // Invalidate all template_clause data
 * queryClient.invalidateQueries({ queryKey: template_clauseKeys.all });
 * ```
 */
export const template_clauseKeys = {
  /**
   * Base key for all template_clause queries
   * Use this to invalidate all template_clause-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: template_clauseKeys.all });
   * ```
   */
  all: [TEMPLATE_CLAUSE_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: template_clauseKeys.lists() });
   * ```
   */
  lists: () => [...template_clauseKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = template_clauseKeys.list({ is_active: true });
   *
   * // List with search
   * const key = template_clauseKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = template_clauseKeys.list();
   * ```
   */
  list: (filters?: TemplateClauseFilterOptions) =>
    [...template_clauseKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: template_clauseKeys.details() });
   * ```
   */
  details: () => [...template_clauseKeys.all, 'detail'] as const,

  /**
   * Key for a specific template_clause by ID
   * Use this for fetching and invalidating individual template_clause records
   *
   * @param id - TemplateClause ID (UUID)
   * @returns Query key tuple for specific template_clause
   *
   * @example
   * ```typescript
   * // Fetch specific template_clause
   * const { data } = useQuery({
   *   queryKey: template_clauseKeys.detail('uuid-here'),
   *   queryFn: () => template_clauseService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: template_clauseKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...template_clauseKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active template_clauses (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = template_clauseKeys.dropdown();
   *
   * // Include inactive items
   * const key = template_clauseKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...template_clauseKeys.all, 'dropdown', { activeOnly }] as const,
} as const;

/**
 * Type representing any valid TemplateClause query key
 * Useful for typing query key arrays in generic contexts
 */
export type TemplateClauseQueryKey =
  | typeof template_clauseKeys.all
  | ReturnType<typeof template_clauseKeys.lists>
  | ReturnType<typeof template_clauseKeys.list>
  | ReturnType<typeof template_clauseKeys.details>
  | ReturnType<typeof template_clauseKeys.detail>
  | ReturnType<typeof template_clauseKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default template_clauseKeys;
