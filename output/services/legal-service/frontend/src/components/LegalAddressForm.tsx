/**
 * LegalAddressForm Component
 *
 * Form component for creating and editing legal_addresses.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * Features:
 * - Create and Edit mode support
 * - Form validation with error messages
 * - Material UI components
 * - Loading state handling
 * - Accessible form fields (WCAG 2.1 Level AA)
 * - Modal wrapper support (form_display: modal)
 *
 * Physical address for legal entities and persons with jurisdiction classification
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:53.413730Z
 */

import React, { useMemo, useEffect } from 'react';
import { useForm, Controller, FormProvider } from 'react-hook-form';
import {
  Alert as MuiAlert,
  Box,
  Grid,
  TextField,
  Button,
  CircularProgress,
  Dialog,
  DialogTitle,
  DialogContent,
  IconButton,
} from '@mui/material';
import { Close as CloseIcon } from '@mui/icons-material';
import type { LegalAddressCreateRequest, LegalAddress } from '../types/legal_address.types';
import { useNotification } from '../contexts/NotificationContext';

// ============================================================================
// Types
// ============================================================================

/**
 * Form data type for LegalAddress
 */
export type LegalAddressFormData = LegalAddressCreateRequest;

/**
 * Props for LegalAddressForm component
 */
export interface LegalAddressFormProps {
  /** Existing legal_address data for edit mode (optional) */
  legalAddress?: LegalAddress;
  /** Form submission handler (can be sync or async) */
  onSubmit: (data: LegalAddressFormData) => void | Promise<void>;
  /** Cancel handler */
  onCancel: () => void;
  /** Loading state (disables form during submission) */
  isLoading?: boolean;
  /** Custom className for the form container */
  className?: string;
}

/**
 * Props for LegalAddressFormModal component (modal wrapper)
 */
export interface LegalAddressFormModalProps extends LegalAddressFormProps {
  /** Whether the modal is open */
  open: boolean;
  /** Handler called when the modal requests to be closed */
  onClose: () => void;
  /** Optional title override for the modal */
  title?: string;
}


// ============================================================================
// Constants
// ============================================================================

/**
 * Default form values for creating a new legal_address
 */
const DEFAULT_FORM_VALUES: Partial<LegalAddressFormData> = {
  address_line_1: '',
  address_line_2: '',
  city: '',
  state_province: '',
  postal_code: '',
  country: '',
  jurisdiction: '',
};

// ============================================================================
// Component
// ============================================================================

/**
 * LegalAddressForm Component
 *
 * Renders a form for creating or editing legal_addresses.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * @example
 * ```tsx
 * // Create mode
 * <LegalAddressForm
 *   onSubmit={handleCreate}
 *   onCancel={handleCancel}
 *   isLoading={isCreating}
 * />
 *
 * // Edit mode
 * <LegalAddressForm
 *   legalAddress={existingLegalAddress}
 *   onSubmit={handleUpdate}
 *   onCancel={handleCancel}
 *   isLoading={isUpdating}
 * />
 * ```
 */
export function LegalAddressForm({
  legalAddress,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: LegalAddressFormProps): React.ReactElement {
  // Determine if we're in edit mode
  const isEditMode = Boolean(legalAddress);

  // Notification context for showing success/error messages
  const { showSuccess, showError } = useNotification();

  // Build default values from existing entity or defaults
  const defaultValues = useMemo<Partial<LegalAddressFormData>>(() => {
    if (legalAddress) {
      // Edit mode: use existing entity values
      return {
        address_line_1: legalAddress.address_line_1,
        address_line_2: legalAddress.address_line_2 ?? '',
        city: legalAddress.city,
        state_province: legalAddress.state_province ?? '',
        postal_code: legalAddress.postal_code,
        country: legalAddress.country,
        jurisdiction: legalAddress.jurisdiction,
      };
    }
    // Create mode: use defaults
    return DEFAULT_FORM_VALUES;
  }, [legalAddress]);

  // Initialize React Hook Form
  const methods = useForm<LegalAddressFormData>({
    defaultValues,
    mode: 'onBlur',
  });

  const {
    control,
    handleSubmit,
    formState: { errors },
    reset,
  } = methods;

  // Reset form when entity data changes (e.g., switching from create to edit mode)
  useEffect(() => {
    reset(defaultValues);
  }, [defaultValues, reset]);

  // Log validation errors to console for debugging
  useEffect(() => {
    if (Object.keys(errors).length > 0) {
      console.log('[LegalAddressForm] Validation errors:', errors);
    }
  }, [errors]);
  // -------------------------------------------------------------------------
  // Form Submission
  // -------------------------------------------------------------------------

  const handleFormSubmit = async (data: LegalAddressFormData) => {
    console.log('[LegalAddressForm] Submitting:', { data, isEditMode });
    try {
      const result = await onSubmit(data);
      console.log('[LegalAddressForm] Form submission successful', { data, result, isEditMode });
      // Show success notification for create operation
      if (!isEditMode) {
        showSuccess('LegalAddress created successfully');
      }
    } catch (error) {
      // Log full error details to console for debugging
      console.error('[LegalAddressForm] Form submission failed', {
        error,
        errorMessage: error instanceof Error ? error.message : String(error),
        errorStack: error instanceof Error ? error.stack : undefined,
        data,
        isEditMode,
      });
      // Show error notification on failure
      const errorMessage = error instanceof Error ? error.message : 'An unexpected error occurred';
      showError(`Failed to ${isEditMode ? 'update' : 'create'} LegalAddress: ${errorMessage}`);
    }
  };

  // -------------------------------------------------------------------------
  // Render
  // -------------------------------------------------------------------------

  // Count validation errors
  const errorCount = Object.keys(errors).length;

  return (
    <FormProvider {...methods}>
      <Box
        component="form"
        onSubmit={handleSubmit(handleFormSubmit)}
        className={className}
        data-testid="form-legal_address"
        noValidate
      >
        {/* Error Summary Alert */}
        {errorCount > 0 && (
          <MuiAlert
            severity="error"
            sx={{ mb: 3 }}
            data-testid="form-errors-summary"
          >
            Please fix {errorCount} error{errorCount !== 1 ? 's' : ''} before submitting
          </MuiAlert>
        )}

        <Grid container spacing={3}>
          {/* Address Line 1 field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="address_line_1"
              control={control}
              rules={{
                required: 'Address Line 1 is required',
                maxLength: { value: 100, message: 'Address Line 1 must be at most 100 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Address Line 1"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-address-line-1"
                />
              )}
            />
          </Grid>

          {/* Address Line 2 field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="address_line_2"
              control={control}
              rules={{
                required: 'Address Line 2 is required',
                maxLength: { value: 100, message: 'Address Line 2 must be at most 100 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Address Line 2"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-address-line-2"
                />
              )}
            />
          </Grid>

          {/* City field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="city"
              control={control}
              rules={{
                required: 'City is required',
                maxLength: { value: 50, message: 'City must be at most 50 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="City"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-city"
                />
              )}
            />
          </Grid>

          {/* State Province field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="state_province"
              control={control}
              rules={{
                required: 'State Province is required',
                maxLength: { value: 50, message: 'State Province must be at most 50 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="State Province"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-state-province"
                />
              )}
            />
          </Grid>

          {/* Postal Code field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="postal_code"
              control={control}
              rules={{
                required: 'Postal Code is required',
                maxLength: { value: 20, message: 'Postal Code must be at most 20 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Postal Code"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-postal-code"
                />
              )}
            />
          </Grid>

          {/* Country field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="country"
              control={control}
              rules={{
                required: 'Country is required',
                maxLength: { value: 2, message: 'Country must be at most 2 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Country"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "ISO 3166-1 alpha-2 country code"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-country"
                />
              )}
            />
          </Grid>

          {/* Jurisdiction field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="jurisdiction"
              control={control}
              rules={{
                required: 'Jurisdiction is required',
                maxLength: { value: 30, message: 'Jurisdiction must be at most 30 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Jurisdiction"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-jurisdiction"
                />
              )}
            />
          </Grid>

          {/* Form Actions */}
          <Grid size={12}>
            <Box display="flex" gap={2} pt={2}>
              <Button
                type="submit"
                variant="contained"
                color="primary"
                disabled={isLoading}
                startIcon={isLoading ? <CircularProgress size={20} /> : undefined}
                data-testid="submit-button"
              >
                {isLoading
                  ? 'Saving...'
                  : isEditMode
                    ? 'Update LegalAddress'
                    : 'Create LegalAddress'}
              </Button>

              <Button
                type="button"
                variant="outlined"
                onClick={onCancel}
                disabled={isLoading}
                data-testid="cancel-button"
              >
                Cancel
              </Button>
            </Box>
          </Grid>
        </Grid>
      </Box>
    </FormProvider>
  );
}

// ============================================================================
// Modal Wrapper Component
// ============================================================================

/**
 * LegalAddressFormModal Component
 *
 * Wraps the LegalAddressForm in a Material UI Dialog for modal display.
 * Use this component when you want to show the form in a popup dialog.
 *
 * @example
 * ```tsx
 * <LegalAddressFormModal
 *   open={isModalOpen}
 *   onClose={handleClose}
 *   onSubmit={handleCreate}
 *   onCancel={handleClose}
 *   isLoading={isCreating}
 * />
 * ```
 */
export function LegalAddressFormModal({
  open,
  onClose,
  title,
  legalAddress,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: LegalAddressFormModalProps): React.ReactElement {
  const isEditMode = Boolean(legalAddress);
  const modalTitle = title ?? (isEditMode ? 'Edit LegalAddress' : 'Create LegalAddress');

  // Handle modal close - prevent closing during loading
  const handleClose = () => {
    if (!isLoading) {
      onClose();
    }
  };

  return (
    <Dialog
      open={open}
      onClose={handleClose}
      maxWidth="md"
      fullWidth
      data-testid="modal-legal_address-form"
    >
      <DialogTitle sx={{ m: 0, p: 2, display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        {modalTitle}
        <IconButton
          aria-label="close"
          onClick={handleClose}
          disabled={isLoading}
          data-testid="modal-close-button"
          sx={{ color: (theme) => theme.palette.grey[500] }}
        >
          <CloseIcon />
        </IconButton>
      </DialogTitle>
      <DialogContent dividers>
        <LegalAddressForm
          legalAddress={legalAddress}
          onSubmit={onSubmit}
          onCancel={onCancel}
          isLoading={isLoading}
          className={className}
        />
      </DialogContent>
    </Dialog>
  );
}


// Default export for convenience
export default LegalAddressForm;
