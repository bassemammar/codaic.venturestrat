/**
 * EquityGrantDetail Component
 *
 * Displays detailed information about a equity_grant in a structured layout.
 * Organized into sections:
 * - Basic Information (all entity fields)
 * - Status (active/inactive indicator)
 * - Audit Trail (created/updated timestamps)
 *
 * Features:
 * - React Query integration for data fetching
 * - Material UI Grid layout for responsive design
 * - Loading skeleton states
 * - Error handling with Alert component
 * - Not found state handling
 * - Edit/Delete action buttons
 * - Back navigation
 * - Status badge indicators (active/inactive)
 * - Formatted dates and numbers
 * - Accessibility support (WCAG 2.1 Level AA)
 *
 * Cap table entry — equity holding in a legal entity with share class and valuation
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:52.487276Z
 */

import React, { useCallback } from 'react';
import { useParams, useNavigate } from 'react-router-dom';
import {
  Box,
  Paper,
  Grid,
  Typography,
  Alert as MuiAlert,
  Skeleton,
  IconButton,
  Button,
  Chip,
  Divider,
  Tooltip,
} from '@mui/material';
import {
  ArrowBack as ArrowBackIcon,
  Edit as EditIcon,
  Delete as DeleteIcon,
  Refresh as RefreshIcon,
} from '@mui/icons-material';
import { useEquityGrant} from '../hooks/useEquityGrants';
import type { EquityGrant} from '../types/equity_grant.types';

// ============================================================================
// Types
// ============================================================================

/**
 * Props for EquityGrantDetail component
 */
interface EquityGrantDetailProps {
  /** Entity ID to display (optional, can be read from URL params) */
  id?: string;
  /** Whether to show the back button */
  showBackButton?: boolean;
  /** Whether to show edit button */
  showEditButton?: boolean;
  /** Whether to show delete button */
  showDeleteButton?: boolean;
  /** Callback when edit button is clicked */
  onEdit?: (equityGrant: EquityGrant) => void;
  /** Callback when delete button is clicked */
  onDelete?: (equityGrant: EquityGrant) => void;
  /** Custom className for the container */
  className?: string;
}

// ============================================================================
// Helper Components
// ============================================================================

/**
 * Field component for displaying label-value pairs
 */
const DetailField: React.FC<{
  label: string;
  value: string | React.ReactNode;
  testId: string;
}> = ({ label, value, testId }) => (
  <div data-testid={testId}>
    <label>
      <Typography variant="body2" color="text.secondary" component="span">
        {label}
      </Typography>
    </label>
    <span>
      <Typography variant="body1" component="span">
        {value}
      </Typography>
    </span>
  </div>
);

/**
 * Loading skeleton for the detail view
 */
const DetailSkeleton: React.FC = () => (
  <Box data-testid="equity_grant-detail-skeleton">
    <Box display="flex" alignItems="center" gap={2} mb={3}>
      <Skeleton variant="circular" width={40} height={40} />
      <Skeleton variant="text" width={200} height={32} />
    </Box>
    <Paper sx={{ p: 3 }}>
      <Skeleton variant="text" width={150} height={28} sx={{ mb: 2 }} />
      {Array.from({ length: 6 }).map((_, index) => (
        <Box key={index} sx={{ mb: 2 }}>
          <Skeleton variant="text" width={100} height={16} />
          <Skeleton variant="text" width={250} height={24} />
        </Box>
      ))}
    </Paper>
  </Box>
);

// ============================================================================
// Helper Functions
// ============================================================================

/**
 * Format a date string for display
 */
function formatDate(value: string | undefined | null): string {
  if (!value) return '-';
  try {
    return new Date(value).toLocaleString();
  } catch {
    return value;
  }
}

/**
 * Format a number for display
 */
function formatNumber(value: number | undefined | null, decimals: number = 2): string {
  if (value === null || value === undefined) return '-';
  return value.toLocaleString(undefined, {
    minimumFractionDigits: decimals,
    maximumFractionDigits: decimals,
  });
}

/**
 * Display a value with fallback for empty/null values
 */
function displayValue(value: string | number | boolean | undefined | null): string {
  if (value === null || value === undefined) return '-';
  if (typeof value === 'string') {
    return value.trim() !== '' ? value : '-';
  }
  if (typeof value === 'boolean') {
    return value ? 'Yes' : 'No';
  }
  return String(value);
}

// ============================================================================
// Component
// ============================================================================

/**
 * EquityGrantDetail Component
 *
 * Renders comprehensive equity_grant information in a structured grid layout.
 *
 * @example
 * ```tsx
 * // Using URL params (in a route like /equity_grants/:id)
 * <EquityGrantDetail />
 *
 * // With explicit ID
 * <EquityGrantDetail id="123" />
 *
 * // With custom handlers
 * <EquityGrantDetail
 *   onEdit={(entity) => navigate(`/equity_grants/${entity.id}/edit`)}
 *   onDelete={(entity) => handleDelete(entity.id)}
 * />
 * ```
 */
export function EquityGrantDetail({
  id: propId,
  showBackButton = true,
  showEditButton = true,
  showDeleteButton = true,
  onEdit,
  onDelete,
  className = '',
}: EquityGrantDetailProps): React.ReactElement {
  const navigate = useNavigate();
  const params = useParams<{ id: string }>();

  // Use prop ID if provided, otherwise get from URL params
  const id = propId ?? params.id;

  // -------------------------------------------------------------------------
  // Data Fetching
  // -------------------------------------------------------------------------

  const {
    data: equityGrant,
    isLoading,
    isError,
    error,
    refetch,
  } = useEquityGrant(id ?? '', {
    enabled: Boolean(id),
  });

  // -------------------------------------------------------------------------
  // Handlers
  // -------------------------------------------------------------------------

  /** Handle back button click */
  const handleBack = useCallback(() => {
    navigate('/equity_grants');
  }, [navigate]);

  /** Handle edit button click */
  const handleEdit = useCallback(() => {
    if (equityGrant) {
      if (onEdit) {
        onEdit(equityGrant);
      } else {
        navigate(`/equity_grants/${equityGrant.id}/edit`);
      }
    }
  }, [navigate, equityGrant, onEdit]);

  /** Handle delete button click */
  const handleDelete = useCallback(() => {
    if (equityGrant && onDelete) {
      onDelete(equityGrant);
    }
  }, [equityGrant, onDelete]);

  /** Handle refresh button click */
  const handleRefresh = useCallback(() => {
    refetch();
  }, [refetch]);

  // -------------------------------------------------------------------------
  // Render States
  // -------------------------------------------------------------------------

  // Loading state
  if (isLoading) {
    return (
      <Box className={className}>
        <DetailSkeleton />
      </Box>
    );
  }

  // Error state
  if (isError) {
    return (
      <Box className={className} p={3}>
        <MuiAlert
          severity="error"
          action={
            <IconButton color="inherit" size="small" onClick={handleRefresh}>
              <RefreshIcon />
            </IconButton>
          }
        >
          Error loading equity_grant: {(error as Error)?.message || 'Unknown error'}
        </MuiAlert>
      </Box>
    );
  }

  // Not found state
  if (!equityGrant) {
    return (
      <Box className={className} p={3}>
        <MuiAlert severity="warning">
          EquityGrant not found
        </MuiAlert>
        {showBackButton && (
          <Box mt={2}>
            <Button
              startIcon={<ArrowBackIcon />}
              onClick={handleBack}
              variant="outlined"
            >
              Back to EquityGrants
            </Button>
          </Box>
        )}
      </Box>
    );
  }

  // -------------------------------------------------------------------------
  // Main Render
  // -------------------------------------------------------------------------

  return (
    <div data-testid="equity_grant-detail" className={className}>
      {/* Header */}
      <Box
        display="flex"
        justifyContent="space-between"
        alignItems="center"
        mb={3}
      >
        <Box display="flex" alignItems="center" gap={2}>
          {showBackButton && (
            <Tooltip title="Back to EquityGrants">
              <IconButton onClick={handleBack}>
                <ArrowBackIcon />
              </IconButton>
            </Tooltip>
          )}
          <Typography variant="h5" component="h1">
            EquityGrant Details
          </Typography>
{equityGrant.is_active !== undefined && (
            <Chip
              size="small"
              label={equityGrant.is_active ? 'Active' : 'Inactive'}
              color={equityGrant.is_active ? 'success' : 'default'}
              variant="outlined"
            />
          )}
        </Box>

        <Box display="flex" gap={1}>
          <Tooltip title="Refresh">
            <span>
              <IconButton onClick={handleRefresh} disabled={isLoading}>
                <RefreshIcon />
              </IconButton>
            </span>
          </Tooltip>

          {showEditButton && (
            <Tooltip title="Edit EquityGrant">
              <IconButton onClick={handleEdit} color="primary">
                <EditIcon />
              </IconButton>
            </Tooltip>
          )}

          {showDeleteButton && onDelete && (
            <Tooltip title="Delete EquityGrant">
              <IconButton onClick={handleDelete} color="error">
                <DeleteIcon />
              </IconButton>
            </Tooltip>
          )}
        </Box>
      </Box>

      {/* Detail Content */}
      <Paper sx={{ p: 3 }}>
        <Grid container spacing={3}>
          {/* Basic Information Section */}
          <Grid size={12}>
            <section data-testid="basic-info-section" aria-label="Basic Information">
              <Typography variant="h6" component="h2" gutterBottom>
                Basic Information
              </Typography>

              <Box sx={{ display: 'flex', flexDirection: 'column', gap: 2 }}>
                {/* ID field */}
                <DetailField
                  label="ID:"
                  value={equityGrant.id}
                  testId="field-id"
                />

                {/* Legal Entity Id field */}
                <DetailField
                  label="Legal Entity Id:"
                  value={displayValue(equityGrant.legal_entity_id)}
                  testId="field-legal-entity-id"
                />

                {/* Holder Person Id field */}
                <DetailField
                  label="Holder Person Id:"
                  value={displayValue(equityGrant.holder_person_id)}
                  testId="field-holder-person-id"
                />

                {/* Holder Entity Id field */}
                <DetailField
                  label="Holder Entity Id:"
                  value={displayValue(equityGrant.holder_entity_id)}
                  testId="field-holder-entity-id"
                />

                {/* Holder Type field */}
                <DetailField
                  label="Holder Type:"
                  value={displayValue(equityGrant.holder_type)}
                  testId="field-holder-type"
                />

                {/* Share Class field */}
                <DetailField
                  label="Share Class:"
                  value={displayValue(equityGrant.share_class)}
                  testId="field-share-class"
                />

                {/* Number Of Shares field */}
                <DetailField
                  label="Number Of Shares:"
                  value={formatNumber(equityGrant.number_of_shares)}
                  testId="field-number-of-shares"
                />

                {/* Percentage field */}
                <DetailField
                  label="Percentage:"
                  value={formatNumber(equityGrant.percentage)}
                  testId="field-percentage"
                />

                {/* Valuation field */}
                <DetailField
                  label="Valuation:"
                  value={formatNumber(equityGrant.valuation)}
                  testId="field-valuation"
                />

                {/* Issue Date field */}
                <DetailField
                  label="Issue Date:"
                  value={formatDate(equityGrant.issue_date)}
                  testId="field-issue-date"
                />

                {/* Source Document Id field */}
                <DetailField
                  label="Source Document Id:"
                  value={displayValue(equityGrant.source_document_id)}
                  testId="field-source-document-id"
                />

              </Box>
            </section>
          </Grid>

          {/* Status Section */}
          <Grid size={12}>
            <Divider sx={{ my: 2 }} />
            <section data-testid="status-section" aria-label="Status">
              <Typography variant="h6" component="h2" gutterBottom>
                Status
              </Typography>

              <Box sx={{ display: 'flex', flexDirection: 'column', gap: 2 }}>
                <div data-testid="field-status">
                  <label>
                    <Typography variant="body2" color="text.secondary" component="span">
                      Status:
                    </Typography>
                  </label>
                  <span className={equityGrant.is_active ? 'active-badge' : 'inactive-badge'}>
                    <Typography variant="body1" component="span">
                      {equityGrant.is_active ? 'Active' : 'Inactive'}
                    </Typography>
                  </span>
                </div>
              </Box>
            </section>
          </Grid>

          {/* Audit Trail Section */}
          <Grid size={12}>
            <Divider sx={{ my: 2 }} />
            <section data-testid="audit-trail-section" aria-label="Audit Trail">
              <Typography variant="h6" component="h2" gutterBottom>
                Audit Trail
              </Typography>

              <Box sx={{ display: 'flex', flexDirection: 'column', gap: 2 }}>
                <DetailField
                  label="Created At:"
                  value={formatDate(equityGrant.created_at)}
                  testId="field-created-at"
                />

                <DetailField
                  label="Updated At:"
                  value={formatDate(equityGrant.updated_at)}
                  testId="field-updated-at"
                />

              </Box>
            </section>
          </Grid>
        </Grid>
      </Paper>
    </div>
  );
}

// Default export for convenience
export default EquityGrantDetail;
