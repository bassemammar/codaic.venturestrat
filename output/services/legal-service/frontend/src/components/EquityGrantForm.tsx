/**
 * EquityGrantForm Component
 *
 * Form component for creating and editing equity_grants.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * Features:
 * - Create and Edit mode support
 * - Form validation with error messages
 * - Material UI components
 * - Loading state handling
 * - Accessible form fields (WCAG 2.1 Level AA)
 * - Modal wrapper support (form_display: modal)
 *
 * Cap table entry — equity holding in a legal entity with share class and valuation
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:52.408223Z
 */

import React, { useMemo, useEffect } from 'react';
import { useForm, Controller, FormProvider } from 'react-hook-form';
import {
  Alert as MuiAlert,
  Box,
  Grid,
  TextField,
  Button,
  CircularProgress,
  Dialog,
  DialogTitle,
  DialogContent,
  IconButton,
} from '@mui/material';
import { Close as CloseIcon } from '@mui/icons-material';
import type { EquityGrantCreateRequest, EquityGrant } from '../types/equity_grant.types';
import { useNotification } from '../contexts/NotificationContext';
// Form select components for foreign key fields
import { FormLegalEntitySelect } from './FormLegalEntitySelect';
import { FormContactPersonSelect } from './FormContactPersonSelect';

// ============================================================================
// Types
// ============================================================================

/**
 * Form data type for EquityGrant
 */
export type EquityGrantFormData = EquityGrantCreateRequest;

/**
 * Props for EquityGrantForm component
 */
export interface EquityGrantFormProps {
  /** Existing equity_grant data for edit mode (optional) */
  equityGrant?: EquityGrant;
  /** Form submission handler (can be sync or async) */
  onSubmit: (data: EquityGrantFormData) => void | Promise<void>;
  /** Cancel handler */
  onCancel: () => void;
  /** Loading state (disables form during submission) */
  isLoading?: boolean;
  /** Custom className for the form container */
  className?: string;
}

/**
 * Props for EquityGrantFormModal component (modal wrapper)
 */
export interface EquityGrantFormModalProps extends EquityGrantFormProps {
  /** Whether the modal is open */
  open: boolean;
  /** Handler called when the modal requests to be closed */
  onClose: () => void;
  /** Optional title override for the modal */
  title?: string;
}


// ============================================================================
// Constants
// ============================================================================

/**
 * Default form values for creating a new equity_grant
 */
const DEFAULT_FORM_VALUES: Partial<EquityGrantFormData> = {
  legal_entity_id: '',
  holder_person_id: '',
  holder_entity_id: '',
  holder_type: '',
  share_class: '',
  number_of_shares: undefined,
  percentage: undefined,
  valuation: undefined,
  issue_date: '',
  source_document_id: '',
};

// ============================================================================
// Component
// ============================================================================

/**
 * EquityGrantForm Component
 *
 * Renders a form for creating or editing equity_grants.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * @example
 * ```tsx
 * // Create mode
 * <EquityGrantForm
 *   onSubmit={handleCreate}
 *   onCancel={handleCancel}
 *   isLoading={isCreating}
 * />
 *
 * // Edit mode
 * <EquityGrantForm
 *   equityGrant={existingEquityGrant}
 *   onSubmit={handleUpdate}
 *   onCancel={handleCancel}
 *   isLoading={isUpdating}
 * />
 * ```
 */
export function EquityGrantForm({
  equityGrant,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: EquityGrantFormProps): React.ReactElement {
  // Determine if we're in edit mode
  const isEditMode = Boolean(equityGrant);

  // Notification context for showing success/error messages
  const { showSuccess, showError } = useNotification();

  // Build default values from existing entity or defaults
  const defaultValues = useMemo<Partial<EquityGrantFormData>>(() => {
    if (equityGrant) {
      // Edit mode: use existing entity values
      return {
        legal_entity_id: equityGrant.legal_entity_id,
        holder_person_id: equityGrant.holder_person_id ?? '',
        holder_entity_id: equityGrant.holder_entity_id ?? '',
        holder_type: equityGrant.holder_type,
        share_class: equityGrant.share_class,
        number_of_shares: equityGrant.number_of_shares,
        percentage: equityGrant.percentage,
        valuation: equityGrant.valuation ?? undefined,
        issue_date: equityGrant.issue_date,
        source_document_id: equityGrant.source_document_id ?? '',
      };
    }
    // Create mode: use defaults
    return DEFAULT_FORM_VALUES;
  }, [equityGrant]);

  // Initialize React Hook Form
  const methods = useForm<EquityGrantFormData>({
    defaultValues,
    mode: 'onBlur',
  });

  const {
    control,
    handleSubmit,
    formState: { errors },
    reset,
  } = methods;

  // Reset form when entity data changes (e.g., switching from create to edit mode)
  useEffect(() => {
    reset(defaultValues);
  }, [defaultValues, reset]);

  // Log validation errors to console for debugging
  useEffect(() => {
    if (Object.keys(errors).length > 0) {
      console.log('[EquityGrantForm] Validation errors:', errors);
    }
  }, [errors]);
  // -------------------------------------------------------------------------
  // Form Submission
  // -------------------------------------------------------------------------

  const handleFormSubmit = async (data: EquityGrantFormData) => {
    console.log('[EquityGrantForm] Submitting:', { data, isEditMode });
    try {
      const result = await onSubmit(data);
      console.log('[EquityGrantForm] Form submission successful', { data, result, isEditMode });
      // Show success notification for create operation
      if (!isEditMode) {
        showSuccess('EquityGrant created successfully');
      }
    } catch (error) {
      // Log full error details to console for debugging
      console.error('[EquityGrantForm] Form submission failed', {
        error,
        errorMessage: error instanceof Error ? error.message : String(error),
        errorStack: error instanceof Error ? error.stack : undefined,
        data,
        isEditMode,
      });
      // Show error notification on failure
      const errorMessage = error instanceof Error ? error.message : 'An unexpected error occurred';
      showError(`Failed to ${isEditMode ? 'update' : 'create'} EquityGrant: ${errorMessage}`);
    }
  };

  // -------------------------------------------------------------------------
  // Render
  // -------------------------------------------------------------------------

  // Count validation errors
  const errorCount = Object.keys(errors).length;

  return (
    <FormProvider {...methods}>
      <Box
        component="form"
        onSubmit={handleSubmit(handleFormSubmit)}
        className={className}
        data-testid="form-equity_grant"
        noValidate
      >
        {/* Error Summary Alert */}
        {errorCount > 0 && (
          <MuiAlert
            severity="error"
            sx={{ mb: 3 }}
            data-testid="form-errors-summary"
          >
            Please fix {errorCount} error{errorCount !== 1 ? 's' : ''} before submitting
          </MuiAlert>
        )}

        <Grid container spacing={3}>
          {/* Legal Entity Id field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* Foreign key field - renders as dropdown select */}
            <FormLegalEntitySelect
              name="legal_entity_id"
              label="Legal Entity"
              required={false}
              disabled={isLoading}
            />
          </Grid>

          {/* Holder Person Id field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* Foreign key field - renders as dropdown select */}
            <FormContactPersonSelect
              name="holder_person_id"
              label="Holder Person"
              required={false}
              disabled={isLoading}
            />
          </Grid>

          {/* Holder Entity Id field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="holder_entity_id"
              control={control}
              rules={{
                required: 'Holder Entity Id is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Holder Entity Id"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Entity holding equity (null if person holder)"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-holder-entity-id"
                />
              )}
            />
          </Grid>

          {/* Holder Type field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="holder_type"
              control={control}
              rules={{
                required: 'Holder Type is required',
                maxLength: { value: 10, message: 'Holder Type must be at most 10 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Holder Type"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Polymorphic holder discriminator"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-holder-type"
                />
              )}
            />
          </Grid>

          {/* Share Class field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="share_class"
              control={control}
              rules={{
                required: 'Share Class is required',
                maxLength: { value: 30, message: 'Share Class must be at most 30 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Share Class"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-share-class"
                />
              )}
            />
          </Grid>

          {/* Number Of Shares field */}
          <Grid size={{ xs: 12, md: 6 }}>
            <Controller
              name="number_of_shares"
              control={control}
              rules={{
                required: 'Number Of Shares is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  type="number"
                  label="Number Of Shares"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Number of shares held (must be > 0)"}
                  placeholder=""
                  value={formField.value ?? ''}
                  onChange={(e) => {
                    const val = e.target.value;
                    formField.onChange(val === '' ? undefined : parseFloat(val));
                  }}
                  inputProps={{
                    step: 'any',
                  }}
                  data-testid="field-number-of-shares"
                />
              )}
            />
          </Grid>

          {/* Percentage field */}
          <Grid size={{ xs: 12, md: 6 }}>
            <Controller
              name="percentage"
              control={control}
              rules={{
                required: 'Percentage is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  type="number"
                  label="Percentage"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Ownership percentage (0.01 to 100.00)"}
                  placeholder=""
                  value={formField.value ?? ''}
                  onChange={(e) => {
                    const val = e.target.value;
                    formField.onChange(val === '' ? undefined : parseFloat(val));
                  }}
                  inputProps={{
                    step: 'any',
                  }}
                  data-testid="field-percentage"
                />
              )}
            />
          </Grid>

          {/* Valuation field */}
          <Grid size={{ xs: 12, md: 6 }}>
            <Controller
              name="valuation"
              control={control}
              rules={{
                required: 'Valuation is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  type="number"
                  label="Valuation"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Valuation at time of issue"}
                  placeholder=""
                  value={formField.value ?? ''}
                  onChange={(e) => {
                    const val = e.target.value;
                    formField.onChange(val === '' ? undefined : parseFloat(val));
                  }}
                  inputProps={{
                    step: 'any',
                  }}
                  data-testid="field-valuation"
                />
              )}
            />
          </Grid>

          {/* Issue Date field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="issue_date"
              control={control}
              rules={{
                required: 'Issue Date is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Issue Date"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-issue-date"
                />
              )}
            />
          </Grid>

          {/* Source Document Id field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="source_document_id"
              control={control}
              rules={{
                required: 'Source Document Id is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Source Document Id"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Reference to LegalDocument that created this grant"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-source-document-id"
                />
              )}
            />
          </Grid>

          {/* Form Actions */}
          <Grid size={12}>
            <Box display="flex" gap={2} pt={2}>
              <Button
                type="submit"
                variant="contained"
                color="primary"
                disabled={isLoading}
                startIcon={isLoading ? <CircularProgress size={20} /> : undefined}
                data-testid="submit-button"
              >
                {isLoading
                  ? 'Saving...'
                  : isEditMode
                    ? 'Update EquityGrant'
                    : 'Create EquityGrant'}
              </Button>

              <Button
                type="button"
                variant="outlined"
                onClick={onCancel}
                disabled={isLoading}
                data-testid="cancel-button"
              >
                Cancel
              </Button>
            </Box>
          </Grid>
        </Grid>
      </Box>
    </FormProvider>
  );
}

// ============================================================================
// Modal Wrapper Component
// ============================================================================

/**
 * EquityGrantFormModal Component
 *
 * Wraps the EquityGrantForm in a Material UI Dialog for modal display.
 * Use this component when you want to show the form in a popup dialog.
 *
 * @example
 * ```tsx
 * <EquityGrantFormModal
 *   open={isModalOpen}
 *   onClose={handleClose}
 *   onSubmit={handleCreate}
 *   onCancel={handleClose}
 *   isLoading={isCreating}
 * />
 * ```
 */
export function EquityGrantFormModal({
  open,
  onClose,
  title,
  equityGrant,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: EquityGrantFormModalProps): React.ReactElement {
  const isEditMode = Boolean(equityGrant);
  const modalTitle = title ?? (isEditMode ? 'Edit EquityGrant' : 'Create EquityGrant');

  // Handle modal close - prevent closing during loading
  const handleClose = () => {
    if (!isLoading) {
      onClose();
    }
  };

  return (
    <Dialog
      open={open}
      onClose={handleClose}
      maxWidth="md"
      fullWidth
      data-testid="modal-equity_grant-form"
    >
      <DialogTitle sx={{ m: 0, p: 2, display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        {modalTitle}
        <IconButton
          aria-label="close"
          onClick={handleClose}
          disabled={isLoading}
          data-testid="modal-close-button"
          sx={{ color: (theme) => theme.palette.grey[500] }}
        >
          <CloseIcon />
        </IconButton>
      </DialogTitle>
      <DialogContent dividers>
        <EquityGrantForm
          equityGrant={equityGrant}
          onSubmit={onSubmit}
          onCancel={onCancel}
          isLoading={isLoading}
          className={className}
        />
      </DialogContent>
    </Dialog>
  );
}


// Default export for convenience
export default EquityGrantForm;
