/**
 * DocumentTemplateForm Component
 *
 * Form component for creating and editing document_templates.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * Features:
 * - Create and Edit mode support
 * - Form validation with error messages
 * - Material UI components
 * - Loading state handling
 * - Accessible form fields (WCAG 2.1 Level AA)
 * - Modal wrapper support (form_display: modal)
 *
 * Legal document template definition — NDA, founders agreement, SAFE, employment, etc.
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:51.641584Z
 */

import React, { useMemo, useEffect } from 'react';
import { useForm, Controller, FormProvider } from 'react-hook-form';
import {
  Alert as MuiAlert,
  Box,
  Grid,
  TextField,
  Button,
  FormControlLabel,
  Checkbox,
  CircularProgress,
  Dialog,
  DialogTitle,
  DialogContent,
  IconButton,
} from '@mui/material';
import { Close as CloseIcon } from '@mui/icons-material';
import type { DocumentTemplateCreateRequest, DocumentTemplate } from '../types/document_template.types';
import { useNotification } from '../contexts/NotificationContext';

// ============================================================================
// Types
// ============================================================================

/**
 * Form data type for DocumentTemplate
 */
export type DocumentTemplateFormData = DocumentTemplateCreateRequest;

/**
 * Props for DocumentTemplateForm component
 */
export interface DocumentTemplateFormProps {
  /** Existing document_template data for edit mode (optional) */
  documentTemplate?: DocumentTemplate;
  /** Form submission handler (can be sync or async) */
  onSubmit: (data: DocumentTemplateFormData) => void | Promise<void>;
  /** Cancel handler */
  onCancel: () => void;
  /** Loading state (disables form during submission) */
  isLoading?: boolean;
  /** Custom className for the form container */
  className?: string;
}

/**
 * Props for DocumentTemplateFormModal component (modal wrapper)
 */
export interface DocumentTemplateFormModalProps extends DocumentTemplateFormProps {
  /** Whether the modal is open */
  open: boolean;
  /** Handler called when the modal requests to be closed */
  onClose: () => void;
  /** Optional title override for the modal */
  title?: string;
}


// ============================================================================
// Constants
// ============================================================================

/**
 * Default form values for creating a new document_template
 */
const DEFAULT_FORM_VALUES: Partial<DocumentTemplateFormData> = {
  name: '',
  document_type: '',
  jurisdiction: '',
  version: "1.0",
  description: '',
  template_content: '',
  clause_ids: "[]",
  is_active: true,
};

// ============================================================================
// Component
// ============================================================================

/**
 * DocumentTemplateForm Component
 *
 * Renders a form for creating or editing document_templates.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * @example
 * ```tsx
 * // Create mode
 * <DocumentTemplateForm
 *   onSubmit={handleCreate}
 *   onCancel={handleCancel}
 *   isLoading={isCreating}
 * />
 *
 * // Edit mode
 * <DocumentTemplateForm
 *   documentTemplate={existingDocumentTemplate}
 *   onSubmit={handleUpdate}
 *   onCancel={handleCancel}
 *   isLoading={isUpdating}
 * />
 * ```
 */
export function DocumentTemplateForm({
  documentTemplate,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: DocumentTemplateFormProps): React.ReactElement {
  // Determine if we're in edit mode
  const isEditMode = Boolean(documentTemplate);

  // Notification context for showing success/error messages
  const { showSuccess, showError } = useNotification();

  // Build default values from existing entity or defaults
  const defaultValues = useMemo<Partial<DocumentTemplateFormData>>(() => {
    if (documentTemplate) {
      // Edit mode: use existing entity values
      return {
        name: documentTemplate.name,
        document_type: documentTemplate.document_type,
        jurisdiction: documentTemplate.jurisdiction,
        version: documentTemplate.version,
        description: documentTemplate.description ?? '',
        template_content: documentTemplate.template_content,
        configuration_schema: documentTemplate.configuration_schema ?? undefined,
        clause_ids: documentTemplate.clause_ids ?? undefined,
        is_active: documentTemplate.is_active ?? true,
      };
    }
    // Create mode: use defaults
    return DEFAULT_FORM_VALUES;
  }, [documentTemplate]);

  // Initialize React Hook Form
  const methods = useForm<DocumentTemplateFormData>({
    defaultValues,
    mode: 'onBlur',
  });

  const {
    control,
    handleSubmit,
    formState: { errors },
    reset,
  } = methods;

  // Reset form when entity data changes (e.g., switching from create to edit mode)
  useEffect(() => {
    reset(defaultValues);
  }, [defaultValues, reset]);

  // Log validation errors to console for debugging
  useEffect(() => {
    if (Object.keys(errors).length > 0) {
      console.log('[DocumentTemplateForm] Validation errors:', errors);
    }
  }, [errors]);
  // -------------------------------------------------------------------------
  // Form Submission
  // -------------------------------------------------------------------------

  const handleFormSubmit = async (data: DocumentTemplateFormData) => {
    console.log('[DocumentTemplateForm] Submitting:', { data, isEditMode });
    try {
      const result = await onSubmit(data);
      console.log('[DocumentTemplateForm] Form submission successful', { data, result, isEditMode });
      // Show success notification for create operation
      if (!isEditMode) {
        showSuccess('DocumentTemplate created successfully');
      }
    } catch (error) {
      // Log full error details to console for debugging
      console.error('[DocumentTemplateForm] Form submission failed', {
        error,
        errorMessage: error instanceof Error ? error.message : String(error),
        errorStack: error instanceof Error ? error.stack : undefined,
        data,
        isEditMode,
      });
      // Show error notification on failure
      const errorMessage = error instanceof Error ? error.message : 'An unexpected error occurred';
      showError(`Failed to ${isEditMode ? 'update' : 'create'} DocumentTemplate: ${errorMessage}`);
    }
  };

  // -------------------------------------------------------------------------
  // Render
  // -------------------------------------------------------------------------

  // Count validation errors
  const errorCount = Object.keys(errors).length;

  return (
    <FormProvider {...methods}>
      <Box
        component="form"
        onSubmit={handleSubmit(handleFormSubmit)}
        className={className}
        data-testid="form-document_template"
        noValidate
      >
        {/* Error Summary Alert */}
        {errorCount > 0 && (
          <MuiAlert
            severity="error"
            sx={{ mb: 3 }}
            data-testid="form-errors-summary"
          >
            Please fix {errorCount} error{errorCount !== 1 ? 's' : ''} before submitting
          </MuiAlert>
        )}

        <Grid container spacing={3}>
          {/* Name field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="name"
              control={control}
              rules={{
                required: 'Name is required',
                maxLength: { value: 100, message: 'Name must be at most 100 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Name"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Template display name, e.g. Mutual NDA (England & Wales)"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-name"
                />
              )}
            />
          </Grid>

          {/* Document Type field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="document_type"
              control={control}
              rules={{
                required: 'Document Type is required',
                maxLength: { value: 50, message: 'Document Type must be at most 50 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Document Type"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-document-type"
                />
              )}
            />
          </Grid>

          {/* Jurisdiction field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="jurisdiction"
              control={control}
              rules={{
                required: 'Jurisdiction is required',
                maxLength: { value: 30, message: 'Jurisdiction must be at most 30 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Jurisdiction"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Which legal jurisdiction this template covers"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-jurisdiction"
                />
              )}
            />
          </Grid>

          {/* Version field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="version"
              control={control}
              rules={{
                required: 'Version is required',
                maxLength: { value: 20, message: 'Version must be at most 20 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Version"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Semantic version of the template"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-version"
                />
              )}
            />
          </Grid>

          {/* Description field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="description"
              control={control}
              rules={{
                required: 'Description is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Description"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-description"
                />
              )}
            />
          </Grid>

          {/* Template Content field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="template_content"
              control={control}
              rules={{
                required: 'Template Content is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Template Content"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Jinja2 template content for document generation"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-template-content"
                />
              )}
            />
          </Grid>

          {/* Configuration Schema field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="configuration_schema"
              control={control}
              rules={{
                required: 'Configuration Schema is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Configuration Schema"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "JSON schema defining required configuration fields for this template"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-configuration-schema"
                />
              )}
            />
          </Grid>

          {/* Clause Ids field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="clause_ids"
              control={control}
              rules={{
                required: 'Clause Ids is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Clause Ids"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Ordered list of TemplateClause IDs used in this template"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-clause-ids"
                />
              )}
            />
          </Grid>

          {/* Active Status field */}
          <Grid size={{ xs: 12, md: 6 }}>
            <Controller
              name="is_active"
              control={control}
              render={({ field: formField }) => (
                <FormControlLabel
                  control={
                    <Checkbox
                      {...formField}
                      checked={formField.value ?? true}
                      onChange={(e) => formField.onChange(e.target.checked)}
                      disabled={isLoading}
                      data-testid="checkbox-is-active"
                    />
                  }
                  label="Active"
                />
              )}
            />
          </Grid>

          {/* Form Actions */}
          <Grid size={12}>
            <Box display="flex" gap={2} pt={2}>
              <Button
                type="submit"
                variant="contained"
                color="primary"
                disabled={isLoading}
                startIcon={isLoading ? <CircularProgress size={20} /> : undefined}
                data-testid="submit-button"
              >
                {isLoading
                  ? 'Saving...'
                  : isEditMode
                    ? 'Update DocumentTemplate'
                    : 'Create DocumentTemplate'}
              </Button>

              <Button
                type="button"
                variant="outlined"
                onClick={onCancel}
                disabled={isLoading}
                data-testid="cancel-button"
              >
                Cancel
              </Button>
            </Box>
          </Grid>
        </Grid>
      </Box>
    </FormProvider>
  );
}

// ============================================================================
// Modal Wrapper Component
// ============================================================================

/**
 * DocumentTemplateFormModal Component
 *
 * Wraps the DocumentTemplateForm in a Material UI Dialog for modal display.
 * Use this component when you want to show the form in a popup dialog.
 *
 * @example
 * ```tsx
 * <DocumentTemplateFormModal
 *   open={isModalOpen}
 *   onClose={handleClose}
 *   onSubmit={handleCreate}
 *   onCancel={handleClose}
 *   isLoading={isCreating}
 * />
 * ```
 */
export function DocumentTemplateFormModal({
  open,
  onClose,
  title,
  documentTemplate,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: DocumentTemplateFormModalProps): React.ReactElement {
  const isEditMode = Boolean(documentTemplate);
  const modalTitle = title ?? (isEditMode ? 'Edit DocumentTemplate' : 'Create DocumentTemplate');

  // Handle modal close - prevent closing during loading
  const handleClose = () => {
    if (!isLoading) {
      onClose();
    }
  };

  return (
    <Dialog
      open={open}
      onClose={handleClose}
      maxWidth="md"
      fullWidth
      data-testid="modal-document_template-form"
    >
      <DialogTitle sx={{ m: 0, p: 2, display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        {modalTitle}
        <IconButton
          aria-label="close"
          onClick={handleClose}
          disabled={isLoading}
          data-testid="modal-close-button"
          sx={{ color: (theme) => theme.palette.grey[500] }}
        >
          <CloseIcon />
        </IconButton>
      </DialogTitle>
      <DialogContent dividers>
        <DocumentTemplateForm
          documentTemplate={documentTemplate}
          onSubmit={onSubmit}
          onCancel={onCancel}
          isLoading={isLoading}
          className={className}
        />
      </DialogContent>
    </Dialog>
  );
}


// Default export for convenience
export default DocumentTemplateForm;
