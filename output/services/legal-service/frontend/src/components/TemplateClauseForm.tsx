/**
 * TemplateClauseForm Component
 *
 * Form component for creating and editing template_clauses.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * Features:
 * - Create and Edit mode support
 * - Form validation with error messages
 * - Material UI components
 * - Loading state handling
 * - Accessible form fields (WCAG 2.1 Level AA)
 * - Modal wrapper support (form_display: modal)
 *
 * Clause library entry with conditional variants (A/B/C/D) for legal document generation
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:55.227985Z
 */

import React, { useMemo, useEffect } from 'react';
import { useForm, Controller, FormProvider } from 'react-hook-form';
import {
  Alert as MuiAlert,
  Box,
  Grid,
  TextField,
  Button,
  FormControlLabel,
  Checkbox,
  CircularProgress,
  Dialog,
  DialogTitle,
  DialogContent,
  IconButton,
} from '@mui/material';
import { Close as CloseIcon } from '@mui/icons-material';
import type { TemplateClauseCreateRequest, TemplateClause } from '../types/template_clause.types';
import { useNotification } from '../contexts/NotificationContext';

// ============================================================================
// Types
// ============================================================================

/**
 * Form data type for TemplateClause
 */
export type TemplateClauseFormData = TemplateClauseCreateRequest;

/**
 * Props for TemplateClauseForm component
 */
export interface TemplateClauseFormProps {
  /** Existing template_clause data for edit mode (optional) */
  templateClause?: TemplateClause;
  /** Form submission handler (can be sync or async) */
  onSubmit: (data: TemplateClauseFormData) => void | Promise<void>;
  /** Cancel handler */
  onCancel: () => void;
  /** Loading state (disables form during submission) */
  isLoading?: boolean;
  /** Custom className for the form container */
  className?: string;
}

/**
 * Props for TemplateClauseFormModal component (modal wrapper)
 */
export interface TemplateClauseFormModalProps extends TemplateClauseFormProps {
  /** Whether the modal is open */
  open: boolean;
  /** Handler called when the modal requests to be closed */
  onClose: () => void;
  /** Optional title override for the modal */
  title?: string;
}


// ============================================================================
// Constants
// ============================================================================

/**
 * Default form values for creating a new template_clause
 */
const DEFAULT_FORM_VALUES: Partial<TemplateClauseFormData> = {
  name: '',
  category: '',
  description: '',
  default_variant: "A",
  applicable_document_types: "[]",
  sort_order: 0,
  is_required: true,
};

// ============================================================================
// Component
// ============================================================================

/**
 * TemplateClauseForm Component
 *
 * Renders a form for creating or editing template_clauses.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * @example
 * ```tsx
 * // Create mode
 * <TemplateClauseForm
 *   onSubmit={handleCreate}
 *   onCancel={handleCancel}
 *   isLoading={isCreating}
 * />
 *
 * // Edit mode
 * <TemplateClauseForm
 *   templateClause={existingTemplateClause}
 *   onSubmit={handleUpdate}
 *   onCancel={handleCancel}
 *   isLoading={isUpdating}
 * />
 * ```
 */
export function TemplateClauseForm({
  templateClause,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: TemplateClauseFormProps): React.ReactElement {
  // Determine if we're in edit mode
  const isEditMode = Boolean(templateClause);

  // Notification context for showing success/error messages
  const { showSuccess, showError } = useNotification();

  // Build default values from existing entity or defaults
  const defaultValues = useMemo<Partial<TemplateClauseFormData>>(() => {
    if (templateClause) {
      // Edit mode: use existing entity values
      return {
        name: templateClause.name,
        category: templateClause.category,
        description: templateClause.description ?? '',
        variants: templateClause.variants,
        default_variant: templateClause.default_variant,
        applicable_document_types: templateClause.applicable_document_types,
        sort_order: templateClause.sort_order,
        is_required: templateClause.is_required,
      };
    }
    // Create mode: use defaults
    return DEFAULT_FORM_VALUES;
  }, [templateClause]);

  // Initialize React Hook Form
  const methods = useForm<TemplateClauseFormData>({
    defaultValues,
    mode: 'onBlur',
  });

  const {
    control,
    handleSubmit,
    formState: { errors },
    reset,
  } = methods;

  // Reset form when entity data changes (e.g., switching from create to edit mode)
  useEffect(() => {
    reset(defaultValues);
  }, [defaultValues, reset]);

  // Log validation errors to console for debugging
  useEffect(() => {
    if (Object.keys(errors).length > 0) {
      console.log('[TemplateClauseForm] Validation errors:', errors);
    }
  }, [errors]);
  // -------------------------------------------------------------------------
  // Form Submission
  // -------------------------------------------------------------------------

  const handleFormSubmit = async (data: TemplateClauseFormData) => {
    console.log('[TemplateClauseForm] Submitting:', { data, isEditMode });
    try {
      const result = await onSubmit(data);
      console.log('[TemplateClauseForm] Form submission successful', { data, result, isEditMode });
      // Show success notification for create operation
      if (!isEditMode) {
        showSuccess('TemplateClause created successfully');
      }
    } catch (error) {
      // Log full error details to console for debugging
      console.error('[TemplateClauseForm] Form submission failed', {
        error,
        errorMessage: error instanceof Error ? error.message : String(error),
        errorStack: error instanceof Error ? error.stack : undefined,
        data,
        isEditMode,
      });
      // Show error notification on failure
      const errorMessage = error instanceof Error ? error.message : 'An unexpected error occurred';
      showError(`Failed to ${isEditMode ? 'update' : 'create'} TemplateClause: ${errorMessage}`);
    }
  };

  // -------------------------------------------------------------------------
  // Render
  // -------------------------------------------------------------------------

  // Count validation errors
  const errorCount = Object.keys(errors).length;

  return (
    <FormProvider {...methods}>
      <Box
        component="form"
        onSubmit={handleSubmit(handleFormSubmit)}
        className={className}
        data-testid="form-template_clause"
        noValidate
      >
        {/* Error Summary Alert */}
        {errorCount > 0 && (
          <MuiAlert
            severity="error"
            sx={{ mb: 3 }}
            data-testid="form-errors-summary"
          >
            Please fix {errorCount} error{errorCount !== 1 ? 's' : ''} before submitting
          </MuiAlert>
        )}

        <Grid container spacing={3}>
          {/* Name field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="name"
              control={control}
              rules={{
                required: 'Name is required',
                maxLength: { value: 100, message: 'Name must be at most 100 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Name"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Clause name, e.g. Purpose, Duration, Data Protection"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-name"
                />
              )}
            />
          </Grid>

          {/* Category field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="category"
              control={control}
              rules={{
                required: 'Category is required',
                maxLength: { value: 50, message: 'Category must be at most 50 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Category"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Clause category for grouping and selection"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-category"
                />
              )}
            />
          </Grid>

          {/* Description field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="description"
              control={control}
              rules={{
                required: 'Description is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Description"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-description"
                />
              )}
            />
          </Grid>

          {/* Variants field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="variants"
              control={control}
              rules={{
                required: 'Variants is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Variants"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Clause variants as {A: {label, content}, B: {label, content}, ...}"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-variants"
                />
              )}
            />
          </Grid>

          {/* Default Variant field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="default_variant"
              control={control}
              rules={{
                required: 'Default Variant is required',
                maxLength: { value: 10, message: 'Default Variant must be at most 10 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Default Variant"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Default variant key"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-default-variant"
                />
              )}
            />
          </Grid>

          {/* Applicable Document Types field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="applicable_document_types"
              control={control}
              rules={{
                required: 'Applicable Document Types is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Applicable Document Types"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Document types this clause applies to, e.g. [\"mutual_nda\", \"one_way_nda\"]"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-applicable-document-types"
                />
              )}
            />
          </Grid>

          {/* Sort Order field */}
          <Grid size={{ xs: 12, md: 6 }}>
            <Controller
              name="sort_order"
              control={control}
              rules={{
                required: 'Sort Order is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  type="number"
                  label="Sort Order"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Display ordering within category"}
                  placeholder=""
                  value={formField.value ?? ''}
                  onChange={(e) => {
                    const val = e.target.value;
                    formField.onChange(val === '' ? undefined : parseFloat(val));
                  }}
                  inputProps={{
                    step: 'any',
                  }}
                  data-testid="field-sort-order"
                />
              )}
            />
          </Grid>

          {/* Is Required field */}
          <Grid size={{ xs: 12, md: 6 }}>
            <Controller
              name="is_required"
              control={control}
              render={({ field: formField }) => (
                <FormControlLabel
                  control={
                    <Checkbox
                      {...formField}
                      checked={formField.value ?? false}
                      onChange={(e) => formField.onChange(e.target.checked)}
                      disabled={isLoading}
                      data-testid="checkbox-is-required"
                    />
                  }
                  label="Is Required"
                />
              )}
            />
          </Grid>

          {/* Form Actions */}
          <Grid size={12}>
            <Box display="flex" gap={2} pt={2}>
              <Button
                type="submit"
                variant="contained"
                color="primary"
                disabled={isLoading}
                startIcon={isLoading ? <CircularProgress size={20} /> : undefined}
                data-testid="submit-button"
              >
                {isLoading
                  ? 'Saving...'
                  : isEditMode
                    ? 'Update TemplateClause'
                    : 'Create TemplateClause'}
              </Button>

              <Button
                type="button"
                variant="outlined"
                onClick={onCancel}
                disabled={isLoading}
                data-testid="cancel-button"
              >
                Cancel
              </Button>
            </Box>
          </Grid>
        </Grid>
      </Box>
    </FormProvider>
  );
}

// ============================================================================
// Modal Wrapper Component
// ============================================================================

/**
 * TemplateClauseFormModal Component
 *
 * Wraps the TemplateClauseForm in a Material UI Dialog for modal display.
 * Use this component when you want to show the form in a popup dialog.
 *
 * @example
 * ```tsx
 * <TemplateClauseFormModal
 *   open={isModalOpen}
 *   onClose={handleClose}
 *   onSubmit={handleCreate}
 *   onCancel={handleClose}
 *   isLoading={isCreating}
 * />
 * ```
 */
export function TemplateClauseFormModal({
  open,
  onClose,
  title,
  templateClause,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: TemplateClauseFormModalProps): React.ReactElement {
  const isEditMode = Boolean(templateClause);
  const modalTitle = title ?? (isEditMode ? 'Edit TemplateClause' : 'Create TemplateClause');

  // Handle modal close - prevent closing during loading
  const handleClose = () => {
    if (!isLoading) {
      onClose();
    }
  };

  return (
    <Dialog
      open={open}
      onClose={handleClose}
      maxWidth="md"
      fullWidth
      data-testid="modal-template_clause-form"
    >
      <DialogTitle sx={{ m: 0, p: 2, display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        {modalTitle}
        <IconButton
          aria-label="close"
          onClick={handleClose}
          disabled={isLoading}
          data-testid="modal-close-button"
          sx={{ color: (theme) => theme.palette.grey[500] }}
        >
          <CloseIcon />
        </IconButton>
      </DialogTitle>
      <DialogContent dividers>
        <TemplateClauseForm
          templateClause={templateClause}
          onSubmit={onSubmit}
          onCancel={onCancel}
          isLoading={isLoading}
          className={className}
        />
      </DialogContent>
    </Dialog>
  );
}


// Default export for convenience
export default TemplateClauseForm;
