/**
 * FormDocumentTemplateSelect Component
 *
 * A dropdown select component for document_template selection with React Hook Form integration.
 * Uses the useDocumentTemplatesDropdown hook to fetch document_template data with loading and error states.
 *
 * Features:
 * - React Hook Form integration via Controller
 * - Fetches document_templates using useDocumentTemplatesDropdown hook
 * - Material UI Autocomplete with search/filter
 * - Loading skeleton while fetching data
 * - Error state with retry button
 * - Support for required/optional fields
 * - Full accessibility support (ARIA attributes)
 * - Memoized for optimal performance
 *
 * Legal document template definition — NDA, founders agreement, SAFE, employment, etc.
 *
 * @example
 * ```tsx
 * import { FormDocumentTemplateSelect } from '../components/FormDocumentTemplateSelect';
 * import { useForm, FormProvider } from 'react-hook-form';
 *
 * function MyForm() {
 *   const methods = useForm();
 *
 *   return (
 *     <FormProvider {...methods}>
 *       <FormDocumentTemplateSelect
 *         name="documentTemplateId"
 *         label="DocumentTemplate"
 *         required={true}
 *       />
 *     </FormProvider>
 *   );
 * }
 *
 * // With active filter disabled (show all including inactive)
 * <FormDocumentTemplateSelect name="documentTemplateId" activeOnly={false} />
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:51.700282Z
 */

import React, { memo, useMemo } from 'react';
import { useFormContext, Controller } from 'react-hook-form';
import {
  Autocomplete,
  TextField,
  Box,
  Skeleton,
  Typography,
  Button,
  Alert,
} from '@mui/material';
import { useDocumentTemplatesDropdown, DocumentTemplateDropdownOption } from '../hooks/useDocumentTemplates';

// ============================================================================
// Types
// ============================================================================

/**
 * Props for the FormDocumentTemplateSelect component
 */
export interface FormDocumentTemplateSelectProps {
  /**
   * Form field name (should match the form schema)
   */
  name: string;

  /**
   * Label text for the select field
   */
  label: string;

  /**
   * Placeholder text when no value is selected
   * @default "Select document template..."
   */
  placeholder?: string;

  /**
   * Whether the field is required
   * @default false
   */
  required?: boolean;

  /**
   * Whether the field is disabled
   * @default false
   */
  disabled?: boolean;

  /**
   * Helper text displayed below the field
   */
  helperText?: string;

  /**
   * Custom className for the container
   */
  className?: string;

  /**
   * Custom styles for the container
   */
  style?: React.CSSProperties;

  /**
   * Whether to take full width
   * @default true
   */
  fullWidth?: boolean;

  /**
   * Size variant
   * @default "medium"
   */
  size?: 'small' | 'medium';

  /**
   * Tab index for keyboard navigation
   */
  tabIndex?: number;

  /**
   * Aria label for accessibility
   */
  'aria-label'?: string;

  /**
   * Aria described by for accessibility
   */
  'aria-describedby'?: string;

  /**
   * Data test id for testing
   */
  'data-testid'?: string;

  /**
   * Filter to only show active document_templates
   * @default true
   */
  activeOnly?: boolean;

  /**
   * Callback when value changes (in addition to form update)
   */
  onChange?: (value: string | null, option: DocumentTemplateDropdownOption | null) => void;

  /**
   * Whether to allow clearing the selection
   * When required is true, this is ignored (always non-clearable)
   * @default true for optional fields
   */
  clearable?: boolean;

  /**
   * Whether to show the full entity data in the dropdown
   * When true, shows additional info like ID
   * @default false
   */
  showExtendedInfo?: boolean;
}

// ============================================================================
// Loading Skeleton Component
// ============================================================================

interface SelectLoadingSkeletonProps {
  label: string;
  required?: boolean;
  fullWidth?: boolean;
  size?: 'small' | 'medium';
  className?: string;
  style?: React.CSSProperties;
  'data-testid'?: string;
}

const SelectLoadingSkeleton: React.FC<SelectLoadingSkeletonProps> = ({
  label,
  required = false,
  fullWidth = true,
  size = 'medium',
  className,
  style,
  'data-testid': dataTestId,
}) => {
  const height = size === 'small' ? 40 : 56;

  return (
    <Box
      className={className}
      style={{ width: fullWidth ? '100%' : 'auto', ...style }}
      data-testid={dataTestId ? `${dataTestId}-loading` : 'document_template-select-loading'}
    >
      <Typography
        variant="body2"
        component="span"
        sx={{ mb: 0.5, display: 'block', color: 'text.secondary' }}
      >
        {label}
        {required && (
          <Typography component="span" sx={{ color: 'error.main', ml: 0.5 }}>
            *
          </Typography>
        )}
      </Typography>
      <Skeleton
        variant="rounded"
        height={height}
        animation="wave"
        sx={{ borderRadius: 1 }}
      />
      <Typography variant="caption" sx={{ mt: 0.5, display: 'block', color: 'text.secondary' }}>
        Loading document templates...
      </Typography>
    </Box>
  );
};

// ============================================================================
// Error State Component
// ============================================================================

interface SelectErrorStateProps {
  error: Error;
  onRetry: () => void;
  label: string;
  fullWidth?: boolean;
  className?: string;
  style?: React.CSSProperties;
  'data-testid'?: string;
}

const SelectErrorState: React.FC<SelectErrorStateProps> = ({
  error,
  onRetry,
  label,
  fullWidth = true,
  className,
  style,
  'data-testid': dataTestId,
}) => {
  return (
    <Box
      className={className}
      style={{ width: fullWidth ? '100%' : 'auto', ...style }}
      data-testid={dataTestId ? `${dataTestId}-error` : 'document_template-select-error'}
    >
      <Typography
        variant="body2"
        component="span"
        sx={{ mb: 0.5, display: 'block', color: 'text.secondary' }}
      >
        {label}
      </Typography>
      <Alert
        severity="error"
        action={
          <Button color="inherit" size="small" onClick={onRetry}>
            Retry
          </Button>
        }
        sx={{ py: 0.5 }}
      >
        Failed to load document templates: {error.message || 'Unknown error'}
      </Alert>
    </Box>
  );
};

// ============================================================================
// Main Component
// ============================================================================

/**
 * FormDocumentTemplateSelect Component
 *
 * A dropdown for selecting document_templates with React Hook Form integration.
 * Automatically fetches document_templates using the useDocumentTemplatesDropdown hook
 * and handles loading and error states.
 */
export const FormDocumentTemplateSelect: React.FC<FormDocumentTemplateSelectProps> = memo(
  ({
    name,
    label,
    placeholder = 'Select document template...',
    required = false,
    disabled = false,
    helperText,
    className,
    style,
    fullWidth = true,
    size = 'medium',
    tabIndex,
    'aria-label': ariaLabel,
    'aria-describedby': ariaDescribedBy,
    'data-testid': dataTestId,
    activeOnly = true,
    onChange,
    clearable = true,
    showExtendedInfo = false,
  }) => {
    const { control } = useFormContext();

    // Fetch document_templates for dropdown
    const {
      options,
      isLoading,
      isError,
      error,
      refetch,
    } = useDocumentTemplatesDropdown(activeOnly);

    // Memoize options for performance
    const dropdownOptions = useMemo(() => options ?? [], [options]);

    // Find option by value
    const findOptionByValue = (value: string | null | undefined): DocumentTemplateDropdownOption | null => {
      if (!value) return null;
      return dropdownOptions.find((opt) => opt.value === value) ?? null;
    };

    // Generate IDs for accessibility
    const inputId = `${name}-document_template-select`;
    const errorId = `${name}-error`;
    const helperId = `${name}-helper`;

    // -------------------------------------------------------------------------
    // Loading State
    // -------------------------------------------------------------------------

    if (isLoading) {
      return (
        <SelectLoadingSkeleton
          label={label}
          required={required}
          fullWidth={fullWidth}
          size={size}
          className={className}
          style={style}
          data-testid={dataTestId}
        />
      );
    }

    // -------------------------------------------------------------------------
    // Error State
    // -------------------------------------------------------------------------

    if (isError && error) {
      return (
        <SelectErrorState
          error={error}
          onRetry={() => refetch()}
          label={label}
          fullWidth={fullWidth}
          className={className}
          style={style}
          data-testid={dataTestId}
        />
      );
    }

    // -------------------------------------------------------------------------
    // Render Select
    // -------------------------------------------------------------------------

    return (
      <Controller
        name={name}
        control={control}
        rules={{
          required: required ? 'Please select a document template' : false,
        }}
        render={({ field, fieldState: { error: fieldError }}) => {
          const hasError = !!fieldError;
          const errorMessage = fieldError?.message;

          // Find the current option based on field value
          const selectedOption = findOptionByValue(field.value);

          // Handle change
          const handleChange = (
            _event: React.SyntheticEvent,
            newValue: DocumentTemplateDropdownOption | null
          ) => {
            const newId = newValue?.value ?? null;
            field.onChange(newId);
            if (onChange) {
              onChange(newId, newValue);
            }
          };

          return (
            <Autocomplete<DocumentTemplateDropdownOption, false, boolean, false>
              id={inputId}
              value={selectedOption}
              onChange={handleChange}
              onBlur={field.onBlur}
              options={dropdownOptions}
              getOptionLabel={(option) => option.label}
              isOptionEqualToValue={(option, value) => option.value === value.value}
              disabled={disabled}
              disableClearable={required || !clearable}
              fullWidth={fullWidth}
              size={size}
              className={className}
              style={style}
              data-testid={dataTestId}
              renderOption={(props, option) => (
                <Box
                  component="li"
                  {...props}
                  key={option.value}
                  sx={{
                    display: 'flex',
                    flexDirection: showExtendedInfo ? 'column' : 'row',
                    alignItems: showExtendedInfo ? 'flex-start' : 'center',
                    gap: 0.5,
                  }}
                >
                  <Typography variant="body2">{option.label}</Typography>
                  {showExtendedInfo && (
                    <Typography variant="caption" color="text.secondary">
                      ID: {option.value}
                    </Typography>
                  )}
                </Box>
              )}
              renderInput={(params) => (
                <TextField
                  {...params}
                  label={label}
                  placeholder={placeholder}
                  required={required}
                  error={hasError}
                  helperText={hasError ? errorMessage : helperText}
                  inputProps={{
                    ...params.inputProps,
                    tabIndex,
                    'aria-label': ariaLabel || label,
                    'aria-describedby': ariaDescribedBy ||
                      (hasError ? errorId : helperText ? helperId : undefined),
                    'aria-invalid': hasError,
                    'aria-required': required,
                  }}
                  FormHelperTextProps={{
                    id: hasError ? errorId : helperId,
                    role: hasError ? 'alert' : undefined,
                  }}
                />
              )}
              noOptionsText="No document templates found"
              loadingText="Loading document templates..."
            />
          );
        }}
      />
    );
  },
  // Custom memo comparison
  (prevProps, nextProps) => {
    return (
      prevProps.name === nextProps.name &&
      prevProps.label === nextProps.label &&
      prevProps.placeholder === nextProps.placeholder &&
      prevProps.helperText === nextProps.helperText &&
      prevProps.disabled === nextProps.disabled &&
      prevProps.required === nextProps.required &&
      prevProps.fullWidth === nextProps.fullWidth &&
      prevProps.size === nextProps.size &&
      prevProps.className === nextProps.className &&
      prevProps.activeOnly === nextProps.activeOnly &&
      prevProps.clearable === nextProps.clearable &&
      prevProps.showExtendedInfo === nextProps.showExtendedInfo
    );
  }
);

FormDocumentTemplateSelect.displayName = 'FormDocumentTemplateSelect';

export default FormDocumentTemplateSelect;
