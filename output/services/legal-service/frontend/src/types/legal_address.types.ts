/**
 * LegalAddress Type Definitions
 *
 * This file contains type definitions for the LegalAddress entity:
 * - LegalAddress: Full entity model with all fields
 * - LegalAddressCreateRequest: Data for creating a new legal_address
 * - LegalAddressUpdateRequest: Partial data for updating a legal_address
 * - LegalAddressListResponse: Paginated list response
 * - LegalAddressFilterOptions: Query parameters for filtering
 * - LegalAddressDropdownOption: Options for select components
 *
 * Physical address for legal entities and persons with jurisdiction classification
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:53.359338Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// LegalAddress Entity
// ============================================================================

/**
 * LegalAddress entity model
 *
 * Represents the full legal_address entity as returned by the API.
 * Physical address for legal entities and persons with jurisdiction classification
 *
 * @remarks
 * This interface matches the backend LegalAddressDTO schema exactly.
 */
export interface LegalAddress {
  /** Unique identifier (UUID) */
  id: string;

  /** Address Line 1 */
  address_line_1: string;

  /** Address Line 2 */
  address_line_2: string | null;

  /** City */
  city: string;

  /** State Province */
  state_province: string | null;

  /** Postal Code */
  postal_code: string;

  /** ISO 3166-1 alpha-2 country code */
  country: string;

  /** Jurisdiction */
  jurisdiction: string;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * LegalAddress creation request data
 *
 * Used when creating a new legal_address. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend LegalAddressCreateDTO schema.
 */
export interface LegalAddressCreateRequest {
  /** Address Line 1 (required) */
  address_line_1: string;

  /** Address Line 2 (optional) */
  address_line_2?: string;

  /** City (required) */
  city: string;

  /** State Province (optional) */
  state_province?: string;

  /** Postal Code (required) */
  postal_code: string;

  /** ISO 3166-1 alpha-2 country code (required) */
  country: string;

  /** Jurisdiction (required) */
  jurisdiction: string;

  /** Active status (defaults to true for new legal_addresses) */
  is_active?: boolean;
}

/**
 * LegalAddress update request data
 *
 * Used when updating an existing legal_address. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend LegalAddressUpdateDTO schema.
 */
export interface LegalAddressUpdateRequest {
  /** Address Line 1 */
  address_line_1?: string;

  /** Address Line 2 */
  address_line_2?: string | null;

  /** City */
  city?: string;

  /** State Province */
  state_province?: string | null;

  /** Postal Code */
  postal_code?: string;

  /** ISO 3166-1 alpha-2 country code */
  country?: string;

  /** Jurisdiction */
  jurisdiction?: string;

  /** Active status */
  is_active?: boolean;
}

/**
 * LegalAddress list response
 *
 * Wrapper for paginated legal_address list queries.
 */
export interface LegalAddressListResponse {
  /** Array of legal_address items */
  items: LegalAddress[];

  /** Alternative property name for items (for API compatibility) */
  legal_addresses?: LegalAddress[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * LegalAddress filter options for list queries
 *
 * Extends BaseEntityFilters with legal_address-specific filtering options.
 */
export interface LegalAddressFilterOptions extends BaseEntityFilters {
}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * LegalAddress dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface LegalAddressDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: LegalAddress;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * LegalAddress summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface LegalAddressSummary {
  /** Unique identifier */
  id: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * LegalAddress form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type LegalAddressFormData = LegalAddressCreateRequest;

/**
 * Default form values for new legal_address
 *
 * Use this when initializing form state for creating a new legal_address.
 */
export const DEFAULT_LEGAL_ADDRESS_FORM_DATA: Partial<LegalAddressFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single legal_address API response
 */
export interface LegalAddressResponse {
  /** The requested legal_address entity */
  data: LegalAddress;
}

/**
 * LegalAddress mutation response (create/update)
 */
export interface LegalAddressMutationResponse {
  /** The created or updated legal_address */
  data: LegalAddress;

  /** Success message */
  message?: string;
}

/**
 * API error response for legal_address operations
 */
export interface LegalAddressApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a LegalAddress
 *
 * @param value - Value to check
 * @returns True if value is a LegalAddress
 */
export function isLegalAddress(value: unknown): value is LegalAddress {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.address_line_1 === 'string' &&
    typeof obj.city === 'string' &&
    typeof obj.postal_code === 'string' &&
    typeof obj.country === 'string' &&
    typeof obj.jurisdiction === 'string'
  );
}

/**
 * Type guard to check if a value is a LegalAddressDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a LegalAddressDropdownOption
 */
export function isLegalAddressDropdownOption(
  value: unknown
): value is LegalAddressDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isLegalAddress(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All legal_address types re-exported for convenience */
export type {
  LegalAddress as LegalAddressEntity,
  LegalAddressCreateRequest as CreateLegalAddressInput,
  LegalAddressUpdateRequest as UpdateLegalAddressInput,
};
