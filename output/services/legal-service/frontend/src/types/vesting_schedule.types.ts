/**
 * VestingSchedule Type Definitions
 *
 * This file contains type definitions for the VestingSchedule entity:
 * - VestingSchedule: Full entity model with all fields
 * - VestingScheduleCreateRequest: Data for creating a new vesting_schedule
 * - VestingScheduleUpdateRequest: Partial data for updating a vesting_schedule
 * - VestingScheduleListResponse: Paginated list response
 * - VestingScheduleFilterOptions: Query parameters for filtering
 * - VestingScheduleDropdownOption: Options for select components
 *
 * Vesting schedule for equity grants — cliff period, total period, and acceleration triggers
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:55.613593Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// VestingSchedule Entity
// ============================================================================

/**
 * VestingSchedule entity model
 *
 * Represents the full vesting_schedule entity as returned by the API.
 * Vesting schedule for equity grants — cliff period, total period, and acceleration triggers
 *
 * @remarks
 * This interface matches the backend VestingScheduleDTO schema exactly.
 */
export interface VestingSchedule {
  /** Unique identifier (UUID) */
  id: string;

  /** Equity Grant Id */
  equity_grant_id: string;

  /** Total vesting period in months (12-60, typically 48 for 4-year vest) */
  total_period_months: number;

  /** Cliff period in months (0-24, typically 12 for 1-year cliff) */
  cliff_months: number;

  /** Start Date */
  start_date: string;

  /** Acceleration event type on change of control */
  acceleration_trigger: string;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * VestingSchedule creation request data
 *
 * Used when creating a new vesting_schedule. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend VestingScheduleCreateDTO schema.
 */
export interface VestingScheduleCreateRequest {
  /** Equity Grant Id (required) */
  equity_grant_id: string;

  /** Total vesting period in months (12-60, typically 48 for 4-year vest) (required) */
  total_period_months: number;

  /** Cliff period in months (0-24, typically 12 for 1-year cliff) (required) */
  cliff_months: number;

  /** Start Date (required) */
  start_date: string;

  /** Acceleration event type on change of control (optional) */
  acceleration_trigger?: string;

  /** Active status (defaults to true for new vesting_schedules) */
  is_active?: boolean;
}

/**
 * VestingSchedule update request data
 *
 * Used when updating an existing vesting_schedule. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend VestingScheduleUpdateDTO schema.
 */
export interface VestingScheduleUpdateRequest {
  /** Equity Grant Id */
  equity_grant_id?: string;

  /** Total vesting period in months (12-60, typically 48 for 4-year vest) */
  total_period_months?: number;

  /** Cliff period in months (0-24, typically 12 for 1-year cliff) */
  cliff_months?: number;

  /** Start Date */
  start_date?: string;

  /** Acceleration event type on change of control */
  acceleration_trigger?: string;

  /** Active status */
  is_active?: boolean;
}

/**
 * VestingSchedule list response
 *
 * Wrapper for paginated vesting_schedule list queries.
 */
export interface VestingScheduleListResponse {
  /** Array of vesting_schedule items */
  items: VestingSchedule[];

  /** Alternative property name for items (for API compatibility) */
  vesting_schedules?: VestingSchedule[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * VestingSchedule filter options for list queries
 *
 * Extends BaseEntityFilters with vesting_schedule-specific filtering options.
 */
export interface VestingScheduleFilterOptions extends BaseEntityFilters {
  /** Filter by equity grant id */
  equity_grant_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * VestingSchedule dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface VestingScheduleDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: VestingSchedule;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * VestingSchedule summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface VestingScheduleSummary {
  /** Unique identifier */
  id: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * VestingSchedule form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type VestingScheduleFormData = VestingScheduleCreateRequest;

/**
 * Default form values for new vesting_schedule
 *
 * Use this when initializing form state for creating a new vesting_schedule.
 */
export const DEFAULT_VESTING_SCHEDULE_FORM_DATA: Partial<VestingScheduleFormData> = {
  is_active: true,
  acceleration_trigger: "none",
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single vesting_schedule API response
 */
export interface VestingScheduleResponse {
  /** The requested vesting_schedule entity */
  data: VestingSchedule;
}

/**
 * VestingSchedule mutation response (create/update)
 */
export interface VestingScheduleMutationResponse {
  /** The created or updated vesting_schedule */
  data: VestingSchedule;

  /** Success message */
  message?: string;
}

/**
 * API error response for vesting_schedule operations
 */
export interface VestingScheduleApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a VestingSchedule
 *
 * @param value - Value to check
 * @returns True if value is a VestingSchedule
 */
export function isVestingSchedule(value: unknown): value is VestingSchedule {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.equity_grant_id === 'string' &&
    typeof obj.total_period_months === 'number' &&
    typeof obj.cliff_months === 'number' &&
    typeof obj.start_date === 'string'
  );
}

/**
 * Type guard to check if a value is a VestingScheduleDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a VestingScheduleDropdownOption
 */
export function isVestingScheduleDropdownOption(
  value: unknown
): value is VestingScheduleDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isVestingSchedule(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All vesting_schedule types re-exported for convenience */
export type {
  VestingSchedule as VestingScheduleEntity,
  VestingScheduleCreateRequest as CreateVestingScheduleInput,
  VestingScheduleUpdateRequest as UpdateVestingScheduleInput,
};
