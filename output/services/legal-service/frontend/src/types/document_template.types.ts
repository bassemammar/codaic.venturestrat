/**
 * DocumentTemplate Type Definitions
 *
 * This file contains type definitions for the DocumentTemplate entity:
 * - DocumentTemplate: Full entity model with all fields
 * - DocumentTemplateCreateRequest: Data for creating a new document_template
 * - DocumentTemplateUpdateRequest: Partial data for updating a document_template
 * - DocumentTemplateListResponse: Paginated list response
 * - DocumentTemplateFilterOptions: Query parameters for filtering
 * - DocumentTemplateDropdownOption: Options for select components
 *
 * Legal document template definition — NDA, founders agreement, SAFE, employment, etc.
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:51.543621Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// DocumentTemplate Entity
// ============================================================================

/**
 * DocumentTemplate entity model
 *
 * Represents the full document_template entity as returned by the API.
 * Legal document template definition — NDA, founders agreement, SAFE, employment, etc.
 *
 * @remarks
 * This interface matches the backend DocumentTemplateDTO schema exactly.
 */
export interface DocumentTemplate {
  /** Unique identifier (UUID) */
  id: string;

  /** Template display name, e.g. Mutual NDA (England & Wales) */
  name: string;

  /** Document Type */
  document_type: string;

  /** Which legal jurisdiction this template covers */
  jurisdiction: string;

  /** Semantic version of the template */
  version: string;

  /** Description */
  description: string | null;

  /** Jinja2 template content for document generation */
  template_content: string;

  /** JSON schema defining required configuration fields for this template */
  configuration_schema: Record<string, any> | null;

  /** Is Active */
  is_active: boolean;

  /** Ordered list of TemplateClause IDs used in this template */
  clause_ids: Record<string, any> | null;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * DocumentTemplate creation request data
 *
 * Used when creating a new document_template. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend DocumentTemplateCreateDTO schema.
 */
export interface DocumentTemplateCreateRequest {
  /** Template display name, e.g. Mutual NDA (England & Wales) (required) */
  name: string;

  /** Document Type (required) */
  document_type: string;

  /** Which legal jurisdiction this template covers (required) */
  jurisdiction: string;

  /** Semantic version of the template (optional) */
  version?: string;

  /** Description (optional) */
  description?: string;

  /** Jinja2 template content for document generation (required) */
  template_content: string;

  /** JSON schema defining required configuration fields for this template (optional) */
  configuration_schema?: Record<string, any>;

  /** Is Active (optional) */
  is_active?: boolean;

  /** Ordered list of TemplateClause IDs used in this template (optional) */
  clause_ids?: Record<string, any>;

}

/**
 * DocumentTemplate update request data
 *
 * Used when updating an existing document_template. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend DocumentTemplateUpdateDTO schema.
 */
export interface DocumentTemplateUpdateRequest {
  /** Template display name, e.g. Mutual NDA (England & Wales) */
  name?: string;

  /** Document Type */
  document_type?: string;

  /** Which legal jurisdiction this template covers */
  jurisdiction?: string;

  /** Semantic version of the template */
  version?: string;

  /** Description */
  description?: string | null;

  /** Jinja2 template content for document generation */
  template_content?: string;

  /** JSON schema defining required configuration fields for this template */
  configuration_schema?: Record<string, any> | null;

  /** Is Active */
  is_active?: boolean;

  /** Ordered list of TemplateClause IDs used in this template */
  clause_ids?: Record<string, any> | null;

}

/**
 * DocumentTemplate list response
 *
 * Wrapper for paginated document_template list queries.
 */
export interface DocumentTemplateListResponse {
  /** Array of document_template items */
  items: DocumentTemplate[];

  /** Alternative property name for items (for API compatibility) */
  document_templates?: DocumentTemplate[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * DocumentTemplate filter options for list queries
 *
 * Extends BaseEntityFilters with document_template-specific filtering options.
 */
export interface DocumentTemplateFilterOptions extends BaseEntityFilters {
  /** Filter by document type */
  document_type?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * DocumentTemplate dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface DocumentTemplateDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: DocumentTemplate;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * DocumentTemplate summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface DocumentTemplateSummary {
  /** Unique identifier */
  id: string;

  /** Template display name, e.g. Mutual NDA (England & Wales) */
  name: string;

  /** Description */
  description: string | null;

}

// ============================================================================
// Form Types
// ============================================================================

/**
 * DocumentTemplate form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type DocumentTemplateFormData = DocumentTemplateCreateRequest;

/**
 * Default form values for new document_template
 *
 * Use this when initializing form state for creating a new document_template.
 */
export const DEFAULT_DOCUMENT_TEMPLATE_FORM_DATA: Partial<DocumentTemplateFormData> = {
  version: "1.0",
  is_active: true,
  clause_ids: "[]",
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single document_template API response
 */
export interface DocumentTemplateResponse {
  /** The requested document_template entity */
  data: DocumentTemplate;
}

/**
 * DocumentTemplate mutation response (create/update)
 */
export interface DocumentTemplateMutationResponse {
  /** The created or updated document_template */
  data: DocumentTemplate;

  /** Success message */
  message?: string;
}

/**
 * API error response for document_template operations
 */
export interface DocumentTemplateApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a DocumentTemplate
 *
 * @param value - Value to check
 * @returns True if value is a DocumentTemplate
 */
export function isDocumentTemplate(value: unknown): value is DocumentTemplate {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.name === 'string' &&
    typeof obj.document_type === 'string' &&
    typeof obj.jurisdiction === 'string' &&
    typeof obj.template_content === 'string'
  );
}

/**
 * Type guard to check if a value is a DocumentTemplateDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a DocumentTemplateDropdownOption
 */
export function isDocumentTemplateDropdownOption(
  value: unknown
): value is DocumentTemplateDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isDocumentTemplate(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All document_template types re-exported for convenience */
export type {
  DocumentTemplate as DocumentTemplateEntity,
  DocumentTemplateCreateRequest as CreateDocumentTemplateInput,
  DocumentTemplateUpdateRequest as UpdateDocumentTemplateInput,
};
