/**
 * TemplateClause Type Definitions
 *
 * This file contains type definitions for the TemplateClause entity:
 * - TemplateClause: Full entity model with all fields
 * - TemplateClauseCreateRequest: Data for creating a new template_clause
 * - TemplateClauseUpdateRequest: Partial data for updating a template_clause
 * - TemplateClauseListResponse: Paginated list response
 * - TemplateClauseFilterOptions: Query parameters for filtering
 * - TemplateClauseDropdownOption: Options for select components
 *
 * Clause library entry with conditional variants (A/B/C/D) for legal document generation
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:55.123959Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// TemplateClause Entity
// ============================================================================

/**
 * TemplateClause entity model
 *
 * Represents the full template_clause entity as returned by the API.
 * Clause library entry with conditional variants (A/B/C/D) for legal document generation
 *
 * @remarks
 * This interface matches the backend TemplateClauseDTO schema exactly.
 */
export interface TemplateClause {
  /** Unique identifier (UUID) */
  id: string;

  /** Clause name, e.g. Purpose, Duration, Data Protection */
  name: string;

  /** Clause category for grouping and selection */
  category: string;

  /** Description */
  description: string | null;

  /** Clause variants as {A: {label, content}, B: {label, content}, ...} */
  variants: Record<string, any>;

  /** Default variant key */
  default_variant: string;

  /** Document types this clause applies to, e.g. ["mutual_nda", "one_way_nda"] */
  applicable_document_types: Record<string, any>;

  /** Display ordering within category */
  sort_order: number;

  /** Is Required */
  is_required: boolean;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * TemplateClause creation request data
 *
 * Used when creating a new template_clause. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend TemplateClauseCreateDTO schema.
 */
export interface TemplateClauseCreateRequest {
  /** Clause name, e.g. Purpose, Duration, Data Protection (required) */
  name: string;

  /** Clause category for grouping and selection (required) */
  category: string;

  /** Description (optional) */
  description?: string;

  /** Clause variants as {A: {label, content}, B: {label, content}, ...} (required) */
  variants: Record<string, any>;

  /** Default variant key (optional) */
  default_variant?: string;

  /** Document types this clause applies to, e.g. ["mutual_nda", "one_way_nda"] (optional) */
  applicable_document_types?: Record<string, any>;

  /** Display ordering within category (optional) */
  sort_order?: number;

  /** Is Required (optional) */
  is_required?: boolean;

  /** Active status (defaults to true for new template_clauses) */
  is_active?: boolean;
}

/**
 * TemplateClause update request data
 *
 * Used when updating an existing template_clause. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend TemplateClauseUpdateDTO schema.
 */
export interface TemplateClauseUpdateRequest {
  /** Clause name, e.g. Purpose, Duration, Data Protection */
  name?: string;

  /** Clause category for grouping and selection */
  category?: string;

  /** Description */
  description?: string | null;

  /** Clause variants as {A: {label, content}, B: {label, content}, ...} */
  variants?: Record<string, any>;

  /** Default variant key */
  default_variant?: string;

  /** Document types this clause applies to, e.g. ["mutual_nda", "one_way_nda"] */
  applicable_document_types?: Record<string, any>;

  /** Display ordering within category */
  sort_order?: number;

  /** Is Required */
  is_required?: boolean;

  /** Active status */
  is_active?: boolean;
}

/**
 * TemplateClause list response
 *
 * Wrapper for paginated template_clause list queries.
 */
export interface TemplateClauseListResponse {
  /** Array of template_clause items */
  items: TemplateClause[];

  /** Alternative property name for items (for API compatibility) */
  template_clauses?: TemplateClause[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * TemplateClause filter options for list queries
 *
 * Extends BaseEntityFilters with template_clause-specific filtering options.
 */
export interface TemplateClauseFilterOptions extends BaseEntityFilters {
  /** Filter by clause category for grouping and selection */
  category?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * TemplateClause dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface TemplateClauseDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: TemplateClause;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * TemplateClause summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface TemplateClauseSummary {
  /** Unique identifier */
  id: string;

  /** Clause name, e.g. Purpose, Duration, Data Protection */
  name: string;

  /** Description */
  description: string | null;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * TemplateClause form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type TemplateClauseFormData = TemplateClauseCreateRequest;

/**
 * Default form values for new template_clause
 *
 * Use this when initializing form state for creating a new template_clause.
 */
export const DEFAULT_TEMPLATE_CLAUSE_FORM_DATA: Partial<TemplateClauseFormData> = {
  is_active: true,
  default_variant: "A",
  applicable_document_types: "[]",
  sort_order: 0,
  is_required: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single template_clause API response
 */
export interface TemplateClauseResponse {
  /** The requested template_clause entity */
  data: TemplateClause;
}

/**
 * TemplateClause mutation response (create/update)
 */
export interface TemplateClauseMutationResponse {
  /** The created or updated template_clause */
  data: TemplateClause;

  /** Success message */
  message?: string;
}

/**
 * API error response for template_clause operations
 */
export interface TemplateClauseApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a TemplateClause
 *
 * @param value - Value to check
 * @returns True if value is a TemplateClause
 */
export function isTemplateClause(value: unknown): value is TemplateClause {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.name === 'string' &&
    typeof obj.category === 'string' &&
    typeof obj.variants === 'object'
  );
}

/**
 * Type guard to check if a value is a TemplateClauseDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a TemplateClauseDropdownOption
 */
export function isTemplateClauseDropdownOption(
  value: unknown
): value is TemplateClauseDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isTemplateClause(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All template_clause types re-exported for convenience */
export type {
  TemplateClause as TemplateClauseEntity,
  TemplateClauseCreateRequest as CreateTemplateClauseInput,
  TemplateClauseUpdateRequest as UpdateTemplateClauseInput,
};
