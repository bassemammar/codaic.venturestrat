/**
 * LegalEntity Type Definitions
 *
 * This file contains type definitions for the LegalEntity entity:
 * - LegalEntity: Full entity model with all fields
 * - LegalEntityCreateRequest: Data for creating a new legal_entity
 * - LegalEntityUpdateRequest: Partial data for updating a legal_entity
 * - LegalEntityListResponse: Paginated list response
 * - LegalEntityFilterOptions: Query parameters for filtering
 * - LegalEntityDropdownOption: Options for select components
 *
 * Legal entity (company or organization) that acts as party in legal documents
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:54.456453Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// LegalEntity Entity
// ============================================================================

/**
 * LegalEntity entity model
 *
 * Represents the full legal_entity entity as returned by the API.
 * Legal entity (company or organization) that acts as party in legal documents
 *
 * @remarks
 * This interface matches the backend LegalEntityDTO schema exactly.
 */
export interface LegalEntity {
  /** Unique identifier (UUID) */
  id: string;

  /** Auth user who created this entity */
  user_id: string;

  /** Registered legal name of the entity */
  legal_name: string;

  /** Legal jurisdiction of incorporation */
  jurisdiction: string;

  /** Company registration or incorporation number */
  registration_number: string;

  /** Incorporation Date */
  incorporation_date: string | null;

  /** Total authorized shares (null if unlimited or N/A) */
  authorized_shares: number | null;

  /** Par value per share */
  par_value: number | null;

  /** Registered office address */
  registered_address_id: string | null;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * LegalEntity creation request data
 *
 * Used when creating a new legal_entity. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend LegalEntityCreateDTO schema.
 */
export interface LegalEntityCreateRequest {
  /** Auth user who created this entity (required) */
  user_id: string;

  /** Registered legal name of the entity (required) */
  legal_name: string;

  /** Legal jurisdiction of incorporation (required) */
  jurisdiction: string;

  /** Company registration or incorporation number (required) */
  registration_number: string;

  /** Incorporation Date (optional) */
  incorporation_date?: string;

  /** Total authorized shares (null if unlimited or N/A) (optional) */
  authorized_shares?: number;

  /** Par value per share (optional) */
  par_value?: number;

  /** Registered office address (optional) */
  registered_address_id?: string;

  /** Active status (defaults to true for new legal_entities) */
  is_active?: boolean;
}

/**
 * LegalEntity update request data
 *
 * Used when updating an existing legal_entity. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend LegalEntityUpdateDTO schema.
 */
export interface LegalEntityUpdateRequest {
  /** Auth user who created this entity */
  user_id?: string;

  /** Registered legal name of the entity */
  legal_name?: string;

  /** Legal jurisdiction of incorporation */
  jurisdiction?: string;

  /** Company registration or incorporation number */
  registration_number?: string;

  /** Incorporation Date */
  incorporation_date?: string | null;

  /** Total authorized shares (null if unlimited or N/A) */
  authorized_shares?: number | null;

  /** Par value per share */
  par_value?: number | null;

  /** Registered office address */
  registered_address_id?: string | null;

  /** Active status */
  is_active?: boolean;
}

/**
 * LegalEntity list response
 *
 * Wrapper for paginated legal_entity list queries.
 */
export interface LegalEntityListResponse {
  /** Array of legal_entity items */
  items: LegalEntity[];

  /** Alternative property name for items (for API compatibility) */
  legal_entities?: LegalEntity[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * LegalEntity filter options for list queries
 *
 * Extends BaseEntityFilters with legal_entity-specific filtering options.
 */
export interface LegalEntityFilterOptions extends BaseEntityFilters {
  /** Filter by auth user who created this entity */
  user_id?: string;

  /** Filter by registered office address */
  registered_address_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * LegalEntity dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface LegalEntityDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: LegalEntity;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * LegalEntity summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface LegalEntitySummary {
  /** Unique identifier */
  id: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * LegalEntity form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type LegalEntityFormData = LegalEntityCreateRequest;

/**
 * Default form values for new legal_entity
 *
 * Use this when initializing form state for creating a new legal_entity.
 */
export const DEFAULT_LEGAL_ENTITY_FORM_DATA: Partial<LegalEntityFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single legal_entity API response
 */
export interface LegalEntityResponse {
  /** The requested legal_entity entity */
  data: LegalEntity;
}

/**
 * LegalEntity mutation response (create/update)
 */
export interface LegalEntityMutationResponse {
  /** The created or updated legal_entity */
  data: LegalEntity;

  /** Success message */
  message?: string;
}

/**
 * API error response for legal_entity operations
 */
export interface LegalEntityApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a LegalEntity
 *
 * @param value - Value to check
 * @returns True if value is a LegalEntity
 */
export function isLegalEntity(value: unknown): value is LegalEntity {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.user_id === 'string' &&
    typeof obj.legal_name === 'string' &&
    typeof obj.jurisdiction === 'string' &&
    typeof obj.registration_number === 'string'
  );
}

/**
 * Type guard to check if a value is a LegalEntityDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a LegalEntityDropdownOption
 */
export function isLegalEntityDropdownOption(
  value: unknown
): value is LegalEntityDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isLegalEntity(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All legal_entity types re-exported for convenience */
export type {
  LegalEntity as LegalEntityEntity,
  LegalEntityCreateRequest as CreateLegalEntityInput,
  LegalEntityUpdateRequest as UpdateLegalEntityInput,
};
