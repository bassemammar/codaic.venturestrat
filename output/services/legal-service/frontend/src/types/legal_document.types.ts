/**
 * LegalDocument Type Definitions
 *
 * This file contains type definitions for the LegalDocument entity:
 * - LegalDocument: Full entity model with all fields
 * - LegalDocumentCreateRequest: Data for creating a new legal_document
 * - LegalDocumentUpdateRequest: Partial data for updating a legal_document
 * - LegalDocumentListResponse: Paginated list response
 * - LegalDocumentFilterOptions: Query parameters for filtering
 * - LegalDocumentDropdownOption: Options for select components
 *
 * Generated legal document with full lifecycle tracking, template reference, and party links
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:53.976171Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// LegalDocument Entity
// ============================================================================

/**
 * LegalDocument entity model
 *
 * Represents the full legal_document entity as returned by the API.
 * Generated legal document with full lifecycle tracking, template reference, and party links
 *
 * @remarks
 * This interface matches the backend LegalDocumentDTO schema exactly.
 */
export interface LegalDocument {
  /** Unique identifier (UUID) */
  id: string;

  /** Auth user who created this document */
  user_id: string;

  /** Template used to generate this document */
  template_id: string | null;

  /** Cross-service reference to investor-service Investor */
  investor_id: string | null;

  /** Document Type */
  document_type: string;

  /** Title */
  title: string;

  /** Status */
  status: string;

  /** JSONB storing all form answers and clause variant selections */
  configuration: Record<string, any>;

  /** Generated markdown content */
  content_markdown: string | null;

  /** Rendered HTML for preview */
  content_html: string | null;

  /** File Path Docx */
  file_path_docx: string | null;

  /** File Path Pdf */
  file_path_pdf: string | null;

  /** Version */
  version: number;

  /** Generated At */
  generated_at: string | null;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * LegalDocument creation request data
 *
 * Used when creating a new legal_document. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend LegalDocumentCreateDTO schema.
 */
export interface LegalDocumentCreateRequest {
  /** Auth user who created this document (required) */
  user_id: string;

  /** Template used to generate this document (optional) */
  template_id?: string;

  /** Cross-service reference to investor-service Investor (optional) */
  investor_id?: string;

  /** Document Type (required) */
  document_type: string;

  /** Title (required) */
  title: string;

  /** Status (optional) */
  status?: string;

  /** JSONB storing all form answers and clause variant selections (optional) */
  configuration?: Record<string, any>;

  /** Generated markdown content (optional) */
  content_markdown?: string;

  /** Rendered HTML for preview (optional) */
  content_html?: string;

  /** File Path Docx (optional) */
  file_path_docx?: string;

  /** File Path Pdf (optional) */
  file_path_pdf?: string;

  /** Version (optional) */
  version?: number;

  /** Generated At (optional) */
  generated_at?: string;

  /** Active status (defaults to true for new legal_documents) */
  is_active?: boolean;
}

/**
 * LegalDocument update request data
 *
 * Used when updating an existing legal_document. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend LegalDocumentUpdateDTO schema.
 */
export interface LegalDocumentUpdateRequest {
  /** Auth user who created this document */
  user_id?: string;

  /** Template used to generate this document */
  template_id?: string | null;

  /** Cross-service reference to investor-service Investor */
  investor_id?: string | null;

  /** Document Type */
  document_type?: string;

  /** Title */
  title?: string;

  /** Status */
  status?: string;

  /** JSONB storing all form answers and clause variant selections */
  configuration?: Record<string, any>;

  /** Generated markdown content */
  content_markdown?: string | null;

  /** Rendered HTML for preview */
  content_html?: string | null;

  /** File Path Docx */
  file_path_docx?: string | null;

  /** File Path Pdf */
  file_path_pdf?: string | null;

  /** Version */
  version?: number;

  /** Generated At */
  generated_at?: string | null;

  /** Active status */
  is_active?: boolean;
}

/**
 * LegalDocument list response
 *
 * Wrapper for paginated legal_document list queries.
 */
export interface LegalDocumentListResponse {
  /** Array of legal_document items */
  items: LegalDocument[];

  /** Alternative property name for items (for API compatibility) */
  legal_documents?: LegalDocument[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * LegalDocument filter options for list queries
 *
 * Extends BaseEntityFilters with legal_document-specific filtering options.
 */
export interface LegalDocumentFilterOptions extends BaseEntityFilters {
  /** Filter by auth user who created this document */
  user_id?: string;

  /** Filter by template used to generate this document */
  template_id?: string;

  /** Filter by cross-service reference to investor-service investor */
  investor_id?: string;

  /** Filter by document type */
  document_type?: string;

  /** Filter by status */
  status?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * LegalDocument dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface LegalDocumentDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: LegalDocument;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * LegalDocument summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface LegalDocumentSummary {
  /** Unique identifier */
  id: string;

  /** Title */
  title: string;

  /** Status */
  status: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * LegalDocument form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type LegalDocumentFormData = LegalDocumentCreateRequest;

/**
 * Default form values for new legal_document
 *
 * Use this when initializing form state for creating a new legal_document.
 */
export const DEFAULT_LEGAL_DOCUMENT_FORM_DATA: Partial<LegalDocumentFormData> = {
  is_active: true,
  status: "draft",
  configuration: "{}",
  version: 1,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single legal_document API response
 */
export interface LegalDocumentResponse {
  /** The requested legal_document entity */
  data: LegalDocument;
}

/**
 * LegalDocument mutation response (create/update)
 */
export interface LegalDocumentMutationResponse {
  /** The created or updated legal_document */
  data: LegalDocument;

  /** Success message */
  message?: string;
}

/**
 * API error response for legal_document operations
 */
export interface LegalDocumentApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a LegalDocument
 *
 * @param value - Value to check
 * @returns True if value is a LegalDocument
 */
export function isLegalDocument(value: unknown): value is LegalDocument {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.user_id === 'string' &&
    typeof obj.document_type === 'string' &&
    typeof obj.title === 'string'
  );
}

/**
 * Type guard to check if a value is a LegalDocumentDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a LegalDocumentDropdownOption
 */
export function isLegalDocumentDropdownOption(
  value: unknown
): value is LegalDocumentDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isLegalDocument(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All legal_document types re-exported for convenience */
export type {
  LegalDocument as LegalDocumentEntity,
  LegalDocumentCreateRequest as CreateLegalDocumentInput,
  LegalDocumentUpdateRequest as UpdateLegalDocumentInput,
};
