/**
 * EquityGrant Type Definitions
 *
 * This file contains type definitions for the EquityGrant entity:
 * - EquityGrant: Full entity model with all fields
 * - EquityGrantCreateRequest: Data for creating a new equity_grant
 * - EquityGrantUpdateRequest: Partial data for updating a equity_grant
 * - EquityGrantListResponse: Paginated list response
 * - EquityGrantFilterOptions: Query parameters for filtering
 * - EquityGrantDropdownOption: Options for select components
 *
 * Cap table entry — equity holding in a legal entity with share class and valuation
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T20:43:52.319388Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// EquityGrant Entity
// ============================================================================

/**
 * EquityGrant entity model
 *
 * Represents the full equity_grant entity as returned by the API.
 * Cap table entry — equity holding in a legal entity with share class and valuation
 *
 * @remarks
 * This interface matches the backend EquityGrantDTO schema exactly.
 */
export interface EquityGrant {
  /** Unique identifier (UUID) */
  id: string;

  /** Company whose shares are held */
  legal_entity_id: string;

  /** Person holding equity (null if entity holder) */
  holder_person_id: string | null;

  /** Entity holding equity (null if person holder) */
  holder_entity_id: string | null;

  /** Polymorphic holder discriminator */
  holder_type: string;

  /** Share Class */
  share_class: string;

  /** Number of shares held (must be > 0) */
  number_of_shares: number;

  /** Ownership percentage (0.01 to 100.00) */
  percentage: number;

  /** Valuation at time of issue */
  valuation: number | null;

  /** Issue Date */
  issue_date: string;

  /** Reference to LegalDocument that created this grant */
  source_document_id: string | null;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * EquityGrant creation request data
 *
 * Used when creating a new equity_grant. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend EquityGrantCreateDTO schema.
 */
export interface EquityGrantCreateRequest {
  /** Company whose shares are held (required) */
  legal_entity_id: string;

  /** Person holding equity (null if entity holder) (optional) */
  holder_person_id?: string;

  /** Entity holding equity (null if person holder) (optional) */
  holder_entity_id?: string;

  /** Polymorphic holder discriminator (required) */
  holder_type: string;

  /** Share Class (required) */
  share_class: string;

  /** Number of shares held (must be > 0) (required) */
  number_of_shares: number;

  /** Ownership percentage (0.01 to 100.00) (required) */
  percentage: number;

  /** Valuation at time of issue (optional) */
  valuation?: number;

  /** Issue Date (required) */
  issue_date: string;

  /** Reference to LegalDocument that created this grant (optional) */
  source_document_id?: string;

  /** Active status (defaults to true for new equity_grants) */
  is_active?: boolean;
}

/**
 * EquityGrant update request data
 *
 * Used when updating an existing equity_grant. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend EquityGrantUpdateDTO schema.
 */
export interface EquityGrantUpdateRequest {
  /** Company whose shares are held */
  legal_entity_id?: string;

  /** Person holding equity (null if entity holder) */
  holder_person_id?: string | null;

  /** Entity holding equity (null if person holder) */
  holder_entity_id?: string | null;

  /** Polymorphic holder discriminator */
  holder_type?: string;

  /** Share Class */
  share_class?: string;

  /** Number of shares held (must be > 0) */
  number_of_shares?: number;

  /** Ownership percentage (0.01 to 100.00) */
  percentage?: number;

  /** Valuation at time of issue */
  valuation?: number | null;

  /** Issue Date */
  issue_date?: string;

  /** Reference to LegalDocument that created this grant */
  source_document_id?: string | null;

  /** Active status */
  is_active?: boolean;
}

/**
 * EquityGrant list response
 *
 * Wrapper for paginated equity_grant list queries.
 */
export interface EquityGrantListResponse {
  /** Array of equity_grant items */
  items: EquityGrant[];

  /** Alternative property name for items (for API compatibility) */
  equity_grants?: EquityGrant[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * EquityGrant filter options for list queries
 *
 * Extends BaseEntityFilters with equity_grant-specific filtering options.
 */
export interface EquityGrantFilterOptions extends BaseEntityFilters {
  /** Filter by company whose shares are held */
  legal_entity_id?: string;

  /** Filter by person holding equity (null if entity holder) */
  holder_person_id?: string;

  /** Filter by entity holding equity (null if person holder) */
  holder_entity_id?: string;

  /** Filter by polymorphic holder discriminator */
  holder_type?: string;

  /** Filter by reference to legaldocument that created this grant */
  source_document_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * EquityGrant dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface EquityGrantDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: EquityGrant;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * EquityGrant summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface EquityGrantSummary {
  /** Unique identifier */
  id: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * EquityGrant form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type EquityGrantFormData = EquityGrantCreateRequest;

/**
 * Default form values for new equity_grant
 *
 * Use this when initializing form state for creating a new equity_grant.
 */
export const DEFAULT_EQUITY_GRANT_FORM_DATA: Partial<EquityGrantFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single equity_grant API response
 */
export interface EquityGrantResponse {
  /** The requested equity_grant entity */
  data: EquityGrant;
}

/**
 * EquityGrant mutation response (create/update)
 */
export interface EquityGrantMutationResponse {
  /** The created or updated equity_grant */
  data: EquityGrant;

  /** Success message */
  message?: string;
}

/**
 * API error response for equity_grant operations
 */
export interface EquityGrantApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a EquityGrant
 *
 * @param value - Value to check
 * @returns True if value is a EquityGrant
 */
export function isEquityGrant(value: unknown): value is EquityGrant {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.legal_entity_id === 'string' &&
    typeof obj.holder_type === 'string' &&
    typeof obj.share_class === 'string' &&
    typeof obj.number_of_shares === 'number' &&
    typeof obj.percentage === 'number' &&
    typeof obj.issue_date === 'string'
  );
}

/**
 * Type guard to check if a value is a EquityGrantDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a EquityGrantDropdownOption
 */
export function isEquityGrantDropdownOption(
  value: unknown
): value is EquityGrantDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isEquityGrant(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All equity_grant types re-exported for convenience */
export type {
  EquityGrant as EquityGrantEntity,
  EquityGrantCreateRequest as CreateEquityGrantInput,
  EquityGrantUpdateRequest as UpdateEquityGrantInput,
};
