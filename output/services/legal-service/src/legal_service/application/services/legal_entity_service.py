"""LegalEntity Application Service - Generated by Code Generator v1.0.0"""

from typing import List, Optional
from uuid import UUID
import structlog

from legal_service.infrastructure.repositories.legal_entity_repository import LegalEntityRepository
from legal_service.infrastructure.orm.legal_entity import LegalEntity
from legal_service.schemas.legal_entity import (
    LegalEntityCreate,
    LegalEntityUpdate,
    LegalEntityResponse,
)
from legal_service.integrations.events import event_publisher
from legal_service.integrations.metrics import entity_operations_total

logger = structlog.get_logger(__name__)


class LegalEntityService:
    """Application service for LegalEntity use cases."""

    def __init__(self, session=None):
        """Initialize service with repository."""
        self.repository = LegalEntityRepository(session)

    async def create_legal_entity(
        self, data: LegalEntityCreate, tenant_id: str = None
    ) -> LegalEntityResponse:
        """Create new LegalEntity."""
        try:
            data_dict = data.model_dump()
            if tenant_id:
                data_dict["tenant_id"] = tenant_id

            # BaseModel.create() returns the created instance directly
            created = await self.repository.create(**data_dict)

            # Publish event
            await event_publisher.publish(
                entity_name="legal_entity",
                action="created",
                data=created.to_dict(),
                tenant_id=tenant_id,
            )

            # Track metric
            entity_operations_total.labels(
                entity="legal_entity", operation="create", status="success"
            ).inc()

            # Convert BaseModel to dict for response
            return LegalEntityResponse(**created.to_dict())

        except Exception as e:
            entity_operations_total.labels(
                entity="legal_entity", operation="create", status="error"
            ).inc()
            logger.error("create_legal_entity_failed", error=str(e))
            raise

    async def get_legal_entity(self, id: UUID) -> Optional[LegalEntityResponse]:
        """Get LegalEntity by ID."""
        orm_obj = await self.repository.get_by_id(id)
        return LegalEntityResponse(**orm_obj.to_dict()) if orm_obj else None

    async def list_legal_entities(
        self, skip: int = 0, limit: int = 100
    ) -> List[LegalEntityResponse]:
        """List LegalEntities with pagination."""
        orm_objs = await self.repository.get_all(skip, limit)
        return [LegalEntityResponse(**obj.to_dict()) for obj in orm_objs]

    async def update_legal_entity(
        self, id: UUID, data: LegalEntityUpdate
    ) -> Optional[LegalEntityResponse]:
        """Update LegalEntity."""
        try:
            orm_obj = await self.repository.get_by_id(id)
            if not orm_obj:
                return None

            # Update fields from DTO
            for key, value in data.model_dump(exclude_unset=True).items():
                setattr(orm_obj, key, value)

            # BaseModel.save() returns self
            updated = await self.repository.update(orm_obj)

            # Publish event
            await event_publisher.publish(
                entity_name="legal_entity",
                action="updated",
                data=updated.to_dict(),
                tenant_id=getattr(updated, "tenant_id", None),
            )

            # Track metric
            entity_operations_total.labels(
                entity="legal_entity", operation="update", status="success"
            ).inc()

            return LegalEntityResponse(**updated.to_dict())

        except Exception as e:
            entity_operations_total.labels(
                entity="legal_entity", operation="update", status="error"
            ).inc()
            logger.error("update_legal_entity_failed", id=str(id), error=str(e))
            raise

    async def delete_legal_entity(self, id: UUID) -> bool:
        """Delete LegalEntity."""
        try:
            # Get entity before deletion
            orm_obj = await self.repository.get_by_id(id)
            if not orm_obj:
                return False

            entity_data = orm_obj.to_dict()

            # Delete
            deleted = await self.repository.delete(id)

            if deleted:
                # Publish event
                await event_publisher.publish(
                    entity_name="legal_entity",
                    action="deleted",
                    data=entity_data,
                    tenant_id=entity_data.get("tenant_id"),
                )

                # Track metric
                entity_operations_total.labels(
                    entity="legal_entity", operation="delete", status="success"
                ).inc()

            return deleted

        except Exception as e:
            entity_operations_total.labels(
                entity="legal_entity", operation="delete", status="error"
            ).inc()
            logger.error("delete_legal_entity_failed", id=str(id), error=str(e))
            raise

    async def search_legal_entities(
        self, domain: list = None, skip: int = 0, limit: int = 100
    ) -> List[LegalEntityResponse]:
        """
        Search LegalEntities with domain filtering.

        Args:
            domain: domain filter (e.g., [('field', '=', 'value')])
            skip: Offset for pagination
            limit: Maximum records to return

        Returns:
            List of matching LegalEntity instances
        """
        orm_objs = await self.repository.search(domain, skip, limit)
        return [LegalEntityResponse(**obj.to_dict()) for obj in orm_objs]

    async def count_legal_entities(self, domain: list = None) -> int:
        """
        Count LegalEntities matching domain.

        Args:
            domain: Optional domain filter

        Returns:
            Number of matching records
        """
        return await self.repository.count(domain)

    async def bulk_create_legal_entities(
        self, records_data: List[LegalEntityCreate]
    ) -> List[LegalEntityResponse]:
        """
        Create multiple LegalEntities efficiently.

        Args:
            records_data: List of create DTOs

        Returns:
            List of created LegalEntity instances
        """
        # Convert DTOs to dicts
        data_dicts = [record.model_dump() for record in records_data]

        # Bulk create
        created_objs = await self.repository.bulk_create(data_dicts)

        return [LegalEntityResponse(**obj.to_dict()) for obj in created_objs]
