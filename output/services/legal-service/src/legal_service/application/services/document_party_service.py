"""DocumentParty Application Service - Generated by Code Generator v1.0.0"""

from typing import List, Optional
from uuid import UUID
import structlog

from legal_service.infrastructure.repositories.document_party_repository import (
    DocumentPartyRepository,
)
from legal_service.infrastructure.orm.document_party import DocumentParty
from legal_service.schemas.document_party import (
    DocumentPartyCreate,
    DocumentPartyUpdate,
    DocumentPartyResponse,
)
from legal_service.integrations.events import event_publisher
from legal_service.integrations.metrics import entity_operations_total

logger = structlog.get_logger(__name__)


class DocumentPartyService:
    """Application service for DocumentParty use cases."""

    def __init__(self, session=None):
        """Initialize service with repository."""
        self.repository = DocumentPartyRepository(session)

    async def create_document_party(
        self, data: DocumentPartyCreate, tenant_id: str = None
    ) -> DocumentPartyResponse:
        """Create new DocumentParty."""
        try:
            data_dict = data.model_dump()
            if tenant_id:
                data_dict["tenant_id"] = tenant_id

            # BaseModel.create() returns the created instance directly
            created = await self.repository.create(**data_dict)

            # Publish event
            await event_publisher.publish(
                entity_name="document_party",
                action="created",
                data=created.to_dict(),
                tenant_id=tenant_id,
            )

            # Track metric
            entity_operations_total.labels(
                entity="document_party", operation="create", status="success"
            ).inc()

            # Convert BaseModel to dict for response
            return DocumentPartyResponse(**created.to_dict())

        except Exception as e:
            entity_operations_total.labels(
                entity="document_party", operation="create", status="error"
            ).inc()
            logger.error("create_document_party_failed", error=str(e))
            raise

    async def get_document_party(self, id: UUID) -> Optional[DocumentPartyResponse]:
        """Get DocumentParty by ID."""
        orm_obj = await self.repository.get_by_id(id)
        return DocumentPartyResponse(**orm_obj.to_dict()) if orm_obj else None

    async def list_document_parties(
        self, skip: int = 0, limit: int = 100
    ) -> List[DocumentPartyResponse]:
        """List DocumentParties with pagination."""
        orm_objs = await self.repository.get_all(skip, limit)
        return [DocumentPartyResponse(**obj.to_dict()) for obj in orm_objs]

    async def update_document_party(
        self, id: UUID, data: DocumentPartyUpdate
    ) -> Optional[DocumentPartyResponse]:
        """Update DocumentParty."""
        try:
            orm_obj = await self.repository.get_by_id(id)
            if not orm_obj:
                return None

            # Update fields from DTO
            for key, value in data.model_dump(exclude_unset=True).items():
                setattr(orm_obj, key, value)

            # BaseModel.save() returns self
            updated = await self.repository.update(orm_obj)

            # Publish event
            await event_publisher.publish(
                entity_name="document_party",
                action="updated",
                data=updated.to_dict(),
                tenant_id=getattr(updated, "tenant_id", None),
            )

            # Track metric
            entity_operations_total.labels(
                entity="document_party", operation="update", status="success"
            ).inc()

            return DocumentPartyResponse(**updated.to_dict())

        except Exception as e:
            entity_operations_total.labels(
                entity="document_party", operation="update", status="error"
            ).inc()
            logger.error("update_document_party_failed", id=str(id), error=str(e))
            raise

    async def delete_document_party(self, id: UUID) -> bool:
        """Delete DocumentParty."""
        try:
            # Get entity before deletion
            orm_obj = await self.repository.get_by_id(id)
            if not orm_obj:
                return False

            entity_data = orm_obj.to_dict()

            # Delete
            deleted = await self.repository.delete(id)

            if deleted:
                # Publish event
                await event_publisher.publish(
                    entity_name="document_party",
                    action="deleted",
                    data=entity_data,
                    tenant_id=entity_data.get("tenant_id"),
                )

                # Track metric
                entity_operations_total.labels(
                    entity="document_party", operation="delete", status="success"
                ).inc()

            return deleted

        except Exception as e:
            entity_operations_total.labels(
                entity="document_party", operation="delete", status="error"
            ).inc()
            logger.error("delete_document_party_failed", id=str(id), error=str(e))
            raise

    async def search_document_parties(
        self, domain: list = None, skip: int = 0, limit: int = 100
    ) -> List[DocumentPartyResponse]:
        """
        Search DocumentParties with domain filtering.

        Args:
            domain: domain filter (e.g., [('field', '=', 'value')])
            skip: Offset for pagination
            limit: Maximum records to return

        Returns:
            List of matching DocumentParty instances
        """
        orm_objs = await self.repository.search(domain, skip, limit)
        return [DocumentPartyResponse(**obj.to_dict()) for obj in orm_objs]

    async def count_document_parties(self, domain: list = None) -> int:
        """
        Count DocumentParties matching domain.

        Args:
            domain: Optional domain filter

        Returns:
            Number of matching records
        """
        return await self.repository.count(domain)

    async def bulk_create_document_parties(
        self, records_data: List[DocumentPartyCreate]
    ) -> List[DocumentPartyResponse]:
        """
        Create multiple DocumentParties efficiently.

        Args:
            records_data: List of create DTOs

        Returns:
            List of created DocumentParty instances
        """
        # Convert DTOs to dicts
        data_dicts = [record.model_dump() for record in records_data]

        # Bulk create
        created_objs = await self.repository.bulk_create(data_dicts)

        return [DocumentPartyResponse(**obj.to_dict()) for obj in created_objs]
