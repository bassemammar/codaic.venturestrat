"""InvestorMarket Application Service - Generated by Code Generator v1.0.0"""

from typing import List, Optional
from uuid import UUID
import structlog

from investor_service.infrastructure.repositories.investor_market_repository import (
    InvestorMarketRepository,
)
from investor_service.infrastructure.orm.investor_market import InvestorMarket
from investor_service.schemas.investor_market import (
    InvestorMarketCreate,
    InvestorMarketUpdate,
    InvestorMarketResponse,
)
from investor_service.integrations.events import event_publisher
from investor_service.integrations.metrics import entity_operations_total

logger = structlog.get_logger(__name__)


class InvestorMarketService:
    """Application service for InvestorMarket use cases."""

    def __init__(self, session=None):
        """Initialize service with repository."""
        self.repository = InvestorMarketRepository(session)

    async def create_investor_market(
        self, data: InvestorMarketCreate, tenant_id: str = None
    ) -> InvestorMarketResponse:
        """Create new InvestorMarket."""
        try:
            data_dict = data.model_dump()
            if tenant_id:
                data_dict["tenant_id"] = tenant_id

            # BaseModel.create() returns the created instance directly
            created = await self.repository.create(**data_dict)

            # Publish event
            await event_publisher.publish(
                entity_name="investor_market",
                action="created",
                data=created.to_dict(),
                tenant_id=tenant_id,
            )

            # Track metric
            entity_operations_total.labels(
                entity="investor_market", operation="create", status="success"
            ).inc()

            # Convert BaseModel to dict for response
            return InvestorMarketResponse(**created.to_dict())

        except Exception as e:
            entity_operations_total.labels(
                entity="investor_market", operation="create", status="error"
            ).inc()
            logger.error("create_investor_market_failed", error=str(e))
            raise

    async def get_investor_market(self, id: UUID) -> Optional[InvestorMarketResponse]:
        """Get InvestorMarket by ID."""
        orm_obj = await self.repository.get_by_id(id)
        return InvestorMarketResponse(**orm_obj.to_dict()) if orm_obj else None

    async def list_investor_markets(
        self, skip: int = 0, limit: int = 100
    ) -> List[InvestorMarketResponse]:
        """List InvestorMarkets with pagination."""
        orm_objs = await self.repository.get_all(skip, limit)
        return [InvestorMarketResponse(**obj.to_dict()) for obj in orm_objs]

    async def update_investor_market(
        self, id: UUID, data: InvestorMarketUpdate
    ) -> Optional[InvestorMarketResponse]:
        """Update InvestorMarket."""
        try:
            orm_obj = await self.repository.get_by_id(id)
            if not orm_obj:
                return None

            # Update fields from DTO
            for key, value in data.model_dump(exclude_unset=True).items():
                setattr(orm_obj, key, value)

            # BaseModel.save() returns self
            updated = await self.repository.update(orm_obj)

            # Publish event
            await event_publisher.publish(
                entity_name="investor_market",
                action="updated",
                data=updated.to_dict(),
                tenant_id=getattr(updated, "tenant_id", None),
            )

            # Track metric
            entity_operations_total.labels(
                entity="investor_market", operation="update", status="success"
            ).inc()

            return InvestorMarketResponse(**updated.to_dict())

        except Exception as e:
            entity_operations_total.labels(
                entity="investor_market", operation="update", status="error"
            ).inc()
            logger.error("update_investor_market_failed", id=str(id), error=str(e))
            raise

    async def delete_investor_market(self, id: UUID) -> bool:
        """Delete InvestorMarket."""
        try:
            # Get entity before deletion
            orm_obj = await self.repository.get_by_id(id)
            if not orm_obj:
                return False

            entity_data = orm_obj.to_dict()

            # Delete
            deleted = await self.repository.delete(id)

            if deleted:
                # Publish event
                await event_publisher.publish(
                    entity_name="investor_market",
                    action="deleted",
                    data=entity_data,
                    tenant_id=entity_data.get("tenant_id"),
                )

                # Track metric
                entity_operations_total.labels(
                    entity="investor_market", operation="delete", status="success"
                ).inc()

            return deleted

        except Exception as e:
            entity_operations_total.labels(
                entity="investor_market", operation="delete", status="error"
            ).inc()
            logger.error("delete_investor_market_failed", id=str(id), error=str(e))
            raise

    async def search_investor_markets(
        self, domain: list = None, skip: int = 0, limit: int = 100
    ) -> List[InvestorMarketResponse]:
        """
        Search InvestorMarkets with domain filtering.

        Args:
            domain: domain filter (e.g., [('field', '=', 'value')])
            skip: Offset for pagination
            limit: Maximum records to return

        Returns:
            List of matching InvestorMarket instances
        """
        orm_objs = await self.repository.search(domain, skip, limit)
        return [InvestorMarketResponse(**obj.to_dict()) for obj in orm_objs]

    async def count_investor_markets(self, domain: list = None) -> int:
        """
        Count InvestorMarkets matching domain.

        Args:
            domain: Optional domain filter

        Returns:
            Number of matching records
        """
        return await self.repository.count(domain)

    async def bulk_create_investor_markets(
        self, records_data: List[InvestorMarketCreate]
    ) -> List[InvestorMarketResponse]:
        """
        Create multiple InvestorMarkets efficiently.

        Args:
            records_data: List of create DTOs

        Returns:
            List of created InvestorMarket instances
        """
        # Convert DTOs to dicts
        data_dicts = [record.model_dump() for record in records_data]

        # Bulk create
        created_objs = await self.repository.bulk_create(data_dicts)

        return [InvestorMarketResponse(**obj.to_dict()) for obj in created_objs]
