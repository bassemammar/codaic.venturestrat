"""InvestorEmail Application Service - Generated by Code Generator v1.0.0"""

from typing import List, Optional
from uuid import UUID
import structlog

from investor_service.infrastructure.repositories.investor_email_repository import (
    InvestorEmailRepository,
)
from investor_service.infrastructure.orm.investor_email import InvestorEmail
from investor_service.schemas.investor_email import (
    InvestorEmailCreate,
    InvestorEmailUpdate,
    InvestorEmailResponse,
)
from investor_service.integrations.events import event_publisher
from investor_service.integrations.metrics import entity_operations_total

logger = structlog.get_logger(__name__)


class InvestorEmailService:
    """Application service for InvestorEmail use cases."""

    def __init__(self, session=None):
        """Initialize service with repository."""
        self.repository = InvestorEmailRepository(session)

    async def create_investor_email(
        self, data: InvestorEmailCreate, tenant_id: str = None
    ) -> InvestorEmailResponse:
        """Create new InvestorEmail."""
        try:
            data_dict = data.model_dump()
            if tenant_id:
                data_dict["tenant_id"] = tenant_id

            # BaseModel.create() returns the created instance directly
            created = await self.repository.create(**data_dict)

            # Publish event
            await event_publisher.publish(
                entity_name="investor_email",
                action="created",
                data=created.to_dict(),
                tenant_id=tenant_id,
            )

            # Track metric
            entity_operations_total.labels(
                entity="investor_email", operation="create", status="success"
            ).inc()

            # Convert BaseModel to dict for response
            return InvestorEmailResponse(**created.to_dict())

        except Exception as e:
            entity_operations_total.labels(
                entity="investor_email", operation="create", status="error"
            ).inc()
            logger.error("create_investor_email_failed", error=str(e))
            raise

    async def get_investor_email(self, id: UUID) -> Optional[InvestorEmailResponse]:
        """Get InvestorEmail by ID."""
        orm_obj = await self.repository.get_by_id(id)
        return InvestorEmailResponse(**orm_obj.to_dict()) if orm_obj else None

    async def list_investor_emails(
        self, skip: int = 0, limit: int = 100
    ) -> List[InvestorEmailResponse]:
        """List InvestorEmails with pagination."""
        orm_objs = await self.repository.get_all(skip, limit)
        return [InvestorEmailResponse(**obj.to_dict()) for obj in orm_objs]

    async def update_investor_email(
        self, id: UUID, data: InvestorEmailUpdate
    ) -> Optional[InvestorEmailResponse]:
        """Update InvestorEmail."""
        try:
            orm_obj = await self.repository.get_by_id(id)
            if not orm_obj:
                return None

            # Update fields from DTO
            for key, value in data.model_dump(exclude_unset=True).items():
                setattr(orm_obj, key, value)

            # BaseModel.save() returns self
            updated = await self.repository.update(orm_obj)

            # Publish event
            await event_publisher.publish(
                entity_name="investor_email",
                action="updated",
                data=updated.to_dict(),
                tenant_id=getattr(updated, "tenant_id", None),
            )

            # Track metric
            entity_operations_total.labels(
                entity="investor_email", operation="update", status="success"
            ).inc()

            return InvestorEmailResponse(**updated.to_dict())

        except Exception as e:
            entity_operations_total.labels(
                entity="investor_email", operation="update", status="error"
            ).inc()
            logger.error("update_investor_email_failed", id=str(id), error=str(e))
            raise

    async def delete_investor_email(self, id: UUID) -> bool:
        """Delete InvestorEmail."""
        try:
            # Get entity before deletion
            orm_obj = await self.repository.get_by_id(id)
            if not orm_obj:
                return False

            entity_data = orm_obj.to_dict()

            # Delete
            deleted = await self.repository.delete(id)

            if deleted:
                # Publish event
                await event_publisher.publish(
                    entity_name="investor_email",
                    action="deleted",
                    data=entity_data,
                    tenant_id=entity_data.get("tenant_id"),
                )

                # Track metric
                entity_operations_total.labels(
                    entity="investor_email", operation="delete", status="success"
                ).inc()

            return deleted

        except Exception as e:
            entity_operations_total.labels(
                entity="investor_email", operation="delete", status="error"
            ).inc()
            logger.error("delete_investor_email_failed", id=str(id), error=str(e))
            raise

    async def search_investor_emails(
        self, domain: list = None, skip: int = 0, limit: int = 100
    ) -> List[InvestorEmailResponse]:
        """
        Search InvestorEmails with domain filtering.

        Args:
            domain: domain filter (e.g., [('field', '=', 'value')])
            skip: Offset for pagination
            limit: Maximum records to return

        Returns:
            List of matching InvestorEmail instances
        """
        orm_objs = await self.repository.search(domain, skip, limit)
        return [InvestorEmailResponse(**obj.to_dict()) for obj in orm_objs]

    async def count_investor_emails(self, domain: list = None) -> int:
        """
        Count InvestorEmails matching domain.

        Args:
            domain: Optional domain filter

        Returns:
            Number of matching records
        """
        return await self.repository.count(domain)

    async def bulk_create_investor_emails(
        self, records_data: List[InvestorEmailCreate]
    ) -> List[InvestorEmailResponse]:
        """
        Create multiple InvestorEmails efficiently.

        Args:
            records_data: List of create DTOs

        Returns:
            List of created InvestorEmail instances
        """
        # Convert DTOs to dicts
        data_dicts = [record.model_dump() for record in records_data]

        # Bulk create
        created_objs = await self.repository.bulk_create(data_dicts)

        return [InvestorEmailResponse(**obj.to_dict()) for obj in created_objs]
