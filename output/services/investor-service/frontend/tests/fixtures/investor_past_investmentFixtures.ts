/**
 * InvestorPastInvestment Test Fixtures
 *
 * Test data fixtures for InvestorPastInvestment E2E and unit tests.
 * Provides predefined test data and factory functions for generating
 * test investor_past_investments.
 *
 * Fixture patterns:
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields with realistic test values
 * - invalid: Invalid data for validation testing
 * - unique: Unique values using timestamps for test isolation
 *
 * Many-to-many junction between investors and their past investments
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:11.503434Z
 */

import type { InvestorPastInvestmentCreateRequest } from '../../types/investor_past_investment.types';

// ============================================================================
// Types
// ============================================================================

/**
 * Test data pattern types
 *
 * @description
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields populated with realistic test data
 * - invalid: Data that should fail validation
 * - unique: Data with timestamps for test isolation
 */
export type TestDataPattern = 'minimal' | 'complete' | 'invalid' | 'unique';

/**
 * InvestorPastInvestment test fixture containing all test data patterns
 */
export interface InvestorPastInvestmentTestFixtures {
  /** Minimal valid investor_past_investment with only required fields */
  minimal: InvestorPastInvestmentCreateRequest;
  /** Complete investor_past_investment with all fields populated */
  complete: InvestorPastInvestmentCreateRequest;
  /** Invalid investor_past_investment for testing validation errors */
  invalid: Partial<InvestorPastInvestmentCreateRequest>;
  /** Unique investor_past_investment with timestamp-based values */
  unique: InvestorPastInvestmentCreateRequest;
  /** Multiple investor_past_investments for list testing */
  list: InvestorPastInvestmentCreateRequest[];
}

// ============================================================================
// Helper Functions
// ============================================================================

/**
 * Generate a unique identifier based on current timestamp
 * Useful for creating unique test data that won't conflict
 *
 * @param prefix - Optional prefix for the identifier
 * @returns Unique string identifier
 */
function generateUniqueId(prefix = ''): string {
  const timestamp = Date.now();
  const random = Math.random().toString(36).substring(2, 8);
  return prefix ? `${prefix}-${timestamp}-${random}` : `${timestamp}-${random}`;
}

/**
 * Generate a random string of specified length
 *
 * @param length - Length of the string to generate
 * @param charset - Character set to use (default: alphanumeric)
 * @returns Random string
 */
function randomString(
  length: number,
  charset = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'
): string {
  let result = '';
  for (let i = 0; i < length; i++) {
    result += charset.charAt(Math.floor(Math.random() * charset.length));
  }
  return result;
}

/**
 * Generate a random integer between min and max (inclusive)
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @returns Random integer
 */
function randomInt(min: number, max: number): number {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

/**
 * Generate a random decimal with specified precision
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @param precision - Number of decimal places
 * @returns Random decimal number
 */
function randomDecimal(min: number, max: number, precision = 2): number {
  const value = Math.random() * (max - min) + min;
  const factor = Math.pow(10, precision);
  return Math.round(value * factor) / factor;
}

/**
 * Generate a random date within a range
 *
 * @param startDate - Start of the range
 * @param endDate - End of the range
 * @returns Random date as ISO string
 */
function randomDate(startDate: Date, endDate: Date): string {
  const start = startDate.getTime();
  const end = endDate.getTime();
  const randomTime = start + Math.random() * (end - start);
  return new Date(randomTime).toISOString().split('T')[0];
}

/**
 * Pick a random element from an array
 *
 * @param array - Array to pick from
 * @returns Random element
 */
function randomPick<T>(array: T[]): T {
  return array[Math.floor(Math.random() * array.length)];
}

// ============================================================================
// Static Test Data
// ============================================================================


/**
 * Sample investor_past_investment names for generating realistic test data
 */
const SAMPLE_NAMES = [
  'Test InvestorPastInvestment Alpha',
  'Test InvestorPastInvestment Beta',
  'Test InvestorPastInvestment Gamma',
  'Sample InvestorPastInvestment One',
  'Sample InvestorPastInvestment Two',
  'Demo InvestorPastInvestment',
  'Example InvestorPastInvestment',
];

/**
 * Sample codes for generating realistic test data
 */
const SAMPLE_CODES = [
  'TST001',
  'TST002',
  'TST003',
  'DEMO01',
  'DEMO02',
  'SMPL01',
  'SMPL02',
];

/**
 * Sample descriptions for generating realistic test data
 */
const SAMPLE_DESCRIPTIONS = [
  'This is a test investor_past_investment for automated testing.',
  'Sample investor_past_investment created for E2E tests.',
  'Demo investor_past_investment with all fields populated.',
  'Example investor_past_investment for validation testing.',
  'Test data fixture for investor_past_investment operations.',
];

// ============================================================================
// Predefined Fixtures
// ============================================================================

/**
 * Minimal valid investor_past_investment fixture
 *
 * Contains only required fields with minimal valid values.
 * Use this when testing that minimal data is accepted.
 */
export const minimalInvestorPastInvestmentFixture: InvestorPastInvestmentCreateRequest = {
};

/**
 * Complete investor_past_investment fixture
 *
 * Contains all fields with realistic test values.
 * Use this when testing full CRUD operations.
 */
export const completeInvestorPastInvestmentFixture: InvestorPastInvestmentCreateRequest = {
  investor_id: 'Test Investor Id',
  past_investment_id: 'Test Past Investment Id',
};

/**
 * Unique investor_past_investment fixture
 *
 * Contains all fields with timestamp-based unique values.
 * Use this when testing to ensure data isolation between test runs.
 * Each field that needs uniqueness includes a timestamp/random component.
 */
export const uniqueInvestorPastInvestmentFixture: InvestorPastInvestmentCreateRequest = {
  investor_id: `Investor Id ${generateUniqueId()}`,
  past_investment_id: `Past Investment Id ${generateUniqueId()}`,
};

/**
 * Invalid investor_past_investment fixture
 *
 * Contains invalid data that should fail validation.
 * Use this when testing validation error handling.
 * Covers: empty required fields, values out of range, invalid formats.
 */
export const invalidInvestorPastInvestmentFixture: Partial<InvestorPastInvestmentCreateRequest> = {
};

/**
 * Invalid data patterns for comprehensive validation testing
 *
 * Contains various invalid data scenarios:
 * - emptyRequired: All required fields left empty
 * - tooShort: String fields below minimum length
 * - tooLong: String fields exceeding maximum length
 * - outOfRangeMin: Numeric fields below minimum value
 * - outOfRangeMax: Numeric fields above maximum value
 * - wrongType: Fields with incorrect data types (as strings for type coercion testing)
 * - specialChars: Fields with potentially problematic characters
 */
export const investorPastInvestmentInvalidPatterns = {
  /** All required string fields empty */
  emptyRequired: {
  } as Partial<InvestorPastInvestmentCreateRequest>,





  /** Fields with SQL injection attempts (for security testing) */
  sqlInjection: {
  } as Partial<InvestorPastInvestmentCreateRequest>,

  /** Fields with XSS attempts (for security testing) */
  xssAttempts: {
  } as Partial<InvestorPastInvestmentCreateRequest>,

  /** Fields with null values (for nullable field testing) */
  nullValues: {
  } as Partial<InvestorPastInvestmentCreateRequest>,

  /** Fields with undefined values (for missing field testing) */
  undefinedValues: {
    // Intentionally empty - represents missing fields
  } as Partial<InvestorPastInvestmentCreateRequest>,
};

/**
 * Collection of investor_past_investment fixtures for list testing
 *
 * Contains multiple investor_past_investments for testing list operations,
 * pagination, filtering, and sorting.
 */
export const investorPastInvestmentListFixtures: InvestorPastInvestmentCreateRequest[] = [
  {
    investor_id: 'Investor Id 1',
    past_investment_id: 'Past Investment Id 1',
  },
  {
    investor_id: 'Investor Id 2',
    past_investment_id: 'Past Investment Id 2',
  },
  {
    investor_id: 'Investor Id 3',
    past_investment_id: 'Past Investment Id 3',
  },
  {
    investor_id: 'Investor Id 4',
    past_investment_id: 'Past Investment Id 4',
  },
  {
    investor_id: 'Investor Id 5',
    past_investment_id: 'Past Investment Id 5',
  },
];

// ============================================================================
// Fixture Collection
// ============================================================================

/**
 * All investor_past_investment test fixtures
 *
 * @example
 * ```typescript
 * import { investorPastInvestmentFixtures } from '../fixtures/investor_past_investmentFixtures';
 *
 * // Use minimal fixture
 * const minData = investorPastInvestmentFixtures.minimal;
 *
 * // Use complete fixture
 * const fullData = investorPastInvestmentFixtures.complete;
 *
 * // Use unique fixture (with timestamp)
 * const uniqueData = generateInvestorPastInvestmentTestData('unique');
 * ```
 */
export const investorPastInvestmentFixtures: InvestorPastInvestmentTestFixtures = {
  minimal: minimalInvestorPastInvestmentFixture,
  complete: completeInvestorPastInvestmentFixture,
  invalid: invalidInvestorPastInvestmentFixture as InvestorPastInvestmentCreateRequest,
  unique: uniqueInvestorPastInvestmentFixture,
  list: investorPastInvestmentListFixtures,
};

// ============================================================================
// Factory Functions
// ============================================================================

/**
 * Generate investor_past_investment test data based on pattern
 *
 * @param pattern - The test data pattern to generate
 * @param overrides - Optional field overrides
 * @returns Generated test data
 *
 * @example
 * ```typescript
 * // Generate complete test data
 * const data = generateInvestorPastInvestmentTestData('complete');
 *
 * // Generate unique test data with overrides
 * const customData = generateInvestorPastInvestmentTestData('unique', {
 *   is_active: false,
 * });
 * ```
 */
export function generateInvestorPastInvestmentTestData(
  pattern: TestDataPattern = 'complete',
  overrides: Partial<InvestorPastInvestmentCreateRequest> = {}
): InvestorPastInvestmentCreateRequest {
  let baseData: InvestorPastInvestmentCreateRequest;

  switch (pattern) {
    case 'minimal':
      baseData = { ...minimalInvestorPastInvestmentFixture };
      break;

    case 'complete':
      baseData = { ...completeInvestorPastInvestmentFixture };
      break;

    case 'invalid':
      // Return invalid data (caller should handle validation failure)
      baseData = { ...invalidInvestorPastInvestmentFixture } as InvestorPastInvestmentCreateRequest;
      break;

    case 'unique':
      // Generate fresh unique values for each call
      baseData = {
        ...uniqueInvestorPastInvestmentFixture,
      };
      break;

    default:
      baseData = { ...completeInvestorPastInvestmentFixture };
  }

  return { ...baseData, ...overrides };
}

/**
 * Generate multiple investor_past_investment fixtures
 *
 * @param count - Number of fixtures to generate
 * @param pattern - The test data pattern to use as base
 * @param modifier - Optional function to modify each fixture
 * @returns Array of generated test data
 *
 * @example
 * ```typescript
 * // Generate 5 unique fixtures
 * const fixtures = generateMultipleInvestorPastInvestmentFixtures(5, 'unique');
 *
 * // Generate fixtures with sequential numbering
 * const numbered = generateMultipleInvestorPastInvestmentFixtures(3, 'complete', (data, index) => ({
 *   ...data,
 *   name: `InvestorPastInvestment #${index + 1}`,
 * }));
 * ```
 */
export function generateMultipleInvestorPastInvestmentFixtures(
  count: number,
  pattern: TestDataPattern = 'unique',
  modifier?: (data: InvestorPastInvestmentCreateRequest, index: number) => InvestorPastInvestmentCreateRequest
): InvestorPastInvestmentCreateRequest[] {
  const fixtures: InvestorPastInvestmentCreateRequest[] = [];

  for (let i = 0; i < count; i++) {
    let fixture = generateInvestorPastInvestmentTestData(pattern);

    if (modifier) {
      fixture = modifier(fixture, i);
    } else if (pattern === 'unique') {
      // Add index to ensure uniqueness even with same timestamp
    }

    fixtures.push(fixture);
  }

  return fixtures;
}

/**
 * Generate random investor_past_investment test data
 *
 * Creates test data with randomized values within valid ranges.
 * Useful for fuzz testing and edge case discovery.
 *
 * @param overrides - Optional field overrides
 * @returns Randomly generated test data
 *
 * @example
 * ```typescript
 * // Generate random test data
 * const randomData = generateRandomInvestorPastInvestmentData();
 *
 * // Generate random data with specific field
 * const customRandom = generateRandomInvestorPastInvestmentData({
 *   is_active: true,
 * });
 * ```
 */
export function generateRandomInvestorPastInvestmentData(
  overrides: Partial<InvestorPastInvestmentCreateRequest> = {}
): InvestorPastInvestmentCreateRequest {
  const data: InvestorPastInvestmentCreateRequest = {
    investor_id: randomString(randomInt(None, Math.min(None, 50))),
    past_investment_id: randomString(randomInt(None, Math.min(None, 50))),
  };

  return { ...data, ...overrides };
}

// ============================================================================
// Validation Test Data
// ============================================================================

/**
 * Edge case test data for boundary testing
 *
 * Contains values at the boundaries of valid ranges.
 */
export const investorPastInvestmentEdgeCases = {
};

/**
 * Special character test data for input sanitization testing
 */
export const investorPastInvestmentSpecialCharacterData: Partial<InvestorPastInvestmentCreateRequest> = {
};

/**
 * Whitespace test data for trimming validation
 */
export const investorPastInvestmentWhitespaceData: Partial<InvestorPastInvestmentCreateRequest> = {
};

// ============================================================================
// Exports
// ============================================================================

// Default export - main fixture collection
export default investorPastInvestmentFixtures;
