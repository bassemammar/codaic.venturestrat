/**
 * InvestorEmail Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all investor_email queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['investoremails'] - All investor_email queries
 * - ['investoremails', 'list'] - All list queries
 * - ['investoremails', 'list', { filters }] - Specific filtered list
 * - ['investoremails', 'detail', id] - Specific investor_email by ID
 *
 * @example
 * ```typescript
 * import { investor_emailKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: investor_emailKeys.list({ is_active: true }),
 *   queryFn: () => investor_emailService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all investor_email queries
 * queryClient.invalidateQueries({ queryKey: investor_emailKeys.all });
 *
 * // Invalidate specific investor_email
 * queryClient.invalidateQueries({ queryKey: investor_emailKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:10.311173Z
 */

import type { InvestorEmailFilterOptions } from '../types/investor_email.types';

/**
 * Root query key for InvestorEmail entity
 * Used as namespace prefix for all investor_email queries
 */
const INVESTOR_EMAIL_ROOT = 'investoremails' as const;

/**
 * InvestorEmail Query Keys
 *
 * Hierarchical query key factory for investor_email queries:
 * - all: Base key for all investor_email queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific investor_email by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = investor_emailKeys.list({ is_active: true });
 * // Result: ['investoremails', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = investor_emailKeys.detail('uuid-here');
 * // Result: ['investoremails', 'detail', 'uuid-here']
 *
 * // Invalidate all investor_email data
 * queryClient.invalidateQueries({ queryKey: investor_emailKeys.all });
 * ```
 */
export const investor_emailKeys = {
  /**
   * Base key for all investor_email queries
   * Use this to invalidate all investor_email-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: investor_emailKeys.all });
   * ```
   */
  all: [INVESTOR_EMAIL_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: investor_emailKeys.lists() });
   * ```
   */
  lists: () => [...investor_emailKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = investor_emailKeys.list({ is_active: true });
   *
   * // List with search
   * const key = investor_emailKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = investor_emailKeys.list();
   * ```
   */
  list: (filters?: InvestorEmailFilterOptions) =>
    [...investor_emailKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: investor_emailKeys.details() });
   * ```
   */
  details: () => [...investor_emailKeys.all, 'detail'] as const,

  /**
   * Key for a specific investor_email by ID
   * Use this for fetching and invalidating individual investor_email records
   *
   * @param id - InvestorEmail ID (UUID)
   * @returns Query key tuple for specific investor_email
   *
   * @example
   * ```typescript
   * // Fetch specific investor_email
   * const { data } = useQuery({
   *   queryKey: investor_emailKeys.detail('uuid-here'),
   *   queryFn: () => investor_emailService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: investor_emailKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...investor_emailKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active investor_emails (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = investor_emailKeys.dropdown();
   *
   * // Include inactive items
   * const key = investor_emailKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...investor_emailKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * InvestorEmails filtered by investor ID
   *
   * @param investorId - Investor ID (UUID)
   * @returns Query key tuple
   */
  byInvestor: (investorId: string) =>
    [...investor_emailKeys.all, 'by-investor', investorId] as const,

  /**
   * InvestorEmails filtered by status
   *
   * @param status - Status value
   * @returns Query key tuple
   */
  byStatus: (status: string) =>
    [...investor_emailKeys.all, 'by-status', status] as const,
} as const;

/**
 * Type representing any valid InvestorEmail query key
 * Useful for typing query key arrays in generic contexts
 */
export type InvestorEmailQueryKey =
  | typeof investor_emailKeys.all
  | ReturnType<typeof investor_emailKeys.lists>
  | ReturnType<typeof investor_emailKeys.list>
  | ReturnType<typeof investor_emailKeys.details>
  | ReturnType<typeof investor_emailKeys.detail>
  | ReturnType<typeof investor_emailKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default investor_emailKeys;
