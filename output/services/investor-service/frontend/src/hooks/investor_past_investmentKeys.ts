/**
 * InvestorPastInvestment Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all investor_past_investment queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['investorpastinvestments'] - All investor_past_investment queries
 * - ['investorpastinvestments', 'list'] - All list queries
 * - ['investorpastinvestments', 'list', { filters }] - Specific filtered list
 * - ['investorpastinvestments', 'detail', id] - Specific investor_past_investment by ID
 *
 * @example
 * ```typescript
 * import { investor_past_investmentKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: investor_past_investmentKeys.list({ is_active: true }),
 *   queryFn: () => investor_past_investmentService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all investor_past_investment queries
 * queryClient.invalidateQueries({ queryKey: investor_past_investmentKeys.all });
 *
 * // Invalidate specific investor_past_investment
 * queryClient.invalidateQueries({ queryKey: investor_past_investmentKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:11.301408Z
 */

import type { InvestorPastInvestmentFilterOptions } from '../types/investor_past_investment.types';

/**
 * Root query key for InvestorPastInvestment entity
 * Used as namespace prefix for all investor_past_investment queries
 */
const INVESTOR_PAST_INVESTMENT_ROOT = 'investorpastinvestments' as const;

/**
 * InvestorPastInvestment Query Keys
 *
 * Hierarchical query key factory for investor_past_investment queries:
 * - all: Base key for all investor_past_investment queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific investor_past_investment by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = investor_past_investmentKeys.list({ is_active: true });
 * // Result: ['investorpastinvestments', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = investor_past_investmentKeys.detail('uuid-here');
 * // Result: ['investorpastinvestments', 'detail', 'uuid-here']
 *
 * // Invalidate all investor_past_investment data
 * queryClient.invalidateQueries({ queryKey: investor_past_investmentKeys.all });
 * ```
 */
export const investor_past_investmentKeys = {
  /**
   * Base key for all investor_past_investment queries
   * Use this to invalidate all investor_past_investment-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: investor_past_investmentKeys.all });
   * ```
   */
  all: [INVESTOR_PAST_INVESTMENT_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: investor_past_investmentKeys.lists() });
   * ```
   */
  lists: () => [...investor_past_investmentKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = investor_past_investmentKeys.list({ is_active: true });
   *
   * // List with search
   * const key = investor_past_investmentKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = investor_past_investmentKeys.list();
   * ```
   */
  list: (filters?: InvestorPastInvestmentFilterOptions) =>
    [...investor_past_investmentKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: investor_past_investmentKeys.details() });
   * ```
   */
  details: () => [...investor_past_investmentKeys.all, 'detail'] as const,

  /**
   * Key for a specific investor_past_investment by ID
   * Use this for fetching and invalidating individual investor_past_investment records
   *
   * @param id - InvestorPastInvestment ID (UUID)
   * @returns Query key tuple for specific investor_past_investment
   *
   * @example
   * ```typescript
   * // Fetch specific investor_past_investment
   * const { data } = useQuery({
   *   queryKey: investor_past_investmentKeys.detail('uuid-here'),
   *   queryFn: () => investor_past_investmentService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: investor_past_investmentKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...investor_past_investmentKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active investor_past_investments (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = investor_past_investmentKeys.dropdown();
   *
   * // Include inactive items
   * const key = investor_past_investmentKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...investor_past_investmentKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * InvestorPastInvestments filtered by investor ID
   *
   * @param investorId - Investor ID (UUID)
   * @returns Query key tuple
   */
  byInvestor: (investorId: string) =>
    [...investor_past_investmentKeys.all, 'by-investor', investorId] as const,

  /**
   * InvestorPastInvestments filtered by past investment ID
   *
   * @param pastInvestmentId - PastInvestment ID (UUID)
   * @returns Query key tuple
   */
  byPastInvestment: (pastInvestmentId: string) =>
    [...investor_past_investmentKeys.all, 'by-past-investment', pastInvestmentId] as const,
} as const;

/**
 * Type representing any valid InvestorPastInvestment query key
 * Useful for typing query key arrays in generic contexts
 */
export type InvestorPastInvestmentQueryKey =
  | typeof investor_past_investmentKeys.all
  | ReturnType<typeof investor_past_investmentKeys.lists>
  | ReturnType<typeof investor_past_investmentKeys.list>
  | ReturnType<typeof investor_past_investmentKeys.details>
  | ReturnType<typeof investor_past_investmentKeys.detail>
  | ReturnType<typeof investor_past_investmentKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default investor_past_investmentKeys;
