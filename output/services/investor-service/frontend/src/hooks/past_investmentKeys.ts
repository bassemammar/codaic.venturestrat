/**
 * PastInvestment Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all past_investment queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['pastinvestments'] - All past_investment queries
 * - ['pastinvestments', 'list'] - All list queries
 * - ['pastinvestments', 'list', { filters }] - Specific filtered list
 * - ['pastinvestments', 'detail', id] - Specific past_investment by ID
 *
 * @example
 * ```typescript
 * import { past_investmentKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: past_investmentKeys.list({ is_active: true }),
 *   queryFn: () => past_investmentService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all past_investment queries
 * queryClient.invalidateQueries({ queryKey: past_investmentKeys.all });
 *
 * // Invalidate specific past_investment
 * queryClient.invalidateQueries({ queryKey: past_investmentKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:12.761593Z
 */

import type { PastInvestmentFilterOptions } from '../types/past_investment.types';

/**
 * Root query key for PastInvestment entity
 * Used as namespace prefix for all past_investment queries
 */
const PAST_INVESTMENT_ROOT = 'pastinvestments' as const;

/**
 * PastInvestment Query Keys
 *
 * Hierarchical query key factory for past_investment queries:
 * - all: Base key for all past_investment queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific past_investment by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = past_investmentKeys.list({ is_active: true });
 * // Result: ['pastinvestments', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = past_investmentKeys.detail('uuid-here');
 * // Result: ['pastinvestments', 'detail', 'uuid-here']
 *
 * // Invalidate all past_investment data
 * queryClient.invalidateQueries({ queryKey: past_investmentKeys.all });
 * ```
 */
export const past_investmentKeys = {
  /**
   * Base key for all past_investment queries
   * Use this to invalidate all past_investment-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: past_investmentKeys.all });
   * ```
   */
  all: [PAST_INVESTMENT_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: past_investmentKeys.lists() });
   * ```
   */
  lists: () => [...past_investmentKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = past_investmentKeys.list({ is_active: true });
   *
   * // List with search
   * const key = past_investmentKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = past_investmentKeys.list();
   * ```
   */
  list: (filters?: PastInvestmentFilterOptions) =>
    [...past_investmentKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: past_investmentKeys.details() });
   * ```
   */
  details: () => [...past_investmentKeys.all, 'detail'] as const,

  /**
   * Key for a specific past_investment by ID
   * Use this for fetching and invalidating individual past_investment records
   *
   * @param id - PastInvestment ID (UUID)
   * @returns Query key tuple for specific past_investment
   *
   * @example
   * ```typescript
   * // Fetch specific past_investment
   * const { data } = useQuery({
   *   queryKey: past_investmentKeys.detail('uuid-here'),
   *   queryFn: () => past_investmentService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: past_investmentKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...past_investmentKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active past_investments (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = past_investmentKeys.dropdown();
   *
   * // Include inactive items
   * const key = past_investmentKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...past_investmentKeys.all, 'dropdown', { activeOnly }] as const,
} as const;

/**
 * Type representing any valid PastInvestment query key
 * Useful for typing query key arrays in generic contexts
 */
export type PastInvestmentQueryKey =
  | typeof past_investmentKeys.all
  | ReturnType<typeof past_investmentKeys.lists>
  | ReturnType<typeof past_investmentKeys.list>
  | ReturnType<typeof past_investmentKeys.details>
  | ReturnType<typeof past_investmentKeys.detail>
  | ReturnType<typeof past_investmentKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default past_investmentKeys;
