/**
 * Investor Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all investor queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['investors'] - All investor queries
 * - ['investors', 'list'] - All list queries
 * - ['investors', 'list', { filters }] - Specific filtered list
 * - ['investors', 'detail', id] - Specific investor by ID
 *
 * @example
 * ```typescript
 * import { investorKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: investorKeys.list({ is_active: true }),
 *   queryFn: () => investorService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all investor queries
 * queryClient.invalidateQueries({ queryKey: investorKeys.all });
 *
 * // Invalidate specific investor
 * queryClient.invalidateQueries({ queryKey: investorKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:09.847869Z
 */

import type { InvestorFilterOptions } from '../types/investor.types';

/**
 * Root query key for Investor entity
 * Used as namespace prefix for all investor queries
 */
const INVESTOR_ROOT = 'investors' as const;

/**
 * Investor Query Keys
 *
 * Hierarchical query key factory for investor queries:
 * - all: Base key for all investor queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific investor by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = investorKeys.list({ is_active: true });
 * // Result: ['investors', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = investorKeys.detail('uuid-here');
 * // Result: ['investors', 'detail', 'uuid-here']
 *
 * // Invalidate all investor data
 * queryClient.invalidateQueries({ queryKey: investorKeys.all });
 * ```
 */
export const investorKeys = {
  /**
   * Base key for all investor queries
   * Use this to invalidate all investor-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: investorKeys.all });
   * ```
   */
  all: [INVESTOR_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: investorKeys.lists() });
   * ```
   */
  lists: () => [...investorKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = investorKeys.list({ is_active: true });
   *
   * // List with search
   * const key = investorKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = investorKeys.list();
   * ```
   */
  list: (filters?: InvestorFilterOptions) =>
    [...investorKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: investorKeys.details() });
   * ```
   */
  details: () => [...investorKeys.all, 'detail'] as const,

  /**
   * Key for a specific investor by ID
   * Use this for fetching and invalidating individual investor records
   *
   * @param id - Investor ID (UUID)
   * @returns Query key tuple for specific investor
   *
   * @example
   * ```typescript
   * // Fetch specific investor
   * const { data } = useQuery({
   *   queryKey: investorKeys.detail('uuid-here'),
   *   queryFn: () => investorService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: investorKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...investorKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active investors (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = investorKeys.dropdown();
   *
   * // Include inactive items
   * const key = investorKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...investorKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * Investors filtered by external ID
   *
   * @param externalId - External ID (UUID)
   * @returns Query key tuple
   */
  byExternal: (externalId: string) =>
    [...investorKeys.all, 'by-external', externalId] as const,
} as const;

/**
 * Type representing any valid Investor query key
 * Useful for typing query key arrays in generic contexts
 */
export type InvestorQueryKey =
  | typeof investorKeys.all
  | ReturnType<typeof investorKeys.lists>
  | ReturnType<typeof investorKeys.list>
  | ReturnType<typeof investorKeys.details>
  | ReturnType<typeof investorKeys.detail>
  | ReturnType<typeof investorKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default investorKeys;
