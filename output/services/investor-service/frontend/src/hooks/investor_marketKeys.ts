/**
 * InvestorMarket Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all investor_market queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['investormarkets'] - All investor_market queries
 * - ['investormarkets', 'list'] - All list queries
 * - ['investormarkets', 'list', { filters }] - Specific filtered list
 * - ['investormarkets', 'detail', id] - Specific investor_market by ID
 *
 * @example
 * ```typescript
 * import { investor_marketKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: investor_marketKeys.list({ is_active: true }),
 *   queryFn: () => investor_marketService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all investor_market queries
 * queryClient.invalidateQueries({ queryKey: investor_marketKeys.all });
 *
 * // Invalidate specific investor_market
 * queryClient.invalidateQueries({ queryKey: investor_marketKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:10.784390Z
 */

import type { InvestorMarketFilterOptions } from '../types/investor_market.types';

/**
 * Root query key for InvestorMarket entity
 * Used as namespace prefix for all investor_market queries
 */
const INVESTOR_MARKET_ROOT = 'investormarkets' as const;

/**
 * InvestorMarket Query Keys
 *
 * Hierarchical query key factory for investor_market queries:
 * - all: Base key for all investor_market queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific investor_market by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = investor_marketKeys.list({ is_active: true });
 * // Result: ['investormarkets', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = investor_marketKeys.detail('uuid-here');
 * // Result: ['investormarkets', 'detail', 'uuid-here']
 *
 * // Invalidate all investor_market data
 * queryClient.invalidateQueries({ queryKey: investor_marketKeys.all });
 * ```
 */
export const investor_marketKeys = {
  /**
   * Base key for all investor_market queries
   * Use this to invalidate all investor_market-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: investor_marketKeys.all });
   * ```
   */
  all: [INVESTOR_MARKET_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: investor_marketKeys.lists() });
   * ```
   */
  lists: () => [...investor_marketKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = investor_marketKeys.list({ is_active: true });
   *
   * // List with search
   * const key = investor_marketKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = investor_marketKeys.list();
   * ```
   */
  list: (filters?: InvestorMarketFilterOptions) =>
    [...investor_marketKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: investor_marketKeys.details() });
   * ```
   */
  details: () => [...investor_marketKeys.all, 'detail'] as const,

  /**
   * Key for a specific investor_market by ID
   * Use this for fetching and invalidating individual investor_market records
   *
   * @param id - InvestorMarket ID (UUID)
   * @returns Query key tuple for specific investor_market
   *
   * @example
   * ```typescript
   * // Fetch specific investor_market
   * const { data } = useQuery({
   *   queryKey: investor_marketKeys.detail('uuid-here'),
   *   queryFn: () => investor_marketService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: investor_marketKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...investor_marketKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active investor_markets (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = investor_marketKeys.dropdown();
   *
   * // Include inactive items
   * const key = investor_marketKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...investor_marketKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * InvestorMarkets filtered by investor ID
   *
   * @param investorId - Investor ID (UUID)
   * @returns Query key tuple
   */
  byInvestor: (investorId: string) =>
    [...investor_marketKeys.all, 'by-investor', investorId] as const,

  /**
   * InvestorMarkets filtered by market ID
   *
   * @param marketId - Market ID (UUID)
   * @returns Query key tuple
   */
  byMarket: (marketId: string) =>
    [...investor_marketKeys.all, 'by-market', marketId] as const,
} as const;

/**
 * Type representing any valid InvestorMarket query key
 * Useful for typing query key arrays in generic contexts
 */
export type InvestorMarketQueryKey =
  | typeof investor_marketKeys.all
  | ReturnType<typeof investor_marketKeys.lists>
  | ReturnType<typeof investor_marketKeys.list>
  | ReturnType<typeof investor_marketKeys.details>
  | ReturnType<typeof investor_marketKeys.detail>
  | ReturnType<typeof investor_marketKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default investor_marketKeys;
