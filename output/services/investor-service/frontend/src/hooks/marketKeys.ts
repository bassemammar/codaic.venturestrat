/**
 * Market Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all market queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['markets'] - All market queries
 * - ['markets', 'list'] - All list queries
 * - ['markets', 'list', { filters }] - Specific filtered list
 * - ['markets', 'detail', id] - Specific market by ID
 *
 * @example
 * ```typescript
 * import { marketKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: marketKeys.list({ is_active: true }),
 *   queryFn: () => marketService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all market queries
 * queryClient.invalidateQueries({ queryKey: marketKeys.all });
 *
 * // Invalidate specific market
 * queryClient.invalidateQueries({ queryKey: marketKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:11.979269Z
 */

import type { MarketFilterOptions } from '../types/market.types';

/**
 * Root query key for Market entity
 * Used as namespace prefix for all market queries
 */
const MARKET_ROOT = 'markets' as const;

/**
 * Market Query Keys
 *
 * Hierarchical query key factory for market queries:
 * - all: Base key for all market queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific market by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = marketKeys.list({ is_active: true });
 * // Result: ['markets', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = marketKeys.detail('uuid-here');
 * // Result: ['markets', 'detail', 'uuid-here']
 *
 * // Invalidate all market data
 * queryClient.invalidateQueries({ queryKey: marketKeys.all });
 * ```
 */
export const marketKeys = {
  /**
   * Base key for all market queries
   * Use this to invalidate all market-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: marketKeys.all });
   * ```
   */
  all: [MARKET_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: marketKeys.lists() });
   * ```
   */
  lists: () => [...marketKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = marketKeys.list({ is_active: true });
   *
   * // List with search
   * const key = marketKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = marketKeys.list();
   * ```
   */
  list: (filters?: MarketFilterOptions) =>
    [...marketKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: marketKeys.details() });
   * ```
   */
  details: () => [...marketKeys.all, 'detail'] as const,

  /**
   * Key for a specific market by ID
   * Use this for fetching and invalidating individual market records
   *
   * @param id - Market ID (UUID)
   * @returns Query key tuple for specific market
   *
   * @example
   * ```typescript
   * // Fetch specific market
   * const { data } = useQuery({
   *   queryKey: marketKeys.detail('uuid-here'),
   *   queryFn: () => marketService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: marketKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...marketKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active markets (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = marketKeys.dropdown();
   *
   * // Include inactive items
   * const key = marketKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...marketKeys.all, 'dropdown', { activeOnly }] as const,
} as const;

/**
 * Type representing any valid Market query key
 * Useful for typing query key arrays in generic contexts
 */
export type MarketQueryKey =
  | typeof marketKeys.all
  | ReturnType<typeof marketKeys.lists>
  | ReturnType<typeof marketKeys.list>
  | ReturnType<typeof marketKeys.details>
  | ReturnType<typeof marketKeys.detail>
  | ReturnType<typeof marketKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default marketKeys;
