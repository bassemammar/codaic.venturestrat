/**
 * Market Type Definitions
 *
 * This file contains type definitions for the Market entity:
 * - Market: Full entity model with all fields
 * - MarketCreateRequest: Data for creating a new market
 * - MarketUpdateRequest: Partial data for updating a market
 * - MarketListResponse: Paginated list response
 * - MarketFilterOptions: Query parameters for filtering
 * - MarketDropdownOption: Options for select components
 *
 * Market sector or industry focus category
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:11.938059Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// Market Entity
// ============================================================================

/**
 * Market entity model
 *
 * Represents the full market entity as returned by the API.
 * Market sector or industry focus category
 *
 * @remarks
 * This interface matches the backend MarketDTO schema exactly.
 */
export interface Market {
  /** Unique identifier (UUID) */
  id: string;

  /** Market/sector name */
  title: string;

  /** Whether this is a country-based market category */
  is_country: boolean;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * Market creation request data
 *
 * Used when creating a new market. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend MarketCreateDTO schema.
 */
export interface MarketCreateRequest {
  /** Market/sector name (required) */
  title: string;

  /** Whether this is a country-based market category (optional) */
  is_country?: boolean;

  /** Active status (defaults to true for new markets) */
  is_active?: boolean;
}

/**
 * Market update request data
 *
 * Used when updating an existing market. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend MarketUpdateDTO schema.
 */
export interface MarketUpdateRequest {
  /** Market/sector name */
  title?: string;

  /** Whether this is a country-based market category */
  is_country?: boolean;

  /** Active status */
  is_active?: boolean;
}

/**
 * Market list response
 *
 * Wrapper for paginated market list queries.
 */
export interface MarketListResponse {
  /** Array of market items */
  items: Market[];

  /** Alternative property name for items (for API compatibility) */
  markets?: Market[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * Market filter options for list queries
 *
 * Extends BaseEntityFilters with market-specific filtering options.
 */
export interface MarketFilterOptions extends BaseEntityFilters {
}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * Market dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface MarketDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: Market;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * Market summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface MarketSummary {
  /** Unique identifier */
  id: string;

  /** Market/sector name */
  title: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * Market form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type MarketFormData = MarketCreateRequest;

/**
 * Default form values for new market
 *
 * Use this when initializing form state for creating a new market.
 */
export const DEFAULT_MARKET_FORM_DATA: Partial<MarketFormData> = {
  is_active: true,
  is_country: false,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single market API response
 */
export interface MarketResponse {
  /** The requested market entity */
  data: Market;
}

/**
 * Market mutation response (create/update)
 */
export interface MarketMutationResponse {
  /** The created or updated market */
  data: Market;

  /** Success message */
  message?: string;
}

/**
 * API error response for market operations
 */
export interface MarketApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a Market
 *
 * @param value - Value to check
 * @returns True if value is a Market
 */
export function isMarket(value: unknown): value is Market {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.title === 'string'
  );
}

/**
 * Type guard to check if a value is a MarketDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a MarketDropdownOption
 */
export function isMarketDropdownOption(
  value: unknown
): value is MarketDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isMarket(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All market types re-exported for convenience */
export type {
  Market as MarketEntity,
  MarketCreateRequest as CreateMarketInput,
  MarketUpdateRequest as UpdateMarketInput,
};
