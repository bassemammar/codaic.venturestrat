/**
 * Investor Type Definitions
 *
 * This file contains type definitions for the Investor entity:
 * - Investor: Full entity model with all fields
 * - InvestorCreateRequest: Data for creating a new investor
 * - InvestorUpdateRequest: Partial data for updating a investor
 * - InvestorListResponse: Paginated list response
 * - InvestorFilterOptions: Query parameters for filtering
 * - InvestorDropdownOption: Options for select components
 *
 * VC investor profile with contact info, location, stages, types, and social links
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:09.823400Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// Investor Entity
// ============================================================================

/**
 * Investor entity model
 *
 * Represents the full investor entity as returned by the API.
 * VC investor profile with contact info, location, stages, types, and social links
 *
 * @remarks
 * This interface matches the backend InvestorDTO schema exactly.
 */
export interface Investor {
  /** Unique identifier (UUID) */
  id: string;

  /** Full name of the investor */
  name: string;

  /** S3 signed URL for profile image */
  avatar: string | null;

  /** Website */
  website: string | null;

  /** Phone */
  phone: string | null;

  /** Job title, e.g. Managing Partner */
  title: string | null;

  /** Original source ID from data import */
  external_id: string;

  /** City */
  city: string | null;

  /** State */
  state: string | null;

  /** Country */
  country: string | null;

  /** Fund or firm name */
  company_name: string | null;

  /** Investment stages, e.g. ["Seed", "Series A"] */
  stages: Record<string, any>;

  /** Investor types, e.g. ["Angel", "VC"] */
  investor_types: Record<string, any>;

  /** Social media links: {linkedin, twitter, crunchbase} */
  social_links: Record<string, any> | null;

  /** Deal pipeline data from source */
  pipelines: Record<string, any> | null;

  /** Founded Companies */
  founded_companies: Record<string, any> | null;

  /** Sorting weight for country-based ordering */
  country_priority: number;

  /** Raw import data from original source */
  source_data: Record<string, any> | null;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * Investor creation request data
 *
 * Used when creating a new investor. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend InvestorCreateDTO schema.
 */
export interface InvestorCreateRequest {
  /** Full name of the investor (required) */
  name: string;

  /** S3 signed URL for profile image (optional) */
  avatar?: string;

  /** Website (optional) */
  website?: string;

  /** Phone (optional) */
  phone?: string;

  /** Job title, e.g. Managing Partner (optional) */
  title?: string;

  /** Original source ID from data import (required) */
  external_id: string;

  /** City (optional) */
  city?: string;

  /** State (optional) */
  state?: string;

  /** Country (optional) */
  country?: string;

  /** Fund or firm name (optional) */
  company_name?: string;

  /** Investment stages, e.g. ["Seed", "Series A"] (optional) */
  stages?: Record<string, any>;

  /** Investor types, e.g. ["Angel", "VC"] (optional) */
  investor_types?: Record<string, any>;

  /** Social media links: {linkedin, twitter, crunchbase} (optional) */
  social_links?: Record<string, any>;

  /** Deal pipeline data from source (optional) */
  pipelines?: Record<string, any>;

  /** Founded Companies (optional) */
  founded_companies?: Record<string, any>;

  /** Sorting weight for country-based ordering (optional) */
  country_priority?: number;

  /** Raw import data from original source (optional) */
  source_data?: Record<string, any>;

  /** Active status (defaults to true for new investors) */
  is_active?: boolean;
}

/**
 * Investor update request data
 *
 * Used when updating an existing investor. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend InvestorUpdateDTO schema.
 */
export interface InvestorUpdateRequest {
  /** Full name of the investor */
  name?: string;

  /** S3 signed URL for profile image */
  avatar?: string | null;

  /** Website */
  website?: string | null;

  /** Phone */
  phone?: string | null;

  /** Job title, e.g. Managing Partner */
  title?: string | null;

  /** Original source ID from data import */
  external_id?: string;

  /** City */
  city?: string | null;

  /** State */
  state?: string | null;

  /** Country */
  country?: string | null;

  /** Fund or firm name */
  company_name?: string | null;

  /** Investment stages, e.g. ["Seed", "Series A"] */
  stages?: Record<string, any>;

  /** Investor types, e.g. ["Angel", "VC"] */
  investor_types?: Record<string, any>;

  /** Social media links: {linkedin, twitter, crunchbase} */
  social_links?: Record<string, any> | null;

  /** Deal pipeline data from source */
  pipelines?: Record<string, any> | null;

  /** Founded Companies */
  founded_companies?: Record<string, any> | null;

  /** Sorting weight for country-based ordering */
  country_priority?: number;

  /** Raw import data from original source */
  source_data?: Record<string, any> | null;

  /** Active status */
  is_active?: boolean;
}

/**
 * Investor list response
 *
 * Wrapper for paginated investor list queries.
 */
export interface InvestorListResponse {
  /** Array of investor items */
  items: Investor[];

  /** Alternative property name for items (for API compatibility) */
  investors?: Investor[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * Investor filter options for list queries
 *
 * Extends BaseEntityFilters with investor-specific filtering options.
 */
export interface InvestorFilterOptions extends BaseEntityFilters {
  /** Filter by original source id from data import */
  external_id?: string;

  /** Filter by state */
  state?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * Investor dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface InvestorDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: Investor;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * Investor summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface InvestorSummary {
  /** Unique identifier */
  id: string;

  /** Full name of the investor */
  name: string;

  /** Job title, e.g. Managing Partner */
  title: string | null;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * Investor form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type InvestorFormData = InvestorCreateRequest;

/**
 * Default form values for new investor
 *
 * Use this when initializing form state for creating a new investor.
 */
export const DEFAULT_INVESTOR_FORM_DATA: Partial<InvestorFormData> = {
  is_active: true,
  stages: "[]",
  investor_types: "[]",
  country_priority: 2,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single investor API response
 */
export interface InvestorResponse {
  /** The requested investor entity */
  data: Investor;
}

/**
 * Investor mutation response (create/update)
 */
export interface InvestorMutationResponse {
  /** The created or updated investor */
  data: Investor;

  /** Success message */
  message?: string;
}

/**
 * API error response for investor operations
 */
export interface InvestorApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a Investor
 *
 * @param value - Value to check
 * @returns True if value is a Investor
 */
export function isInvestor(value: unknown): value is Investor {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.name === 'string' &&
    typeof obj.external_id === 'string'
  );
}

/**
 * Type guard to check if a value is a InvestorDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a InvestorDropdownOption
 */
export function isInvestorDropdownOption(
  value: unknown
): value is InvestorDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isInvestor(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All investor types re-exported for convenience */
export type {
  Investor as InvestorEntity,
  InvestorCreateRequest as CreateInvestorInput,
  InvestorUpdateRequest as UpdateInvestorInput,
};
