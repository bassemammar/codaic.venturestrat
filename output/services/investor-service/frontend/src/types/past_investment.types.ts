/**
 * PastInvestment Type Definitions
 *
 * This file contains type definitions for the PastInvestment entity:
 * - PastInvestment: Full entity model with all fields
 * - PastInvestmentCreateRequest: Data for creating a new past_investment
 * - PastInvestmentUpdateRequest: Partial data for updating a past_investment
 * - PastInvestmentListResponse: Paginated list response
 * - PastInvestmentFilterOptions: Query parameters for filtering
 * - PastInvestmentDropdownOption: Options for select components
 *
 * Portfolio company that an investor has previously invested in
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:12.728030Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// PastInvestment Entity
// ============================================================================

/**
 * PastInvestment entity model
 *
 * Represents the full past_investment entity as returned by the API.
 * Portfolio company that an investor has previously invested in
 *
 * @remarks
 * This interface matches the backend PastInvestmentDTO schema exactly.
 */
export interface PastInvestment {
  /** Unique identifier (UUID) */
  id: string;

  /** Company name */
  title: string;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * PastInvestment creation request data
 *
 * Used when creating a new past_investment. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend PastInvestmentCreateDTO schema.
 */
export interface PastInvestmentCreateRequest {
  /** Company name (required) */
  title: string;

  /** Active status (defaults to true for new past_investments) */
  is_active?: boolean;
}

/**
 * PastInvestment update request data
 *
 * Used when updating an existing past_investment. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend PastInvestmentUpdateDTO schema.
 */
export interface PastInvestmentUpdateRequest {
  /** Company name */
  title?: string;

  /** Active status */
  is_active?: boolean;
}

/**
 * PastInvestment list response
 *
 * Wrapper for paginated past_investment list queries.
 */
export interface PastInvestmentListResponse {
  /** Array of past_investment items */
  items: PastInvestment[];

  /** Alternative property name for items (for API compatibility) */
  past_investments?: PastInvestment[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * PastInvestment filter options for list queries
 *
 * Extends BaseEntityFilters with past_investment-specific filtering options.
 */
export interface PastInvestmentFilterOptions extends BaseEntityFilters {
}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * PastInvestment dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface PastInvestmentDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: PastInvestment;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * PastInvestment summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface PastInvestmentSummary {
  /** Unique identifier */
  id: string;

  /** Company name */
  title: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * PastInvestment form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type PastInvestmentFormData = PastInvestmentCreateRequest;

/**
 * Default form values for new past_investment
 *
 * Use this when initializing form state for creating a new past_investment.
 */
export const DEFAULT_PAST_INVESTMENT_FORM_DATA: Partial<PastInvestmentFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single past_investment API response
 */
export interface PastInvestmentResponse {
  /** The requested past_investment entity */
  data: PastInvestment;
}

/**
 * PastInvestment mutation response (create/update)
 */
export interface PastInvestmentMutationResponse {
  /** The created or updated past_investment */
  data: PastInvestment;

  /** Success message */
  message?: string;
}

/**
 * API error response for past_investment operations
 */
export interface PastInvestmentApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a PastInvestment
 *
 * @param value - Value to check
 * @returns True if value is a PastInvestment
 */
export function isPastInvestment(value: unknown): value is PastInvestment {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.title === 'string'
  );
}

/**
 * Type guard to check if a value is a PastInvestmentDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a PastInvestmentDropdownOption
 */
export function isPastInvestmentDropdownOption(
  value: unknown
): value is PastInvestmentDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isPastInvestment(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All past_investment types re-exported for convenience */
export type {
  PastInvestment as PastInvestmentEntity,
  PastInvestmentCreateRequest as CreatePastInvestmentInput,
  PastInvestmentUpdateRequest as UpdatePastInvestmentInput,
};
