/**
 * InvestorMarket Type Definitions
 *
 * This file contains type definitions for the InvestorMarket entity:
 * - InvestorMarket: Full entity model with all fields
 * - InvestorMarketCreateRequest: Data for creating a new investor_market
 * - InvestorMarketUpdateRequest: Partial data for updating a investor_market
 * - InvestorMarketListResponse: Paginated list response
 * - InvestorMarketFilterOptions: Query parameters for filtering
 * - InvestorMarketDropdownOption: Options for select components
 *
 * Many-to-many junction between investors and markets
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:10.760333Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// InvestorMarket Entity
// ============================================================================

/**
 * InvestorMarket entity model
 *
 * Represents the full investor_market entity as returned by the API.
 * Many-to-many junction between investors and markets
 *
 * @remarks
 * This interface matches the backend InvestorMarketDTO schema exactly.
 */
export interface InvestorMarket {
  /** Unique identifier (UUID) */
  id: string;

  /** Investor Id */
  investor_id: string;

  /** Market Id */
  market_id: string;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * InvestorMarket creation request data
 *
 * Used when creating a new investor_market. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend InvestorMarketCreateDTO schema.
 */
export interface InvestorMarketCreateRequest {
  /** Investor Id (required) */
  investor_id: string;

  /** Market Id (required) */
  market_id: string;

  /** Active status (defaults to true for new investor_markets) */
  is_active?: boolean;
}

/**
 * InvestorMarket update request data
 *
 * Used when updating an existing investor_market. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend InvestorMarketUpdateDTO schema.
 */
export interface InvestorMarketUpdateRequest {
  /** Investor Id */
  investor_id?: string;

  /** Market Id */
  market_id?: string;

  /** Active status */
  is_active?: boolean;
}

/**
 * InvestorMarket list response
 *
 * Wrapper for paginated investor_market list queries.
 */
export interface InvestorMarketListResponse {
  /** Array of investor_market items */
  items: InvestorMarket[];

  /** Alternative property name for items (for API compatibility) */
  investor_markets?: InvestorMarket[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * InvestorMarket filter options for list queries
 *
 * Extends BaseEntityFilters with investor_market-specific filtering options.
 */
export interface InvestorMarketFilterOptions extends BaseEntityFilters {
  /** Filter by investor id */
  investor_id?: string;

  /** Filter by market id */
  market_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * InvestorMarket dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface InvestorMarketDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: InvestorMarket;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * InvestorMarket summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface InvestorMarketSummary {
  /** Unique identifier */
  id: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * InvestorMarket form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type InvestorMarketFormData = InvestorMarketCreateRequest;

/**
 * Default form values for new investor_market
 *
 * Use this when initializing form state for creating a new investor_market.
 */
export const DEFAULT_INVESTOR_MARKET_FORM_DATA: Partial<InvestorMarketFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single investor_market API response
 */
export interface InvestorMarketResponse {
  /** The requested investor_market entity */
  data: InvestorMarket;
}

/**
 * InvestorMarket mutation response (create/update)
 */
export interface InvestorMarketMutationResponse {
  /** The created or updated investor_market */
  data: InvestorMarket;

  /** Success message */
  message?: string;
}

/**
 * API error response for investor_market operations
 */
export interface InvestorMarketApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a InvestorMarket
 *
 * @param value - Value to check
 * @returns True if value is a InvestorMarket
 */
export function isInvestorMarket(value: unknown): value is InvestorMarket {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.investor_id === 'string' &&
    typeof obj.market_id === 'string'
  );
}

/**
 * Type guard to check if a value is a InvestorMarketDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a InvestorMarketDropdownOption
 */
export function isInvestorMarketDropdownOption(
  value: unknown
): value is InvestorMarketDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isInvestorMarket(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All investor_market types re-exported for convenience */
export type {
  InvestorMarket as InvestorMarketEntity,
  InvestorMarketCreateRequest as CreateInvestorMarketInput,
  InvestorMarketUpdateRequest as UpdateInvestorMarketInput,
};
