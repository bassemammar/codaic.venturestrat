/**
 * InvestorEmail Type Definitions
 *
 * This file contains type definitions for the InvestorEmail entity:
 * - InvestorEmail: Full entity model with all fields
 * - InvestorEmailCreateRequest: Data for creating a new investor_email
 * - InvestorEmailUpdateRequest: Partial data for updating a investor_email
 * - InvestorEmailListResponse: Paginated list response
 * - InvestorEmailFilterOptions: Query parameters for filtering
 * - InvestorEmailDropdownOption: Options for select components
 *
 * Email addresses associated with an investor
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:10.285171Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// InvestorEmail Entity
// ============================================================================

/**
 * InvestorEmail entity model
 *
 * Represents the full investor_email entity as returned by the API.
 * Email addresses associated with an investor
 *
 * @remarks
 * This interface matches the backend InvestorEmailDTO schema exactly.
 */
export interface InvestorEmail {
  /** Unique identifier (UUID) */
  id: string;

  /** Reference to the investor */
  investor_id: string;

  /** Email */
  email: string;

  /** Email validation status */
  status: string;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * InvestorEmail creation request data
 *
 * Used when creating a new investor_email. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend InvestorEmailCreateDTO schema.
 */
export interface InvestorEmailCreateRequest {
  /** Reference to the investor (required) */
  investor_id: string;

  /** Email (required) */
  email: string;

  /** Email validation status (optional) */
  status?: string;

  /** Active status (defaults to true for new investor_emails) */
  is_active?: boolean;
}

/**
 * InvestorEmail update request data
 *
 * Used when updating an existing investor_email. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend InvestorEmailUpdateDTO schema.
 */
export interface InvestorEmailUpdateRequest {
  /** Reference to the investor */
  investor_id?: string;

  /** Email */
  email?: string;

  /** Email validation status */
  status?: string;

  /** Active status */
  is_active?: boolean;
}

/**
 * InvestorEmail list response
 *
 * Wrapper for paginated investor_email list queries.
 */
export interface InvestorEmailListResponse {
  /** Array of investor_email items */
  items: InvestorEmail[];

  /** Alternative property name for items (for API compatibility) */
  investor_emails?: InvestorEmail[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * InvestorEmail filter options for list queries
 *
 * Extends BaseEntityFilters with investor_email-specific filtering options.
 */
export interface InvestorEmailFilterOptions extends BaseEntityFilters {
  /** Filter by reference to the investor */
  investor_id?: string;

  /** Filter by email validation status */
  status?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * InvestorEmail dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface InvestorEmailDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: InvestorEmail;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * InvestorEmail summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface InvestorEmailSummary {
  /** Unique identifier */
  id: string;

  /** Email validation status */
  status: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * InvestorEmail form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type InvestorEmailFormData = InvestorEmailCreateRequest;

/**
 * Default form values for new investor_email
 *
 * Use this when initializing form state for creating a new investor_email.
 */
export const DEFAULT_INVESTOR_EMAIL_FORM_DATA: Partial<InvestorEmailFormData> = {
  is_active: true,
  status: "valid",
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single investor_email API response
 */
export interface InvestorEmailResponse {
  /** The requested investor_email entity */
  data: InvestorEmail;
}

/**
 * InvestorEmail mutation response (create/update)
 */
export interface InvestorEmailMutationResponse {
  /** The created or updated investor_email */
  data: InvestorEmail;

  /** Success message */
  message?: string;
}

/**
 * API error response for investor_email operations
 */
export interface InvestorEmailApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a InvestorEmail
 *
 * @param value - Value to check
 * @returns True if value is a InvestorEmail
 */
export function isInvestorEmail(value: unknown): value is InvestorEmail {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.investor_id === 'string' &&
    typeof obj.email === 'string'
  );
}

/**
 * Type guard to check if a value is a InvestorEmailDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a InvestorEmailDropdownOption
 */
export function isInvestorEmailDropdownOption(
  value: unknown
): value is InvestorEmailDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isInvestorEmail(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All investor_email types re-exported for convenience */
export type {
  InvestorEmail as InvestorEmailEntity,
  InvestorEmailCreateRequest as CreateInvestorEmailInput,
  InvestorEmailUpdateRequest as UpdateInvestorEmailInput,
};
