/**
 * InvestorPastInvestment Type Definitions
 *
 * This file contains type definitions for the InvestorPastInvestment entity:
 * - InvestorPastInvestment: Full entity model with all fields
 * - InvestorPastInvestmentCreateRequest: Data for creating a new investor_past_investment
 * - InvestorPastInvestmentUpdateRequest: Partial data for updating a investor_past_investment
 * - InvestorPastInvestmentListResponse: Paginated list response
 * - InvestorPastInvestmentFilterOptions: Query parameters for filtering
 * - InvestorPastInvestmentDropdownOption: Options for select components
 *
 * Many-to-many junction between investors and their past investments
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:11.274933Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// InvestorPastInvestment Entity
// ============================================================================

/**
 * InvestorPastInvestment entity model
 *
 * Represents the full investor_past_investment entity as returned by the API.
 * Many-to-many junction between investors and their past investments
 *
 * @remarks
 * This interface matches the backend InvestorPastInvestmentDTO schema exactly.
 */
export interface InvestorPastInvestment {
  /** Unique identifier (UUID) */
  id: string;

  /** Investor Id */
  investor_id: string;

  /** Past Investment Id */
  past_investment_id: string;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * InvestorPastInvestment creation request data
 *
 * Used when creating a new investor_past_investment. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend InvestorPastInvestmentCreateDTO schema.
 */
export interface InvestorPastInvestmentCreateRequest {
  /** Investor Id (required) */
  investor_id: string;

  /** Past Investment Id (required) */
  past_investment_id: string;

  /** Active status (defaults to true for new investor_past_investments) */
  is_active?: boolean;
}

/**
 * InvestorPastInvestment update request data
 *
 * Used when updating an existing investor_past_investment. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend InvestorPastInvestmentUpdateDTO schema.
 */
export interface InvestorPastInvestmentUpdateRequest {
  /** Investor Id */
  investor_id?: string;

  /** Past Investment Id */
  past_investment_id?: string;

  /** Active status */
  is_active?: boolean;
}

/**
 * InvestorPastInvestment list response
 *
 * Wrapper for paginated investor_past_investment list queries.
 */
export interface InvestorPastInvestmentListResponse {
  /** Array of investor_past_investment items */
  items: InvestorPastInvestment[];

  /** Alternative property name for items (for API compatibility) */
  investor_past_investments?: InvestorPastInvestment[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * InvestorPastInvestment filter options for list queries
 *
 * Extends BaseEntityFilters with investor_past_investment-specific filtering options.
 */
export interface InvestorPastInvestmentFilterOptions extends BaseEntityFilters {
  /** Filter by investor id */
  investor_id?: string;

  /** Filter by past investment id */
  past_investment_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * InvestorPastInvestment dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface InvestorPastInvestmentDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: InvestorPastInvestment;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * InvestorPastInvestment summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface InvestorPastInvestmentSummary {
  /** Unique identifier */
  id: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * InvestorPastInvestment form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type InvestorPastInvestmentFormData = InvestorPastInvestmentCreateRequest;

/**
 * Default form values for new investor_past_investment
 *
 * Use this when initializing form state for creating a new investor_past_investment.
 */
export const DEFAULT_INVESTOR_PAST_INVESTMENT_FORM_DATA: Partial<InvestorPastInvestmentFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single investor_past_investment API response
 */
export interface InvestorPastInvestmentResponse {
  /** The requested investor_past_investment entity */
  data: InvestorPastInvestment;
}

/**
 * InvestorPastInvestment mutation response (create/update)
 */
export interface InvestorPastInvestmentMutationResponse {
  /** The created or updated investor_past_investment */
  data: InvestorPastInvestment;

  /** Success message */
  message?: string;
}

/**
 * API error response for investor_past_investment operations
 */
export interface InvestorPastInvestmentApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a InvestorPastInvestment
 *
 * @param value - Value to check
 * @returns True if value is a InvestorPastInvestment
 */
export function isInvestorPastInvestment(value: unknown): value is InvestorPastInvestment {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.investor_id === 'string' &&
    typeof obj.past_investment_id === 'string'
  );
}

/**
 * Type guard to check if a value is a InvestorPastInvestmentDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a InvestorPastInvestmentDropdownOption
 */
export function isInvestorPastInvestmentDropdownOption(
  value: unknown
): value is InvestorPastInvestmentDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isInvestorPastInvestment(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All investor_past_investment types re-exported for convenience */
export type {
  InvestorPastInvestment as InvestorPastInvestmentEntity,
  InvestorPastInvestmentCreateRequest as CreateInvestorPastInvestmentInput,
  InvestorPastInvestmentUpdateRequest as UpdateInvestorPastInvestmentInput,
};
