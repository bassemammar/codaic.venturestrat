/**
 * InvestorDetail Component
 *
 * Displays detailed information about a investor in a structured layout.
 * Organized into sections:
 * - Basic Information (all entity fields)
 * - Status (active/inactive indicator)
 * - Audit Trail (created/updated timestamps)
 *
 * Features:
 * - React Query integration for data fetching
 * - Material UI Grid layout for responsive design
 * - Loading skeleton states
 * - Error handling with Alert component
 * - Not found state handling
 * - Edit/Delete action buttons
 * - Back navigation
 * - Status badge indicators (active/inactive)
 * - Formatted dates and numbers
 * - Accessibility support (WCAG 2.1 Level AA)
 *
 * VC investor profile with contact info, location, stages, types, and social links
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:09.945322Z
 */

import React, { useCallback } from 'react';
import { useParams, useNavigate } from 'react-router-dom';
import {
  Box,
  Paper,
  Grid,
  Typography,
  Alert as MuiAlert,
  Skeleton,
  IconButton,
  Button,
  Chip,
  Divider,
  Tooltip,
} from '@mui/material';
import {
  ArrowBack as ArrowBackIcon,
  Edit as EditIcon,
  Delete as DeleteIcon,
  Refresh as RefreshIcon,
} from '@mui/icons-material';
import { useInvestor} from '../hooks/useInvestors';
import type { Investor} from '../types/investor.types';

// ============================================================================
// Types
// ============================================================================

/**
 * Props for InvestorDetail component
 */
interface InvestorDetailProps {
  /** Entity ID to display (optional, can be read from URL params) */
  id?: string;
  /** Whether to show the back button */
  showBackButton?: boolean;
  /** Whether to show edit button */
  showEditButton?: boolean;
  /** Whether to show delete button */
  showDeleteButton?: boolean;
  /** Callback when edit button is clicked */
  onEdit?: (investor: Investor) => void;
  /** Callback when delete button is clicked */
  onDelete?: (investor: Investor) => void;
  /** Custom className for the container */
  className?: string;
}

// ============================================================================
// Helper Components
// ============================================================================

/**
 * Field component for displaying label-value pairs
 */
const DetailField: React.FC<{
  label: string;
  value: string | React.ReactNode;
  testId: string;
}> = ({ label, value, testId }) => (
  <div data-testid={testId}>
    <label>
      <Typography variant="body2" color="text.secondary" component="span">
        {label}
      </Typography>
    </label>
    <span>
      <Typography variant="body1" component="span">
        {value}
      </Typography>
    </span>
  </div>
);

/**
 * Loading skeleton for the detail view
 */
const DetailSkeleton: React.FC = () => (
  <Box data-testid="investor-detail-skeleton">
    <Box display="flex" alignItems="center" gap={2} mb={3}>
      <Skeleton variant="circular" width={40} height={40} />
      <Skeleton variant="text" width={200} height={32} />
    </Box>
    <Paper sx={{ p: 3 }}>
      <Skeleton variant="text" width={150} height={28} sx={{ mb: 2 }} />
      {Array.from({ length: 6 }).map((_, index) => (
        <Box key={index} sx={{ mb: 2 }}>
          <Skeleton variant="text" width={100} height={16} />
          <Skeleton variant="text" width={250} height={24} />
        </Box>
      ))}
    </Paper>
  </Box>
);

// ============================================================================
// Helper Functions
// ============================================================================

/**
 * Format a date string for display
 */
function formatDate(value: string | undefined | null): string {
  if (!value) return '-';
  try {
    return new Date(value).toLocaleString();
  } catch {
    return value;
  }
}

/**
 * Format a number for display
 */
function formatNumber(value: number | undefined | null, decimals: number = 2): string {
  if (value === null || value === undefined) return '-';
  return value.toLocaleString(undefined, {
    minimumFractionDigits: decimals,
    maximumFractionDigits: decimals,
  });
}

/**
 * Display a value with fallback for empty/null values
 */
function displayValue(value: string | number | boolean | undefined | null): string {
  if (value === null || value === undefined) return '-';
  if (typeof value === 'string') {
    return value.trim() !== '' ? value : '-';
  }
  if (typeof value === 'boolean') {
    return value ? 'Yes' : 'No';
  }
  return String(value);
}

// ============================================================================
// Component
// ============================================================================

/**
 * InvestorDetail Component
 *
 * Renders comprehensive investor information in a structured grid layout.
 *
 * @example
 * ```tsx
 * // Using URL params (in a route like /investors/:id)
 * <InvestorDetail />
 *
 * // With explicit ID
 * <InvestorDetail id="123" />
 *
 * // With custom handlers
 * <InvestorDetail
 *   onEdit={(entity) => navigate(`/investors/${entity.id}/edit`)}
 *   onDelete={(entity) => handleDelete(entity.id)}
 * />
 * ```
 */
export function InvestorDetail({
  id: propId,
  showBackButton = true,
  showEditButton = true,
  showDeleteButton = true,
  onEdit,
  onDelete,
  className = '',
}: InvestorDetailProps): React.ReactElement {
  const navigate = useNavigate();
  const params = useParams<{ id: string }>();

  // Use prop ID if provided, otherwise get from URL params
  const id = propId ?? params.id;

  // -------------------------------------------------------------------------
  // Data Fetching
  // -------------------------------------------------------------------------

  const {
    data: investor,
    isLoading,
    isError,
    error,
    refetch,
  } = useInvestor(id ?? '', {
    enabled: Boolean(id),
  });

  // -------------------------------------------------------------------------
  // Handlers
  // -------------------------------------------------------------------------

  /** Handle back button click */
  const handleBack = useCallback(() => {
    navigate('/investors');
  }, [navigate]);

  /** Handle edit button click */
  const handleEdit = useCallback(() => {
    if (investor) {
      if (onEdit) {
        onEdit(investor);
      } else {
        navigate(`/investors/${investor.id}/edit`);
      }
    }
  }, [navigate, investor, onEdit]);

  /** Handle delete button click */
  const handleDelete = useCallback(() => {
    if (investor && onDelete) {
      onDelete(investor);
    }
  }, [investor, onDelete]);

  /** Handle refresh button click */
  const handleRefresh = useCallback(() => {
    refetch();
  }, [refetch]);

  // -------------------------------------------------------------------------
  // Render States
  // -------------------------------------------------------------------------

  // Loading state
  if (isLoading) {
    return (
      <Box className={className}>
        <DetailSkeleton />
      </Box>
    );
  }

  // Error state
  if (isError) {
    return (
      <Box className={className} p={3}>
        <MuiAlert
          severity="error"
          action={
            <IconButton color="inherit" size="small" onClick={handleRefresh}>
              <RefreshIcon />
            </IconButton>
          }
        >
          Error loading investor: {(error as Error)?.message || 'Unknown error'}
        </MuiAlert>
      </Box>
    );
  }

  // Not found state
  if (!investor) {
    return (
      <Box className={className} p={3}>
        <MuiAlert severity="warning">
          Investor not found
        </MuiAlert>
        {showBackButton && (
          <Box mt={2}>
            <Button
              startIcon={<ArrowBackIcon />}
              onClick={handleBack}
              variant="outlined"
            >
              Back to Investors
            </Button>
          </Box>
        )}
      </Box>
    );
  }

  // -------------------------------------------------------------------------
  // Main Render
  // -------------------------------------------------------------------------

  return (
    <div data-testid="investor-detail" className={className}>
      {/* Header */}
      <Box
        display="flex"
        justifyContent="space-between"
        alignItems="center"
        mb={3}
      >
        <Box display="flex" alignItems="center" gap={2}>
          {showBackButton && (
            <Tooltip title="Back to Investors">
              <IconButton onClick={handleBack}>
                <ArrowBackIcon />
              </IconButton>
            </Tooltip>
          )}
          <Typography variant="h5" component="h1">
            Investor Details
          </Typography>
{investor.is_active !== undefined && (
            <Chip
              size="small"
              label={investor.is_active ? 'Active' : 'Inactive'}
              color={investor.is_active ? 'success' : 'default'}
              variant="outlined"
            />
          )}
        </Box>

        <Box display="flex" gap={1}>
          <Tooltip title="Refresh">
            <span>
              <IconButton onClick={handleRefresh} disabled={isLoading}>
                <RefreshIcon />
              </IconButton>
            </span>
          </Tooltip>

          {showEditButton && (
            <Tooltip title="Edit Investor">
              <IconButton onClick={handleEdit} color="primary">
                <EditIcon />
              </IconButton>
            </Tooltip>
          )}

          {showDeleteButton && onDelete && (
            <Tooltip title="Delete Investor">
              <IconButton onClick={handleDelete} color="error">
                <DeleteIcon />
              </IconButton>
            </Tooltip>
          )}
        </Box>
      </Box>

      {/* Detail Content */}
      <Paper sx={{ p: 3 }}>
        <Grid container spacing={3}>
          {/* Basic Information Section */}
          <Grid size={12}>
            <section data-testid="basic-info-section" aria-label="Basic Information">
              <Typography variant="h6" component="h2" gutterBottom>
                Basic Information
              </Typography>

              <Box sx={{ display: 'flex', flexDirection: 'column', gap: 2 }}>
                {/* ID field */}
                <DetailField
                  label="ID:"
                  value={investor.id}
                  testId="field-id"
                />

                {/* Name field */}
                <DetailField
                  label="Name:"
                  value={displayValue(investor.name)}
                  testId="field-name"
                />

                {/* Avatar field */}
                <DetailField
                  label="Avatar:"
                  value={displayValue(investor.avatar)}
                  testId="field-avatar"
                />

                {/* Website field */}
                <DetailField
                  label="Website:"
                  value={displayValue(investor.website)}
                  testId="field-website"
                />

                {/* Phone field */}
                <DetailField
                  label="Phone:"
                  value={displayValue(investor.phone)}
                  testId="field-phone"
                />

                {/* Title field */}
                <DetailField
                  label="Title:"
                  value={displayValue(investor.title)}
                  testId="field-title"
                />

                {/* External Id field */}
                <DetailField
                  label="External Id:"
                  value={displayValue(investor.external_id)}
                  testId="field-external-id"
                />

                {/* City field */}
                <DetailField
                  label="City:"
                  value={displayValue(investor.city)}
                  testId="field-city"
                />

                {/* State field */}
                <DetailField
                  label="State:"
                  value={displayValue(investor.state)}
                  testId="field-state"
                />

                {/* Country field */}
                <DetailField
                  label="Country:"
                  value={displayValue(investor.country)}
                  testId="field-country"
                />

                {/* Company Name field */}
                <DetailField
                  label="Company Name:"
                  value={displayValue(investor.company_name)}
                  testId="field-company-name"
                />

                {/* Stages field */}
                <DetailField
                  label="Stages:"
                  value={investor.stages ? JSON.stringify(investor.stages, null, 2) : '-'}
                  testId="field-stages"
                />

                {/* Investor Types field */}
                <DetailField
                  label="Investor Types:"
                  value={investor.investor_types ? JSON.stringify(investor.investor_types, null, 2) : '-'}
                  testId="field-investor-types"
                />

                {/* Social Links field */}
                <DetailField
                  label="Social Links:"
                  value={investor.social_links ? JSON.stringify(investor.social_links, null, 2) : '-'}
                  testId="field-social-links"
                />

                {/* Pipelines field */}
                <DetailField
                  label="Pipelines:"
                  value={investor.pipelines ? JSON.stringify(investor.pipelines, null, 2) : '-'}
                  testId="field-pipelines"
                />

                {/* Founded Companies field */}
                <DetailField
                  label="Founded Companies:"
                  value={investor.founded_companies ? JSON.stringify(investor.founded_companies, null, 2) : '-'}
                  testId="field-founded-companies"
                />

                {/* Country Priority field */}
                <DetailField
                  label="Country Priority:"
                  value={formatNumber(investor.country_priority)}
                  testId="field-country-priority"
                />

                {/* Source Data field */}
                <DetailField
                  label="Source Data:"
                  value={investor.source_data ? JSON.stringify(investor.source_data, null, 2) : '-'}
                  testId="field-source-data"
                />

              </Box>
            </section>
          </Grid>

          {/* Status Section */}
          <Grid size={12}>
            <Divider sx={{ my: 2 }} />
            <section data-testid="status-section" aria-label="Status">
              <Typography variant="h6" component="h2" gutterBottom>
                Status
              </Typography>

              <Box sx={{ display: 'flex', flexDirection: 'column', gap: 2 }}>
                <div data-testid="field-status">
                  <label>
                    <Typography variant="body2" color="text.secondary" component="span">
                      Status:
                    </Typography>
                  </label>
                  <span className={investor.is_active ? 'active-badge' : 'inactive-badge'}>
                    <Typography variant="body1" component="span">
                      {investor.is_active ? 'Active' : 'Inactive'}
                    </Typography>
                  </span>
                </div>
              </Box>
            </section>
          </Grid>

          {/* Audit Trail Section */}
          <Grid size={12}>
            <Divider sx={{ my: 2 }} />
            <section data-testid="audit-trail-section" aria-label="Audit Trail">
              <Typography variant="h6" component="h2" gutterBottom>
                Audit Trail
              </Typography>

              <Box sx={{ display: 'flex', flexDirection: 'column', gap: 2 }}>
                <DetailField
                  label="Created At:"
                  value={formatDate(investor.created_at)}
                  testId="field-created-at"
                />

                <DetailField
                  label="Updated At:"
                  value={formatDate(investor.updated_at)}
                  testId="field-updated-at"
                />

              </Box>
            </section>
          </Grid>
        </Grid>
      </Paper>
    </div>
  );
}

// Default export for convenience
export default InvestorDetail;
