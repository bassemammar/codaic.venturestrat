/**
 * InvestorEmailForm Component
 *
 * Form component for creating and editing investor_emails.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * Features:
 * - Create and Edit mode support
 * - Form validation with error messages
 * - Material UI components
 * - Loading state handling
 * - Accessible form fields (WCAG 2.1 Level AA)
 * - Modal wrapper support (form_display: modal)
 *
 * Email addresses associated with an investor
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:10.352159Z
 */

import React, { useMemo, useEffect } from 'react';
import { useForm, Controller, FormProvider } from 'react-hook-form';
import {
  Alert as MuiAlert,
  Box,
  Grid,
  TextField,
  Button,
  CircularProgress,
  Dialog,
  DialogTitle,
  DialogContent,
  IconButton,
} from '@mui/material';
import { Close as CloseIcon } from '@mui/icons-material';
import type { InvestorEmailCreateRequest, InvestorEmail } from '../types/investor_email.types';
import { useNotification } from '../contexts/NotificationContext';
// Form select components for foreign key fields
import { FormInvestorSelect } from './FormInvestorSelect';

// ============================================================================
// Types
// ============================================================================

/**
 * Form data type for InvestorEmail
 */
export type InvestorEmailFormData = InvestorEmailCreateRequest;

/**
 * Props for InvestorEmailForm component
 */
export interface InvestorEmailFormProps {
  /** Existing investor_email data for edit mode (optional) */
  investorEmail?: InvestorEmail;
  /** Form submission handler (can be sync or async) */
  onSubmit: (data: InvestorEmailFormData) => void | Promise<void>;
  /** Cancel handler */
  onCancel: () => void;
  /** Loading state (disables form during submission) */
  isLoading?: boolean;
  /** Custom className for the form container */
  className?: string;
}

/**
 * Props for InvestorEmailFormModal component (modal wrapper)
 */
export interface InvestorEmailFormModalProps extends InvestorEmailFormProps {
  /** Whether the modal is open */
  open: boolean;
  /** Handler called when the modal requests to be closed */
  onClose: () => void;
  /** Optional title override for the modal */
  title?: string;
}


// ============================================================================
// Constants
// ============================================================================

/**
 * Default form values for creating a new investor_email
 */
const DEFAULT_FORM_VALUES: Partial<InvestorEmailFormData> = {
  investor_id: '',
  email: '',
  status: "valid",
};

// ============================================================================
// Component
// ============================================================================

/**
 * InvestorEmailForm Component
 *
 * Renders a form for creating or editing investor_emails.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * @example
 * ```tsx
 * // Create mode
 * <InvestorEmailForm
 *   onSubmit={handleCreate}
 *   onCancel={handleCancel}
 *   isLoading={isCreating}
 * />
 *
 * // Edit mode
 * <InvestorEmailForm
 *   investorEmail={existingInvestorEmail}
 *   onSubmit={handleUpdate}
 *   onCancel={handleCancel}
 *   isLoading={isUpdating}
 * />
 * ```
 */
export function InvestorEmailForm({
  investorEmail,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: InvestorEmailFormProps): React.ReactElement {
  // Determine if we're in edit mode
  const isEditMode = Boolean(investorEmail);

  // Notification context for showing success/error messages
  const { showSuccess, showError } = useNotification();

  // Build default values from existing entity or defaults
  const defaultValues = useMemo<Partial<InvestorEmailFormData>>(() => {
    if (investorEmail) {
      // Edit mode: use existing entity values
      return {
        investor_id: investorEmail.investor_id,
        email: investorEmail.email,
        status: investorEmail.status,
      };
    }
    // Create mode: use defaults
    return DEFAULT_FORM_VALUES;
  }, [investorEmail]);

  // Initialize React Hook Form
  const methods = useForm<InvestorEmailFormData>({
    defaultValues,
    mode: 'onBlur',
  });

  const {
    control,
    handleSubmit,
    formState: { errors },
    reset,
  } = methods;

  // Reset form when entity data changes (e.g., switching from create to edit mode)
  useEffect(() => {
    reset(defaultValues);
  }, [defaultValues, reset]);

  // Log validation errors to console for debugging
  useEffect(() => {
    if (Object.keys(errors).length > 0) {
      console.log('[InvestorEmailForm] Validation errors:', errors);
    }
  }, [errors]);
  // -------------------------------------------------------------------------
  // Form Submission
  // -------------------------------------------------------------------------

  const handleFormSubmit = async (data: InvestorEmailFormData) => {
    console.log('[InvestorEmailForm] Submitting:', { data, isEditMode });
    try {
      const result = await onSubmit(data);
      console.log('[InvestorEmailForm] Form submission successful', { data, result, isEditMode });
      // Show success notification for create operation
      if (!isEditMode) {
        showSuccess('InvestorEmail created successfully');
      }
    } catch (error) {
      // Log full error details to console for debugging
      console.error('[InvestorEmailForm] Form submission failed', {
        error,
        errorMessage: error instanceof Error ? error.message : String(error),
        errorStack: error instanceof Error ? error.stack : undefined,
        data,
        isEditMode,
      });
      // Show error notification on failure
      const errorMessage = error instanceof Error ? error.message : 'An unexpected error occurred';
      showError(`Failed to ${isEditMode ? 'update' : 'create'} InvestorEmail: ${errorMessage}`);
    }
  };

  // -------------------------------------------------------------------------
  // Render
  // -------------------------------------------------------------------------

  // Count validation errors
  const errorCount = Object.keys(errors).length;

  return (
    <FormProvider {...methods}>
      <Box
        component="form"
        onSubmit={handleSubmit(handleFormSubmit)}
        className={className}
        data-testid="form-investor_email"
        noValidate
      >
        {/* Error Summary Alert */}
        {errorCount > 0 && (
          <MuiAlert
            severity="error"
            sx={{ mb: 3 }}
            data-testid="form-errors-summary"
          >
            Please fix {errorCount} error{errorCount !== 1 ? 's' : ''} before submitting
          </MuiAlert>
        )}

        <Grid container spacing={3}>
          {/* Investor Id field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* Foreign key field - renders as dropdown select */}
            <FormInvestorSelect
              name="investor_id"
              label="Investor"
              required={false}
              disabled={isLoading}
            />
          </Grid>

          {/* Email field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="email"
              control={control}
              rules={{
                required: 'Email is required',
                maxLength: { value: 255, message: 'Email must be at most 255 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Email"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-email"
                />
              )}
            />
          </Grid>

          {/* Status field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="status"
              control={control}
              rules={{
                required: 'Status is required',
                maxLength: { value: 20, message: 'Status must be at most 20 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Status"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Email validation status"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-status"
                />
              )}
            />
          </Grid>

          {/* Form Actions */}
          <Grid size={12}>
            <Box display="flex" gap={2} pt={2}>
              <Button
                type="submit"
                variant="contained"
                color="primary"
                disabled={isLoading}
                startIcon={isLoading ? <CircularProgress size={20} /> : undefined}
                data-testid="submit-button"
              >
                {isLoading
                  ? 'Saving...'
                  : isEditMode
                    ? 'Update InvestorEmail'
                    : 'Create InvestorEmail'}
              </Button>

              <Button
                type="button"
                variant="outlined"
                onClick={onCancel}
                disabled={isLoading}
                data-testid="cancel-button"
              >
                Cancel
              </Button>
            </Box>
          </Grid>
        </Grid>
      </Box>
    </FormProvider>
  );
}

// ============================================================================
// Modal Wrapper Component
// ============================================================================

/**
 * InvestorEmailFormModal Component
 *
 * Wraps the InvestorEmailForm in a Material UI Dialog for modal display.
 * Use this component when you want to show the form in a popup dialog.
 *
 * @example
 * ```tsx
 * <InvestorEmailFormModal
 *   open={isModalOpen}
 *   onClose={handleClose}
 *   onSubmit={handleCreate}
 *   onCancel={handleClose}
 *   isLoading={isCreating}
 * />
 * ```
 */
export function InvestorEmailFormModal({
  open,
  onClose,
  title,
  investorEmail,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: InvestorEmailFormModalProps): React.ReactElement {
  const isEditMode = Boolean(investorEmail);
  const modalTitle = title ?? (isEditMode ? 'Edit InvestorEmail' : 'Create InvestorEmail');

  // Handle modal close - prevent closing during loading
  const handleClose = () => {
    if (!isLoading) {
      onClose();
    }
  };

  return (
    <Dialog
      open={open}
      onClose={handleClose}
      maxWidth="md"
      fullWidth
      data-testid="modal-investor_email-form"
    >
      <DialogTitle sx={{ m: 0, p: 2, display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        {modalTitle}
        <IconButton
          aria-label="close"
          onClick={handleClose}
          disabled={isLoading}
          data-testid="modal-close-button"
          sx={{ color: (theme) => theme.palette.grey[500] }}
        >
          <CloseIcon />
        </IconButton>
      </DialogTitle>
      <DialogContent dividers>
        <InvestorEmailForm
          investorEmail={investorEmail}
          onSubmit={onSubmit}
          onCancel={onCancel}
          isLoading={isLoading}
          className={className}
        />
      </DialogContent>
    </Dialog>
  );
}


// Default export for convenience
export default InvestorEmailForm;
