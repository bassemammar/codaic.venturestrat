/**
 * UsageRecordForm Component
 *
 * Form component for creating and editing usage_records.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * Features:
 * - Create and Edit mode support
 * - Form validation with error messages
 * - Material UI components
 * - Loading state handling
 * - Accessible form fields (WCAG 2.1 Level AA)
 * - Modal wrapper support (form_display: modal)
 *
 * Daily usage tracking for subscription limit enforcement
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:44.338788Z
 */

import React, { useMemo, useEffect } from 'react';
import { useForm, Controller, FormProvider } from 'react-hook-form';
import {
  Alert as MuiAlert,
  Box,
  Grid,
  TextField,
  Button,
  CircularProgress,
  Dialog,
  DialogTitle,
  DialogContent,
  IconButton,
} from '@mui/material';
import { Close as CloseIcon } from '@mui/icons-material';
import type { UsageRecordCreateRequest, UsageRecord } from '../types/usage_record.types';
import { useNotification } from '../contexts/NotificationContext';

// ============================================================================
// Types
// ============================================================================

/**
 * Form data type for UsageRecord
 */
export type UsageRecordFormData = UsageRecordCreateRequest;

/**
 * Props for UsageRecordForm component
 */
export interface UsageRecordFormProps {
  /** Existing usage_record data for edit mode (optional) */
  usageRecord?: UsageRecord;
  /** Form submission handler (can be sync or async) */
  onSubmit: (data: UsageRecordFormData) => void | Promise<void>;
  /** Cancel handler */
  onCancel: () => void;
  /** Loading state (disables form during submission) */
  isLoading?: boolean;
  /** Custom className for the form container */
  className?: string;
}

/**
 * Props for UsageRecordFormModal component (modal wrapper)
 */
export interface UsageRecordFormModalProps extends UsageRecordFormProps {
  /** Whether the modal is open */
  open: boolean;
  /** Handler called when the modal requests to be closed */
  onClose: () => void;
  /** Optional title override for the modal */
  title?: string;
}


// ============================================================================
// Constants
// ============================================================================

/**
 * Default form values for creating a new usage_record
 */
const DEFAULT_FORM_VALUES: Partial<UsageRecordFormData> = {
  user_id: '',
  date: '',
  month: undefined,
  year: undefined,
  ai_drafts_used: 0,
  emails_sent: 0,
  investors_added: 0,
  monthly_emails_sent: 0,
  monthly_investors_added: 0,
  monthly_follow_ups_sent: 0,
};

// ============================================================================
// Component
// ============================================================================

/**
 * UsageRecordForm Component
 *
 * Renders a form for creating or editing usage_records.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * @example
 * ```tsx
 * // Create mode
 * <UsageRecordForm
 *   onSubmit={handleCreate}
 *   onCancel={handleCancel}
 *   isLoading={isCreating}
 * />
 *
 * // Edit mode
 * <UsageRecordForm
 *   usageRecord={existingUsageRecord}
 *   onSubmit={handleUpdate}
 *   onCancel={handleCancel}
 *   isLoading={isUpdating}
 * />
 * ```
 */
export function UsageRecordForm({
  usageRecord,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: UsageRecordFormProps): React.ReactElement {
  // Determine if we're in edit mode
  const isEditMode = Boolean(usageRecord);

  // Notification context for showing success/error messages
  const { showSuccess, showError } = useNotification();

  // Build default values from existing entity or defaults
  const defaultValues = useMemo<Partial<UsageRecordFormData>>(() => {
    if (usageRecord) {
      // Edit mode: use existing entity values
      return {
        user_id: usageRecord.user_id,
        date: usageRecord.date,
        month: usageRecord.month,
        year: usageRecord.year,
        ai_drafts_used: usageRecord.ai_drafts_used,
        emails_sent: usageRecord.emails_sent,
        investors_added: usageRecord.investors_added,
        monthly_emails_sent: usageRecord.monthly_emails_sent,
        monthly_investors_added: usageRecord.monthly_investors_added,
        monthly_follow_ups_sent: usageRecord.monthly_follow_ups_sent,
      };
    }
    // Create mode: use defaults
    return DEFAULT_FORM_VALUES;
  }, [usageRecord]);

  // Initialize React Hook Form
  const methods = useForm<UsageRecordFormData>({
    defaultValues,
    mode: 'onBlur',
  });

  const {
    control,
    handleSubmit,
    formState: { errors },
    reset,
  } = methods;

  // Reset form when entity data changes (e.g., switching from create to edit mode)
  useEffect(() => {
    reset(defaultValues);
  }, [defaultValues, reset]);

  // Log validation errors to console for debugging
  useEffect(() => {
    if (Object.keys(errors).length > 0) {
      console.log('[UsageRecordForm] Validation errors:', errors);
    }
  }, [errors]);
  // -------------------------------------------------------------------------
  // Form Submission
  // -------------------------------------------------------------------------

  const handleFormSubmit = async (data: UsageRecordFormData) => {
    console.log('[UsageRecordForm] Submitting:', { data, isEditMode });
    try {
      const result = await onSubmit(data);
      console.log('[UsageRecordForm] Form submission successful', { data, result, isEditMode });
      // Show success notification for create operation
      if (!isEditMode) {
        showSuccess('UsageRecord created successfully');
      }
    } catch (error) {
      // Log full error details to console for debugging
      console.error('[UsageRecordForm] Form submission failed', {
        error,
        errorMessage: error instanceof Error ? error.message : String(error),
        errorStack: error instanceof Error ? error.stack : undefined,
        data,
        isEditMode,
      });
      // Show error notification on failure
      const errorMessage = error instanceof Error ? error.message : 'An unexpected error occurred';
      showError(`Failed to ${isEditMode ? 'update' : 'create'} UsageRecord: ${errorMessage}`);
    }
  };

  // -------------------------------------------------------------------------
  // Render
  // -------------------------------------------------------------------------

  // Count validation errors
  const errorCount = Object.keys(errors).length;

  return (
    <FormProvider {...methods}>
      <Box
        component="form"
        onSubmit={handleSubmit(handleFormSubmit)}
        className={className}
        data-testid="form-usage_record"
        noValidate
      >
        {/* Error Summary Alert */}
        {errorCount > 0 && (
          <MuiAlert
            severity="error"
            sx={{ mb: 3 }}
            data-testid="form-errors-summary"
          >
            Please fix {errorCount} error{errorCount !== 1 ? 's' : ''} before submitting
          </MuiAlert>
        )}

        <Grid container spacing={3}>
          {/* User Id field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="user_id"
              control={control}
              rules={{
                required: 'User Id is required',
                maxLength: { value: 100, message: 'User Id must be at most 100 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="User Id"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-user-id"
                />
              )}
            />
          </Grid>

          {/* Date field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="date"
              control={control}
              rules={{
                required: 'Date is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Date"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Tracking date"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-date"
                />
              )}
            />
          </Grid>

          {/* Month field */}
          <Grid size={{ xs: 12, md: 6 }}>
            <Controller
              name="month"
              control={control}
              rules={{
                required: 'Month is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  type="number"
                  label="Month"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Month number (1-12)"}
                  placeholder=""
                  value={formField.value ?? ''}
                  onChange={(e) => {
                    const val = e.target.value;
                    formField.onChange(val === '' ? undefined : parseFloat(val));
                  }}
                  inputProps={{
                    step: 'any',
                  }}
                  data-testid="field-month"
                />
              )}
            />
          </Grid>

          {/* Year field */}
          <Grid size={{ xs: 12, md: 6 }}>
            <Controller
              name="year"
              control={control}
              rules={{
                required: 'Year is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  type="number"
                  label="Year"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value ?? ''}
                  onChange={(e) => {
                    const val = e.target.value;
                    formField.onChange(val === '' ? undefined : parseFloat(val));
                  }}
                  inputProps={{
                    step: 'any',
                  }}
                  data-testid="field-year"
                />
              )}
            />
          </Grid>

          {/* Ai Drafts Used field */}
          <Grid size={{ xs: 12, md: 6 }}>
            <Controller
              name="ai_drafts_used"
              control={control}
              rules={{
                required: 'Ai Drafts Used is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  type="number"
                  label="Ai Drafts Used"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value ?? ''}
                  onChange={(e) => {
                    const val = e.target.value;
                    formField.onChange(val === '' ? undefined : parseFloat(val));
                  }}
                  inputProps={{
                    step: 'any',
                  }}
                  data-testid="field-ai-drafts-used"
                />
              )}
            />
          </Grid>

          {/* Emails Sent field */}
          <Grid size={{ xs: 12, md: 6 }}>
            <Controller
              name="emails_sent"
              control={control}
              rules={{
                required: 'Emails Sent is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  type="number"
                  label="Emails Sent"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value ?? ''}
                  onChange={(e) => {
                    const val = e.target.value;
                    formField.onChange(val === '' ? undefined : parseFloat(val));
                  }}
                  inputProps={{
                    step: 'any',
                  }}
                  data-testid="field-emails-sent"
                />
              )}
            />
          </Grid>

          {/* Investors Added field */}
          <Grid size={{ xs: 12, md: 6 }}>
            <Controller
              name="investors_added"
              control={control}
              rules={{
                required: 'Investors Added is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  type="number"
                  label="Investors Added"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value ?? ''}
                  onChange={(e) => {
                    const val = e.target.value;
                    formField.onChange(val === '' ? undefined : parseFloat(val));
                  }}
                  inputProps={{
                    step: 'any',
                  }}
                  data-testid="field-investors-added"
                />
              )}
            />
          </Grid>

          {/* Monthly Emails Sent field */}
          <Grid size={{ xs: 12, md: 6 }}>
            <Controller
              name="monthly_emails_sent"
              control={control}
              rules={{
                required: 'Monthly Emails Sent is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  type="number"
                  label="Monthly Emails Sent"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value ?? ''}
                  onChange={(e) => {
                    const val = e.target.value;
                    formField.onChange(val === '' ? undefined : parseFloat(val));
                  }}
                  inputProps={{
                    step: 'any',
                  }}
                  data-testid="field-monthly-emails-sent"
                />
              )}
            />
          </Grid>

          {/* Monthly Investors Added field */}
          <Grid size={{ xs: 12, md: 6 }}>
            <Controller
              name="monthly_investors_added"
              control={control}
              rules={{
                required: 'Monthly Investors Added is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  type="number"
                  label="Monthly Investors Added"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value ?? ''}
                  onChange={(e) => {
                    const val = e.target.value;
                    formField.onChange(val === '' ? undefined : parseFloat(val));
                  }}
                  inputProps={{
                    step: 'any',
                  }}
                  data-testid="field-monthly-investors-added"
                />
              )}
            />
          </Grid>

          {/* Monthly Follow Ups Sent field */}
          <Grid size={{ xs: 12, md: 6 }}>
            <Controller
              name="monthly_follow_ups_sent"
              control={control}
              rules={{
                required: 'Monthly Follow Ups Sent is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  type="number"
                  label="Monthly Follow Ups Sent"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value ?? ''}
                  onChange={(e) => {
                    const val = e.target.value;
                    formField.onChange(val === '' ? undefined : parseFloat(val));
                  }}
                  inputProps={{
                    step: 'any',
                  }}
                  data-testid="field-monthly-follow-ups-sent"
                />
              )}
            />
          </Grid>

          {/* Form Actions */}
          <Grid size={12}>
            <Box display="flex" gap={2} pt={2}>
              <Button
                type="submit"
                variant="contained"
                color="primary"
                disabled={isLoading}
                startIcon={isLoading ? <CircularProgress size={20} /> : undefined}
                data-testid="submit-button"
              >
                {isLoading
                  ? 'Saving...'
                  : isEditMode
                    ? 'Update UsageRecord'
                    : 'Create UsageRecord'}
              </Button>

              <Button
                type="button"
                variant="outlined"
                onClick={onCancel}
                disabled={isLoading}
                data-testid="cancel-button"
              >
                Cancel
              </Button>
            </Box>
          </Grid>
        </Grid>
      </Box>
    </FormProvider>
  );
}

// ============================================================================
// Modal Wrapper Component
// ============================================================================

/**
 * UsageRecordFormModal Component
 *
 * Wraps the UsageRecordForm in a Material UI Dialog for modal display.
 * Use this component when you want to show the form in a popup dialog.
 *
 * @example
 * ```tsx
 * <UsageRecordFormModal
 *   open={isModalOpen}
 *   onClose={handleClose}
 *   onSubmit={handleCreate}
 *   onCancel={handleClose}
 *   isLoading={isCreating}
 * />
 * ```
 */
export function UsageRecordFormModal({
  open,
  onClose,
  title,
  usageRecord,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: UsageRecordFormModalProps): React.ReactElement {
  const isEditMode = Boolean(usageRecord);
  const modalTitle = title ?? (isEditMode ? 'Edit UsageRecord' : 'Create UsageRecord');

  // Handle modal close - prevent closing during loading
  const handleClose = () => {
    if (!isLoading) {
      onClose();
    }
  };

  return (
    <Dialog
      open={open}
      onClose={handleClose}
      maxWidth="md"
      fullWidth
      data-testid="modal-usage_record-form"
    >
      <DialogTitle sx={{ m: 0, p: 2, display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        {modalTitle}
        <IconButton
          aria-label="close"
          onClick={handleClose}
          disabled={isLoading}
          data-testid="modal-close-button"
          sx={{ color: (theme) => theme.palette.grey[500] }}
        >
          <CloseIcon />
        </IconButton>
      </DialogTitle>
      <DialogContent dividers>
        <UsageRecordForm
          usageRecord={usageRecord}
          onSubmit={onSubmit}
          onCancel={onCancel}
          isLoading={isLoading}
          className={className}
        />
      </DialogContent>
    </Dialog>
  );
}


// Default export for convenience
export default UsageRecordForm;
