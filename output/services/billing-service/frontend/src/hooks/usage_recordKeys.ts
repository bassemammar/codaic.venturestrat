/**
 * UsageRecord Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all usage_record queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['usagerecords'] - All usage_record queries
 * - ['usagerecords', 'list'] - All list queries
 * - ['usagerecords', 'list', { filters }] - Specific filtered list
 * - ['usagerecords', 'detail', id] - Specific usage_record by ID
 *
 * @example
 * ```typescript
 * import { usage_recordKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: usage_recordKeys.list({ is_active: true }),
 *   queryFn: () => usage_recordService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all usage_record queries
 * queryClient.invalidateQueries({ queryKey: usage_recordKeys.all });
 *
 * // Invalidate specific usage_record
 * queryClient.invalidateQueries({ queryKey: usage_recordKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:44.299186Z
 */

import type { UsageRecordFilterOptions } from '../types/usage_record.types';

/**
 * Root query key for UsageRecord entity
 * Used as namespace prefix for all usage_record queries
 */
const USAGE_RECORD_ROOT = 'usagerecords' as const;

/**
 * UsageRecord Query Keys
 *
 * Hierarchical query key factory for usage_record queries:
 * - all: Base key for all usage_record queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific usage_record by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = usage_recordKeys.list({ is_active: true });
 * // Result: ['usagerecords', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = usage_recordKeys.detail('uuid-here');
 * // Result: ['usagerecords', 'detail', 'uuid-here']
 *
 * // Invalidate all usage_record data
 * queryClient.invalidateQueries({ queryKey: usage_recordKeys.all });
 * ```
 */
export const usage_recordKeys = {
  /**
   * Base key for all usage_record queries
   * Use this to invalidate all usage_record-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: usage_recordKeys.all });
   * ```
   */
  all: [USAGE_RECORD_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: usage_recordKeys.lists() });
   * ```
   */
  lists: () => [...usage_recordKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = usage_recordKeys.list({ is_active: true });
   *
   * // List with search
   * const key = usage_recordKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = usage_recordKeys.list();
   * ```
   */
  list: (filters?: UsageRecordFilterOptions) =>
    [...usage_recordKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: usage_recordKeys.details() });
   * ```
   */
  details: () => [...usage_recordKeys.all, 'detail'] as const,

  /**
   * Key for a specific usage_record by ID
   * Use this for fetching and invalidating individual usage_record records
   *
   * @param id - UsageRecord ID (UUID)
   * @returns Query key tuple for specific usage_record
   *
   * @example
   * ```typescript
   * // Fetch specific usage_record
   * const { data } = useQuery({
   *   queryKey: usage_recordKeys.detail('uuid-here'),
   *   queryFn: () => usage_recordService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: usage_recordKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...usage_recordKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active usage_records (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = usage_recordKeys.dropdown();
   *
   * // Include inactive items
   * const key = usage_recordKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...usage_recordKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * UsageRecords filtered by user ID
   *
   * @param userId - User ID (UUID)
   * @returns Query key tuple
   */
  byUser: (userId: string) =>
    [...usage_recordKeys.all, 'by-user', userId] as const,
} as const;

/**
 * Type representing any valid UsageRecord query key
 * Useful for typing query key arrays in generic contexts
 */
export type UsageRecordQueryKey =
  | typeof usage_recordKeys.all
  | ReturnType<typeof usage_recordKeys.lists>
  | ReturnType<typeof usage_recordKeys.list>
  | ReturnType<typeof usage_recordKeys.details>
  | ReturnType<typeof usage_recordKeys.detail>
  | ReturnType<typeof usage_recordKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default usage_recordKeys;
