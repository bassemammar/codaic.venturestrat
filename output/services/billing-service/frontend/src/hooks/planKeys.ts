/**
 * Plan Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all plan queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['plans'] - All plan queries
 * - ['plans', 'list'] - All list queries
 * - ['plans', 'list', { filters }] - Specific filtered list
 * - ['plans', 'detail', id] - Specific plan by ID
 *
 * @example
 * ```typescript
 * import { planKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: planKeys.list({ is_active: true }),
 *   queryFn: () => planService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all plan queries
 * queryClient.invalidateQueries({ queryKey: planKeys.all });
 *
 * // Invalidate specific plan
 * queryClient.invalidateQueries({ queryKey: planKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:43.327531Z
 */

import type { PlanFilterOptions } from '../types/plan.types';

/**
 * Root query key for Plan entity
 * Used as namespace prefix for all plan queries
 */
const PLAN_ROOT = 'plans' as const;

/**
 * Plan Query Keys
 *
 * Hierarchical query key factory for plan queries:
 * - all: Base key for all plan queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific plan by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = planKeys.list({ is_active: true });
 * // Result: ['plans', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = planKeys.detail('uuid-here');
 * // Result: ['plans', 'detail', 'uuid-here']
 *
 * // Invalidate all plan data
 * queryClient.invalidateQueries({ queryKey: planKeys.all });
 * ```
 */
export const planKeys = {
  /**
   * Base key for all plan queries
   * Use this to invalidate all plan-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: planKeys.all });
   * ```
   */
  all: [PLAN_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: planKeys.lists() });
   * ```
   */
  lists: () => [...planKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = planKeys.list({ is_active: true });
   *
   * // List with search
   * const key = planKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = planKeys.list();
   * ```
   */
  list: (filters?: PlanFilterOptions) =>
    [...planKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: planKeys.details() });
   * ```
   */
  details: () => [...planKeys.all, 'detail'] as const,

  /**
   * Key for a specific plan by ID
   * Use this for fetching and invalidating individual plan records
   *
   * @param id - Plan ID (UUID)
   * @returns Query key tuple for specific plan
   *
   * @example
   * ```typescript
   * // Fetch specific plan
   * const { data } = useQuery({
   *   queryKey: planKeys.detail('uuid-here'),
   *   queryFn: () => planService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: planKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...planKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active plans (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = planKeys.dropdown();
   *
   * // Include inactive items
   * const key = planKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...planKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * Key for plan by code lookup
   * Use this when fetching by business code instead of UUID
   *
   * @param code - Plan code (e.g., 'USD', 'EUR')
   * @returns Query key tuple for code lookup
   *
   * @example
   * ```typescript
   * const { data } = useQuery({
   *   queryKey: planKeys.byCode('USD'),
   *   queryFn: () => planService.getByCode('USD'),
   * });
   * ```
   */
  byCode: (code: string) =>
    [...planKeys.all, 'by-code', code.toUpperCase()] as const,
} as const;

/**
 * Type representing any valid Plan query key
 * Useful for typing query key arrays in generic contexts
 */
export type PlanQueryKey =
  | typeof planKeys.all
  | ReturnType<typeof planKeys.lists>
  | ReturnType<typeof planKeys.list>
  | ReturnType<typeof planKeys.details>
  | ReturnType<typeof planKeys.detail>
  | ReturnType<typeof planKeys.dropdown>
  | ReturnType<typeof planKeys.byCode>
;

/**
 * Default export for convenience
 */
export default planKeys;
