/**
 * Subscription Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all subscription queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['subscriptions'] - All subscription queries
 * - ['subscriptions', 'list'] - All list queries
 * - ['subscriptions', 'list', { filters }] - Specific filtered list
 * - ['subscriptions', 'detail', id] - Specific subscription by ID
 *
 * @example
 * ```typescript
 * import { subscriptionKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: subscriptionKeys.list({ is_active: true }),
 *   queryFn: () => subscriptionService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all subscription queries
 * queryClient.invalidateQueries({ queryKey: subscriptionKeys.all });
 *
 * // Invalidate specific subscription
 * queryClient.invalidateQueries({ queryKey: subscriptionKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:43.875551Z
 */

import type { SubscriptionFilterOptions } from '../types/subscription.types';

/**
 * Root query key for Subscription entity
 * Used as namespace prefix for all subscription queries
 */
const SUBSCRIPTION_ROOT = 'subscriptions' as const;

/**
 * Subscription Query Keys
 *
 * Hierarchical query key factory for subscription queries:
 * - all: Base key for all subscription queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific subscription by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = subscriptionKeys.list({ is_active: true });
 * // Result: ['subscriptions', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = subscriptionKeys.detail('uuid-here');
 * // Result: ['subscriptions', 'detail', 'uuid-here']
 *
 * // Invalidate all subscription data
 * queryClient.invalidateQueries({ queryKey: subscriptionKeys.all });
 * ```
 */
export const subscriptionKeys = {
  /**
   * Base key for all subscription queries
   * Use this to invalidate all subscription-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: subscriptionKeys.all });
   * ```
   */
  all: [SUBSCRIPTION_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: subscriptionKeys.lists() });
   * ```
   */
  lists: () => [...subscriptionKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = subscriptionKeys.list({ is_active: true });
   *
   * // List with search
   * const key = subscriptionKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = subscriptionKeys.list();
   * ```
   */
  list: (filters?: SubscriptionFilterOptions) =>
    [...subscriptionKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: subscriptionKeys.details() });
   * ```
   */
  details: () => [...subscriptionKeys.all, 'detail'] as const,

  /**
   * Key for a specific subscription by ID
   * Use this for fetching and invalidating individual subscription records
   *
   * @param id - Subscription ID (UUID)
   * @returns Query key tuple for specific subscription
   *
   * @example
   * ```typescript
   * // Fetch specific subscription
   * const { data } = useQuery({
   *   queryKey: subscriptionKeys.detail('uuid-here'),
   *   queryFn: () => subscriptionService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: subscriptionKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...subscriptionKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active subscriptions (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = subscriptionKeys.dropdown();
   *
   * // Include inactive items
   * const key = subscriptionKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...subscriptionKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * Subscriptions filtered by user ID
   *
   * @param userId - User ID (UUID)
   * @returns Query key tuple
   */
  byUser: (userId: string) =>
    [...subscriptionKeys.all, 'by-user', userId] as const,

  /**
   * Subscriptions filtered by plan ID
   *
   * @param planId - Plan ID (UUID)
   * @returns Query key tuple
   */
  byPlan: (planId: string) =>
    [...subscriptionKeys.all, 'by-plan', planId] as const,

  /**
   * Subscriptions filtered by status
   *
   * @param status - Status value
   * @returns Query key tuple
   */
  byStatus: (status: string) =>
    [...subscriptionKeys.all, 'by-status', status] as const,

  /**
   * Subscriptions filtered by stripe customer ID
   *
   * @param stripeCustomerId - StripeCustomer ID (UUID)
   * @returns Query key tuple
   */
  byStripeCustomer: (stripeCustomerId: string) =>
    [...subscriptionKeys.all, 'by-stripe-customer', stripeCustomerId] as const,

  /**
   * Subscriptions filtered by stripe subscription ID
   *
   * @param stripeSubscriptionId - StripeSubscription ID (UUID)
   * @returns Query key tuple
   */
  byStripeSubscription: (stripeSubscriptionId: string) =>
    [...subscriptionKeys.all, 'by-stripe-subscription', stripeSubscriptionId] as const,

  /**
   * Subscriptions filtered by stripe payment method ID
   *
   * @param stripePaymentMethodId - StripePaymentMethod ID (UUID)
   * @returns Query key tuple
   */
  byStripePaymentMethod: (stripePaymentMethodId: string) =>
    [...subscriptionKeys.all, 'by-stripe-payment-method', stripePaymentMethodId] as const,
} as const;

/**
 * Type representing any valid Subscription query key
 * Useful for typing query key arrays in generic contexts
 */
export type SubscriptionQueryKey =
  | typeof subscriptionKeys.all
  | ReturnType<typeof subscriptionKeys.lists>
  | ReturnType<typeof subscriptionKeys.list>
  | ReturnType<typeof subscriptionKeys.details>
  | ReturnType<typeof subscriptionKeys.detail>
  | ReturnType<typeof subscriptionKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default subscriptionKeys;
