/**
 * UsageRecord Type Definitions
 *
 * This file contains type definitions for the UsageRecord entity:
 * - UsageRecord: Full entity model with all fields
 * - UsageRecordCreateRequest: Data for creating a new usage_record
 * - UsageRecordUpdateRequest: Partial data for updating a usage_record
 * - UsageRecordListResponse: Paginated list response
 * - UsageRecordFilterOptions: Query parameters for filtering
 * - UsageRecordDropdownOption: Options for select components
 *
 * Daily usage tracking for subscription limit enforcement
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:44.274855Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// UsageRecord Entity
// ============================================================================

/**
 * UsageRecord entity model
 *
 * Represents the full usage_record entity as returned by the API.
 * Daily usage tracking for subscription limit enforcement
 *
 * @remarks
 * This interface matches the backend UsageRecordDTO schema exactly.
 */
export interface UsageRecord {
  /** Unique identifier (UUID) */
  id: string;

  /** User Id */
  user_id: string;

  /** Tracking date */
  date: string;

  /** Month number (1-12) */
  month: number;

  /** Year */
  year: number;

  /** Ai Drafts Used */
  ai_drafts_used: number;

  /** Emails Sent */
  emails_sent: number;

  /** Investors Added */
  investors_added: number;

  /** Monthly Emails Sent */
  monthly_emails_sent: number;

  /** Monthly Investors Added */
  monthly_investors_added: number;

  /** Monthly Follow Ups Sent */
  monthly_follow_ups_sent: number;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * UsageRecord creation request data
 *
 * Used when creating a new usage_record. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend UsageRecordCreateDTO schema.
 */
export interface UsageRecordCreateRequest {
  /** User Id (required) */
  user_id: string;

  /** Tracking date (required) */
  date: string;

  /** Month number (1-12) (required) */
  month: number;

  /** Year (required) */
  year: number;

  /** Ai Drafts Used (optional) */
  ai_drafts_used?: number;

  /** Emails Sent (optional) */
  emails_sent?: number;

  /** Investors Added (optional) */
  investors_added?: number;

  /** Monthly Emails Sent (optional) */
  monthly_emails_sent?: number;

  /** Monthly Investors Added (optional) */
  monthly_investors_added?: number;

  /** Monthly Follow Ups Sent (optional) */
  monthly_follow_ups_sent?: number;

  /** Active status (defaults to true for new usage_records) */
  is_active?: boolean;
}

/**
 * UsageRecord update request data
 *
 * Used when updating an existing usage_record. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend UsageRecordUpdateDTO schema.
 */
export interface UsageRecordUpdateRequest {
  /** User Id */
  user_id?: string;

  /** Tracking date */
  date?: string;

  /** Month number (1-12) */
  month?: number;

  /** Year */
  year?: number;

  /** Ai Drafts Used */
  ai_drafts_used?: number;

  /** Emails Sent */
  emails_sent?: number;

  /** Investors Added */
  investors_added?: number;

  /** Monthly Emails Sent */
  monthly_emails_sent?: number;

  /** Monthly Investors Added */
  monthly_investors_added?: number;

  /** Monthly Follow Ups Sent */
  monthly_follow_ups_sent?: number;

  /** Active status */
  is_active?: boolean;
}

/**
 * UsageRecord list response
 *
 * Wrapper for paginated usage_record list queries.
 */
export interface UsageRecordListResponse {
  /** Array of usage_record items */
  items: UsageRecord[];

  /** Alternative property name for items (for API compatibility) */
  usage_records?: UsageRecord[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * UsageRecord filter options for list queries
 *
 * Extends BaseEntityFilters with usage_record-specific filtering options.
 */
export interface UsageRecordFilterOptions extends BaseEntityFilters {
  /** Filter by user id */
  user_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * UsageRecord dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface UsageRecordDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: UsageRecord;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * UsageRecord summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface UsageRecordSummary {
  /** Unique identifier */
  id: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * UsageRecord form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type UsageRecordFormData = UsageRecordCreateRequest;

/**
 * Default form values for new usage_record
 *
 * Use this when initializing form state for creating a new usage_record.
 */
export const DEFAULT_USAGE_RECORD_FORM_DATA: Partial<UsageRecordFormData> = {
  is_active: true,
  ai_drafts_used: 0,
  emails_sent: 0,
  investors_added: 0,
  monthly_emails_sent: 0,
  monthly_investors_added: 0,
  monthly_follow_ups_sent: 0,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single usage_record API response
 */
export interface UsageRecordResponse {
  /** The requested usage_record entity */
  data: UsageRecord;
}

/**
 * UsageRecord mutation response (create/update)
 */
export interface UsageRecordMutationResponse {
  /** The created or updated usage_record */
  data: UsageRecord;

  /** Success message */
  message?: string;
}

/**
 * API error response for usage_record operations
 */
export interface UsageRecordApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a UsageRecord
 *
 * @param value - Value to check
 * @returns True if value is a UsageRecord
 */
export function isUsageRecord(value: unknown): value is UsageRecord {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.user_id === 'string' &&
    typeof obj.date === 'string' &&
    typeof obj.month === 'number' &&
    typeof obj.year === 'number'
  );
}

/**
 * Type guard to check if a value is a UsageRecordDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a UsageRecordDropdownOption
 */
export function isUsageRecordDropdownOption(
  value: unknown
): value is UsageRecordDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isUsageRecord(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All usage_record types re-exported for convenience */
export type {
  UsageRecord as UsageRecordEntity,
  UsageRecordCreateRequest as CreateUsageRecordInput,
  UsageRecordUpdateRequest as UpdateUsageRecordInput,
};
