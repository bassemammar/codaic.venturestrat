/**
 * Plan Type Definitions
 *
 * This file contains type definitions for the Plan entity:
 * - Plan: Full entity model with all fields
 * - PlanCreateRequest: Data for creating a new plan
 * - PlanUpdateRequest: Partial data for updating a plan
 * - PlanListResponse: Paginated list response
 * - PlanFilterOptions: Query parameters for filtering
 * - PlanDropdownOption: Options for select components
 *
 * Subscription plan tier definition
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:43.299808Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// Plan Entity
// ============================================================================

/**
 * Plan entity model
 *
 * Represents the full plan entity as returned by the API.
 * Subscription plan tier definition
 *
 * @remarks
 * This interface matches the backend PlanDTO schema exactly.
 */
export interface Plan {
  /** Unique identifier (UUID) */
  id: string;

  /** Name */
  name: string;

  /** Code */
  code: string;

  /** Price Monthly */
  price_monthly: number;

  /** Price Quarterly */
  price_quarterly: number | null;

  /** Price Annually */
  price_annually: number | null;

  /** Usage limits: {ai_drafts_per_day, emails_per_day, emails_per_month, investors_per_day, investors_per_month, follow_ups_per_month} */
  limits: Record<string, any>;

  /** Feature flags: {show_full_contact_info, advanced_filters, priority_support, custom_integrations, can_download_csv} */
  features: Record<string, any>;

  /** Whether limits are enforced daily (free) or monthly (paid) */
  usage_basis: string;

  /** Is Active */
  is_active: boolean;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * Plan creation request data
 *
 * Used when creating a new plan. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend PlanCreateDTO schema.
 */
export interface PlanCreateRequest {
  /** Name (required) */
  name: string;

  /** Code (required) */
  code: string;

  /** Price Monthly (optional) */
  price_monthly?: number;

  /** Price Quarterly (optional) */
  price_quarterly?: number;

  /** Price Annually (optional) */
  price_annually?: number;

  /** Usage limits: {ai_drafts_per_day, emails_per_day, emails_per_month, investors_per_day, investors_per_month, follow_ups_per_month} (required) */
  limits: Record<string, any>;

  /** Feature flags: {show_full_contact_info, advanced_filters, priority_support, custom_integrations, can_download_csv} (required) */
  features: Record<string, any>;

  /** Whether limits are enforced daily (free) or monthly (paid) (required) */
  usage_basis: string;

  /** Is Active (optional) */
  is_active?: boolean;

}

/**
 * Plan update request data
 *
 * Used when updating an existing plan. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend PlanUpdateDTO schema.
 */
export interface PlanUpdateRequest {
  /** Name */
  name?: string;

  /** Code */
  code?: string;

  /** Price Monthly */
  price_monthly?: number;

  /** Price Quarterly */
  price_quarterly?: number | null;

  /** Price Annually */
  price_annually?: number | null;

  /** Usage limits: {ai_drafts_per_day, emails_per_day, emails_per_month, investors_per_day, investors_per_month, follow_ups_per_month} */
  limits?: Record<string, any>;

  /** Feature flags: {show_full_contact_info, advanced_filters, priority_support, custom_integrations, can_download_csv} */
  features?: Record<string, any>;

  /** Whether limits are enforced daily (free) or monthly (paid) */
  usage_basis?: string;

  /** Is Active */
  is_active?: boolean;

}

/**
 * Plan list response
 *
 * Wrapper for paginated plan list queries.
 */
export interface PlanListResponse {
  /** Array of plan items */
  items: Plan[];

  /** Alternative property name for items (for API compatibility) */
  plans?: Plan[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * Plan filter options for list queries
 *
 * Extends BaseEntityFilters with plan-specific filtering options.
 */
export interface PlanFilterOptions extends BaseEntityFilters {
}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * Plan dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface PlanDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: Plan;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * Plan summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface PlanSummary {
  /** Unique identifier */
  id: string;

  /** Name */
  name: string;

  /** Code */
  code: string;

}

// ============================================================================
// Form Types
// ============================================================================

/**
 * Plan form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type PlanFormData = PlanCreateRequest;

/**
 * Default form values for new plan
 *
 * Use this when initializing form state for creating a new plan.
 */
export const DEFAULT_PLAN_FORM_DATA: Partial<PlanFormData> = {
  price_monthly: "0.00",
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single plan API response
 */
export interface PlanResponse {
  /** The requested plan entity */
  data: Plan;
}

/**
 * Plan mutation response (create/update)
 */
export interface PlanMutationResponse {
  /** The created or updated plan */
  data: Plan;

  /** Success message */
  message?: string;
}

/**
 * API error response for plan operations
 */
export interface PlanApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a Plan
 *
 * @param value - Value to check
 * @returns True if value is a Plan
 */
export function isPlan(value: unknown): value is Plan {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.name === 'string' &&
    typeof obj.code === 'string' &&
    typeof obj.limits === 'object' &&
    typeof obj.features === 'object' &&
    typeof obj.usage_basis === 'string'
  );
}

/**
 * Type guard to check if a value is a PlanDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a PlanDropdownOption
 */
export function isPlanDropdownOption(
  value: unknown
): value is PlanDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isPlan(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All plan types re-exported for convenience */
export type {
  Plan as PlanEntity,
  PlanCreateRequest as CreatePlanInput,
  PlanUpdateRequest as UpdatePlanInput,
};
