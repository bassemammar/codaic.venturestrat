/**
 * Subscription Test Fixtures
 *
 * Test data fixtures for Subscription E2E and unit tests.
 * Provides predefined test data and factory functions for generating
 * test subscriptions.
 *
 * Fixture patterns:
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields with realistic test values
 * - invalid: Invalid data for validation testing
 * - unique: Unique values using timestamps for test isolation
 *
 * User subscription linked to Stripe
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:44.039657Z
 */

import type { SubscriptionCreateRequest } from '../../types/subscription.types';

// ============================================================================
// Types
// ============================================================================

/**
 * Test data pattern types
 *
 * @description
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields populated with realistic test data
 * - invalid: Data that should fail validation
 * - unique: Data with timestamps for test isolation
 */
export type TestDataPattern = 'minimal' | 'complete' | 'invalid' | 'unique';

/**
 * Subscription test fixture containing all test data patterns
 */
export interface SubscriptionTestFixtures {
  /** Minimal valid subscription with only required fields */
  minimal: SubscriptionCreateRequest;
  /** Complete subscription with all fields populated */
  complete: SubscriptionCreateRequest;
  /** Invalid subscription for testing validation errors */
  invalid: Partial<SubscriptionCreateRequest>;
  /** Unique subscription with timestamp-based values */
  unique: SubscriptionCreateRequest;
  /** Multiple subscriptions for list testing */
  list: SubscriptionCreateRequest[];
}

// ============================================================================
// Helper Functions
// ============================================================================

/**
 * Generate a unique identifier based on current timestamp
 * Useful for creating unique test data that won't conflict
 *
 * @param prefix - Optional prefix for the identifier
 * @returns Unique string identifier
 */
function generateUniqueId(prefix = ''): string {
  const timestamp = Date.now();
  const random = Math.random().toString(36).substring(2, 8);
  return prefix ? `${prefix}-${timestamp}-${random}` : `${timestamp}-${random}`;
}

/**
 * Generate a random string of specified length
 *
 * @param length - Length of the string to generate
 * @param charset - Character set to use (default: alphanumeric)
 * @returns Random string
 */
function randomString(
  length: number,
  charset = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'
): string {
  let result = '';
  for (let i = 0; i < length; i++) {
    result += charset.charAt(Math.floor(Math.random() * charset.length));
  }
  return result;
}

/**
 * Generate a random integer between min and max (inclusive)
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @returns Random integer
 */
function randomInt(min: number, max: number): number {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

/**
 * Generate a random decimal with specified precision
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @param precision - Number of decimal places
 * @returns Random decimal number
 */
function randomDecimal(min: number, max: number, precision = 2): number {
  const value = Math.random() * (max - min) + min;
  const factor = Math.pow(10, precision);
  return Math.round(value * factor) / factor;
}

/**
 * Generate a random date within a range
 *
 * @param startDate - Start of the range
 * @param endDate - End of the range
 * @returns Random date as ISO string
 */
function randomDate(startDate: Date, endDate: Date): string {
  const start = startDate.getTime();
  const end = endDate.getTime();
  const randomTime = start + Math.random() * (end - start);
  return new Date(randomTime).toISOString().split('T')[0];
}

/**
 * Pick a random element from an array
 *
 * @param array - Array to pick from
 * @returns Random element
 */
function randomPick<T>(array: T[]): T {
  return array[Math.floor(Math.random() * array.length)];
}

// ============================================================================
// Static Test Data
// ============================================================================


/**
 * Sample subscription names for generating realistic test data
 */
const SAMPLE_NAMES = [
  'Test Subscription Alpha',
  'Test Subscription Beta',
  'Test Subscription Gamma',
  'Sample Subscription One',
  'Sample Subscription Two',
  'Demo Subscription',
  'Example Subscription',
];

/**
 * Sample codes for generating realistic test data
 */
const SAMPLE_CODES = [
  'TST001',
  'TST002',
  'TST003',
  'DEMO01',
  'DEMO02',
  'SMPL01',
  'SMPL02',
];

/**
 * Sample descriptions for generating realistic test data
 */
const SAMPLE_DESCRIPTIONS = [
  'This is a test subscription for automated testing.',
  'Sample subscription created for E2E tests.',
  'Demo subscription with all fields populated.',
  'Example subscription for validation testing.',
  'Test data fixture for subscription operations.',
];

// ============================================================================
// Predefined Fixtures
// ============================================================================

/**
 * Minimal valid subscription fixture
 *
 * Contains only required fields with minimal valid values.
 * Use this when testing that minimal data is accepted.
 */
export const minimalSubscriptionFixture: SubscriptionCreateRequest = {
};

/**
 * Complete subscription fixture
 *
 * Contains all fields with realistic test values.
 * Use this when testing full CRUD operations.
 */
export const completeSubscriptionFixture: SubscriptionCreateRequest = {
  user_id: 'Test User Id',
  plan_id: 'Test Plan Id',
  status: 'Test Status',
  stripe_customer_id: 'Test Stripe Customer Id',
  stripe_subscription_id: 'Test Stripe Subscription Id',
  stripe_payment_method_id: 'Test Stripe Payment Method Id',
  billing_period: 'Test Billing Period',
  current_period_end: 'Test Current Period End',
  cancel_at_period_end: true,
  trial_ends_at: 'Test Trial Ends At',
};

/**
 * Unique subscription fixture
 *
 * Contains all fields with timestamp-based unique values.
 * Use this when testing to ensure data isolation between test runs.
 * Each field that needs uniqueness includes a timestamp/random component.
 */
export const uniqueSubscriptionFixture: SubscriptionCreateRequest = {
  user_id: `User Id ${generateUniqueId()}`,
  plan_id: `Plan Id ${generateUniqueId()}`,
  status: `Status ${generateUniqueId()}`,
  stripe_customer_id: `Stripe Customer Id ${generateUniqueId()}`,
  stripe_subscription_id: `Stripe Subscription Id ${generateUniqueId()}`,
  stripe_payment_method_id: `Stripe Payment Method Id ${generateUniqueId()}`,
  billing_period: `Billing Period ${generateUniqueId()}`,
  current_period_end: `Current Period End ${generateUniqueId()}`,
  cancel_at_period_end: true,
  trial_ends_at: `Trial Ends At ${generateUniqueId()}`,
};

/**
 * Invalid subscription fixture
 *
 * Contains invalid data that should fail validation.
 * Use this when testing validation error handling.
 * Covers: empty required fields, values out of range, invalid formats.
 */
export const invalidSubscriptionFixture: Partial<SubscriptionCreateRequest> = {
};

/**
 * Invalid data patterns for comprehensive validation testing
 *
 * Contains various invalid data scenarios:
 * - emptyRequired: All required fields left empty
 * - tooShort: String fields below minimum length
 * - tooLong: String fields exceeding maximum length
 * - outOfRangeMin: Numeric fields below minimum value
 * - outOfRangeMax: Numeric fields above maximum value
 * - wrongType: Fields with incorrect data types (as strings for type coercion testing)
 * - specialChars: Fields with potentially problematic characters
 */
export const subscriptionInvalidPatterns = {
  /** All required string fields empty */
  emptyRequired: {
  } as Partial<SubscriptionCreateRequest>,


  /** String fields exceeding maximum length */
  tooLong: {
    user_id: 'x'.repeat(150), // max: 100
    status: 'x'.repeat(80), // max: 30
    stripe_customer_id: 'x'.repeat(150), // max: 100
    stripe_subscription_id: 'x'.repeat(150), // max: 100
    stripe_payment_method_id: 'x'.repeat(150), // max: 100
    billing_period: 'x'.repeat(70), // max: 20
  } as Partial<SubscriptionCreateRequest>,



  /** Fields with SQL injection attempts (for security testing) */
  sqlInjection: {
  } as Partial<SubscriptionCreateRequest>,

  /** Fields with XSS attempts (for security testing) */
  xssAttempts: {
  } as Partial<SubscriptionCreateRequest>,

  /** Fields with null values (for nullable field testing) */
  nullValues: {
    stripe_customer_id: null,
    stripe_subscription_id: null,
    stripe_payment_method_id: null,
    billing_period: null,
    current_period_end: null,
    trial_ends_at: null,
  } as Partial<SubscriptionCreateRequest>,

  /** Fields with undefined values (for missing field testing) */
  undefinedValues: {
    // Intentionally empty - represents missing fields
  } as Partial<SubscriptionCreateRequest>,
};

/**
 * Collection of subscription fixtures for list testing
 *
 * Contains multiple subscriptions for testing list operations,
 * pagination, filtering, and sorting.
 */
export const subscriptionListFixtures: SubscriptionCreateRequest[] = [
  {
    user_id: 'User Id 1',
    plan_id: 'Plan Id 1',
    status: 'Status 1',
    stripe_customer_id: 'Stripe Customer Id 1',
    stripe_subscription_id: 'Stripe Subscription Id 1',
    stripe_payment_method_id: 'Stripe Payment Method Id 1',
    billing_period: 'Billing Period 1',
    current_period_end: 'Current Period End 1',
    cancel_at_period_end: true,
    trial_ends_at: 'Trial Ends At 1',
  },
  {
    user_id: 'User Id 2',
    plan_id: 'Plan Id 2',
    status: 'Status 2',
    stripe_customer_id: 'Stripe Customer Id 2',
    stripe_subscription_id: 'Stripe Subscription Id 2',
    stripe_payment_method_id: 'Stripe Payment Method Id 2',
    billing_period: 'Billing Period 2',
    current_period_end: 'Current Period End 2',
    cancel_at_period_end: false,
    trial_ends_at: 'Trial Ends At 2',
  },
  {
    user_id: 'User Id 3',
    plan_id: 'Plan Id 3',
    status: 'Status 3',
    stripe_customer_id: 'Stripe Customer Id 3',
    stripe_subscription_id: 'Stripe Subscription Id 3',
    stripe_payment_method_id: 'Stripe Payment Method Id 3',
    billing_period: 'Billing Period 3',
    current_period_end: 'Current Period End 3',
    cancel_at_period_end: true,
    trial_ends_at: 'Trial Ends At 3',
  },
  {
    user_id: 'User Id 4',
    plan_id: 'Plan Id 4',
    status: 'Status 4',
    stripe_customer_id: 'Stripe Customer Id 4',
    stripe_subscription_id: 'Stripe Subscription Id 4',
    stripe_payment_method_id: 'Stripe Payment Method Id 4',
    billing_period: 'Billing Period 4',
    current_period_end: 'Current Period End 4',
    cancel_at_period_end: false,
    trial_ends_at: 'Trial Ends At 4',
  },
  {
    user_id: 'User Id 5',
    plan_id: 'Plan Id 5',
    status: 'Status 5',
    stripe_customer_id: 'Stripe Customer Id 5',
    stripe_subscription_id: 'Stripe Subscription Id 5',
    stripe_payment_method_id: 'Stripe Payment Method Id 5',
    billing_period: 'Billing Period 5',
    current_period_end: 'Current Period End 5',
    cancel_at_period_end: true,
    trial_ends_at: 'Trial Ends At 5',
  },
];

// ============================================================================
// Fixture Collection
// ============================================================================

/**
 * All subscription test fixtures
 *
 * @example
 * ```typescript
 * import { subscriptionFixtures } from '../fixtures/subscriptionFixtures';
 *
 * // Use minimal fixture
 * const minData = subscriptionFixtures.minimal;
 *
 * // Use complete fixture
 * const fullData = subscriptionFixtures.complete;
 *
 * // Use unique fixture (with timestamp)
 * const uniqueData = generateSubscriptionTestData('unique');
 * ```
 */
export const subscriptionFixtures: SubscriptionTestFixtures = {
  minimal: minimalSubscriptionFixture,
  complete: completeSubscriptionFixture,
  invalid: invalidSubscriptionFixture as SubscriptionCreateRequest,
  unique: uniqueSubscriptionFixture,
  list: subscriptionListFixtures,
};

// ============================================================================
// Factory Functions
// ============================================================================

/**
 * Generate subscription test data based on pattern
 *
 * @param pattern - The test data pattern to generate
 * @param overrides - Optional field overrides
 * @returns Generated test data
 *
 * @example
 * ```typescript
 * // Generate complete test data
 * const data = generateSubscriptionTestData('complete');
 *
 * // Generate unique test data with overrides
 * const customData = generateSubscriptionTestData('unique', {
 *   is_active: false,
 * });
 * ```
 */
export function generateSubscriptionTestData(
  pattern: TestDataPattern = 'complete',
  overrides: Partial<SubscriptionCreateRequest> = {}
): SubscriptionCreateRequest {
  let baseData: SubscriptionCreateRequest;

  switch (pattern) {
    case 'minimal':
      baseData = { ...minimalSubscriptionFixture };
      break;

    case 'complete':
      baseData = { ...completeSubscriptionFixture };
      break;

    case 'invalid':
      // Return invalid data (caller should handle validation failure)
      baseData = { ...invalidSubscriptionFixture } as SubscriptionCreateRequest;
      break;

    case 'unique':
      // Generate fresh unique values for each call
      baseData = {
        ...uniqueSubscriptionFixture,
      };
      break;

    default:
      baseData = { ...completeSubscriptionFixture };
  }

  return { ...baseData, ...overrides };
}

/**
 * Generate multiple subscription fixtures
 *
 * @param count - Number of fixtures to generate
 * @param pattern - The test data pattern to use as base
 * @param modifier - Optional function to modify each fixture
 * @returns Array of generated test data
 *
 * @example
 * ```typescript
 * // Generate 5 unique fixtures
 * const fixtures = generateMultipleSubscriptionFixtures(5, 'unique');
 *
 * // Generate fixtures with sequential numbering
 * const numbered = generateMultipleSubscriptionFixtures(3, 'complete', (data, index) => ({
 *   ...data,
 *   name: `Subscription #${index + 1}`,
 * }));
 * ```
 */
export function generateMultipleSubscriptionFixtures(
  count: number,
  pattern: TestDataPattern = 'unique',
  modifier?: (data: SubscriptionCreateRequest, index: number) => SubscriptionCreateRequest
): SubscriptionCreateRequest[] {
  const fixtures: SubscriptionCreateRequest[] = [];

  for (let i = 0; i < count; i++) {
    let fixture = generateSubscriptionTestData(pattern);

    if (modifier) {
      fixture = modifier(fixture, i);
    } else if (pattern === 'unique') {
      // Add index to ensure uniqueness even with same timestamp
    }

    fixtures.push(fixture);
  }

  return fixtures;
}

/**
 * Generate random subscription test data
 *
 * Creates test data with randomized values within valid ranges.
 * Useful for fuzz testing and edge case discovery.
 *
 * @param overrides - Optional field overrides
 * @returns Randomly generated test data
 *
 * @example
 * ```typescript
 * // Generate random test data
 * const randomData = generateRandomSubscriptionData();
 *
 * // Generate random data with specific field
 * const customRandom = generateRandomSubscriptionData({
 *   is_active: true,
 * });
 * ```
 */
export function generateRandomSubscriptionData(
  overrides: Partial<SubscriptionCreateRequest> = {}
): SubscriptionCreateRequest {
  const data: SubscriptionCreateRequest = {
    user_id: randomString(randomInt(None, Math.min(100, 50))),
    plan_id: randomString(randomInt(None, Math.min(None, 50))),
    status: randomString(randomInt(None, Math.min(30, 50))),
    stripe_customer_id: randomString(randomInt(None, Math.min(100, 50))),
    stripe_subscription_id: randomString(randomInt(None, Math.min(100, 50))),
    stripe_payment_method_id: randomString(randomInt(None, Math.min(100, 50))),
    billing_period: randomString(randomInt(None, Math.min(20, 50))),
    current_period_end: randomString(randomInt(None, Math.min(None, 50))),
    cancel_at_period_end: Math.random() > 0.5,
    trial_ends_at: randomString(randomInt(None, Math.min(None, 50))),
  };

  return { ...data, ...overrides };
}

// ============================================================================
// Validation Test Data
// ============================================================================

/**
 * Edge case test data for boundary testing
 *
 * Contains values at the boundaries of valid ranges.
 */
export const subscriptionEdgeCases = {
  /** user_id at maximum length (100 chars) */
  user_idMaxLength: 'x'.repeat(100),
  /** status at maximum length (30 chars) */
  statusMaxLength: 'x'.repeat(30),
  /** stripe_customer_id at maximum length (100 chars) */
  stripe_customer_idMaxLength: 'x'.repeat(100),
  /** stripe_subscription_id at maximum length (100 chars) */
  stripe_subscription_idMaxLength: 'x'.repeat(100),
  /** stripe_payment_method_id at maximum length (100 chars) */
  stripe_payment_method_idMaxLength: 'x'.repeat(100),
  /** billing_period at maximum length (20 chars) */
  billing_periodMaxLength: 'x'.repeat(20),
};

/**
 * Special character test data for input sanitization testing
 */
export const subscriptionSpecialCharacterData: Partial<SubscriptionCreateRequest> = {
};

/**
 * Whitespace test data for trimming validation
 */
export const subscriptionWhitespaceData: Partial<SubscriptionCreateRequest> = {
};

// ============================================================================
// Exports
// ============================================================================

// Default export - main fixture collection
export default subscriptionFixtures;
