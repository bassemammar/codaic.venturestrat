/**
 * UsageRecord Test Fixtures
 *
 * Test data fixtures for UsageRecord E2E and unit tests.
 * Provides predefined test data and factory functions for generating
 * test usage_records.
 *
 * Fixture patterns:
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields with realistic test values
 * - invalid: Invalid data for validation testing
 * - unique: Unique values using timestamps for test isolation
 *
 * Daily usage tracking for subscription limit enforcement
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:44.524386Z
 */

import type { UsageRecordCreateRequest } from '../../types/usage_record.types';

// ============================================================================
// Types
// ============================================================================

/**
 * Test data pattern types
 *
 * @description
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields populated with realistic test data
 * - invalid: Data that should fail validation
 * - unique: Data with timestamps for test isolation
 */
export type TestDataPattern = 'minimal' | 'complete' | 'invalid' | 'unique';

/**
 * UsageRecord test fixture containing all test data patterns
 */
export interface UsageRecordTestFixtures {
  /** Minimal valid usage_record with only required fields */
  minimal: UsageRecordCreateRequest;
  /** Complete usage_record with all fields populated */
  complete: UsageRecordCreateRequest;
  /** Invalid usage_record for testing validation errors */
  invalid: Partial<UsageRecordCreateRequest>;
  /** Unique usage_record with timestamp-based values */
  unique: UsageRecordCreateRequest;
  /** Multiple usage_records for list testing */
  list: UsageRecordCreateRequest[];
}

// ============================================================================
// Helper Functions
// ============================================================================

/**
 * Generate a unique identifier based on current timestamp
 * Useful for creating unique test data that won't conflict
 *
 * @param prefix - Optional prefix for the identifier
 * @returns Unique string identifier
 */
function generateUniqueId(prefix = ''): string {
  const timestamp = Date.now();
  const random = Math.random().toString(36).substring(2, 8);
  return prefix ? `${prefix}-${timestamp}-${random}` : `${timestamp}-${random}`;
}

/**
 * Generate a random string of specified length
 *
 * @param length - Length of the string to generate
 * @param charset - Character set to use (default: alphanumeric)
 * @returns Random string
 */
function randomString(
  length: number,
  charset = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'
): string {
  let result = '';
  for (let i = 0; i < length; i++) {
    result += charset.charAt(Math.floor(Math.random() * charset.length));
  }
  return result;
}

/**
 * Generate a random integer between min and max (inclusive)
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @returns Random integer
 */
function randomInt(min: number, max: number): number {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

/**
 * Generate a random decimal with specified precision
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @param precision - Number of decimal places
 * @returns Random decimal number
 */
function randomDecimal(min: number, max: number, precision = 2): number {
  const value = Math.random() * (max - min) + min;
  const factor = Math.pow(10, precision);
  return Math.round(value * factor) / factor;
}

/**
 * Generate a random date within a range
 *
 * @param startDate - Start of the range
 * @param endDate - End of the range
 * @returns Random date as ISO string
 */
function randomDate(startDate: Date, endDate: Date): string {
  const start = startDate.getTime();
  const end = endDate.getTime();
  const randomTime = start + Math.random() * (end - start);
  return new Date(randomTime).toISOString().split('T')[0];
}

/**
 * Pick a random element from an array
 *
 * @param array - Array to pick from
 * @returns Random element
 */
function randomPick<T>(array: T[]): T {
  return array[Math.floor(Math.random() * array.length)];
}

// ============================================================================
// Static Test Data
// ============================================================================


/**
 * Sample usage_record names for generating realistic test data
 */
const SAMPLE_NAMES = [
  'Test UsageRecord Alpha',
  'Test UsageRecord Beta',
  'Test UsageRecord Gamma',
  'Sample UsageRecord One',
  'Sample UsageRecord Two',
  'Demo UsageRecord',
  'Example UsageRecord',
];

/**
 * Sample codes for generating realistic test data
 */
const SAMPLE_CODES = [
  'TST001',
  'TST002',
  'TST003',
  'DEMO01',
  'DEMO02',
  'SMPL01',
  'SMPL02',
];

/**
 * Sample descriptions for generating realistic test data
 */
const SAMPLE_DESCRIPTIONS = [
  'This is a test usage_record for automated testing.',
  'Sample usage_record created for E2E tests.',
  'Demo usage_record with all fields populated.',
  'Example usage_record for validation testing.',
  'Test data fixture for usage_record operations.',
];

// ============================================================================
// Predefined Fixtures
// ============================================================================

/**
 * Minimal valid usage_record fixture
 *
 * Contains only required fields with minimal valid values.
 * Use this when testing that minimal data is accepted.
 */
export const minimalUsageRecordFixture: UsageRecordCreateRequest = {
};

/**
 * Complete usage_record fixture
 *
 * Contains all fields with realistic test values.
 * Use this when testing full CRUD operations.
 */
export const completeUsageRecordFixture: UsageRecordCreateRequest = {
  user_id: 'Test User Id',
  date: 'Test Date',
  month: 42,
  year: 42,
  ai_drafts_used: 42,
  emails_sent: 42,
  investors_added: 42,
  monthly_emails_sent: 42,
  monthly_investors_added: 42,
  monthly_follow_ups_sent: 42,
};

/**
 * Unique usage_record fixture
 *
 * Contains all fields with timestamp-based unique values.
 * Use this when testing to ensure data isolation between test runs.
 * Each field that needs uniqueness includes a timestamp/random component.
 */
export const uniqueUsageRecordFixture: UsageRecordCreateRequest = {
  user_id: `User Id ${generateUniqueId()}`,
  date: `Date ${generateUniqueId()}`,
  month: Math.floor(Math.random() * 1000),
  year: Math.floor(Math.random() * 1000),
  ai_drafts_used: Math.floor(Math.random() * 1000),
  emails_sent: Math.floor(Math.random() * 1000),
  investors_added: Math.floor(Math.random() * 1000),
  monthly_emails_sent: Math.floor(Math.random() * 1000),
  monthly_investors_added: Math.floor(Math.random() * 1000),
  monthly_follow_ups_sent: Math.floor(Math.random() * 1000),
};

/**
 * Invalid usage_record fixture
 *
 * Contains invalid data that should fail validation.
 * Use this when testing validation error handling.
 * Covers: empty required fields, values out of range, invalid formats.
 */
export const invalidUsageRecordFixture: Partial<UsageRecordCreateRequest> = {
};

/**
 * Invalid data patterns for comprehensive validation testing
 *
 * Contains various invalid data scenarios:
 * - emptyRequired: All required fields left empty
 * - tooShort: String fields below minimum length
 * - tooLong: String fields exceeding maximum length
 * - outOfRangeMin: Numeric fields below minimum value
 * - outOfRangeMax: Numeric fields above maximum value
 * - wrongType: Fields with incorrect data types (as strings for type coercion testing)
 * - specialChars: Fields with potentially problematic characters
 */
export const usageRecordInvalidPatterns = {
  /** All required string fields empty */
  emptyRequired: {
  } as Partial<UsageRecordCreateRequest>,


  /** String fields exceeding maximum length */
  tooLong: {
    user_id: 'x'.repeat(150), // max: 100
  } as Partial<UsageRecordCreateRequest>,



  /** Fields with SQL injection attempts (for security testing) */
  sqlInjection: {
  } as Partial<UsageRecordCreateRequest>,

  /** Fields with XSS attempts (for security testing) */
  xssAttempts: {
  } as Partial<UsageRecordCreateRequest>,

  /** Fields with null values (for nullable field testing) */
  nullValues: {
  } as Partial<UsageRecordCreateRequest>,

  /** Fields with undefined values (for missing field testing) */
  undefinedValues: {
    // Intentionally empty - represents missing fields
  } as Partial<UsageRecordCreateRequest>,
};

/**
 * Collection of usage_record fixtures for list testing
 *
 * Contains multiple usage_records for testing list operations,
 * pagination, filtering, and sorting.
 */
export const usageRecordListFixtures: UsageRecordCreateRequest[] = [
  {
    user_id: 'User Id 1',
    date: 'Date 1',
    month: 10,
    year: 10,
    ai_drafts_used: 10,
    emails_sent: 10,
    investors_added: 10,
    monthly_emails_sent: 10,
    monthly_investors_added: 10,
    monthly_follow_ups_sent: 10,
  },
  {
    user_id: 'User Id 2',
    date: 'Date 2',
    month: 20,
    year: 20,
    ai_drafts_used: 20,
    emails_sent: 20,
    investors_added: 20,
    monthly_emails_sent: 20,
    monthly_investors_added: 20,
    monthly_follow_ups_sent: 20,
  },
  {
    user_id: 'User Id 3',
    date: 'Date 3',
    month: 30,
    year: 30,
    ai_drafts_used: 30,
    emails_sent: 30,
    investors_added: 30,
    monthly_emails_sent: 30,
    monthly_investors_added: 30,
    monthly_follow_ups_sent: 30,
  },
  {
    user_id: 'User Id 4',
    date: 'Date 4',
    month: 40,
    year: 40,
    ai_drafts_used: 40,
    emails_sent: 40,
    investors_added: 40,
    monthly_emails_sent: 40,
    monthly_investors_added: 40,
    monthly_follow_ups_sent: 40,
  },
  {
    user_id: 'User Id 5',
    date: 'Date 5',
    month: 50,
    year: 50,
    ai_drafts_used: 50,
    emails_sent: 50,
    investors_added: 50,
    monthly_emails_sent: 50,
    monthly_investors_added: 50,
    monthly_follow_ups_sent: 50,
  },
];

// ============================================================================
// Fixture Collection
// ============================================================================

/**
 * All usage_record test fixtures
 *
 * @example
 * ```typescript
 * import { usageRecordFixtures } from '../fixtures/usage_recordFixtures';
 *
 * // Use minimal fixture
 * const minData = usageRecordFixtures.minimal;
 *
 * // Use complete fixture
 * const fullData = usageRecordFixtures.complete;
 *
 * // Use unique fixture (with timestamp)
 * const uniqueData = generateUsageRecordTestData('unique');
 * ```
 */
export const usageRecordFixtures: UsageRecordTestFixtures = {
  minimal: minimalUsageRecordFixture,
  complete: completeUsageRecordFixture,
  invalid: invalidUsageRecordFixture as UsageRecordCreateRequest,
  unique: uniqueUsageRecordFixture,
  list: usageRecordListFixtures,
};

// ============================================================================
// Factory Functions
// ============================================================================

/**
 * Generate usage_record test data based on pattern
 *
 * @param pattern - The test data pattern to generate
 * @param overrides - Optional field overrides
 * @returns Generated test data
 *
 * @example
 * ```typescript
 * // Generate complete test data
 * const data = generateUsageRecordTestData('complete');
 *
 * // Generate unique test data with overrides
 * const customData = generateUsageRecordTestData('unique', {
 *   is_active: false,
 * });
 * ```
 */
export function generateUsageRecordTestData(
  pattern: TestDataPattern = 'complete',
  overrides: Partial<UsageRecordCreateRequest> = {}
): UsageRecordCreateRequest {
  let baseData: UsageRecordCreateRequest;

  switch (pattern) {
    case 'minimal':
      baseData = { ...minimalUsageRecordFixture };
      break;

    case 'complete':
      baseData = { ...completeUsageRecordFixture };
      break;

    case 'invalid':
      // Return invalid data (caller should handle validation failure)
      baseData = { ...invalidUsageRecordFixture } as UsageRecordCreateRequest;
      break;

    case 'unique':
      // Generate fresh unique values for each call
      baseData = {
        ...uniqueUsageRecordFixture,
      };
      break;

    default:
      baseData = { ...completeUsageRecordFixture };
  }

  return { ...baseData, ...overrides };
}

/**
 * Generate multiple usage_record fixtures
 *
 * @param count - Number of fixtures to generate
 * @param pattern - The test data pattern to use as base
 * @param modifier - Optional function to modify each fixture
 * @returns Array of generated test data
 *
 * @example
 * ```typescript
 * // Generate 5 unique fixtures
 * const fixtures = generateMultipleUsageRecordFixtures(5, 'unique');
 *
 * // Generate fixtures with sequential numbering
 * const numbered = generateMultipleUsageRecordFixtures(3, 'complete', (data, index) => ({
 *   ...data,
 *   name: `UsageRecord #${index + 1}`,
 * }));
 * ```
 */
export function generateMultipleUsageRecordFixtures(
  count: number,
  pattern: TestDataPattern = 'unique',
  modifier?: (data: UsageRecordCreateRequest, index: number) => UsageRecordCreateRequest
): UsageRecordCreateRequest[] {
  const fixtures: UsageRecordCreateRequest[] = [];

  for (let i = 0; i < count; i++) {
    let fixture = generateUsageRecordTestData(pattern);

    if (modifier) {
      fixture = modifier(fixture, i);
    } else if (pattern === 'unique') {
      // Add index to ensure uniqueness even with same timestamp
    }

    fixtures.push(fixture);
  }

  return fixtures;
}

/**
 * Generate random usage_record test data
 *
 * Creates test data with randomized values within valid ranges.
 * Useful for fuzz testing and edge case discovery.
 *
 * @param overrides - Optional field overrides
 * @returns Randomly generated test data
 *
 * @example
 * ```typescript
 * // Generate random test data
 * const randomData = generateRandomUsageRecordData();
 *
 * // Generate random data with specific field
 * const customRandom = generateRandomUsageRecordData({
 *   is_active: true,
 * });
 * ```
 */
export function generateRandomUsageRecordData(
  overrides: Partial<UsageRecordCreateRequest> = {}
): UsageRecordCreateRequest {
  const data: UsageRecordCreateRequest = {
    user_id: randomString(randomInt(None, Math.min(100, 50))),
    date: randomString(randomInt(None, Math.min(None, 50))),
    month: randomInt(0, 1000),
    year: randomInt(0, 1000),
    ai_drafts_used: randomInt(0, 1000),
    emails_sent: randomInt(0, 1000),
    investors_added: randomInt(0, 1000),
    monthly_emails_sent: randomInt(0, 1000),
    monthly_investors_added: randomInt(0, 1000),
    monthly_follow_ups_sent: randomInt(0, 1000),
  };

  return { ...data, ...overrides };
}

// ============================================================================
// Validation Test Data
// ============================================================================

/**
 * Edge case test data for boundary testing
 *
 * Contains values at the boundaries of valid ranges.
 */
export const usageRecordEdgeCases = {
  /** user_id at maximum length (100 chars) */
  user_idMaxLength: 'x'.repeat(100),
};

/**
 * Special character test data for input sanitization testing
 */
export const usageRecordSpecialCharacterData: Partial<UsageRecordCreateRequest> = {
};

/**
 * Whitespace test data for trimming validation
 */
export const usageRecordWhitespaceData: Partial<UsageRecordCreateRequest> = {
};

// ============================================================================
// Exports
// ============================================================================

// Default export - main fixture collection
export default usageRecordFixtures;
