"""Plan Application Service - Generated by Code Generator v1.0.0"""

from typing import List, Optional
from uuid import UUID
import structlog

from billing_service.infrastructure.repositories.plan_repository import PlanRepository
from billing_service.infrastructure.orm.plan import Plan
from billing_service.schemas.plan import PlanCreate, PlanUpdate, PlanResponse
from billing_service.integrations.events import event_publisher
from billing_service.integrations.metrics import entity_operations_total

logger = structlog.get_logger(__name__)


class PlanService:
    """Application service for Plan use cases."""

    def __init__(self, session=None):
        """Initialize service with repository."""
        self.repository = PlanRepository(session)

    async def create_plan(self, data: PlanCreate, tenant_id: str = None) -> PlanResponse:
        """Create new Plan."""
        try:
            data_dict = data.model_dump()
            if tenant_id:
                data_dict["tenant_id"] = tenant_id

            # BaseModel.create() returns the created instance directly
            created = await self.repository.create(**data_dict)

            # Publish event
            await event_publisher.publish(
                entity_name="plan",
                action="created",
                data=created.to_dict(),
                tenant_id=tenant_id,
            )

            # Track metric
            entity_operations_total.labels(
                entity="plan", operation="create", status="success"
            ).inc()

            # Convert BaseModel to dict for response
            return PlanResponse(**created.to_dict())

        except Exception as e:
            entity_operations_total.labels(entity="plan", operation="create", status="error").inc()
            logger.error("create_plan_failed", error=str(e))
            raise

    async def get_plan(self, id: UUID) -> Optional[PlanResponse]:
        """Get Plan by ID."""
        orm_obj = await self.repository.get_by_id(id)
        return PlanResponse(**orm_obj.to_dict()) if orm_obj else None

    async def list_plans(self, skip: int = 0, limit: int = 100) -> List[PlanResponse]:
        """List Plans with pagination."""
        orm_objs = await self.repository.get_all(skip, limit)
        return [PlanResponse(**obj.to_dict()) for obj in orm_objs]

    async def update_plan(self, id: UUID, data: PlanUpdate) -> Optional[PlanResponse]:
        """Update Plan."""
        try:
            orm_obj = await self.repository.get_by_id(id)
            if not orm_obj:
                return None

            # Update fields from DTO
            for key, value in data.model_dump(exclude_unset=True).items():
                setattr(orm_obj, key, value)

            # BaseModel.save() returns self
            updated = await self.repository.update(orm_obj)

            # Publish event
            await event_publisher.publish(
                entity_name="plan",
                action="updated",
                data=updated.to_dict(),
                tenant_id=getattr(updated, "tenant_id", None),
            )

            # Track metric
            entity_operations_total.labels(
                entity="plan", operation="update", status="success"
            ).inc()

            return PlanResponse(**updated.to_dict())

        except Exception as e:
            entity_operations_total.labels(entity="plan", operation="update", status="error").inc()
            logger.error("update_plan_failed", id=str(id), error=str(e))
            raise

    async def delete_plan(self, id: UUID) -> bool:
        """Delete Plan."""
        try:
            # Get entity before deletion
            orm_obj = await self.repository.get_by_id(id)
            if not orm_obj:
                return False

            entity_data = orm_obj.to_dict()

            # Delete
            deleted = await self.repository.delete(id)

            if deleted:
                # Publish event
                await event_publisher.publish(
                    entity_name="plan",
                    action="deleted",
                    data=entity_data,
                    tenant_id=entity_data.get("tenant_id"),
                )

                # Track metric
                entity_operations_total.labels(
                    entity="plan", operation="delete", status="success"
                ).inc()

            return deleted

        except Exception as e:
            entity_operations_total.labels(entity="plan", operation="delete", status="error").inc()
            logger.error("delete_plan_failed", id=str(id), error=str(e))
            raise

    async def search_plans(
        self, domain: list = None, skip: int = 0, limit: int = 100
    ) -> List[PlanResponse]:
        """
        Search Plans with domain filtering.

        Args:
            domain: domain filter (e.g., [('field', '=', 'value')])
            skip: Offset for pagination
            limit: Maximum records to return

        Returns:
            List of matching Plan instances
        """
        orm_objs = await self.repository.search(domain, skip, limit)
        return [PlanResponse(**obj.to_dict()) for obj in orm_objs]

    async def count_plans(self, domain: list = None) -> int:
        """
        Count Plans matching domain.

        Args:
            domain: Optional domain filter

        Returns:
            Number of matching records
        """
        return await self.repository.count(domain)

    async def bulk_create_plans(self, records_data: List[PlanCreate]) -> List[PlanResponse]:
        """
        Create multiple Plans efficiently.

        Args:
            records_data: List of create DTOs

        Returns:
            List of created Plan instances
        """
        # Convert DTOs to dicts
        data_dicts = [record.model_dump() for record in records_data]

        # Bulk create
        created_objs = await self.repository.bulk_create(data_dicts)

        return [PlanResponse(**obj.to_dict()) for obj in created_objs]
