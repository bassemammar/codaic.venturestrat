/**
 * LifecycleEmail Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all lifecycle_email queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['lifecycleemails'] - All lifecycle_email queries
 * - ['lifecycleemails', 'list'] - All list queries
 * - ['lifecycleemails', 'list', { filters }] - Specific filtered list
 * - ['lifecycleemails', 'detail', id] - Specific lifecycle_email by ID
 *
 * @example
 * ```typescript
 * import { lifecycle_emailKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: lifecycle_emailKeys.list({ is_active: true }),
 *   queryFn: () => lifecycle_emailService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all lifecycle_email queries
 * queryClient.invalidateQueries({ queryKey: lifecycle_emailKeys.all });
 *
 * // Invalidate specific lifecycle_email
 * queryClient.invalidateQueries({ queryKey: lifecycle_emailKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:40.813001Z
 */

import type { LifecycleEmailFilterOptions } from '../types/lifecycle_email.types';

/**
 * Root query key for LifecycleEmail entity
 * Used as namespace prefix for all lifecycle_email queries
 */
const LIFECYCLE_EMAIL_ROOT = 'lifecycleemails' as const;

/**
 * LifecycleEmail Query Keys
 *
 * Hierarchical query key factory for lifecycle_email queries:
 * - all: Base key for all lifecycle_email queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific lifecycle_email by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = lifecycle_emailKeys.list({ is_active: true });
 * // Result: ['lifecycleemails', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = lifecycle_emailKeys.detail('uuid-here');
 * // Result: ['lifecycleemails', 'detail', 'uuid-here']
 *
 * // Invalidate all lifecycle_email data
 * queryClient.invalidateQueries({ queryKey: lifecycle_emailKeys.all });
 * ```
 */
export const lifecycle_emailKeys = {
  /**
   * Base key for all lifecycle_email queries
   * Use this to invalidate all lifecycle_email-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: lifecycle_emailKeys.all });
   * ```
   */
  all: [LIFECYCLE_EMAIL_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: lifecycle_emailKeys.lists() });
   * ```
   */
  lists: () => [...lifecycle_emailKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = lifecycle_emailKeys.list({ is_active: true });
   *
   * // List with search
   * const key = lifecycle_emailKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = lifecycle_emailKeys.list();
   * ```
   */
  list: (filters?: LifecycleEmailFilterOptions) =>
    [...lifecycle_emailKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: lifecycle_emailKeys.details() });
   * ```
   */
  details: () => [...lifecycle_emailKeys.all, 'detail'] as const,

  /**
   * Key for a specific lifecycle_email by ID
   * Use this for fetching and invalidating individual lifecycle_email records
   *
   * @param id - LifecycleEmail ID (UUID)
   * @returns Query key tuple for specific lifecycle_email
   *
   * @example
   * ```typescript
   * // Fetch specific lifecycle_email
   * const { data } = useQuery({
   *   queryKey: lifecycle_emailKeys.detail('uuid-here'),
   *   queryFn: () => lifecycle_emailService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: lifecycle_emailKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...lifecycle_emailKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active lifecycle_emails (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = lifecycle_emailKeys.dropdown();
   *
   * // Include inactive items
   * const key = lifecycle_emailKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...lifecycle_emailKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * LifecycleEmails filtered by user ID
   *
   * @param userId - User ID (UUID)
   * @returns Query key tuple
   */
  byUser: (userId: string) =>
    [...lifecycle_emailKeys.all, 'by-user', userId] as const,

  /**
   * LifecycleEmails filtered by status
   *
   * @param status - Status value
   * @returns Query key tuple
   */
  byStatus: (status: string) =>
    [...lifecycle_emailKeys.all, 'by-status', status] as const,
} as const;

/**
 * Type representing any valid LifecycleEmail query key
 * Useful for typing query key arrays in generic contexts
 */
export type LifecycleEmailQueryKey =
  | typeof lifecycle_emailKeys.all
  | ReturnType<typeof lifecycle_emailKeys.lists>
  | ReturnType<typeof lifecycle_emailKeys.list>
  | ReturnType<typeof lifecycle_emailKeys.details>
  | ReturnType<typeof lifecycle_emailKeys.detail>
  | ReturnType<typeof lifecycle_emailKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default lifecycle_emailKeys;
