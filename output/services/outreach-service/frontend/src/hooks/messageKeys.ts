/**
 * Message Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all message queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['messages'] - All message queries
 * - ['messages', 'list'] - All list queries
 * - ['messages', 'list', { filters }] - Specific filtered list
 * - ['messages', 'detail', id] - Specific message by ID
 *
 * @example
 * ```typescript
 * import { messageKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: messageKeys.list({ is_active: true }),
 *   queryFn: () => messageService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all message queries
 * queryClient.invalidateQueries({ queryKey: messageKeys.all });
 *
 * // Invalidate specific message
 * queryClient.invalidateQueries({ queryKey: messageKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:41.373305Z
 */

import type { MessageFilterOptions } from '../types/message.types';

/**
 * Root query key for Message entity
 * Used as namespace prefix for all message queries
 */
const MESSAGE_ROOT = 'messages' as const;

/**
 * Message Query Keys
 *
 * Hierarchical query key factory for message queries:
 * - all: Base key for all message queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific message by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = messageKeys.list({ is_active: true });
 * // Result: ['messages', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = messageKeys.detail('uuid-here');
 * // Result: ['messages', 'detail', 'uuid-here']
 *
 * // Invalidate all message data
 * queryClient.invalidateQueries({ queryKey: messageKeys.all });
 * ```
 */
export const messageKeys = {
  /**
   * Base key for all message queries
   * Use this to invalidate all message-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: messageKeys.all });
   * ```
   */
  all: [MESSAGE_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: messageKeys.lists() });
   * ```
   */
  lists: () => [...messageKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = messageKeys.list({ is_active: true });
   *
   * // List with search
   * const key = messageKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = messageKeys.list();
   * ```
   */
  list: (filters?: MessageFilterOptions) =>
    [...messageKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: messageKeys.details() });
   * ```
   */
  details: () => [...messageKeys.all, 'detail'] as const,

  /**
   * Key for a specific message by ID
   * Use this for fetching and invalidating individual message records
   *
   * @param id - Message ID (UUID)
   * @returns Query key tuple for specific message
   *
   * @example
   * ```typescript
   * // Fetch specific message
   * const { data } = useQuery({
   *   queryKey: messageKeys.detail('uuid-here'),
   *   queryFn: () => messageService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: messageKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...messageKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active messages (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = messageKeys.dropdown();
   *
   * // Include inactive items
   * const key = messageKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...messageKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * Messages filtered by user ID
   *
   * @param userId - User ID (UUID)
   * @returns Query key tuple
   */
  byUser: (userId: string) =>
    [...messageKeys.all, 'by-user', userId] as const,

  /**
   * Messages filtered by investor ID
   *
   * @param investorId - Investor ID (UUID)
   * @returns Query key tuple
   */
  byInvestor: (investorId: string) =>
    [...messageKeys.all, 'by-investor', investorId] as const,

  /**
   * Messages filtered by email account ID
   *
   * @param emailAccountId - EmailAccount ID (UUID)
   * @returns Query key tuple
   */
  byEmailAccount: (emailAccountId: string) =>
    [...messageKeys.all, 'by-email-account', emailAccountId] as const,

  /**
   * Messages filtered by status
   *
   * @param status - Status value
   * @returns Query key tuple
   */
  byStatus: (status: string) =>
    [...messageKeys.all, 'by-status', status] as const,

  /**
   * Messages filtered by thread ID
   *
   * @param threadId - Thread ID (UUID)
   * @returns Query key tuple
   */
  byThread: (threadId: string) =>
    [...messageKeys.all, 'by-thread', threadId] as const,

  /**
   * Messages filtered by provider message ID
   *
   * @param providerMessageId - ProviderMessage ID (UUID)
   * @returns Query key tuple
   */
  byProviderMessage: (providerMessageId: string) =>
    [...messageKeys.all, 'by-provider-message', providerMessageId] as const,

  /**
   * Messages filtered by previous message ID
   *
   * @param previousMessageId - PreviousMessage ID (UUID)
   * @returns Query key tuple
   */
  byPreviousMessage: (previousMessageId: string) =>
    [...messageKeys.all, 'by-previous-message', previousMessageId] as const,

  /**
   * Messages filtered by job ID
   *
   * @param jobId - Job ID (UUID)
   * @returns Query key tuple
   */
  byJob: (jobId: string) =>
    [...messageKeys.all, 'by-job', jobId] as const,
} as const;

/**
 * Type representing any valid Message query key
 * Useful for typing query key arrays in generic contexts
 */
export type MessageQueryKey =
  | typeof messageKeys.all
  | ReturnType<typeof messageKeys.lists>
  | ReturnType<typeof messageKeys.list>
  | ReturnType<typeof messageKeys.details>
  | ReturnType<typeof messageKeys.detail>
  | ReturnType<typeof messageKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default messageKeys;
