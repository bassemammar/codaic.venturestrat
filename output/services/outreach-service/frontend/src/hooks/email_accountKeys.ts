/**
 * EmailAccount Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all email_account queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['emailaccounts'] - All email_account queries
 * - ['emailaccounts', 'list'] - All list queries
 * - ['emailaccounts', 'list', { filters }] - Specific filtered list
 * - ['emailaccounts', 'detail', id] - Specific email_account by ID
 *
 * @example
 * ```typescript
 * import { email_accountKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: email_accountKeys.list({ is_active: true }),
 *   queryFn: () => email_accountService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all email_account queries
 * queryClient.invalidateQueries({ queryKey: email_accountKeys.all });
 *
 * // Invalidate specific email_account
 * queryClient.invalidateQueries({ queryKey: email_accountKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:39.767827Z
 */

import type { EmailAccountFilterOptions } from '../types/email_account.types';

/**
 * Root query key for EmailAccount entity
 * Used as namespace prefix for all email_account queries
 */
const EMAIL_ACCOUNT_ROOT = 'emailaccounts' as const;

/**
 * EmailAccount Query Keys
 *
 * Hierarchical query key factory for email_account queries:
 * - all: Base key for all email_account queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific email_account by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = email_accountKeys.list({ is_active: true });
 * // Result: ['emailaccounts', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = email_accountKeys.detail('uuid-here');
 * // Result: ['emailaccounts', 'detail', 'uuid-here']
 *
 * // Invalidate all email_account data
 * queryClient.invalidateQueries({ queryKey: email_accountKeys.all });
 * ```
 */
export const email_accountKeys = {
  /**
   * Base key for all email_account queries
   * Use this to invalidate all email_account-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: email_accountKeys.all });
   * ```
   */
  all: [EMAIL_ACCOUNT_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: email_accountKeys.lists() });
   * ```
   */
  lists: () => [...email_accountKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = email_accountKeys.list({ is_active: true });
   *
   * // List with search
   * const key = email_accountKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = email_accountKeys.list();
   * ```
   */
  list: (filters?: EmailAccountFilterOptions) =>
    [...email_accountKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: email_accountKeys.details() });
   * ```
   */
  details: () => [...email_accountKeys.all, 'detail'] as const,

  /**
   * Key for a specific email_account by ID
   * Use this for fetching and invalidating individual email_account records
   *
   * @param id - EmailAccount ID (UUID)
   * @returns Query key tuple for specific email_account
   *
   * @example
   * ```typescript
   * // Fetch specific email_account
   * const { data } = useQuery({
   *   queryKey: email_accountKeys.detail('uuid-here'),
   *   queryFn: () => email_accountService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: email_accountKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...email_accountKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active email_accounts (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = email_accountKeys.dropdown();
   *
   * // Include inactive items
   * const key = email_accountKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...email_accountKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * EmailAccounts filtered by user ID
   *
   * @param userId - User ID (UUID)
   * @returns Query key tuple
   */
  byUser: (userId: string) =>
    [...email_accountKeys.all, 'by-user', userId] as const,

  /**
   * EmailAccounts filtered by watch history ID
   *
   * @param watchHistoryId - WatchHistory ID (UUID)
   * @returns Query key tuple
   */
  byWatchHistory: (watchHistoryId: string) =>
    [...email_accountKeys.all, 'by-watch-history', watchHistoryId] as const,
} as const;

/**
 * Type representing any valid EmailAccount query key
 * Useful for typing query key arrays in generic contexts
 */
export type EmailAccountQueryKey =
  | typeof email_accountKeys.all
  | ReturnType<typeof email_accountKeys.lists>
  | ReturnType<typeof email_accountKeys.list>
  | ReturnType<typeof email_accountKeys.details>
  | ReturnType<typeof email_accountKeys.detail>
  | ReturnType<typeof email_accountKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default email_accountKeys;
