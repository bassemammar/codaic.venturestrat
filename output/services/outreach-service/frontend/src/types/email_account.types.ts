/**
 * EmailAccount Type Definitions
 *
 * This file contains type definitions for the EmailAccount entity:
 * - EmailAccount: Full entity model with all fields
 * - EmailAccountCreateRequest: Data for creating a new email_account
 * - EmailAccountUpdateRequest: Partial data for updating a email_account
 * - EmailAccountListResponse: Paginated list response
 * - EmailAccountFilterOptions: Query parameters for filtering
 * - EmailAccountDropdownOption: Options for select components
 *
 * OAuth email account for sending via Gmail, Microsoft, or SendGrid
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:39.743416Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// EmailAccount Entity
// ============================================================================

/**
 * EmailAccount entity model
 *
 * Represents the full email_account entity as returned by the API.
 * OAuth email account for sending via Gmail, Microsoft, or SendGrid
 *
 * @remarks
 * This interface matches the backend EmailAccountDTO schema exactly.
 */
export interface EmailAccount {
  /** Unique identifier (UUID) */
  id: string;

  /** Auth user reference */
  user_id: string;

  /** Provider */
  provider: string;

  /** Email Address */
  email_address: string;

  /** Encrypted OAuth access token */
  access_token: string | null;

  /** Encrypted OAuth refresh token */
  refresh_token: string | null;

  /** Token Expires At */
  token_expires_at: string | null;

  /** Gmail push notification history ID */
  watch_history_id: string | null;

  /** Is Active */
  is_active: boolean;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * EmailAccount creation request data
 *
 * Used when creating a new email_account. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend EmailAccountCreateDTO schema.
 */
export interface EmailAccountCreateRequest {
  /** Auth user reference (required) */
  user_id: string;

  /** Provider (required) */
  provider: string;

  /** Email Address (required) */
  email_address: string;

  /** Encrypted OAuth access token (optional) */
  access_token?: string;

  /** Encrypted OAuth refresh token (optional) */
  refresh_token?: string;

  /** Token Expires At (optional) */
  token_expires_at?: string;

  /** Gmail push notification history ID (optional) */
  watch_history_id?: string;

  /** Is Active (optional) */
  is_active?: boolean;

}

/**
 * EmailAccount update request data
 *
 * Used when updating an existing email_account. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend EmailAccountUpdateDTO schema.
 */
export interface EmailAccountUpdateRequest {
  /** Auth user reference */
  user_id?: string;

  /** Provider */
  provider?: string;

  /** Email Address */
  email_address?: string;

  /** Encrypted OAuth access token */
  access_token?: string | null;

  /** Encrypted OAuth refresh token */
  refresh_token?: string | null;

  /** Token Expires At */
  token_expires_at?: string | null;

  /** Gmail push notification history ID */
  watch_history_id?: string | null;

  /** Is Active */
  is_active?: boolean;

}

/**
 * EmailAccount list response
 *
 * Wrapper for paginated email_account list queries.
 */
export interface EmailAccountListResponse {
  /** Array of email_account items */
  items: EmailAccount[];

  /** Alternative property name for items (for API compatibility) */
  email_accounts?: EmailAccount[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * EmailAccount filter options for list queries
 *
 * Extends BaseEntityFilters with email_account-specific filtering options.
 */
export interface EmailAccountFilterOptions extends BaseEntityFilters {
  /** Filter by auth user reference */
  user_id?: string;

  /** Filter by gmail push notification history id */
  watch_history_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * EmailAccount dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface EmailAccountDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: EmailAccount;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * EmailAccount summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface EmailAccountSummary {
  /** Unique identifier */
  id: string;

}

// ============================================================================
// Form Types
// ============================================================================

/**
 * EmailAccount form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type EmailAccountFormData = EmailAccountCreateRequest;

/**
 * Default form values for new email_account
 *
 * Use this when initializing form state for creating a new email_account.
 */
export const DEFAULT_EMAIL_ACCOUNT_FORM_DATA: Partial<EmailAccountFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single email_account API response
 */
export interface EmailAccountResponse {
  /** The requested email_account entity */
  data: EmailAccount;
}

/**
 * EmailAccount mutation response (create/update)
 */
export interface EmailAccountMutationResponse {
  /** The created or updated email_account */
  data: EmailAccount;

  /** Success message */
  message?: string;
}

/**
 * API error response for email_account operations
 */
export interface EmailAccountApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a EmailAccount
 *
 * @param value - Value to check
 * @returns True if value is a EmailAccount
 */
export function isEmailAccount(value: unknown): value is EmailAccount {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.user_id === 'string' &&
    typeof obj.provider === 'string' &&
    typeof obj.email_address === 'string'
  );
}

/**
 * Type guard to check if a value is a EmailAccountDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a EmailAccountDropdownOption
 */
export function isEmailAccountDropdownOption(
  value: unknown
): value is EmailAccountDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isEmailAccount(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All email_account types re-exported for convenience */
export type {
  EmailAccount as EmailAccountEntity,
  EmailAccountCreateRequest as CreateEmailAccountInput,
  EmailAccountUpdateRequest as UpdateEmailAccountInput,
};
