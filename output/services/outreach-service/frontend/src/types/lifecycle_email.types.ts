/**
 * LifecycleEmail Type Definitions
 *
 * This file contains type definitions for the LifecycleEmail entity:
 * - LifecycleEmail: Full entity model with all fields
 * - LifecycleEmailCreateRequest: Data for creating a new lifecycle_email
 * - LifecycleEmailUpdateRequest: Partial data for updating a lifecycle_email
 * - LifecycleEmailListResponse: Paginated list response
 * - LifecycleEmailFilterOptions: Query parameters for filtering
 * - LifecycleEmailDropdownOption: Options for select components
 *
 * Tracks drip campaign email execution per user
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:40.788156Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// LifecycleEmail Entity
// ============================================================================

/**
 * LifecycleEmail entity model
 *
 * Represents the full lifecycle_email entity as returned by the API.
 * Tracks drip campaign email execution per user
 *
 * @remarks
 * This interface matches the backend LifecycleEmailDTO schema exactly.
 */
export interface LifecycleEmail {
  /** Unique identifier (UUID) */
  id: string;

  /** User Id */
  user_id: string;

  /** Lifecycle template identifier: welcome, onboarding_reminder, gmail_reminder, etc. */
  template_code: string;

  /** Status */
  status: string;

  /** When this lifecycle email should be sent */
  scheduled_for: string;

  /** Sent At */
  sent_at: string | null;

  /** Why this email was skipped */
  skip_reason: string | null;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * LifecycleEmail creation request data
 *
 * Used when creating a new lifecycle_email. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend LifecycleEmailCreateDTO schema.
 */
export interface LifecycleEmailCreateRequest {
  /** User Id (required) */
  user_id: string;

  /** Lifecycle template identifier: welcome, onboarding_reminder, gmail_reminder, etc. (required) */
  template_code: string;

  /** Status (optional) */
  status?: string;

  /** When this lifecycle email should be sent (required) */
  scheduled_for: string;

  /** Sent At (optional) */
  sent_at?: string;

  /** Why this email was skipped (optional) */
  skip_reason?: string;

  /** Active status (defaults to true for new lifecycle_emails) */
  is_active?: boolean;
}

/**
 * LifecycleEmail update request data
 *
 * Used when updating an existing lifecycle_email. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend LifecycleEmailUpdateDTO schema.
 */
export interface LifecycleEmailUpdateRequest {
  /** User Id */
  user_id?: string;

  /** Lifecycle template identifier: welcome, onboarding_reminder, gmail_reminder, etc. */
  template_code?: string;

  /** Status */
  status?: string;

  /** When this lifecycle email should be sent */
  scheduled_for?: string;

  /** Sent At */
  sent_at?: string | null;

  /** Why this email was skipped */
  skip_reason?: string | null;

  /** Active status */
  is_active?: boolean;
}

/**
 * LifecycleEmail list response
 *
 * Wrapper for paginated lifecycle_email list queries.
 */
export interface LifecycleEmailListResponse {
  /** Array of lifecycle_email items */
  items: LifecycleEmail[];

  /** Alternative property name for items (for API compatibility) */
  lifecycle_emails?: LifecycleEmail[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * LifecycleEmail filter options for list queries
 *
 * Extends BaseEntityFilters with lifecycle_email-specific filtering options.
 */
export interface LifecycleEmailFilterOptions extends BaseEntityFilters {
  /** Filter by user id */
  user_id?: string;

  /** Filter by status */
  status?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * LifecycleEmail dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface LifecycleEmailDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: LifecycleEmail;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * LifecycleEmail summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface LifecycleEmailSummary {
  /** Unique identifier */
  id: string;

  /** Status */
  status: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * LifecycleEmail form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type LifecycleEmailFormData = LifecycleEmailCreateRequest;

/**
 * Default form values for new lifecycle_email
 *
 * Use this when initializing form state for creating a new lifecycle_email.
 */
export const DEFAULT_LIFECYCLE_EMAIL_FORM_DATA: Partial<LifecycleEmailFormData> = {
  is_active: true,
  status: "pending",
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single lifecycle_email API response
 */
export interface LifecycleEmailResponse {
  /** The requested lifecycle_email entity */
  data: LifecycleEmail;
}

/**
 * LifecycleEmail mutation response (create/update)
 */
export interface LifecycleEmailMutationResponse {
  /** The created or updated lifecycle_email */
  data: LifecycleEmail;

  /** Success message */
  message?: string;
}

/**
 * API error response for lifecycle_email operations
 */
export interface LifecycleEmailApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a LifecycleEmail
 *
 * @param value - Value to check
 * @returns True if value is a LifecycleEmail
 */
export function isLifecycleEmail(value: unknown): value is LifecycleEmail {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.user_id === 'string' &&
    typeof obj.template_code === 'string' &&
    typeof obj.scheduled_for === 'string'
  );
}

/**
 * Type guard to check if a value is a LifecycleEmailDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a LifecycleEmailDropdownOption
 */
export function isLifecycleEmailDropdownOption(
  value: unknown
): value is LifecycleEmailDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isLifecycleEmail(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All lifecycle_email types re-exported for convenience */
export type {
  LifecycleEmail as LifecycleEmailEntity,
  LifecycleEmailCreateRequest as CreateLifecycleEmailInput,
  LifecycleEmailUpdateRequest as UpdateLifecycleEmailInput,
};
