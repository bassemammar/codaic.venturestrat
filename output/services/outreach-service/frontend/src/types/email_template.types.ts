/**
 * EmailTemplate Type Definitions
 *
 * This file contains type definitions for the EmailTemplate entity:
 * - EmailTemplate: Full entity model with all fields
 * - EmailTemplateCreateRequest: Data for creating a new email_template
 * - EmailTemplateUpdateRequest: Partial data for updating a email_template
 * - EmailTemplateListResponse: Paginated list response
 * - EmailTemplateFilterOptions: Query parameters for filtering
 * - EmailTemplateDropdownOption: Options for select components
 *
 * Reusable email template for outreach and lifecycle emails
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:40.225669Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// EmailTemplate Entity
// ============================================================================

/**
 * EmailTemplate entity model
 *
 * Represents the full email_template entity as returned by the API.
 * Reusable email template for outreach and lifecycle emails
 *
 * @remarks
 * This interface matches the backend EmailTemplateDTO schema exactly.
 */
export interface EmailTemplate {
  /** Unique identifier (UUID) */
  id: string;

  /** Owner user ID, null for system templates */
  user_id: string | null;

  /** Name */
  name: string;

  /** Subject */
  subject: string;

  /** HTML template body */
  body: string;

  /** Category */
  category: string;

  /** Is Active */
  is_active: boolean;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * EmailTemplate creation request data
 *
 * Used when creating a new email_template. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend EmailTemplateCreateDTO schema.
 */
export interface EmailTemplateCreateRequest {
  /** Owner user ID, null for system templates (optional) */
  user_id?: string;

  /** Name (required) */
  name: string;

  /** Subject (required) */
  subject: string;

  /** HTML template body (required) */
  body: string;

  /** Category (required) */
  category: string;

  /** Is Active (optional) */
  is_active?: boolean;

}

/**
 * EmailTemplate update request data
 *
 * Used when updating an existing email_template. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend EmailTemplateUpdateDTO schema.
 */
export interface EmailTemplateUpdateRequest {
  /** Owner user ID, null for system templates */
  user_id?: string | null;

  /** Name */
  name?: string;

  /** Subject */
  subject?: string;

  /** HTML template body */
  body?: string;

  /** Category */
  category?: string;

  /** Is Active */
  is_active?: boolean;

}

/**
 * EmailTemplate list response
 *
 * Wrapper for paginated email_template list queries.
 */
export interface EmailTemplateListResponse {
  /** Array of email_template items */
  items: EmailTemplate[];

  /** Alternative property name for items (for API compatibility) */
  email_templates?: EmailTemplate[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * EmailTemplate filter options for list queries
 *
 * Extends BaseEntityFilters with email_template-specific filtering options.
 */
export interface EmailTemplateFilterOptions extends BaseEntityFilters {
  /** Filter by owner user id, null for system templates */
  user_id?: string;

  /** Filter by category */
  category?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * EmailTemplate dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface EmailTemplateDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: EmailTemplate;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * EmailTemplate summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface EmailTemplateSummary {
  /** Unique identifier */
  id: string;

  /** Name */
  name: string;

}

// ============================================================================
// Form Types
// ============================================================================

/**
 * EmailTemplate form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type EmailTemplateFormData = EmailTemplateCreateRequest;

/**
 * Default form values for new email_template
 *
 * Use this when initializing form state for creating a new email_template.
 */
export const DEFAULT_EMAIL_TEMPLATE_FORM_DATA: Partial<EmailTemplateFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single email_template API response
 */
export interface EmailTemplateResponse {
  /** The requested email_template entity */
  data: EmailTemplate;
}

/**
 * EmailTemplate mutation response (create/update)
 */
export interface EmailTemplateMutationResponse {
  /** The created or updated email_template */
  data: EmailTemplate;

  /** Success message */
  message?: string;
}

/**
 * API error response for email_template operations
 */
export interface EmailTemplateApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a EmailTemplate
 *
 * @param value - Value to check
 * @returns True if value is a EmailTemplate
 */
export function isEmailTemplate(value: unknown): value is EmailTemplate {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.name === 'string' &&
    typeof obj.subject === 'string' &&
    typeof obj.body === 'string' &&
    typeof obj.category === 'string'
  );
}

/**
 * Type guard to check if a value is a EmailTemplateDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a EmailTemplateDropdownOption
 */
export function isEmailTemplateDropdownOption(
  value: unknown
): value is EmailTemplateDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isEmailTemplate(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All email_template types re-exported for convenience */
export type {
  EmailTemplate as EmailTemplateEntity,
  EmailTemplateCreateRequest as CreateEmailTemplateInput,
  EmailTemplateUpdateRequest as UpdateEmailTemplateInput,
};
