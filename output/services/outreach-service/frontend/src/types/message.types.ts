/**
 * Message Type Definitions
 *
 * This file contains type definitions for the Message entity:
 * - Message: Full entity model with all fields
 * - MessageCreateRequest: Data for creating a new message
 * - MessageUpdateRequest: Partial data for updating a message
 * - MessageListResponse: Paginated list response
 * - MessageFilterOptions: Query parameters for filtering
 * - MessageDropdownOption: Options for select components
 *
 * Email message — draft, scheduled, sent, or received reply
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:41.340910Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// Message Entity
// ============================================================================

/**
 * Message entity model
 *
 * Represents the full message entity as returned by the API.
 * Email message — draft, scheduled, sent, or received reply
 *
 * @remarks
 * This interface matches the backend MessageDTO schema exactly.
 */
export interface Message {
  /** Unique identifier (UUID) */
  id: string;

  /** User Id */
  user_id: string;

  /** Cross-service reference to investor-service Investor */
  investor_id: string | null;

  /** Email Account Id */
  email_account_id: string | null;

  /** Status */
  status: string;

  /** Recipient email addresses array */
  to_addresses: Record<string, any>;

  /** Cc Addresses */
  cc_addresses: Record<string, any>;

  /** Subject */
  subject: string;

  /** From Address */
  from_address: string;

  /** HTML email body */
  body: string;

  /** Array of {key, name, size, type} for S3 attachments */
  attachments: Record<string, any>;

  /** Gmail thread ID for conversation threading */
  thread_id: string | null;

  /** Message-ID header from email provider */
  provider_message_id: string | null;

  /** References header for email threading */
  provider_references: string | null;

  /** Self-referential FK for reply threading */
  previous_message_id: string | null;

  /** When to send if scheduled */
  scheduled_for: string | null;

  /** Kafka event reference for scheduled sends */
  job_id: string | null;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * Message creation request data
 *
 * Used when creating a new message. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend MessageCreateDTO schema.
 */
export interface MessageCreateRequest {
  /** User Id (required) */
  user_id: string;

  /** Cross-service reference to investor-service Investor (optional) */
  investor_id?: string;

  /** Email Account Id (optional) */
  email_account_id?: string;

  /** Status (optional) */
  status?: string;

  /** Recipient email addresses array (required) */
  to_addresses: Record<string, any>;

  /** Cc Addresses (optional) */
  cc_addresses?: Record<string, any>;

  /** Subject (required) */
  subject: string;

  /** From Address (required) */
  from_address: string;

  /** HTML email body (required) */
  body: string;

  /** Array of {key, name, size, type} for S3 attachments (optional) */
  attachments?: Record<string, any>;

  /** Gmail thread ID for conversation threading (optional) */
  thread_id?: string;

  /** Message-ID header from email provider (optional) */
  provider_message_id?: string;

  /** References header for email threading (optional) */
  provider_references?: string;

  /** Self-referential FK for reply threading (optional) */
  previous_message_id?: string;

  /** When to send if scheduled (optional) */
  scheduled_for?: string;

  /** Kafka event reference for scheduled sends (optional) */
  job_id?: string;

  /** Active status (defaults to true for new messages) */
  is_active?: boolean;
}

/**
 * Message update request data
 *
 * Used when updating an existing message. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend MessageUpdateDTO schema.
 */
export interface MessageUpdateRequest {
  /** User Id */
  user_id?: string;

  /** Cross-service reference to investor-service Investor */
  investor_id?: string | null;

  /** Email Account Id */
  email_account_id?: string | null;

  /** Status */
  status?: string;

  /** Recipient email addresses array */
  to_addresses?: Record<string, any>;

  /** Cc Addresses */
  cc_addresses?: Record<string, any>;

  /** Subject */
  subject?: string;

  /** From Address */
  from_address?: string;

  /** HTML email body */
  body?: string;

  /** Array of {key, name, size, type} for S3 attachments */
  attachments?: Record<string, any>;

  /** Gmail thread ID for conversation threading */
  thread_id?: string | null;

  /** Message-ID header from email provider */
  provider_message_id?: string | null;

  /** References header for email threading */
  provider_references?: string | null;

  /** Self-referential FK for reply threading */
  previous_message_id?: string | null;

  /** When to send if scheduled */
  scheduled_for?: string | null;

  /** Kafka event reference for scheduled sends */
  job_id?: string | null;

  /** Active status */
  is_active?: boolean;
}

/**
 * Message list response
 *
 * Wrapper for paginated message list queries.
 */
export interface MessageListResponse {
  /** Array of message items */
  items: Message[];

  /** Alternative property name for items (for API compatibility) */
  messages?: Message[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * Message filter options for list queries
 *
 * Extends BaseEntityFilters with message-specific filtering options.
 */
export interface MessageFilterOptions extends BaseEntityFilters {
  /** Filter by user id */
  user_id?: string;

  /** Filter by cross-service reference to investor-service investor */
  investor_id?: string;

  /** Filter by email account id */
  email_account_id?: string;

  /** Filter by status */
  status?: string;

  /** Filter by gmail thread id for conversation threading */
  thread_id?: string;

  /** Filter by message-id header from email provider */
  provider_message_id?: string;

  /** Filter by self-referential fk for reply threading */
  previous_message_id?: string;

  /** Filter by kafka event reference for scheduled sends */
  job_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * Message dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface MessageDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: Message;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * Message summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface MessageSummary {
  /** Unique identifier */
  id: string;

  /** Status */
  status: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * Message form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type MessageFormData = MessageCreateRequest;

/**
 * Default form values for new message
 *
 * Use this when initializing form state for creating a new message.
 */
export const DEFAULT_MESSAGE_FORM_DATA: Partial<MessageFormData> = {
  is_active: true,
  status: "draft",
  cc_addresses: "[]",
  attachments: "[]",
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single message API response
 */
export interface MessageResponse {
  /** The requested message entity */
  data: Message;
}

/**
 * Message mutation response (create/update)
 */
export interface MessageMutationResponse {
  /** The created or updated message */
  data: Message;

  /** Success message */
  message?: string;
}

/**
 * API error response for message operations
 */
export interface MessageApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a Message
 *
 * @param value - Value to check
 * @returns True if value is a Message
 */
export function isMessage(value: unknown): value is Message {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.user_id === 'string' &&
    typeof obj.to_addresses === 'object' &&
    typeof obj.subject === 'string' &&
    typeof obj.from_address === 'string' &&
    typeof obj.body === 'string'
  );
}

/**
 * Type guard to check if a value is a MessageDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a MessageDropdownOption
 */
export function isMessageDropdownOption(
  value: unknown
): value is MessageDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isMessage(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All message types re-exported for convenience */
export type {
  Message as MessageEntity,
  MessageCreateRequest as CreateMessageInput,
  MessageUpdateRequest as UpdateMessageInput,
};
