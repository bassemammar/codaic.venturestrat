/**
 * FormLifecycleEmailSelect Component
 *
 * A dropdown select component for lifecycle_email selection with React Hook Form integration.
 * Uses the useLifecycleEmailsDropdown hook to fetch lifecycle_email data with loading and error states.
 *
 * Features:
 * - React Hook Form integration via Controller
 * - Fetches lifecycle_emails using useLifecycleEmailsDropdown hook
 * - Material UI Autocomplete with search/filter
 * - Loading skeleton while fetching data
 * - Error state with retry button
 * - Support for required/optional fields
 * - Full accessibility support (ARIA attributes)
 * - Memoized for optimal performance
 *
 * Tracks drip campaign email execution per user
 *
 * @example
 * ```tsx
 * import { FormLifecycleEmailSelect } from '../components/FormLifecycleEmailSelect';
 * import { useForm, FormProvider } from 'react-hook-form';
 *
 * function MyForm() {
 *   const methods = useForm();
 *
 *   return (
 *     <FormProvider {...methods}>
 *       <FormLifecycleEmailSelect
 *         name="lifecycleEmailId"
 *         label="LifecycleEmail"
 *         required={true}
 *       />
 *     </FormProvider>
 *   );
 * }
 *
 * // With active filter disabled (show all including inactive)
 * <FormLifecycleEmailSelect name="lifecycleEmailId" activeOnly={false} />
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:40.939526Z
 */

import React, { memo, useMemo } from 'react';
import { useFormContext, Controller } from 'react-hook-form';
import {
  Autocomplete,
  TextField,
  Box,
  Skeleton,
  Typography,
  Button,
  Alert,
} from '@mui/material';
import { useLifecycleEmailsDropdown, LifecycleEmailDropdownOption } from '../hooks/useLifecycleEmails';

// ============================================================================
// Types
// ============================================================================

/**
 * Props for the FormLifecycleEmailSelect component
 */
export interface FormLifecycleEmailSelectProps {
  /**
   * Form field name (should match the form schema)
   */
  name: string;

  /**
   * Label text for the select field
   */
  label: string;

  /**
   * Placeholder text when no value is selected
   * @default "Select lifecycle email..."
   */
  placeholder?: string;

  /**
   * Whether the field is required
   * @default false
   */
  required?: boolean;

  /**
   * Whether the field is disabled
   * @default false
   */
  disabled?: boolean;

  /**
   * Helper text displayed below the field
   */
  helperText?: string;

  /**
   * Custom className for the container
   */
  className?: string;

  /**
   * Custom styles for the container
   */
  style?: React.CSSProperties;

  /**
   * Whether to take full width
   * @default true
   */
  fullWidth?: boolean;

  /**
   * Size variant
   * @default "medium"
   */
  size?: 'small' | 'medium';

  /**
   * Tab index for keyboard navigation
   */
  tabIndex?: number;

  /**
   * Aria label for accessibility
   */
  'aria-label'?: string;

  /**
   * Aria described by for accessibility
   */
  'aria-describedby'?: string;

  /**
   * Data test id for testing
   */
  'data-testid'?: string;

  /**
   * Filter to only show active lifecycle_emails
   * @default true
   */
  activeOnly?: boolean;

  /**
   * Callback when value changes (in addition to form update)
   */
  onChange?: (value: string | null, option: LifecycleEmailDropdownOption | null) => void;

  /**
   * Whether to allow clearing the selection
   * When required is true, this is ignored (always non-clearable)
   * @default true for optional fields
   */
  clearable?: boolean;

  /**
   * Whether to show the full entity data in the dropdown
   * When true, shows additional info like ID
   * @default false
   */
  showExtendedInfo?: boolean;
}

// ============================================================================
// Loading Skeleton Component
// ============================================================================

interface SelectLoadingSkeletonProps {
  label: string;
  required?: boolean;
  fullWidth?: boolean;
  size?: 'small' | 'medium';
  className?: string;
  style?: React.CSSProperties;
  'data-testid'?: string;
}

const SelectLoadingSkeleton: React.FC<SelectLoadingSkeletonProps> = ({
  label,
  required = false,
  fullWidth = true,
  size = 'medium',
  className,
  style,
  'data-testid': dataTestId,
}) => {
  const height = size === 'small' ? 40 : 56;

  return (
    <Box
      className={className}
      style={{ width: fullWidth ? '100%' : 'auto', ...style }}
      data-testid={dataTestId ? `${dataTestId}-loading` : 'lifecycle_email-select-loading'}
    >
      <Typography
        variant="body2"
        component="span"
        sx={{ mb: 0.5, display: 'block', color: 'text.secondary' }}
      >
        {label}
        {required && (
          <Typography component="span" sx={{ color: 'error.main', ml: 0.5 }}>
            *
          </Typography>
        )}
      </Typography>
      <Skeleton
        variant="rounded"
        height={height}
        animation="wave"
        sx={{ borderRadius: 1 }}
      />
      <Typography variant="caption" sx={{ mt: 0.5, display: 'block', color: 'text.secondary' }}>
        Loading lifecycle emails...
      </Typography>
    </Box>
  );
};

// ============================================================================
// Error State Component
// ============================================================================

interface SelectErrorStateProps {
  error: Error;
  onRetry: () => void;
  label: string;
  fullWidth?: boolean;
  className?: string;
  style?: React.CSSProperties;
  'data-testid'?: string;
}

const SelectErrorState: React.FC<SelectErrorStateProps> = ({
  error,
  onRetry,
  label,
  fullWidth = true,
  className,
  style,
  'data-testid': dataTestId,
}) => {
  return (
    <Box
      className={className}
      style={{ width: fullWidth ? '100%' : 'auto', ...style }}
      data-testid={dataTestId ? `${dataTestId}-error` : 'lifecycle_email-select-error'}
    >
      <Typography
        variant="body2"
        component="span"
        sx={{ mb: 0.5, display: 'block', color: 'text.secondary' }}
      >
        {label}
      </Typography>
      <Alert
        severity="error"
        action={
          <Button color="inherit" size="small" onClick={onRetry}>
            Retry
          </Button>
        }
        sx={{ py: 0.5 }}
      >
        Failed to load lifecycle emails: {error.message || 'Unknown error'}
      </Alert>
    </Box>
  );
};

// ============================================================================
// Main Component
// ============================================================================

/**
 * FormLifecycleEmailSelect Component
 *
 * A dropdown for selecting lifecycle_emails with React Hook Form integration.
 * Automatically fetches lifecycle_emails using the useLifecycleEmailsDropdown hook
 * and handles loading and error states.
 */
export const FormLifecycleEmailSelect: React.FC<FormLifecycleEmailSelectProps> = memo(
  ({
    name,
    label,
    placeholder = 'Select lifecycle email...',
    required = false,
    disabled = false,
    helperText,
    className,
    style,
    fullWidth = true,
    size = 'medium',
    tabIndex,
    'aria-label': ariaLabel,
    'aria-describedby': ariaDescribedBy,
    'data-testid': dataTestId,
    activeOnly = true,
    onChange,
    clearable = true,
    showExtendedInfo = false,
  }) => {
    const { control } = useFormContext();

    // Fetch lifecycle_emails for dropdown
    const {
      options,
      isLoading,
      isError,
      error,
      refetch,
    } = useLifecycleEmailsDropdown(activeOnly);

    // Memoize options for performance
    const dropdownOptions = useMemo(() => options ?? [], [options]);

    // Find option by value
    const findOptionByValue = (value: string | null | undefined): LifecycleEmailDropdownOption | null => {
      if (!value) return null;
      return dropdownOptions.find((opt) => opt.value === value) ?? null;
    };

    // Generate IDs for accessibility
    const inputId = `${name}-lifecycle_email-select`;
    const errorId = `${name}-error`;
    const helperId = `${name}-helper`;

    // -------------------------------------------------------------------------
    // Loading State
    // -------------------------------------------------------------------------

    if (isLoading) {
      return (
        <SelectLoadingSkeleton
          label={label}
          required={required}
          fullWidth={fullWidth}
          size={size}
          className={className}
          style={style}
          data-testid={dataTestId}
        />
      );
    }

    // -------------------------------------------------------------------------
    // Error State
    // -------------------------------------------------------------------------

    if (isError && error) {
      return (
        <SelectErrorState
          error={error}
          onRetry={() => refetch()}
          label={label}
          fullWidth={fullWidth}
          className={className}
          style={style}
          data-testid={dataTestId}
        />
      );
    }

    // -------------------------------------------------------------------------
    // Render Select
    // -------------------------------------------------------------------------

    return (
      <Controller
        name={name}
        control={control}
        rules={{
          required: required ? 'Please select a lifecycle email' : false,
        }}
        render={({ field, fieldState: { error: fieldError }}) => {
          const hasError = !!fieldError;
          const errorMessage = fieldError?.message;

          // Find the current option based on field value
          const selectedOption = findOptionByValue(field.value);

          // Handle change
          const handleChange = (
            _event: React.SyntheticEvent,
            newValue: LifecycleEmailDropdownOption | null
          ) => {
            const newId = newValue?.value ?? null;
            field.onChange(newId);
            if (onChange) {
              onChange(newId, newValue);
            }
          };

          return (
            <Autocomplete<LifecycleEmailDropdownOption, false, boolean, false>
              id={inputId}
              value={selectedOption}
              onChange={handleChange}
              onBlur={field.onBlur}
              options={dropdownOptions}
              getOptionLabel={(option) => option.label}
              isOptionEqualToValue={(option, value) => option.value === value.value}
              disabled={disabled}
              disableClearable={required || !clearable}
              fullWidth={fullWidth}
              size={size}
              className={className}
              style={style}
              data-testid={dataTestId}
              renderOption={(props, option) => (
                <Box
                  component="li"
                  {...props}
                  key={option.value}
                  sx={{
                    display: 'flex',
                    flexDirection: showExtendedInfo ? 'column' : 'row',
                    alignItems: showExtendedInfo ? 'flex-start' : 'center',
                    gap: 0.5,
                  }}
                >
                  <Typography variant="body2">{option.label}</Typography>
                  {showExtendedInfo && (
                    <Typography variant="caption" color="text.secondary">
                      ID: {option.value}
                    </Typography>
                  )}
                </Box>
              )}
              renderInput={(params) => (
                <TextField
                  {...params}
                  label={label}
                  placeholder={placeholder}
                  required={required}
                  error={hasError}
                  helperText={hasError ? errorMessage : helperText}
                  inputProps={{
                    ...params.inputProps,
                    tabIndex,
                    'aria-label': ariaLabel || label,
                    'aria-describedby': ariaDescribedBy ||
                      (hasError ? errorId : helperText ? helperId : undefined),
                    'aria-invalid': hasError,
                    'aria-required': required,
                  }}
                  FormHelperTextProps={{
                    id: hasError ? errorId : helperId,
                    role: hasError ? 'alert' : undefined,
                  }}
                />
              )}
              noOptionsText="No lifecycle emails found"
              loadingText="Loading lifecycle emails..."
            />
          );
        }}
      />
    );
  },
  // Custom memo comparison
  (prevProps, nextProps) => {
    return (
      prevProps.name === nextProps.name &&
      prevProps.label === nextProps.label &&
      prevProps.placeholder === nextProps.placeholder &&
      prevProps.helperText === nextProps.helperText &&
      prevProps.disabled === nextProps.disabled &&
      prevProps.required === nextProps.required &&
      prevProps.fullWidth === nextProps.fullWidth &&
      prevProps.size === nextProps.size &&
      prevProps.className === nextProps.className &&
      prevProps.activeOnly === nextProps.activeOnly &&
      prevProps.clearable === nextProps.clearable &&
      prevProps.showExtendedInfo === nextProps.showExtendedInfo
    );
  }
);

FormLifecycleEmailSelect.displayName = 'FormLifecycleEmailSelect';

export default FormLifecycleEmailSelect;
