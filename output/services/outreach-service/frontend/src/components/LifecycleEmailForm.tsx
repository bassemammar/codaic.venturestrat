/**
 * LifecycleEmailForm Component
 *
 * Form component for creating and editing lifecycle_emails.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * Features:
 * - Create and Edit mode support
 * - Form validation with error messages
 * - Material UI components
 * - Loading state handling
 * - Accessible form fields (WCAG 2.1 Level AA)
 * - Modal wrapper support (form_display: modal)
 *
 * Tracks drip campaign email execution per user
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-03-10T13:09:40.856506Z
 */

import React, { useMemo, useEffect } from 'react';
import { useForm, Controller, FormProvider } from 'react-hook-form';
import {
  Alert as MuiAlert,
  Box,
  Grid,
  TextField,
  Button,
  CircularProgress,
  Dialog,
  DialogTitle,
  DialogContent,
  IconButton,
} from '@mui/material';
import { Close as CloseIcon } from '@mui/icons-material';
import type { LifecycleEmailCreateRequest, LifecycleEmail } from '../types/lifecycle_email.types';
import { useNotification } from '../contexts/NotificationContext';

// ============================================================================
// Types
// ============================================================================

/**
 * Form data type for LifecycleEmail
 */
export type LifecycleEmailFormData = LifecycleEmailCreateRequest;

/**
 * Props for LifecycleEmailForm component
 */
export interface LifecycleEmailFormProps {
  /** Existing lifecycle_email data for edit mode (optional) */
  lifecycleEmail?: LifecycleEmail;
  /** Form submission handler (can be sync or async) */
  onSubmit: (data: LifecycleEmailFormData) => void | Promise<void>;
  /** Cancel handler */
  onCancel: () => void;
  /** Loading state (disables form during submission) */
  isLoading?: boolean;
  /** Custom className for the form container */
  className?: string;
}

/**
 * Props for LifecycleEmailFormModal component (modal wrapper)
 */
export interface LifecycleEmailFormModalProps extends LifecycleEmailFormProps {
  /** Whether the modal is open */
  open: boolean;
  /** Handler called when the modal requests to be closed */
  onClose: () => void;
  /** Optional title override for the modal */
  title?: string;
}


// ============================================================================
// Constants
// ============================================================================

/**
 * Default form values for creating a new lifecycle_email
 */
const DEFAULT_FORM_VALUES: Partial<LifecycleEmailFormData> = {
  user_id: '',
  template_code: '',
  status: "pending",
  scheduled_for: '',
  sent_at: '',
  skip_reason: '',
};

// ============================================================================
// Component
// ============================================================================

/**
 * LifecycleEmailForm Component
 *
 * Renders a form for creating or editing lifecycle_emails.
 * Uses React Hook Form with Controller pattern for form state management.
 *
 * @example
 * ```tsx
 * // Create mode
 * <LifecycleEmailForm
 *   onSubmit={handleCreate}
 *   onCancel={handleCancel}
 *   isLoading={isCreating}
 * />
 *
 * // Edit mode
 * <LifecycleEmailForm
 *   lifecycleEmail={existingLifecycleEmail}
 *   onSubmit={handleUpdate}
 *   onCancel={handleCancel}
 *   isLoading={isUpdating}
 * />
 * ```
 */
export function LifecycleEmailForm({
  lifecycleEmail,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: LifecycleEmailFormProps): React.ReactElement {
  // Determine if we're in edit mode
  const isEditMode = Boolean(lifecycleEmail);

  // Notification context for showing success/error messages
  const { showSuccess, showError } = useNotification();

  // Build default values from existing entity or defaults
  const defaultValues = useMemo<Partial<LifecycleEmailFormData>>(() => {
    if (lifecycleEmail) {
      // Edit mode: use existing entity values
      return {
        user_id: lifecycleEmail.user_id,
        template_code: lifecycleEmail.template_code,
        status: lifecycleEmail.status,
        scheduled_for: lifecycleEmail.scheduled_for,
        sent_at: lifecycleEmail.sent_at ?? '',
        skip_reason: lifecycleEmail.skip_reason ?? '',
      };
    }
    // Create mode: use defaults
    return DEFAULT_FORM_VALUES;
  }, [lifecycleEmail]);

  // Initialize React Hook Form
  const methods = useForm<LifecycleEmailFormData>({
    defaultValues,
    mode: 'onBlur',
  });

  const {
    control,
    handleSubmit,
    formState: { errors },
    reset,
  } = methods;

  // Reset form when entity data changes (e.g., switching from create to edit mode)
  useEffect(() => {
    reset(defaultValues);
  }, [defaultValues, reset]);

  // Log validation errors to console for debugging
  useEffect(() => {
    if (Object.keys(errors).length > 0) {
      console.log('[LifecycleEmailForm] Validation errors:', errors);
    }
  }, [errors]);
  // -------------------------------------------------------------------------
  // Form Submission
  // -------------------------------------------------------------------------

  const handleFormSubmit = async (data: LifecycleEmailFormData) => {
    console.log('[LifecycleEmailForm] Submitting:', { data, isEditMode });
    try {
      const result = await onSubmit(data);
      console.log('[LifecycleEmailForm] Form submission successful', { data, result, isEditMode });
      // Show success notification for create operation
      if (!isEditMode) {
        showSuccess('LifecycleEmail created successfully');
      }
    } catch (error) {
      // Log full error details to console for debugging
      console.error('[LifecycleEmailForm] Form submission failed', {
        error,
        errorMessage: error instanceof Error ? error.message : String(error),
        errorStack: error instanceof Error ? error.stack : undefined,
        data,
        isEditMode,
      });
      // Show error notification on failure
      const errorMessage = error instanceof Error ? error.message : 'An unexpected error occurred';
      showError(`Failed to ${isEditMode ? 'update' : 'create'} LifecycleEmail: ${errorMessage}`);
    }
  };

  // -------------------------------------------------------------------------
  // Render
  // -------------------------------------------------------------------------

  // Count validation errors
  const errorCount = Object.keys(errors).length;

  return (
    <FormProvider {...methods}>
      <Box
        component="form"
        onSubmit={handleSubmit(handleFormSubmit)}
        className={className}
        data-testid="form-lifecycle_email"
        noValidate
      >
        {/* Error Summary Alert */}
        {errorCount > 0 && (
          <MuiAlert
            severity="error"
            sx={{ mb: 3 }}
            data-testid="form-errors-summary"
          >
            Please fix {errorCount} error{errorCount !== 1 ? 's' : ''} before submitting
          </MuiAlert>
        )}

        <Grid container spacing={3}>
          {/* User Id field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="user_id"
              control={control}
              rules={{
                required: 'User Id is required',
                maxLength: { value: 100, message: 'User Id must be at most 100 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="User Id"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-user-id"
                />
              )}
            />
          </Grid>

          {/* Template Code field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="template_code"
              control={control}
              rules={{
                required: 'Template Code is required',
                maxLength: { value: 50, message: 'Template Code must be at most 50 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Template Code"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Lifecycle template identifier: welcome, onboarding_reminder, gmail_reminder, etc."}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-template-code"
                />
              )}
            />
          </Grid>

          {/* Status field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="status"
              control={control}
              rules={{
                required: 'Status is required',
                maxLength: { value: 20, message: 'Status must be at most 20 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Status"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-status"
                />
              )}
            />
          </Grid>

          {/* Scheduled For field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="scheduled_for"
              control={control}
              rules={{
                required: 'Scheduled For is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Scheduled For"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "When this lifecycle email should be sent"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-scheduled-for"
                />
              )}
            />
          </Grid>

          {/* Sent At field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="sent_at"
              control={control}
              rules={{
                required: 'Sent At is required',
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Sent At"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-sent-at"
                />
              )}
            />
          </Grid>

          {/* Skip Reason field */}
          <Grid size={{ xs: 12, md: 6 }}>
            {/* 4. Default: plain text field */}
            <Controller
              name="skip_reason"
              control={control}
              rules={{
                required: 'Skip Reason is required',
                maxLength: { value: 200, message: 'Skip Reason must be at most 200 characters' },
              }}
              render={({ field: formField, fieldState }) => (
                <TextField
                  {...formField}
                  label="Skip Reason"
                  fullWidth
                  required
                  disabled={isLoading}
                  error={!!fieldState.error}
                  helperText={fieldState.error?.message || "Why this email was skipped"}
                  placeholder=""
                  value={formField.value || ''}
                  data-testid="field-skip-reason"
                />
              )}
            />
          </Grid>

          {/* Form Actions */}
          <Grid size={12}>
            <Box display="flex" gap={2} pt={2}>
              <Button
                type="submit"
                variant="contained"
                color="primary"
                disabled={isLoading}
                startIcon={isLoading ? <CircularProgress size={20} /> : undefined}
                data-testid="submit-button"
              >
                {isLoading
                  ? 'Saving...'
                  : isEditMode
                    ? 'Update LifecycleEmail'
                    : 'Create LifecycleEmail'}
              </Button>

              <Button
                type="button"
                variant="outlined"
                onClick={onCancel}
                disabled={isLoading}
                data-testid="cancel-button"
              >
                Cancel
              </Button>
            </Box>
          </Grid>
        </Grid>
      </Box>
    </FormProvider>
  );
}

// ============================================================================
// Modal Wrapper Component
// ============================================================================

/**
 * LifecycleEmailFormModal Component
 *
 * Wraps the LifecycleEmailForm in a Material UI Dialog for modal display.
 * Use this component when you want to show the form in a popup dialog.
 *
 * @example
 * ```tsx
 * <LifecycleEmailFormModal
 *   open={isModalOpen}
 *   onClose={handleClose}
 *   onSubmit={handleCreate}
 *   onCancel={handleClose}
 *   isLoading={isCreating}
 * />
 * ```
 */
export function LifecycleEmailFormModal({
  open,
  onClose,
  title,
  lifecycleEmail,
  onSubmit,
  onCancel,
  isLoading = false,
  className = '',
}: LifecycleEmailFormModalProps): React.ReactElement {
  const isEditMode = Boolean(lifecycleEmail);
  const modalTitle = title ?? (isEditMode ? 'Edit LifecycleEmail' : 'Create LifecycleEmail');

  // Handle modal close - prevent closing during loading
  const handleClose = () => {
    if (!isLoading) {
      onClose();
    }
  };

  return (
    <Dialog
      open={open}
      onClose={handleClose}
      maxWidth="md"
      fullWidth
      data-testid="modal-lifecycle_email-form"
    >
      <DialogTitle sx={{ m: 0, p: 2, display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        {modalTitle}
        <IconButton
          aria-label="close"
          onClick={handleClose}
          disabled={isLoading}
          data-testid="modal-close-button"
          sx={{ color: (theme) => theme.palette.grey[500] }}
        >
          <CloseIcon />
        </IconButton>
      </DialogTitle>
      <DialogContent dividers>
        <LifecycleEmailForm
          lifecycleEmail={lifecycleEmail}
          onSubmit={onSubmit}
          onCancel={onCancel}
          isLoading={isLoading}
          className={className}
        />
      </DialogContent>
    </Dialog>
  );
}


// Default export for convenience
export default LifecycleEmailForm;
