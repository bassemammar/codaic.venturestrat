/**
 * Permission Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all permission queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['permissions'] - All permission queries
 * - ['permissions', 'list'] - All list queries
 * - ['permissions', 'list', { filters }] - Specific filtered list
 * - ['permissions', 'detail', id] - Specific permission by ID
 *
 * @example
 * ```typescript
 * import { permissionKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: permissionKeys.list({ is_active: true }),
 *   queryFn: () => permissionService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all permission queries
 * queryClient.invalidateQueries({ queryKey: permissionKeys.all });
 *
 * // Invalidate specific permission
 * queryClient.invalidateQueries({ queryKey: permissionKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-02-23T11:13:14.571838Z
 */

import type { PermissionFilterOptions } from '../types/permission.types';

/**
 * Root query key for Permission entity
 * Used as namespace prefix for all permission queries
 */
const PERMISSION_ROOT = 'permissions' as const;

/**
 * Permission Query Keys
 *
 * Hierarchical query key factory for permission queries:
 * - all: Base key for all permission queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific permission by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = permissionKeys.list({ is_active: true });
 * // Result: ['permissions', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = permissionKeys.detail('uuid-here');
 * // Result: ['permissions', 'detail', 'uuid-here']
 *
 * // Invalidate all permission data
 * queryClient.invalidateQueries({ queryKey: permissionKeys.all });
 * ```
 */
export const permissionKeys = {
  /**
   * Base key for all permission queries
   * Use this to invalidate all permission-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: permissionKeys.all });
   * ```
   */
  all: [PERMISSION_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: permissionKeys.lists() });
   * ```
   */
  lists: () => [...permissionKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = permissionKeys.list({ is_active: true });
   *
   * // List with search
   * const key = permissionKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = permissionKeys.list();
   * ```
   */
  list: (filters?: PermissionFilterOptions) =>
    [...permissionKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: permissionKeys.details() });
   * ```
   */
  details: () => [...permissionKeys.all, 'detail'] as const,

  /**
   * Key for a specific permission by ID
   * Use this for fetching and invalidating individual permission records
   *
   * @param id - Permission ID (UUID)
   * @returns Query key tuple for specific permission
   *
   * @example
   * ```typescript
   * // Fetch specific permission
   * const { data } = useQuery({
   *   queryKey: permissionKeys.detail('uuid-here'),
   *   queryFn: () => permissionService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: permissionKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...permissionKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active permissions (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = permissionKeys.dropdown();
   *
   * // Include inactive items
   * const key = permissionKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...permissionKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * Key for permission by code lookup
   * Use this when fetching by business code instead of UUID
   *
   * @param code - Permission code (e.g., 'USD', 'EUR')
   * @returns Query key tuple for code lookup
   *
   * @example
   * ```typescript
   * const { data } = useQuery({
   *   queryKey: permissionKeys.byCode('USD'),
   *   queryFn: () => permissionService.getByCode('USD'),
   * });
   * ```
   */
  byCode: (code: string) =>
    [...permissionKeys.all, 'by-code', code.toUpperCase()] as const,

  /**
   * Permissions filtered by tenant ID
   *
   * @param tenantId - Tenant ID (UUID)
   * @returns Query key tuple
   */
  byTenant: (tenantId: string) =>
    [...permissionKeys.all, 'by-tenant', tenantId] as const,
} as const;

/**
 * Type representing any valid Permission query key
 * Useful for typing query key arrays in generic contexts
 */
export type PermissionQueryKey =
  | typeof permissionKeys.all
  | ReturnType<typeof permissionKeys.lists>
  | ReturnType<typeof permissionKeys.list>
  | ReturnType<typeof permissionKeys.details>
  | ReturnType<typeof permissionKeys.detail>
  | ReturnType<typeof permissionKeys.dropdown>
  | ReturnType<typeof permissionKeys.byCode>
;

/**
 * Default export for convenience
 */
export default permissionKeys;
