/**
 * Role Type Definitions
 *
 * This file contains type definitions for the Role entity:
 * - Role: Full entity model with all fields
 * - RoleCreateRequest: Data for creating a new role
 * - RoleUpdateRequest: Partial data for updating a role
 * - RoleListResponse: Paginated list response
 * - RoleFilterOptions: Query parameters for filtering
 * - RoleDropdownOption: Options for select components
 *
 * Authorization role with JSON permissions
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-02-23T11:13:14.998057Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// Role Entity
// ============================================================================

/**
 * Role entity model
 *
 * Represents the full role entity as returned by the API.
 * Authorization role with JSON permissions
 *
 * @remarks
 * This interface matches the backend RoleDTO schema exactly.
 */
export interface Role {
  /** Unique identifier (UUID) */
  id: string;

  /** Human-readable role name */
  name: string;

  /** Unique role code (e.g. admin, trader, viewer) */
  code: string;

  /** Role description */
  description: string | null;

  /** JSON array of permission codes granted to this role */
  permissions: Record<string, any> | null;

  /** Whether this is a built-in system role */
  is_system: boolean;

  /** Owning tenant */
  tenant_id: string;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * Role creation request data
 *
 * Used when creating a new role. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend RoleCreateDTO schema.
 */
export interface RoleCreateRequest {
  /** Human-readable role name (required) */
  name: string;

  /** Unique role code (e.g. admin, trader, viewer) (required) */
  code: string;

  /** Role description (optional) */
  description?: string;

  /** JSON array of permission codes granted to this role (optional) */
  permissions?: Record<string, any>;

  /** Whether this is a built-in system role (optional) */
  is_system?: boolean;

  /** Owning tenant (required) */
  tenant_id: string;

  /** Active status (defaults to true for new roles) */
  is_active?: boolean;
}

/**
 * Role update request data
 *
 * Used when updating an existing role. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend RoleUpdateDTO schema.
 */
export interface RoleUpdateRequest {
  /** Human-readable role name */
  name?: string;

  /** Unique role code (e.g. admin, trader, viewer) */
  code?: string;

  /** Role description */
  description?: string | null;

  /** JSON array of permission codes granted to this role */
  permissions?: Record<string, any> | null;

  /** Whether this is a built-in system role */
  is_system?: boolean;

  /** Owning tenant */
  tenant_id?: string;

  /** Active status */
  is_active?: boolean;
}

/**
 * Role list response
 *
 * Wrapper for paginated role list queries.
 */
export interface RoleListResponse {
  /** Array of role items */
  items: Role[];

  /** Alternative property name for items (for API compatibility) */
  roles?: Role[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * Role filter options for list queries
 *
 * Extends BaseEntityFilters with role-specific filtering options.
 */
export interface RoleFilterOptions extends BaseEntityFilters {
  /** Filter by owning tenant */
  tenant_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * Role dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface RoleDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: Role;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * Role summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface RoleSummary {
  /** Unique identifier */
  id: string;

  /** Human-readable role name */
  name: string;

  /** Unique role code (e.g. admin, trader, viewer) */
  code: string;

  /** Role description */
  description: string | null;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * Role form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type RoleFormData = RoleCreateRequest;

/**
 * Default form values for new role
 *
 * Use this when initializing form state for creating a new role.
 */
export const DEFAULT_ROLE_FORM_DATA: Partial<RoleFormData> = {
  is_active: true,
  is_system: false,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single role API response
 */
export interface RoleResponse {
  /** The requested role entity */
  data: Role;
}

/**
 * Role mutation response (create/update)
 */
export interface RoleMutationResponse {
  /** The created or updated role */
  data: Role;

  /** Success message */
  message?: string;
}

/**
 * API error response for role operations
 */
export interface RoleApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a Role
 *
 * @param value - Value to check
 * @returns True if value is a Role
 */
export function isRole(value: unknown): value is Role {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.name === 'string' &&
    typeof obj.code === 'string' &&
    typeof obj.tenant_id === 'string'
  );
}

/**
 * Type guard to check if a value is a RoleDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a RoleDropdownOption
 */
export function isRoleDropdownOption(
  value: unknown
): value is RoleDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isRole(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All role types re-exported for convenience */
export type {
  Role as RoleEntity,
  RoleCreateRequest as CreateRoleInput,
  RoleUpdateRequest as UpdateRoleInput,
};
