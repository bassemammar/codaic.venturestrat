/**
 * Session Type Definitions
 *
 * This file contains type definitions for the Session entity:
 * - Session: Full entity model with all fields
 * - SessionCreateRequest: Data for creating a new session
 * - SessionUpdateRequest: Partial data for updating a session
 * - SessionListResponse: Paginated list response
 * - SessionFilterOptions: Query parameters for filtering
 * - SessionDropdownOption: Options for select components
 *
 * Active user session with token tracking
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-02-23T11:13:15.420777Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// Session Entity
// ============================================================================

/**
 * Session entity model
 *
 * Represents the full session entity as returned by the API.
 * Active user session with token tracking
 *
 * @remarks
 * This interface matches the backend SessionDTO schema exactly.
 */
export interface Session {
  /** Unique identifier (UUID) */
  id: string;

  /** User who owns this session */
  user_id: string;

  /** SHA256 hash of the access token */
  token_hash: string;

  /** SHA256 hash of the refresh token */
  refresh_token_hash: string | null;

  /** Client IP address */
  ip_address: string | null;

  /** Client user agent string */
  user_agent: string | null;

  /** Whether session is still active */
  is_active: boolean;

  /** When this session expires */
  expires_at: string;

  /** Tenant context for this session */
  tenant_id: string;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * Session creation request data
 *
 * Used when creating a new session. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend SessionCreateDTO schema.
 */
export interface SessionCreateRequest {
  /** User who owns this session (required) */
  user_id: string;

  /** SHA256 hash of the access token (required) */
  token_hash: string;

  /** SHA256 hash of the refresh token (optional) */
  refresh_token_hash?: string;

  /** Client IP address (optional) */
  ip_address?: string;

  /** Client user agent string (optional) */
  user_agent?: string;

  /** Whether session is still active (optional) */
  is_active?: boolean;

  /** When this session expires (required) */
  expires_at: string;

  /** Tenant context for this session (required) */
  tenant_id: string;

}

/**
 * Session update request data
 *
 * Used when updating an existing session. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend SessionUpdateDTO schema.
 */
export interface SessionUpdateRequest {
  /** User who owns this session */
  user_id?: string;

  /** SHA256 hash of the access token */
  token_hash?: string;

  /** SHA256 hash of the refresh token */
  refresh_token_hash?: string | null;

  /** Client IP address */
  ip_address?: string | null;

  /** Client user agent string */
  user_agent?: string | null;

  /** Whether session is still active */
  is_active?: boolean;

  /** When this session expires */
  expires_at?: string;

  /** Tenant context for this session */
  tenant_id?: string;

}

/**
 * Session list response
 *
 * Wrapper for paginated session list queries.
 */
export interface SessionListResponse {
  /** Array of session items */
  items: Session[];

  /** Alternative property name for items (for API compatibility) */
  sessions?: Session[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * Session filter options for list queries
 *
 * Extends BaseEntityFilters with session-specific filtering options.
 */
export interface SessionFilterOptions extends BaseEntityFilters {
  /** Filter by user who owns this session */
  user_id?: string;

  /** Filter by tenant context for this session */
  tenant_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * Session dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface SessionDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: Session;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * Session summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface SessionSummary {
  /** Unique identifier */
  id: string;

}

// ============================================================================
// Form Types
// ============================================================================

/**
 * Session form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type SessionFormData = SessionCreateRequest;

/**
 * Default form values for new session
 *
 * Use this when initializing form state for creating a new session.
 */
export const DEFAULT_SESSION_FORM_DATA: Partial<SessionFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single session API response
 */
export interface SessionResponse {
  /** The requested session entity */
  data: Session;
}

/**
 * Session mutation response (create/update)
 */
export interface SessionMutationResponse {
  /** The created or updated session */
  data: Session;

  /** Success message */
  message?: string;
}

/**
 * API error response for session operations
 */
export interface SessionApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a Session
 *
 * @param value - Value to check
 * @returns True if value is a Session
 */
export function isSession(value: unknown): value is Session {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.user_id === 'string' &&
    typeof obj.token_hash === 'string' &&
    typeof obj.expires_at === 'string' &&
    typeof obj.tenant_id === 'string'
  );
}

/**
 * Type guard to check if a value is a SessionDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a SessionDropdownOption
 */
export function isSessionDropdownOption(
  value: unknown
): value is SessionDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isSession(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All session types re-exported for convenience */
export type {
  Session as SessionEntity,
  SessionCreateRequest as CreateSessionInput,
  SessionUpdateRequest as UpdateSessionInput,
};
