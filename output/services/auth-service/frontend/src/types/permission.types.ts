/**
 * Permission Type Definitions
 *
 * This file contains type definitions for the Permission entity:
 * - Permission: Full entity model with all fields
 * - PermissionCreateRequest: Data for creating a new permission
 * - PermissionUpdateRequest: Partial data for updating a permission
 * - PermissionListResponse: Paginated list response
 * - PermissionFilterOptions: Query parameters for filtering
 * - PermissionDropdownOption: Options for select components
 *
 * Granular permission for resource access control
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-02-23T11:13:14.547472Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// Permission Entity
// ============================================================================

/**
 * Permission entity model
 *
 * Represents the full permission entity as returned by the API.
 * Granular permission for resource access control
 *
 * @remarks
 * This interface matches the backend PermissionDTO schema exactly.
 */
export interface Permission {
  /** Unique identifier (UUID) */
  id: string;

  /** Resource name (e.g. trade, position, risk) */
  resource: string;

  /** Action type (e.g. create, read, update, delete, approve) */
  action: string;

  /** Unique permission code (e.g. trade:create) */
  code: string;

  /** Human-readable permission description */
  description: string | null;

  /** Service this permission applies to */
  service_name: string | null;

  /** Owning tenant */
  tenant_id: string;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * Permission creation request data
 *
 * Used when creating a new permission. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend PermissionCreateDTO schema.
 */
export interface PermissionCreateRequest {
  /** Resource name (e.g. trade, position, risk) (required) */
  resource: string;

  /** Action type (e.g. create, read, update, delete, approve) (required) */
  action: string;

  /** Unique permission code (e.g. trade:create) (required) */
  code: string;

  /** Human-readable permission description (optional) */
  description?: string;

  /** Service this permission applies to (optional) */
  service_name?: string;

  /** Owning tenant (required) */
  tenant_id: string;

  /** Active status (defaults to true for new permissions) */
  is_active?: boolean;
}

/**
 * Permission update request data
 *
 * Used when updating an existing permission. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend PermissionUpdateDTO schema.
 */
export interface PermissionUpdateRequest {
  /** Resource name (e.g. trade, position, risk) */
  resource?: string;

  /** Action type (e.g. create, read, update, delete, approve) */
  action?: string;

  /** Unique permission code (e.g. trade:create) */
  code?: string;

  /** Human-readable permission description */
  description?: string | null;

  /** Service this permission applies to */
  service_name?: string | null;

  /** Owning tenant */
  tenant_id?: string;

  /** Active status */
  is_active?: boolean;
}

/**
 * Permission list response
 *
 * Wrapper for paginated permission list queries.
 */
export interface PermissionListResponse {
  /** Array of permission items */
  items: Permission[];

  /** Alternative property name for items (for API compatibility) */
  permissions?: Permission[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * Permission filter options for list queries
 *
 * Extends BaseEntityFilters with permission-specific filtering options.
 */
export interface PermissionFilterOptions extends BaseEntityFilters {
  /** Filter by owning tenant */
  tenant_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * Permission dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface PermissionDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: Permission;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * Permission summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface PermissionSummary {
  /** Unique identifier */
  id: string;

  /** Unique permission code (e.g. trade:create) */
  code: string;

  /** Human-readable permission description */
  description: string | null;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * Permission form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type PermissionFormData = PermissionCreateRequest;

/**
 * Default form values for new permission
 *
 * Use this when initializing form state for creating a new permission.
 */
export const DEFAULT_PERMISSION_FORM_DATA: Partial<PermissionFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single permission API response
 */
export interface PermissionResponse {
  /** The requested permission entity */
  data: Permission;
}

/**
 * Permission mutation response (create/update)
 */
export interface PermissionMutationResponse {
  /** The created or updated permission */
  data: Permission;

  /** Success message */
  message?: string;
}

/**
 * API error response for permission operations
 */
export interface PermissionApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a Permission
 *
 * @param value - Value to check
 * @returns True if value is a Permission
 */
export function isPermission(value: unknown): value is Permission {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.resource === 'string' &&
    typeof obj.action === 'string' &&
    typeof obj.code === 'string' &&
    typeof obj.tenant_id === 'string'
  );
}

/**
 * Type guard to check if a value is a PermissionDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a PermissionDropdownOption
 */
export function isPermissionDropdownOption(
  value: unknown
): value is PermissionDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isPermission(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All permission types re-exported for convenience */
export type {
  Permission as PermissionEntity,
  PermissionCreateRequest as CreatePermissionInput,
  PermissionUpdateRequest as UpdatePermissionInput,
};
