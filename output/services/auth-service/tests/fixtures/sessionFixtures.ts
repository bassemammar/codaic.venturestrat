/**
 * Session Test Fixtures
 *
 * Test data fixtures for Session E2E and unit tests.
 * Provides predefined test data and factory functions for generating
 * test sessions.
 *
 * Fixture patterns:
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields with realistic test values
 * - invalid: Invalid data for validation testing
 * - unique: Unique values using timestamps for test isolation
 *
 * Active user session with token tracking
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-02-20T17:11:22.383214Z
 */

import type { SessionCreateRequest } from '../../types/session.types';

// ============================================================================
// Types
// ============================================================================

/**
 * Test data pattern types
 *
 * @description
 * - minimal: Only required fields with minimal valid values
 * - complete: All fields populated with realistic test data
 * - invalid: Data that should fail validation
 * - unique: Data with timestamps for test isolation
 */
export type TestDataPattern = 'minimal' | 'complete' | 'invalid' | 'unique';

/**
 * Session test fixture containing all test data patterns
 */
export interface SessionTestFixtures {
  /** Minimal valid session with only required fields */
  minimal: SessionCreateRequest;
  /** Complete session with all fields populated */
  complete: SessionCreateRequest;
  /** Invalid session for testing validation errors */
  invalid: Partial<SessionCreateRequest>;
  /** Unique session with timestamp-based values */
  unique: SessionCreateRequest;
  /** Multiple sessions for list testing */
  list: SessionCreateRequest[];
}

// ============================================================================
// Helper Functions
// ============================================================================

/**
 * Generate a unique identifier based on current timestamp
 * Useful for creating unique test data that won't conflict
 *
 * @param prefix - Optional prefix for the identifier
 * @returns Unique string identifier
 */
function generateUniqueId(prefix = ''): string {
  const timestamp = Date.now();
  const random = Math.random().toString(36).substring(2, 8);
  return prefix ? `${prefix}-${timestamp}-${random}` : `${timestamp}-${random}`;
}

/**
 * Generate a random string of specified length
 *
 * @param length - Length of the string to generate
 * @param charset - Character set to use (default: alphanumeric)
 * @returns Random string
 */
function randomString(
  length: number,
  charset = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'
): string {
  let result = '';
  for (let i = 0; i < length; i++) {
    result += charset.charAt(Math.floor(Math.random() * charset.length));
  }
  return result;
}

/**
 * Generate a random integer between min and max (inclusive)
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @returns Random integer
 */
function randomInt(min: number, max: number): number {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

/**
 * Generate a random decimal with specified precision
 *
 * @param min - Minimum value
 * @param max - Maximum value
 * @param precision - Number of decimal places
 * @returns Random decimal number
 */
function randomDecimal(min: number, max: number, precision = 2): number {
  const value = Math.random() * (max - min) + min;
  const factor = Math.pow(10, precision);
  return Math.round(value * factor) / factor;
}

/**
 * Generate a random date within a range
 *
 * @param startDate - Start of the range
 * @param endDate - End of the range
 * @returns Random date as ISO string
 */
function randomDate(startDate: Date, endDate: Date): string {
  const start = startDate.getTime();
  const end = endDate.getTime();
  const randomTime = start + Math.random() * (end - start);
  return new Date(randomTime).toISOString().split('T')[0];
}

/**
 * Pick a random element from an array
 *
 * @param array - Array to pick from
 * @returns Random element
 */
function randomPick<T>(array: T[]): T {
  return array[Math.floor(Math.random() * array.length)];
}

// ============================================================================
// Static Test Data
// ============================================================================


/**
 * Sample session names for generating realistic test data
 */
const SAMPLE_NAMES = [
  'Test Session Alpha',
  'Test Session Beta',
  'Test Session Gamma',
  'Sample Session One',
  'Sample Session Two',
  'Demo Session',
  'Example Session',
];

/**
 * Sample codes for generating realistic test data
 */
const SAMPLE_CODES = [
  'TST001',
  'TST002',
  'TST003',
  'DEMO01',
  'DEMO02',
  'SMPL01',
  'SMPL02',
];

/**
 * Sample descriptions for generating realistic test data
 */
const SAMPLE_DESCRIPTIONS = [
  'This is a test session for automated testing.',
  'Sample session created for E2E tests.',
  'Demo session with all fields populated.',
  'Example session for validation testing.',
  'Test data fixture for session operations.',
];

// ============================================================================
// Predefined Fixtures
// ============================================================================

/**
 * Minimal valid session fixture
 *
 * Contains only required fields with minimal valid values.
 * Use this when testing that minimal data is accepted.
 */
export const minimalSessionFixture: SessionCreateRequest = {
  is_active: true,
};

/**
 * Complete session fixture
 *
 * Contains all fields with realistic test values.
 * Use this when testing full CRUD operations.
 */
export const completeSessionFixture: SessionCreateRequest = {
  user_id: 'Test User Id',
  token_hash: 'Test Token Hash',
  refresh_token_hash: 'Test Refresh Token Hash',
  ip_address: 'Test Ip Address',
  user_agent: 'Test User Agent',
  expires_at: 'Test Expires At',
  tenant_id: 'Test Tenant Id',
  is_active: true,
};

/**
 * Unique session fixture
 *
 * Contains all fields with timestamp-based unique values.
 * Use this when testing to ensure data isolation between test runs.
 * Each field that needs uniqueness includes a timestamp/random component.
 */
export const uniqueSessionFixture: SessionCreateRequest = {
  user_id: `User Id ${generateUniqueId()}`,
  token_hash: `Token Hash ${generateUniqueId()}`,
  refresh_token_hash: `Refresh Token Hash ${generateUniqueId()}`,
  ip_address: `Ip Address ${generateUniqueId()}`,
  user_agent: `User Agent ${generateUniqueId()}`,
  expires_at: `Expires At ${generateUniqueId()}`,
  tenant_id: `Tenant Id ${generateUniqueId()}`,
  is_active: true,
};

/**
 * Invalid session fixture
 *
 * Contains invalid data that should fail validation.
 * Use this when testing validation error handling.
 * Covers: empty required fields, values out of range, invalid formats.
 */
export const invalidSessionFixture: Partial<SessionCreateRequest> = {
};

/**
 * Invalid data patterns for comprehensive validation testing
 *
 * Contains various invalid data scenarios:
 * - emptyRequired: All required fields left empty
 * - tooShort: String fields below minimum length
 * - tooLong: String fields exceeding maximum length
 * - outOfRangeMin: Numeric fields below minimum value
 * - outOfRangeMax: Numeric fields above maximum value
 * - wrongType: Fields with incorrect data types (as strings for type coercion testing)
 * - specialChars: Fields with potentially problematic characters
 */
export const sessionInvalidPatterns = {
  /** All required string fields empty */
  emptyRequired: {
  } as Partial<SessionCreateRequest>,


  /** String fields exceeding maximum length */
  tooLong: {
    token_hash: 'x'.repeat(305), // max: 255
    refresh_token_hash: 'x'.repeat(305), // max: 255
    ip_address: 'x'.repeat(95), // max: 45
    user_agent: 'x'.repeat(550), // max: 500
  } as Partial<SessionCreateRequest>,



  /** Fields with SQL injection attempts (for security testing) */
  sqlInjection: {
  } as Partial<SessionCreateRequest>,

  /** Fields with XSS attempts (for security testing) */
  xssAttempts: {
  } as Partial<SessionCreateRequest>,

  /** Fields with null values (for nullable field testing) */
  nullValues: {
    refresh_token_hash: null,
    ip_address: null,
    user_agent: null,
  } as Partial<SessionCreateRequest>,

  /** Fields with undefined values (for missing field testing) */
  undefinedValues: {
    // Intentionally empty - represents missing fields
  } as Partial<SessionCreateRequest>,
};

/**
 * Collection of session fixtures for list testing
 *
 * Contains multiple sessions for testing list operations,
 * pagination, filtering, and sorting.
 */
export const sessionListFixtures: SessionCreateRequest[] = [
  {
    user_id: 'User Id 1',
    token_hash: 'Token Hash 1',
    refresh_token_hash: 'Refresh Token Hash 1',
    ip_address: 'Ip Address 1',
    user_agent: 'User Agent 1',
    expires_at: 'Expires At 1',
    tenant_id: 'Tenant Id 1',
    is_active: true,
  },
  {
    user_id: 'User Id 2',
    token_hash: 'Token Hash 2',
    refresh_token_hash: 'Refresh Token Hash 2',
    ip_address: 'Ip Address 2',
    user_agent: 'User Agent 2',
    expires_at: 'Expires At 2',
    tenant_id: 'Tenant Id 2',
    is_active: true,
  },
  {
    user_id: 'User Id 3',
    token_hash: 'Token Hash 3',
    refresh_token_hash: 'Refresh Token Hash 3',
    ip_address: 'Ip Address 3',
    user_agent: 'User Agent 3',
    expires_at: 'Expires At 3',
    tenant_id: 'Tenant Id 3',
    is_active: true,
  },
  {
    user_id: 'User Id 4',
    token_hash: 'Token Hash 4',
    refresh_token_hash: 'Refresh Token Hash 4',
    ip_address: 'Ip Address 4',
    user_agent: 'User Agent 4',
    expires_at: 'Expires At 4',
    tenant_id: 'Tenant Id 4',
    is_active: true,
  },
  {
    user_id: 'User Id 5',
    token_hash: 'Token Hash 5',
    refresh_token_hash: 'Refresh Token Hash 5',
    ip_address: 'Ip Address 5',
    user_agent: 'User Agent 5',
    expires_at: 'Expires At 5',
    tenant_id: 'Tenant Id 5',
    is_active: false,
  },
];

// ============================================================================
// Fixture Collection
// ============================================================================

/**
 * All session test fixtures
 *
 * @example
 * ```typescript
 * import { sessionFixtures } from '../fixtures/sessionFixtures';
 *
 * // Use minimal fixture
 * const minData = sessionFixtures.minimal;
 *
 * // Use complete fixture
 * const fullData = sessionFixtures.complete;
 *
 * // Use unique fixture (with timestamp)
 * const uniqueData = generateSessionTestData('unique');
 * ```
 */
export const sessionFixtures: SessionTestFixtures = {
  minimal: minimalSessionFixture,
  complete: completeSessionFixture,
  invalid: invalidSessionFixture as SessionCreateRequest,
  unique: uniqueSessionFixture,
  list: sessionListFixtures,
};

// ============================================================================
// Factory Functions
// ============================================================================

/**
 * Generate session test data based on pattern
 *
 * @param pattern - The test data pattern to generate
 * @param overrides - Optional field overrides
 * @returns Generated test data
 *
 * @example
 * ```typescript
 * // Generate complete test data
 * const data = generateSessionTestData('complete');
 *
 * // Generate unique test data with overrides
 * const customData = generateSessionTestData('unique', {
 *   is_active: false,
 * });
 * ```
 */
export function generateSessionTestData(
  pattern: TestDataPattern = 'complete',
  overrides: Partial<SessionCreateRequest> = {}
): SessionCreateRequest {
  let baseData: SessionCreateRequest;

  switch (pattern) {
    case 'minimal':
      baseData = { ...minimalSessionFixture };
      break;

    case 'complete':
      baseData = { ...completeSessionFixture };
      break;

    case 'invalid':
      // Return invalid data (caller should handle validation failure)
      baseData = { ...invalidSessionFixture } as SessionCreateRequest;
      break;

    case 'unique':
      // Generate fresh unique values for each call
      baseData = {
        ...uniqueSessionFixture,
      };
      break;

    default:
      baseData = { ...completeSessionFixture };
  }

  return { ...baseData, ...overrides };
}

/**
 * Generate multiple session fixtures
 *
 * @param count - Number of fixtures to generate
 * @param pattern - The test data pattern to use as base
 * @param modifier - Optional function to modify each fixture
 * @returns Array of generated test data
 *
 * @example
 * ```typescript
 * // Generate 5 unique fixtures
 * const fixtures = generateMultipleSessionFixtures(5, 'unique');
 *
 * // Generate fixtures with sequential numbering
 * const numbered = generateMultipleSessionFixtures(3, 'complete', (data, index) => ({
 *   ...data,
 *   name: `Session #${index + 1}`,
 * }));
 * ```
 */
export function generateMultipleSessionFixtures(
  count: number,
  pattern: TestDataPattern = 'unique',
  modifier?: (data: SessionCreateRequest, index: number) => SessionCreateRequest
): SessionCreateRequest[] {
  const fixtures: SessionCreateRequest[] = [];

  for (let i = 0; i < count; i++) {
    let fixture = generateSessionTestData(pattern);

    if (modifier) {
      fixture = modifier(fixture, i);
    } else if (pattern === 'unique') {
      // Add index to ensure uniqueness even with same timestamp
    }

    fixtures.push(fixture);
  }

  return fixtures;
}

/**
 * Generate random session test data
 *
 * Creates test data with randomized values within valid ranges.
 * Useful for fuzz testing and edge case discovery.
 *
 * @param overrides - Optional field overrides
 * @returns Randomly generated test data
 *
 * @example
 * ```typescript
 * // Generate random test data
 * const randomData = generateRandomSessionData();
 *
 * // Generate random data with specific field
 * const customRandom = generateRandomSessionData({
 *   is_active: true,
 * });
 * ```
 */
export function generateRandomSessionData(
  overrides: Partial<SessionCreateRequest> = {}
): SessionCreateRequest {
  const data: SessionCreateRequest = {
    user_id: randomString(randomInt(None, Math.min(None, 50))),
    token_hash: randomString(randomInt(None, Math.min(255, 50))),
    refresh_token_hash: randomString(randomInt(None, Math.min(255, 50))),
    ip_address: randomString(randomInt(None, Math.min(45, 50))),
    user_agent: randomString(randomInt(None, Math.min(500, 50))),
    expires_at: randomString(randomInt(None, Math.min(None, 50))),
    tenant_id: randomString(randomInt(None, Math.min(None, 50))),
    is_active: Math.random() > 0.2, // 80% chance of being active
  };

  return { ...data, ...overrides };
}

// ============================================================================
// Validation Test Data
// ============================================================================

/**
 * Edge case test data for boundary testing
 *
 * Contains values at the boundaries of valid ranges.
 */
export const sessionEdgeCases = {
  /** token_hash at maximum length (255 chars) */
  token_hashMaxLength: 'x'.repeat(255),
  /** refresh_token_hash at maximum length (255 chars) */
  refresh_token_hashMaxLength: 'x'.repeat(255),
  /** ip_address at maximum length (45 chars) */
  ip_addressMaxLength: 'x'.repeat(45),
  /** user_agent at maximum length (500 chars) */
  user_agentMaxLength: 'x'.repeat(500),
};

/**
 * Special character test data for input sanitization testing
 */
export const sessionSpecialCharacterData: Partial<SessionCreateRequest> = {
};

/**
 * Whitespace test data for trimming validation
 */
export const sessionWhitespaceData: Partial<SessionCreateRequest> = {
};

// ============================================================================
// Exports
// ============================================================================

// Default export - main fixture collection
export default sessionFixtures;
