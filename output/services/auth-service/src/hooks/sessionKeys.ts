/**
 * Session Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all session queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['sessions'] - All session queries
 * - ['sessions', 'list'] - All list queries
 * - ['sessions', 'list', { filters }] - Specific filtered list
 * - ['sessions', 'detail', id] - Specific session by ID
 *
 * @example
 * ```typescript
 * import { sessionKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: sessionKeys.list({ is_active: true }),
 *   queryFn: () => sessionService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all session queries
 * queryClient.invalidateQueries({ queryKey: sessionKeys.all });
 *
 * // Invalidate specific session
 * queryClient.invalidateQueries({ queryKey: sessionKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-02-20T17:11:22.087668Z
 */

import type { SessionFilterOptions } from '../types/session.types';

/**
 * Root query key for Session entity
 * Used as namespace prefix for all session queries
 */
const SESSION_ROOT = 'sessions' as const;

/**
 * Session Query Keys
 *
 * Hierarchical query key factory for session queries:
 * - all: Base key for all session queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific session by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = sessionKeys.list({ is_active: true });
 * // Result: ['sessions', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = sessionKeys.detail('uuid-here');
 * // Result: ['sessions', 'detail', 'uuid-here']
 *
 * // Invalidate all session data
 * queryClient.invalidateQueries({ queryKey: sessionKeys.all });
 * ```
 */
export const sessionKeys = {
  /**
   * Base key for all session queries
   * Use this to invalidate all session-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: sessionKeys.all });
   * ```
   */
  all: [SESSION_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: sessionKeys.lists() });
   * ```
   */
  lists: () => [...sessionKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = sessionKeys.list({ is_active: true });
   *
   * // List with search
   * const key = sessionKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = sessionKeys.list();
   * ```
   */
  list: (filters?: SessionFilterOptions) =>
    [...sessionKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: sessionKeys.details() });
   * ```
   */
  details: () => [...sessionKeys.all, 'detail'] as const,

  /**
   * Key for a specific session by ID
   * Use this for fetching and invalidating individual session records
   *
   * @param id - Session ID (UUID)
   * @returns Query key tuple for specific session
   *
   * @example
   * ```typescript
   * // Fetch specific session
   * const { data } = useQuery({
   *   queryKey: sessionKeys.detail('uuid-here'),
   *   queryFn: () => sessionService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: sessionKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...sessionKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active sessions (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = sessionKeys.dropdown();
   *
   * // Include inactive items
   * const key = sessionKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...sessionKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * Sessions filtered by user ID
   *
   * @param userId - User ID (UUID)
   * @returns Query key tuple
   */
  byUser: (userId: string) =>
    [...sessionKeys.all, 'by-user', userId] as const,

  /**
   * Sessions filtered by tenant ID
   *
   * @param tenantId - Tenant ID (UUID)
   * @returns Query key tuple
   */
  byTenant: (tenantId: string) =>
    [...sessionKeys.all, 'by-tenant', tenantId] as const,
} as const;

/**
 * Type representing any valid Session query key
 * Useful for typing query key arrays in generic contexts
 */
export type SessionQueryKey =
  | typeof sessionKeys.all
  | ReturnType<typeof sessionKeys.lists>
  | ReturnType<typeof sessionKeys.list>
  | ReturnType<typeof sessionKeys.details>
  | ReturnType<typeof sessionKeys.detail>
  | ReturnType<typeof sessionKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default sessionKeys;
