/**
 * User Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all user queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['users'] - All user queries
 * - ['users', 'list'] - All list queries
 * - ['users', 'list', { filters }] - Specific filtered list
 * - ['users', 'detail', id] - Specific user by ID
 *
 * @example
 * ```typescript
 * import { userKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: userKeys.list({ is_active: true }),
 *   queryFn: () => userService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all user queries
 * queryClient.invalidateQueries({ queryKey: userKeys.all });
 *
 * // Invalidate specific user
 * queryClient.invalidateQueries({ queryKey: userKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-02-20T17:11:23.001910Z
 */

import type { UserFilterOptions } from '../types/user.types';

/**
 * Root query key for User entity
 * Used as namespace prefix for all user queries
 */
const USER_ROOT = 'users' as const;

/**
 * User Query Keys
 *
 * Hierarchical query key factory for user queries:
 * - all: Base key for all user queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific user by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = userKeys.list({ is_active: true });
 * // Result: ['users', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = userKeys.detail('uuid-here');
 * // Result: ['users', 'detail', 'uuid-here']
 *
 * // Invalidate all user data
 * queryClient.invalidateQueries({ queryKey: userKeys.all });
 * ```
 */
export const userKeys = {
  /**
   * Base key for all user queries
   * Use this to invalidate all user-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: userKeys.all });
   * ```
   */
  all: [USER_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: userKeys.lists() });
   * ```
   */
  lists: () => [...userKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = userKeys.list({ is_active: true });
   *
   * // List with search
   * const key = userKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = userKeys.list();
   * ```
   */
  list: (filters?: UserFilterOptions) =>
    [...userKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: userKeys.details() });
   * ```
   */
  details: () => [...userKeys.all, 'detail'] as const,

  /**
   * Key for a specific user by ID
   * Use this for fetching and invalidating individual user records
   *
   * @param id - User ID (UUID)
   * @returns Query key tuple for specific user
   *
   * @example
   * ```typescript
   * // Fetch specific user
   * const { data } = useQuery({
   *   queryKey: userKeys.detail('uuid-here'),
   *   queryFn: () => userService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: userKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...userKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active users (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = userKeys.dropdown();
   *
   * // Include inactive items
   * const key = userKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...userKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * Users filtered by default tenant ID
   *
   * @param defaultTenantId - DefaultTenant ID (UUID)
   * @returns Query key tuple
   */
  byDefaultTenant: (defaultTenantId: string) =>
    [...userKeys.all, 'by-default-tenant', defaultTenantId] as const,

  /**
   * Users filtered by tenant ID
   *
   * @param tenantId - Tenant ID (UUID)
   * @returns Query key tuple
   */
  byTenant: (tenantId: string) =>
    [...userKeys.all, 'by-tenant', tenantId] as const,
} as const;

/**
 * Type representing any valid User query key
 * Useful for typing query key arrays in generic contexts
 */
export type UserQueryKey =
  | typeof userKeys.all
  | ReturnType<typeof userKeys.lists>
  | ReturnType<typeof userKeys.list>
  | ReturnType<typeof userKeys.details>
  | ReturnType<typeof userKeys.detail>
  | ReturnType<typeof userKeys.dropdown>
;

/**
 * Default export for convenience
 */
export default userKeys;
