/**
 * Role Query Key Factory
 *
 * Centralized query key factory for React Query cache management.
 * Provides consistent, type-safe query keys for all role queries.
 *
 * Query keys follow a hierarchical structure for efficient cache invalidation:
 * - ['roles'] - All role queries
 * - ['roles', 'list'] - All list queries
 * - ['roles', 'list', { filters }] - Specific filtered list
 * - ['roles', 'detail', id] - Specific role by ID
 *
 * @example
 * ```typescript
 * import { roleKeys } from './queryKeys';
 *
 * // Use in useQuery
 * const { data } = useQuery({
 *   queryKey: roleKeys.list({ is_active: true }),
 *   queryFn: () => roleService.getAll({ is_active: true }),
 * });
 *
 * // Invalidate all role queries
 * queryClient.invalidateQueries({ queryKey: roleKeys.all });
 *
 * // Invalidate specific role
 * queryClient.invalidateQueries({ queryKey: roleKeys.detail('uuid') });
 * ```
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-02-20T17:11:21.532934Z
 */

import type { RoleFilterOptions } from '../types/role.types';

/**
 * Root query key for Role entity
 * Used as namespace prefix for all role queries
 */
const ROLE_ROOT = 'roles' as const;

/**
 * Role Query Keys
 *
 * Hierarchical query key factory for role queries:
 * - all: Base key for all role queries
 * - lists: All list queries (for bulk invalidation)
 * - list(filters): Specific filtered list
 * - details: All detail queries
 * - detail(id): Specific role by ID
 * - dropdown(activeOnly): Dropdown options query
 *
 * @example
 * ```typescript
 * // List with filters
 * const key = roleKeys.list({ is_active: true });
 * // Result: ['roles', 'list', { is_active: true }]
 *
 * // Detail by ID
 * const key = roleKeys.detail('uuid-here');
 * // Result: ['roles', 'detail', 'uuid-here']
 *
 * // Invalidate all role data
 * queryClient.invalidateQueries({ queryKey: roleKeys.all });
 * ```
 */
export const roleKeys = {
  /**
   * Base key for all role queries
   * Use this to invalidate all role-related queries at once
   *
   * @example
   * ```typescript
   * // Invalidate everything after a major change
   * queryClient.invalidateQueries({ queryKey: roleKeys.all });
   * ```
   */
  all: [ROLE_ROOT] as const,

  /**
   * Key for all list queries
   * Use this to invalidate all list queries without affecting detail queries
   *
   * @returns Query key tuple for list namespace
   *
   * @example
   * ```typescript
   * // Invalidate all list queries
   * queryClient.invalidateQueries({ queryKey: roleKeys.lists() });
   * ```
   */
  lists: () => [...roleKeys.all, 'list'] as const,

  /**
   * Key for a specific filtered list query
   * Include filters in the key to enable automatic re-fetching when filters change
   *
   * @param filters - Optional filter options for the query
   * @returns Query key tuple including filters
   *
   * @example
   * ```typescript
   * // List all active
   * const key = roleKeys.list({ is_active: true });
   *
   * // List with search
   * const key = roleKeys.list({ search: 'test' });
   *
   * // Unfiltered list
   * const key = roleKeys.list();
   * ```
   */
  list: (filters?: RoleFilterOptions) =>
    [...roleKeys.lists(), filters ?? {}] as const,

  /**
   * Key for all detail queries
   * Use this to invalidate all detail queries without affecting list queries
   *
   * @returns Query key tuple for detail namespace
   *
   * @example
   * ```typescript
   * // Invalidate all detail queries
   * queryClient.invalidateQueries({ queryKey: roleKeys.details() });
   * ```
   */
  details: () => [...roleKeys.all, 'detail'] as const,

  /**
   * Key for a specific role by ID
   * Use this for fetching and invalidating individual role records
   *
   * @param id - Role ID (UUID)
   * @returns Query key tuple for specific role
   *
   * @example
   * ```typescript
   * // Fetch specific role
   * const { data } = useQuery({
   *   queryKey: roleKeys.detail('uuid-here'),
   *   queryFn: () => roleService.getById('uuid-here'),
   * });
   *
   * // Invalidate after update
   * queryClient.invalidateQueries({
   *   queryKey: roleKeys.detail('uuid-here')
   * });
   * ```
   */
  detail: (id: string) => [...roleKeys.details(), id] as const,

  /**
   * Key for dropdown options query
   * Separate key allows independent caching of dropdown data
   *
   * @param activeOnly - Whether to include only active roles (default: true)
   * @returns Query key tuple for dropdown query
   *
   * @example
   * ```typescript
   * // Active items only (default)
   * const key = roleKeys.dropdown();
   *
   * // Include inactive items
   * const key = roleKeys.dropdown(false);
   * ```
   */
  dropdown: (activeOnly: boolean = true) =>
    [...roleKeys.all, 'dropdown', { activeOnly }] as const,

  /**
   * Key for role by code lookup
   * Use this when fetching by business code instead of UUID
   *
   * @param code - Role code (e.g., 'USD', 'EUR')
   * @returns Query key tuple for code lookup
   *
   * @example
   * ```typescript
   * const { data } = useQuery({
   *   queryKey: roleKeys.byCode('USD'),
   *   queryFn: () => roleService.getByCode('USD'),
   * });
   * ```
   */
  byCode: (code: string) =>
    [...roleKeys.all, 'by-code', code.toUpperCase()] as const,

  /**
   * Roles filtered by tenant ID
   *
   * @param tenantId - Tenant ID (UUID)
   * @returns Query key tuple
   */
  byTenant: (tenantId: string) =>
    [...roleKeys.all, 'by-tenant', tenantId] as const,
} as const;

/**
 * Type representing any valid Role query key
 * Useful for typing query key arrays in generic contexts
 */
export type RoleQueryKey =
  | typeof roleKeys.all
  | ReturnType<typeof roleKeys.lists>
  | ReturnType<typeof roleKeys.list>
  | ReturnType<typeof roleKeys.details>
  | ReturnType<typeof roleKeys.detail>
  | ReturnType<typeof roleKeys.dropdown>
  | ReturnType<typeof roleKeys.byCode>
;

/**
 * Default export for convenience
 */
export default roleKeys;
