/**
 * User Type Definitions
 *
 * This file contains type definitions for the User entity:
 * - User: Full entity model with all fields
 * - UserCreateRequest: Data for creating a new user
 * - UserUpdateRequest: Partial data for updating a user
 * - UserListResponse: Paginated list response
 * - UserFilterOptions: Query parameters for filtering
 * - UserDropdownOption: Options for select components
 *
 * System user with authentication credentials
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-02-20T17:11:22.947699Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// User Entity
// ============================================================================

/**
 * User entity model
 *
 * Represents the full user entity as returned by the API.
 * System user with authentication credentials
 *
 * @remarks
 * This interface matches the backend UserDTO schema exactly.
 */
export interface User {
  /** Unique identifier (UUID) */
  id: string;

  /** Unique login username */
  username: string;

  /** User email address */
  email: string;

  /** Bcrypt hashed password */
  hashed_password: string;

  /** User first name */
  first_name: string | null;

  /** User last name */
  last_name: string | null;

  /** Whether user account is active */
  is_active: boolean;

  /** Timestamp of last successful login */
  last_login: string | null;

  /** Number of consecutive failed login attempts */
  failed_login_count: number;

  /** Account locked until this timestamp */
  locked_until: string | null;

  /** Default tenant for this user */
  default_tenant_id: string | null;

  /** Owning tenant */
  tenant_id: string;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * User creation request data
 *
 * Used when creating a new user. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend UserCreateDTO schema.
 */
export interface UserCreateRequest {
  /** Unique login username (required) */
  username: string;

  /** User email address (required) */
  email: string;

  /** Bcrypt hashed password (required) */
  hashed_password: string;

  /** User first name (optional) */
  first_name?: string;

  /** User last name (optional) */
  last_name?: string;

  /** Whether user account is active (optional) */
  is_active?: boolean;

  /** Timestamp of last successful login (optional) */
  last_login?: string;

  /** Number of consecutive failed login attempts (optional) */
  failed_login_count?: number;

  /** Account locked until this timestamp (optional) */
  locked_until?: string;

  /** Default tenant for this user (optional) */
  default_tenant_id?: string;

  /** Owning tenant (required) */
  tenant_id: string;

}

/**
 * User update request data
 *
 * Used when updating an existing user. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend UserUpdateDTO schema.
 */
export interface UserUpdateRequest {
  /** Unique login username */
  username?: string;

  /** User email address */
  email?: string;

  /** Bcrypt hashed password */
  hashed_password?: string;

  /** User first name */
  first_name?: string | null;

  /** User last name */
  last_name?: string | null;

  /** Whether user account is active */
  is_active?: boolean;

  /** Timestamp of last successful login */
  last_login?: string | null;

  /** Number of consecutive failed login attempts */
  failed_login_count?: number;

  /** Account locked until this timestamp */
  locked_until?: string | null;

  /** Default tenant for this user */
  default_tenant_id?: string | null;

  /** Owning tenant */
  tenant_id?: string;

}

/**
 * User list response
 *
 * Wrapper for paginated user list queries.
 */
export interface UserListResponse {
  /** Array of user items */
  items: User[];

  /** Alternative property name for items (for API compatibility) */
  users?: User[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * User filter options for list queries
 *
 * Extends BaseEntityFilters with user-specific filtering options.
 */
export interface UserFilterOptions extends BaseEntityFilters {
  /** Filter by default tenant for this user */
  default_tenant_id?: string;

  /** Filter by owning tenant */
  tenant_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * User dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface UserDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: User;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * User summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface UserSummary {
  /** Unique identifier */
  id: string;

}

// ============================================================================
// Form Types
// ============================================================================

/**
 * User form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type UserFormData = UserCreateRequest;

/**
 * Default form values for new user
 *
 * Use this when initializing form state for creating a new user.
 */
export const DEFAULT_USER_FORM_DATA: Partial<UserFormData> = {
  is_active: true,
  failed_login_count: 0,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single user API response
 */
export interface UserResponse {
  /** The requested user entity */
  data: User;
}

/**
 * User mutation response (create/update)
 */
export interface UserMutationResponse {
  /** The created or updated user */
  data: User;

  /** Success message */
  message?: string;
}

/**
 * API error response for user operations
 */
export interface UserApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a User
 *
 * @param value - Value to check
 * @returns True if value is a User
 */
export function isUser(value: unknown): value is User {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.username === 'string' &&
    typeof obj.email === 'string' &&
    typeof obj.hashed_password === 'string' &&
    typeof obj.tenant_id === 'string'
  );
}

/**
 * Type guard to check if a value is a UserDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a UserDropdownOption
 */
export function isUserDropdownOption(
  value: unknown
): value is UserDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isUser(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All user types re-exported for convenience */
export type {
  User as UserEntity,
  UserCreateRequest as CreateUserInput,
  UserUpdateRequest as UpdateUserInput,
};
