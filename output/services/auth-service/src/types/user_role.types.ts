/**
 * UserRole Type Definitions
 *
 * This file contains type definitions for the UserRole entity:
 * - UserRole: Full entity model with all fields
 * - UserRoleCreateRequest: Data for creating a new user_role
 * - UserRoleUpdateRequest: Partial data for updating a user_role
 * - UserRoleListResponse: Paginated list response
 * - UserRoleFilterOptions: Query parameters for filtering
 * - UserRoleDropdownOption: Options for select components
 *
 * Many-to-many mapping between users and roles
 *
 * Generated by CodeGen OS v1.0.0
 * Generated at: 2026-02-20T17:11:24.022844Z
 */

// ============================================================================
// Base Types
// ============================================================================

/**
 * Base filter interface with common query parameters
 * Provides pagination, sorting, and search capabilities
 */
export interface BaseEntityFilters {
  /** Search query string - searches across multiple text fields */
  search?: string;
  /** Active/inactive status filter */
  is_active?: boolean;
  /** Pagination: page number (1-indexed) */
  page?: number;
  /** Pagination: items per page */
  page_size?: number;
  /** Sort field name */
  sort_by?: string;
  /** Sort direction */
  sort_order?: 'asc' | 'desc';
}

/**
 * Pagination metadata returned by the API
 */
export interface PaginationMeta {
  /** Current page number (1-indexed) */
  page: number;
  /** Number of items per page */
  page_size: number;
  /** Total number of items matching the query */
  total: number;
  /** Total number of pages */
  total_pages: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_prev: boolean;
}

// ============================================================================
// UserRole Entity
// ============================================================================

/**
 * UserRole entity model
 *
 * Represents the full user_role entity as returned by the API.
 * Many-to-many mapping between users and roles
 *
 * @remarks
 * This interface matches the backend UserRoleDTO schema exactly.
 */
export interface UserRole {
  /** Unique identifier (UUID) */
  id: string;

  /** User assigned to this role */
  user_id: string;

  /** Role assigned to user */
  role_id: string;

  /** Tenant context */
  tenant_id: string;

  /** Creation timestamp (ISO 8601) */
  created_at: string;

  /** Last update timestamp (ISO 8601) */
  updated_at: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Request/Response Types
// ============================================================================

/**
 * UserRole creation request data
 *
 * Used when creating a new user_role. Omits system-generated fields
 * like id, timestamps, and audit fields.
 *
 * @remarks
 * Matches the backend UserRoleCreateDTO schema.
 */
export interface UserRoleCreateRequest {
  /** User assigned to this role (required) */
  user_id: string;

  /** Role assigned to user (required) */
  role_id: string;

  /** Tenant context (required) */
  tenant_id: string;

  /** Active status (defaults to true for new user_roles) */
  is_active?: boolean;
}

/**
 * UserRole update request data
 *
 * Used when updating an existing user_role. All fields are optional
 * to support partial updates.
 *
 * @remarks
 * Matches the backend UserRoleUpdateDTO schema.
 */
export interface UserRoleUpdateRequest {
  /** User assigned to this role */
  user_id?: string;

  /** Role assigned to user */
  role_id?: string;

  /** Tenant context */
  tenant_id?: string;

  /** Active status */
  is_active?: boolean;
}

/**
 * UserRole list response
 *
 * Wrapper for paginated user_role list queries.
 */
export interface UserRoleListResponse {
  /** Array of user_role items */
  items: UserRole[];

  /** Alternative property name for items (for API compatibility) */
  user_roles?: UserRole[];

  /** Pagination metadata */
  pagination?: PaginationMeta;

  /** Total count of items matching the query */
  total?: number;
}

// ============================================================================
// Filter Types
// ============================================================================

/**
 * UserRole filter options for list queries
 *
 * Extends BaseEntityFilters with user_role-specific filtering options.
 */
export interface UserRoleFilterOptions extends BaseEntityFilters {
  /** Filter by user assigned to this role */
  user_id?: string;

  /** Filter by role assigned to user */
  role_id?: string;

  /** Filter by tenant context */
  tenant_id?: string;

}

// ============================================================================
// Dropdown/Select Types
// ============================================================================

/**
 * UserRole dropdown option for select components
 *
 * Provides a standardized structure for dropdown/select options with
 * the full entity data attached for additional context.
 */
export interface UserRoleDropdownOption {
  /** Option value (typically the entity ID) */
  value: string;

  /** Display label for the option */
  label: string;

  /** Full entity data for additional context */
  data: UserRole;

  /** Whether this option should be disabled */
  disabled?: boolean;
}

// ============================================================================
// Summary Types
// ============================================================================

/**
 * UserRole summary for list views
 *
 * Contains minimal fields for efficient list rendering.
 * Use this type when you only need basic display information.
 */
export interface UserRoleSummary {
  /** Unique identifier */
  id: string;

  /** Active status */
  is_active: boolean;
}

// ============================================================================
// Form Types
// ============================================================================

/**
 * UserRole form data for React Hook Form integration
 *
 * Type alias for the create request that can be used with form libraries.
 * Provides sensible defaults for form state.
 */
export type UserRoleFormData = UserRoleCreateRequest;

/**
 * Default form values for new user_role
 *
 * Use this when initializing form state for creating a new user_role.
 */
export const DEFAULT_USER_ROLE_FORM_DATA: Partial<UserRoleFormData> = {
  is_active: true,
};

// ============================================================================
// API Response Types
// ============================================================================

/**
 * Single user_role API response
 */
export interface UserRoleResponse {
  /** The requested user_role entity */
  data: UserRole;
}

/**
 * UserRole mutation response (create/update)
 */
export interface UserRoleMutationResponse {
  /** The created or updated user_role */
  data: UserRole;

  /** Success message */
  message?: string;
}

/**
 * API error response for user_role operations
 */
export interface UserRoleApiError {
  /** HTTP status code */
  status_code: number;

  /** Error message */
  message: string;

  /** Error code for programmatic handling */
  error_code?: string;

  /** Detailed error information */
  detail?: string | Record<string, unknown>;

  /** Field-level validation errors */
  validation_errors?: Record<string, string[]>;
}

// ============================================================================
// Type Guards
// ============================================================================

/**
 * Type guard to check if a value is a UserRole
 *
 * @param value - Value to check
 * @returns True if value is a UserRole
 */
export function isUserRole(value: unknown): value is UserRole {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.id === 'string' &&
    typeof obj.user_id === 'string' &&
    typeof obj.role_id === 'string' &&
    typeof obj.tenant_id === 'string'
  );
}

/**
 * Type guard to check if a value is a UserRoleDropdownOption
 *
 * @param value - Value to check
 * @returns True if value is a UserRoleDropdownOption
 */
export function isUserRoleDropdownOption(
  value: unknown
): value is UserRoleDropdownOption {
  if (!value || typeof value !== 'object') {
    return false;
  }
  const obj = value as Record<string, unknown>;
  return (
    typeof obj.value === 'string' &&
    typeof obj.label === 'string' &&
    isUserRole(obj.data)
  );
}

// ============================================================================
// Exports
// ============================================================================

/** All user_role types re-exported for convenience */
export type {
  UserRole as UserRoleEntity,
  UserRoleCreateRequest as CreateUserRoleInput,
  UserRoleUpdateRequest as UpdateUserRoleInput,
};
