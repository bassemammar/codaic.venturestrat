"""Role Application Service - Generated by Code Generator v1.0.0"""

from typing import List, Optional
from uuid import UUID
import structlog

from auth_service.infrastructure.repositories.role_repository import RoleRepository
from auth_service.infrastructure.orm.role import Role
from auth_service.schemas.role import RoleCreate, RoleUpdate, RoleResponse
from auth_service.integrations.events import event_publisher
from auth_service.integrations.metrics import entity_operations_total

logger = structlog.get_logger(__name__)


class RoleService:
    """Application service for Role use cases."""

    def __init__(self, session=None):
        """Initialize service with repository."""
        self.repository = RoleRepository(session)

    async def create_role(self, data: RoleCreate, tenant_id: str = None) -> RoleResponse:
        """Create new Role."""
        try:
            data_dict = data.model_dump()
            if tenant_id:
                data_dict["tenant_id"] = tenant_id

            # BaseModel.create() returns the created instance directly
            created = await self.repository.create(**data_dict)

            # Publish event
            await event_publisher.publish(
                entity_name="role",
                action="created",
                data=created.to_dict(),
                tenant_id=tenant_id,
            )

            # Track metric
            entity_operations_total.labels(
                entity="role", operation="create", status="success"
            ).inc()

            # Convert BaseModel to dict for response
            return RoleResponse(**created.to_dict())

        except Exception as e:
            entity_operations_total.labels(entity="role", operation="create", status="error").inc()
            logger.error("create_role_failed", error=str(e))
            raise

    async def get_role(self, id: UUID) -> Optional[RoleResponse]:
        """Get Role by ID."""
        orm_obj = await self.repository.get_by_id(id)
        return RoleResponse(**orm_obj.to_dict()) if orm_obj else None

    async def list_roles(self, skip: int = 0, limit: int = 100) -> List[RoleResponse]:
        """List Roles with pagination."""
        orm_objs = await self.repository.get_all(skip, limit)
        return [RoleResponse(**obj.to_dict()) for obj in orm_objs]

    async def update_role(self, id: UUID, data: RoleUpdate) -> Optional[RoleResponse]:
        """Update Role."""
        try:
            orm_obj = await self.repository.get_by_id(id)
            if not orm_obj:
                return None

            # Update fields from DTO
            for key, value in data.model_dump(exclude_unset=True).items():
                setattr(orm_obj, key, value)

            # BaseModel.save() returns self
            updated = await self.repository.update(orm_obj)

            # Publish event
            await event_publisher.publish(
                entity_name="role",
                action="updated",
                data=updated.to_dict(),
                tenant_id=getattr(updated, "tenant_id", None),
            )

            # Track metric
            entity_operations_total.labels(
                entity="role", operation="update", status="success"
            ).inc()

            return RoleResponse(**updated.to_dict())

        except Exception as e:
            entity_operations_total.labels(entity="role", operation="update", status="error").inc()
            logger.error("update_role_failed", id=str(id), error=str(e))
            raise

    async def delete_role(self, id: UUID) -> bool:
        """Delete Role."""
        try:
            # Get entity before deletion
            orm_obj = await self.repository.get_by_id(id)
            if not orm_obj:
                return False

            entity_data = orm_obj.to_dict()

            # Delete
            deleted = await self.repository.delete(id)

            if deleted:
                # Publish event
                await event_publisher.publish(
                    entity_name="role",
                    action="deleted",
                    data=entity_data,
                    tenant_id=entity_data.get("tenant_id"),
                )

                # Track metric
                entity_operations_total.labels(
                    entity="role", operation="delete", status="success"
                ).inc()

            return deleted

        except Exception as e:
            entity_operations_total.labels(entity="role", operation="delete", status="error").inc()
            logger.error("delete_role_failed", id=str(id), error=str(e))
            raise

    async def search_roles(
        self, domain: list = None, skip: int = 0, limit: int = 100
    ) -> List[RoleResponse]:
        """
        Search Roles with domain filtering.

        Args:
            domain: domain filter (e.g., [('field', '=', 'value')])
            skip: Offset for pagination
            limit: Maximum records to return

        Returns:
            List of matching Role instances
        """
        orm_objs = await self.repository.search(domain, skip, limit)
        return [RoleResponse(**obj.to_dict()) for obj in orm_objs]

    async def count_roles(self, domain: list = None) -> int:
        """
        Count Roles matching domain.

        Args:
            domain: Optional domain filter

        Returns:
            Number of matching records
        """
        return await self.repository.count(domain)

    async def bulk_create_roles(self, records_data: List[RoleCreate]) -> List[RoleResponse]:
        """
        Create multiple Roles efficiently.

        Args:
            records_data: List of create DTOs

        Returns:
            List of created Role instances
        """
        # Convert DTOs to dicts
        data_dicts = [record.model_dump() for record in records_data]

        # Bulk create
        created_objs = await self.repository.bulk_create(data_dicts)

        return [RoleResponse(**obj.to_dict()) for obj in created_objs]
