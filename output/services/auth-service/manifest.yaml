# auth-service Service Manifest
# Generated by VentureStrat CLI
# Version: 1.0.0

manifest_version: 1
name: auth-service
version: 1.0.0
description: Authentication and authorization service
depends: []
provides:
  apis:
    rest: /api/v1
  events:
    - topic: auth.permission.created
      schema: Permission.Created
    - topic: auth.permission.updated
      schema: Permission.Updated
    - topic: auth.permission.deleted
      schema: Permission.Deleted
    - topic: auth.role.created
      schema: Role.Created
    - topic: auth.role.updated
      schema: Role.Updated
    - topic: auth.role.deleted
      schema: Role.Deleted
    - topic: auth.session.created
      schema: Session.Created
    - topic: auth.session.updated
      schema: Session.Updated
    - topic: auth.session.deleted
      schema: Session.Deleted
    - topic: auth.user.created
      schema: User.Created
    - topic: auth.user.updated
      schema: User.Updated
    - topic: auth.user.deleted
      schema: User.Deleted
    - topic: auth.user_role.created
      schema: UserRole.Created
    - topic: auth.user_role.updated
      schema: UserRole.Updated
    - topic: auth.user_role.deleted
      schema: UserRole.Deleted
  - topic: auth_service.data.processed
    schema: AuthService.DataProcessed
  - topic: auth_service.status.changed
    schema: AuthService.StatusChanged
health:
  liveness: /health/live
  readiness: /health/ready
  interval: 10
  timeout: 5
  failure_threshold: 3
  success_threshold: 1
  deregister_after: 60
resources:
  cpu:
    request: 100m
    limit: 500m
  memory:
    request: 128Mi
    limit: 512Mi
tags:
- fastapi
- development
metadata:
  team: platform
  service_type: fastapi
  protocol: http